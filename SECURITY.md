# Security Policy

Jangbogo 프로젝트의 보안 정책, 취약점 보고 절차, 그리고 **이 저장소를 다룰 때 지켜야 할 규칙**입니다.

---

## 이 프로그램이 다루는 데이터의 성격

가장 먼저 알아야 할 것은 이것입니다.

> **Jangbogo 가 다루는 것은 공공 데이터가 아니라, 프로그램을 실행한 사람의 개인 구매 내역입니다.**

- DB 파일 하나에 그 사람이 **언제 무엇을 얼마에 샀는지**가 전부 들어갑니다. 생활 반경·건강 상태·가족 구성까지 추정할 수 있는 종류의 기록입니다.
- 내보내기 결과(CSV/XLSX)는 그것을 사람이 바로 읽을 수 있는 형태로 담습니다. DB 보다 열어 보기 쉬워서, 공개되면 그 자리에서 피해가 납니다.
- 수집 로그와 실패 스크린샷에도 주문 화면이 그대로 남습니다.

그리고 이것은 **개발자에게도 똑같이 적용됩니다.**

> **개발 중에 유입된 데이터는 개발자 개인의 것입니다.** "테스트 데이터"가 아니라 그 사람의 실제 구매 내역이고, 저장소나 배포본에 들어가면 그대로 공개됩니다.

이 저장소는 **PUBLIC** 입니다. 한 번 push 된 것은 나중에 파일을 지워도 회수할 수 없습니다.

---

## 지원 버전

| 버전 | 지원 여부 |
| ---- | --------- |
| 0.18.x | ✅ 지원 (현재 계열) |
| 0.17.x | ⚠️ 제한적 지원 (심각한 취약점만) |
| < 0.17.0 | ❌ 지원 종료 |

**보안 패치는 최신 계열로만 나갑니다.** 구 버전에 개별 백포트를 제공하지 않으므로, 최신 릴리스로 올리는 것이 유일한 완전한 대응입니다.

---

## 저장소 위생 — 과거에 개발용 DB 가 커밋된 적이 있습니다

이 항목은 **다시 같은 실수가 나오지 않게 하려고** 공개해 둡니다. 이 저장소에 접근하는 모든 사람이 읽어야 합니다.

### 무슨 일이 있었나

**개발용 SQLite DB 파일이 이 PUBLIC 저장소에 커밋돼 한동안 올라가 있었습니다.** 지금은 `.gitignore` 로 막혀 있고 파일도 추적에서 제거됐지만, 당시에는 그것을 잡아 줄 검사가 **하나도 없었습니다.**

특히 나쁜 조합이 두 가지 있었습니다.

1. **SQLite 는 DB 본체 옆에 `-wal`·`-shm` 파일을 따로 만듭니다.** `db/*.db` 같은 확장자 규칙은 이것들을 그대로 통과시킵니다. 내용은 본체와 같은 개인 데이터입니다.
2. **git 은 이미 추적 중인 파일에 무시 규칙을 적용하지 않습니다.** 규칙을 넣기 전에 한 번이라도 `add` 된 파일은 `.gitignore` 를 아무리 고쳐도 계속 따라옵니다. "무시 규칙이 있다"와 "실제로 추적되지 않는다"는 다른 말입니다.

### 왜 심각한가

- 대상이 **사람의 구매 이력**입니다. 소스 코드가 새는 것과 성질이 다릅니다.
- 공개 저장소의 이력은 clone·fork·미러·검색엔진 캐시로 즉시 퍼집니다. **원본을 지워도 사본은 남습니다.**
- 사고가 났다는 사실 자체가 조용합니다. 빌드는 초록이고, CI 도 통과하고, 아무도 알려 주지 않습니다.

### 히스토리를 재작성하지 않기로 한 이유

이 사고에 대해 **git 히스토리 재작성(force push)은 하지 않기로 결정했습니다.**

- **이미 공개된 것은 회수할 수 없습니다.** clone·fork·캐시가 남아 있는 이상, 이력을 지우는 것은 "지웠다는 기분"만 주고 실질적인 회수는 되지 않습니다.
- **재작성은 모든 커밋 해시를 바꿉니다.** fork·PR·로컬 클론·이슈에 걸린 링크가 전부 끊기고, 협업하던 사람들이 각자 강제 리셋을 해야 합니다. 되돌릴 수 없는 파괴적 작업이기도 합니다.
- 그래서 **덮는 대신 기록으로 남기고, 같은 실수가 반복되지 않게 검사를 넣는 쪽**을 택했습니다. 이 문서가 그 기록입니다.

### 지금 있는 가드

같은 사고를 자동으로 잡도록 네 층을 둡니다. 전부 파일만 읽으며, 앱을 기동하거나 네트워크를 쓰지 않습니다.

| 층 | 무엇을 보는가 | 잡는 것 |
| --- | --- | --- |
| **추적 파일 검사** | `git ls-files` 로 **git 이 실제로 추적 중인 파일** 목록 | 이미 추적이 시작된 개인 데이터 (`db/`·`logs/`·`exports/`·`config/backup/`·`profiles/` 아래 전부, DB·로그 확장자, 값이 든 설정 파일) |
| **`.gitignore` 규칙 감시** | 무시 규칙이 지워지지 않았는가 | 애초에 스테이징되는 것 자체를 막는 층 |
| **배포 산출물 검사** | `packageDist` 가 방금 만든 ZIP 을 스스로 열어 엔트리 **이름**을 검사 (+ 검사 코드가 살아 있는지 소스로 확인) | 배포본에 개인 데이터가 실리는 것. 걸리면 **빌드가 실패**합니다 — 경고로 끝나지 않습니다 |
| **백업·임시 파일 감시** | 소스 트리에 `.bak`·`.orig`·`.tmp` 류가 있는지, `src/main/java` 에 자바 소스 아닌 파일이 있는지 | 확장자만 바꿔 두면 다른 모든 소스 검사를 통째로 우회한다는 문제 |

두 가지를 의도적으로 이렇게 만들었습니다.

- **무시 규칙 감시와 추적 파일 검사는 역할이 다릅니다.** 앞은 *들어오지 못하게*, 뒤는 *이미 들어온 것을* 잡습니다. 한쪽만으로는 위의 2번 구멍이 그대로 남습니다.
- **git 을 돌리지 못하면 건너뛰지 않고 실패시킵니다.** "개인 데이터가 없다"와 "확인하지 못했다"를 같은 초록으로 만들면, 검사가 죽은 것을 아무도 모르는 상태가 됩니다. 그건 검사가 없는 것보다 나쁩니다.

### 기여자가 지켜야 할 실천 규칙

1. **개발용 DB 를 절대 커밋하지 않습니다.** `db/` 아래에는 런타임이 만드는 것 말고 커밋할 것이 없습니다. `-wal`·`-shm`·`-journal`·백업본도 전부 같습니다.
2. **`git add -A` / `git add .` 를 쓰지 않습니다.** 파일을 하나씩 지정해 스테이징하세요. 위 사고의 대부분은 "전부 담기"에서 나옵니다. 커밋 전에 `git status` 와 `git diff --cached --stat` 로 **무엇이 담겼는지 눈으로 확인**하세요.
3. **`.gitignore` 규칙을 지우지 않습니다.** 특히 `profiles/`·`.locks/` 는 **폴더가 아직 없는 시점에 미리 막아 둔 것**이라 "안 쓰는 규칙"으로 오해받기 쉽습니다. 그 폴더가 생기는 순간 사람이 로그인한 브라우저 세션이 통째로 들어갑니다.
4. **테스트 데이터는 합성 데이터로 만듭니다.** 실제 영수증·주문 화면은 상품명·금액·구매일시가 모두 든 구매 이력 그 자체라, 부분 마스킹으로는 재식별 위험이 남습니다. 실물을 가리는 대신 **처음부터 존재하지 않게** 손으로 지은 픽스처를 씁니다.
5. **로그·스크린샷·내보내기 파일을 이슈나 PR 에 그대로 붙이지 않습니다.** 버그 리포트에 로그가 필요하면 해당 줄만 발췌하고, 상품명·주문번호·계정 아이디를 지운 뒤 올리세요.
6. **백업본을 저장소 안에 두지 않습니다.** `Foo.java.bak` 같은 파일은 소스 검사가 전부 건너뛰는 사각지대가 되고, 보통 "고치기 전 상태"라 방금 제거한 문제를 그대로 담고 있습니다.
7. **배포본은 빈 DB 로 시작합니다.** 릴리스 ZIP 에 담기는 것은 애플리케이션 JAR·실행 스크립트·안내 문서·번들 JRE·라이선스 고지뿐이고, DB·내보내기·로그·설정 파일은 들어가지 않습니다. 배포본을 푼 폴더에서 DB 가 보인다면 그것은 **그 자리에서 앱을 실행해 런타임이 만든 것**이지 배포에 포함된 것이 아닙니다. 그 폴더를 통째로 남에게 전달하지 마세요.
8. **저장소 트리 안에서 배포본을 압축 해제하지 마세요.** `dist/.../db/` 처럼 한 단계 아래에 같은 구조가 생기고, 그 자리에서 앱을 실행하면 저장소 안에 실데이터가 생깁니다.
9. **빌드 산출물 폴더(`build/`) 안에서 앱을 실행했다면 그 폴더에 실데이터가 있습니다.** `clean` 계열 작업은 그것을 지웁니다 — 실행 전에 확인하세요.

### 실수로 커밋했다면

1. **아직 push 하지 않았다면** — `git rm --cached <파일>` 로 추적에서 빼고, `.gitignore` 에 규칙이 있는지 확인한 뒤 다시 커밋하세요.
2. **이미 push 했다면** — 스스로 이력을 재작성하지 말고 **먼저 비공개로 보고하세요**(아래 연락처). 무엇이 노출됐는지에 따라 대응이 갈리며, 성급한 force push 는 다른 사람의 작업을 깨뜨리면서 정작 회수는 못 합니다.
3. **어느 경우든 노출된 값은 "이미 공개된 것"으로 간주하고 다루세요.**

---

## 보안 취약점 보고

보안 취약점을 발견하셨다면 **공개 이슈로 제출하지 마시고** 다음 방법으로 비공개 보고해주세요.

### 보고 방법

**GitHub Security Advisory** (권장):
https://github.com/kiunsea/jangbogo/security/advisories/new

또는

**GitHub Issues** (민감하지 않은 경우):
https://github.com/kiunsea/jangbogo/issues

### 보고 시 포함할 정보

1. **취약점 유형**
   - SQL Injection, XSS, CSRF, 인증 우회, 경로 탐색, 개인 데이터 노출 등

2. **영향 범위**
   - 어떤 기능에 영향을 미치는지
   - 공격 시나리오

3. **재현 방법**
   - 단계별 재현 절차
   - PoC (Proof of Concept) 코드
   - ⚠️ **재현 자료에 실제 구매 내역·계정 정보를 넣지 마세요.** 합성 데이터로 재현해 주시면 충분합니다.

4. **영향도 평가**
   - 높음 / 중간 / 낮음
   - CVSS 점수 (가능한 경우)

5. **제안 해결 방법** (선택사항)

### 응답 시간

- **초기 응답**: 48시간 이내
- **진행 상황 업데이트**: 주 1회
- **패치 릴리스**: 심각도에 따라 1주일~1개월

---

## 보안 모범 사례

### 1. 관리자 계정 보안

❌ **하지 말아야 할 것:**
```yaml
# application.yml에 평문 저장
admin:
  id: admin
  pass: 1234
```

✅ **권장 방법:**
```bash
# 환경 변수 사용
set ADMIN_ID=strong_admin_id
set ADMIN_PASS=VeryStr0ng!P@ssw0rd#2025
```

또는:
```properties
# config/admin.properties (Git 제외 — .gitignore 에 등록돼 있습니다)
admin.id=strong_admin_id
admin.pass=VeryStr0ng!P@ssw0rd#2025
```

기본값을 그대로 쓰지 마세요. 애플리케이션은 `config/admin.properties` 를 선택적으로 import 하며, 환경 변수가 우선합니다.

### 2. 네트워크 보안

✅ **기본 설정 (안전)**
```yaml
server:
  address: 127.0.0.1  # localhost만 허용
  port: 8282
```

❌ **외부 노출 금지**
```yaml
# 이렇게 설정하지 마세요!
server:
  address: 0.0.0.0  # 모든 네트워크 인터페이스
```

관리자 세션은 30분 미사용 시 만료되며, 인증 인터셉터가 전역으로 걸립니다. `/actuator` 류의 인증 예외는 두지 않습니다 — 지금 의존성이 없더라도 나중에 추가되는 순간 무인증으로 열리기 때문입니다.

### 3. 데이터베이스 파일 보안

- ✅ DB 파일과 그 `-wal`/`-shm` 동반 파일의 **접근 권한을 제한**하세요. 셋 다 같은 개인 데이터입니다.
- ✅ 백업을 만든다면 **저장소 밖**에 두고, 백업 폴더도 접근 권한을 제한하세요.
- ✅ Windows 파일 시스템 암호화(BitLocker) 권장.
- ❌ DB 파일을 이슈·PR·메신저·클라우드 공유 폴더에 올리지 마세요. 파일 하나가 곧 구매 이력 전체입니다.

### 4. 쇼핑몰 계정·세션 보안

- ✅ 쇼핑몰 계정 정보는 AES 로 암호화되어 저장됩니다.
- ✅ 저장된 브라우저 세션 스냅샷도 암호화해서 보관하며, **평문으로 디스크에 쓰는 경로를 두지 않습니다.** 스냅샷은 살아 있는 인증 토큰이기 때문입니다.
- ✅ 세션 값(쿠키·키)은 로그·HTTP 응답·`toString` 어디에도 싣지 않습니다.
- ✅ `config/mall_account.yml` 과 브라우저 프로필 폴더(`profiles/`)는 `.gitignore` 로 제외돼 있습니다.
- ⚠️ 세션 재사용 기능은 **기본으로 꺼져 있습니다.** 마스터 스위치와 쇼핑몰별 옵트인이 모두 켜져야 동작하는 이중 게이트입니다.

### 5. 암호화 키

- ⚠️ **기본 암호화 키는 소스에 포함돼 있으므로 이미 공개된 값입니다.** 실제로 쓰실 때는 반드시 덮어쓰세요.
- ✅ `jangbogo.crypto.key` / `jangbogo.crypto.iv` 시스템 프로퍼티 또는 `JANGBOGO_CRYPTO_KEY` 환경 변수로 지정할 수 있습니다. 기본값을 쓰면 기동 시 경고가 남습니다.
- ⚠️ 키를 바꾸면 **기존 DB 에 저장된 암호문을 더 이상 열 수 없습니다.** 처음 설치할 때 정하는 것이 가장 안전합니다.

### 6. 로그·스크린샷 보안

정확히 알아 두셔야 할 부분입니다.

- ✅ **비밀번호·세션 값·암호화 키는 로그에 남기지 않습니다.** 계정 객체의 `toString` 도 비밀번호를 마스킹합니다.
- ⚠️ **그렇다고 로그 파일이 안전한 것은 아닙니다.** 애플리케이션 로거는 DEBUG 로 동작하므로 **구매 상세가 로그에 남습니다.**
- ⚠️ **수집 실패 스크린샷(`logs/screenshots/`)에는 주문 목록 화면이 그대로 찍힙니다.** 진단에 필요해서 저장하는 것이며, 30일이 지나면 자동 정리됩니다.
- ❌ 따라서 **로그 폴더를 통째로 공유하지 마세요.** 이슈에 첨부할 때는 필요한 줄만 발췌하고 개인 정보를 지우세요.
- 로그·스크린샷 폴더는 `.gitignore` 로 제외돼 있고, 추적 파일 검사와 배포 산출물 검사가 함께 감시합니다.

---

## 알려진 보안 고려사항

### 1. Selenium WebDriver

**이슈**: Selenium 이 실제 브라우저를 제어해 쇼핑몰에 로그인합니다.

**완화 방법**:
- WebDriver 는 로컬에서만 실행됩니다.
- 캡처용 브라우저는 전용 프로필을 쓰며, 서버가 유휴·만료 상태를 회수합니다.
- 프로필 단위 파일 락으로 두 프로세스가 같은 프로필을 동시에 열지 못하게 막습니다(서비스 인스턴스와 사용자가 직접 띄운 인스턴스는 다른 프로세스입니다).

### 2. 로컬 데이터 저장

**이슈**: 모든 데이터가 로컬 파일로 저장됩니다.

**완화 방법**:
- 파일 접근 권한 관리와 디스크 암호화(BitLocker 등).
- 백업은 저장소 밖에, 접근 권한을 제한해서.

### 3. localhost 바인딩

**이슈**: 서버는 localhost(127.0.0.1)에만 바인딩됩니다.

**장점**: 외부에서 직접 접근할 수 없습니다.
**단점**: 원격 접속이 불가능합니다.

원격 접속이 필요하면 VPN 을 쓰거나, 리버스 프록시(nginx/Apache) + HTTPS + 인증을 앞에 두세요. `0.0.0.0` 으로 직접 여는 방식은 권장하지 않습니다.

### 4. 개발용 기능

**이슈**: 데이터를 통째로 지우는 개발용 엔드포인트가 존재합니다.

**완화 방법**: `dev` 프로파일 가드가 붙어 있어 `--spring.profiles.active=dev` 로 띄우지 않으면 **빈 자체가 등록되지 않습니다.** 운영 배포본에서는 존재하지 않습니다.

---

## 보안 업데이트 내역

최근 릴리스의 보안 관련 변경 요약입니다. 전체 내역은 [CHANGELOG.md](CHANGELOG.md) 를 참고하세요.

### v0.18.x (2026-08)

- 다른 프로세스가 쥔 **살아 있는 브라우저 세션을 강제 종료할 수 있던 경로**를 막았습니다 — 프로필 락을 획득한 상태에서만 정리하고, 못 잡으면 물러납니다.
- **세션 만료 감지**를 추가해 죽은 세션으로 계속 두드리지 않고 해당 쇼핑몰을 일시중단합니다.
- 기능이 "테스트는 초록인데 실제 호출자가 0건"인 채로 완성되는 것을 막는 **배선 감시 가드**를 도입했습니다.

### v0.17.0 (2026-08)

- **SQL 주입 두 형태 제거**: 수집한 페이지 텍스트에서 만든 값이 조건절에 그대로 조립되던 자리와, 따옴표 없는 숫자 자리. 후자는 방어라고 있던 필터가 값을 *거르는 게 아니라 깎는* 방식이라 **다른 행을 조용히 열어 줬습니다.** 엄격 파싱 + 파라미터 바인딩으로 교체했습니다.
- 롤백 누락으로 **원래 실패 원인이 덮이던 자리**들을 정리했습니다.
- DAO 보안 가드를 **전수 스캔으로 승격**하고, 소스 트리의 백업·임시 파일을 감시하는 가드를 신설했습니다.

### v0.16.x (2026-08)

- 브라우저 **세션 스냅샷을 암호화해 저장**합니다. 저장할 때마다 전용 키/IV 를 새로 만들고, 평문으로 디스크에 쓰는 경로를 두지 않습니다.
- 살아 있는 세션을 쥔 **캡처 브라우저를 서버가 회수**합니다(탭이 닫히거나 페이지를 벗어나도).
- 재캡처 저장이 실패해도 **이전 세션이 파손되지 않도록** 원자적으로 교체합니다.
- **세션 값(쿠키·키)은 로그·HTTP 응답·`toString` 어디에도 싣지 않습니다.**

### v0.15.0 (2026-08)

- 세션 프로필 재사용에 **기본 꺼짐 마스터 스위치 + 쇼핑몰별 옵트인**의 이중 게이트를 뒀습니다.
- 프로필 이름에 경로 구분자나 상위 참조가 섞이면 **거부**합니다(이름이 DB 에서 오므로 루트를 벗어날 수 있습니다).
- 프로필 루트 기본값을 저장소 트리 밖으로 잡아, 개발 환경에서 로그인 세션이 커밋되는 경로를 없앴습니다.
- 프로세스 간에 유효한 **파일 락**을 도입했습니다.

### v0.14.0 (2026-08)

- **개발용 파괴 엔드포인트를 운영 산출물에서 제외**했습니다(`dev` 프로파일 가드).
- **암호화 키를 외부에서 지정**할 수 있게 했습니다(시스템 프로퍼티/환경 변수). 기본값 사용 시 기동 경고.
- 수집한 값이 들어가던 **SQL 문자열 조립을 제거**하고 PreparedStatement 로 통일했습니다.
- 쓰지도 않는 **`/actuator` 인증 예외를 미리 제거**했습니다.

### v0.13.10 (2026-08)

- 배포 ZIP 에 **라이선스 전문(`LICENSE`)·`NOTICE`·대응 소스 안내가 빠져 있던 문제**를 고쳤습니다. AGPL-3.0 제4·6조 준수 사항이며, 자동 검사로 재발을 막습니다.

### v0.8.0 (2026-04)

- 실패 스크린샷 서빙 API 에서 **지정된 폴더 밖 파일 접근(경로 탐색)을 차단**했습니다.

### v0.6.0 (2026-01)

- 중복 수집 방지 강화, 내보내기 식별자 누락 수정.

### v0.5.0 (2025-11)

- 관리자 계정 환경 변수 지원, 세션 타임아웃(30분), localhost 전용 바인딩, 쇼핑몰 계정 AES 암호화, 전역 세션 검사.

---

## 보안 체크리스트

### 저장소에 커밋하기 전

- [ ] `git status` 로 담길 파일을 눈으로 확인했는가 (`git add -A` / `git add .` 를 쓰지 않았는가)
- [ ] DB·로그·내보내기·스크린샷·브라우저 프로필이 포함돼 있지 않은가
- [ ] 백업/임시 파일(`.bak`·`.orig`·`.tmp` 등)이 섞이지 않았는가
- [ ] 테스트 픽스처가 합성 데이터인가 (실물 마스킹이 아닌가)
- [ ] `.gitignore` 규칙을 지우지 않았는가

### 배포 전

- [ ] 기본 관리자 계정을 변경했는가
- [ ] 암호화 키를 외부 값으로 지정했는가
- [ ] 서버가 `127.0.0.1` 에 바인딩돼 있는가 (외부 노출 시 HTTPS + 인증)
- [ ] 배포 ZIP 산출물 검사가 통과했는가 (실패 시 빌드가 멈춥니다)
- [ ] 라이선스 전문·고지가 배포물에 포함되는가
- [ ] 의존성 취약점 검사 (Dependabot 등)

---

## 의존성 보안

### 자동 보안 스캔

GitHub Dependabot 을 활성화해 의존성 취약점을 자동으로 검사하세요.

### 수동 검사

```bash
# 현재 의존성 트리 확인
.\gradlew dependencies

# 테스트 전체 실행 (보안 가드 포함)
.\gradlew test
```

> 취약점 스캔 플러그인(OWASP dependency-check 등)은 현재 빌드에 적용돼 있지 않습니다. 필요하면 별도로 추가해 주세요.

### 주요 의존성

- Spring Boot 3.5.6 (Log4j2 사용, Logback 제외)
- Selenium 4.31.0
- SQLite JDBC 3.45.3.0
- Jackson (Spring Boot 관리 버전)
- Apache Commons Net 3.11.1
- Bootstrap 5

---

## 책임 있는 공개 (Responsible Disclosure)

1. **비공개 보고**: 취약점 발견 시 비공개로 보고
2. **확인 및 수정**: 확인 후 패치 작성
3. **패치 릴리스**: 보안 패치 릴리스
4. **공개**: 패치 릴리스 후 90일 이내 공개
5. **크레딧**: 보고자에게 크레딧 제공

---

## 보안 관련 리소스

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE - Common Weakness Enumeration](https://cwe.mitre.org/)
- [Spring Security Guide](https://spring.io/guides/topicals/spring-security-architecture)

---

## 라이선스

이 보안 정책은 프로젝트의 AGPL-3.0-or-later 라이선스에 따릅니다.

---

## 연락처

보안 관련 문의:

- **Email**: kiunsea@gmail.com
- **Website**: https://jiniebox.com
- **GitHub Issues**: https://github.com/kiunsea/jangbogo/issues (비보안 이슈만)

---

**마지막 업데이트**: 2026-08-07
**버전**: 0.18.1

**Copyright © 2025 [jiniebox.com](https://jiniebox.com)**
