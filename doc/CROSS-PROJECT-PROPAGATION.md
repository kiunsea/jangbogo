# 프로젝트 간 전파 (Cross-Project Propagation)

> 작성 2026-07-28 · 갱신 2026-08-07 · 상태: **운용 중 — 대장 21행 (열림 12 / 보류 1 / 닫힘 8)**
> 열림 12 내역: 수신측 검토 대기 11 (OPS 사례 1 · 파생 관찰 3 · P3-1 · P4 소급 6) ·
> jangbogo 내부 미구현 1 (GAP-1). **역방향 이식 대기는 P1-8 하나뿐이고 그것은 `보류` 다** —
> 즉 지금 열려 있는 것 중 이쪽이 코드로 해야 할 일은 GAP-1 뿐이다.
>
> **2026-08-07 에 바뀐 것.** (1) 역방향 2건(P1-5·P1-7)과 내부 2건(P1-6·P1-9)은 코드가 이미 착지해
> 있었는데 대장만 열린 채였다 — 코드로 확인하고 닫았다. (2) v0.13.7~v0.16.1 구간이 대장에 한 줄도
> 없어 소급 등재했다(P4-1~P4-6). (3) 발송측 배송 상태 부재를 미결로 등재했다(GAP-1).

**이 문서는 PUBLIC 저장소에 있다.** 로컬 절대경로, 비공개 저장소의 이름·내부 구조·파일 경로·
자격증명·고객명은 적지 않는다. 이쪽(jangbogo) 코드 위치는 저장소 기준 상대경로 + 줄번호로 적고,
**수신측 위치는 적지 않는다** — 대장 번호로만 가리키고 실제 매핑은 수신측 사본이 갖는다.

> **왜 이렇게 나눴나 (2026-08-02 결정).** 초판은 수신측의 모듈 배치·클래스명·줄번호를 그대로
> 적고 있었다. 그건 비공개 저장소의 내부 지도이며, 미확인 결함 항목은 좌표가 붙은 약점 목록이
> 된다. 대장의 운용 가치(무엇이 열려 있는가)는 번호만으로 유지되므로, **판정과 상태는 공개하고
> 수신측 좌표는 비공개로 내렸다.**

---

## 1. 목적과 범위

이 프로젝트에 외부 자동화 패키지를 import 하는 방안은 폐기했다. 이유는 두 가지다. 첫째,
공개 저장소가 비공개 저장소를 빌드 타임 의존으로 삼으면 CI·외부 기여자·릴리스 수령자 모두에게
**재현 불가능한 빌드**가 된다. 둘째, 발행 좌표에 브라우저를 모는 concrete 구현이 없어 **목표
기여도가 0** 이었고 오히려 재구현 부채만 늘었다.

(초기 검토에는 라이선스 사유도 적혀 있었으나, 2026-08-02 에 배포 형태를 다시 따져 본 결과 그
서술은 결합을 전제한 낡은 것이었다. 폐기 결정을 지탱하는 것은 위 두 가지다.)

**대신 채택한 것이 전파다.** 코드 의존은 두지 않고, 한쪽에서 나온 버그 수정·기능 개선이 다른
쪽에도 **적용 검토**되도록만 한다. 강제 적용이 아니라 검토 의무이며, 두 저장소를 한 사람이
유지하므로 절차는 최소로 둔다. 새 도구·이슈 트래커·라벨 체계를 도입하지 않는다 — 양쪽 다
GitHub 이슈 실사용이 0 이라 도입 즉시 사문화된다.

범위: 브라우저 자동화 공통 표면(드라이버 옵션·클릭·대기·프로필), 무음 실패/감지 부재 계열,
설정·스키마 정본 이중화. 범위 밖: 쇼핑몰 파서, `jbg_*` 스키마, 트레이/WinSW 배포, UI 문구.

---

## 2. 전파 절차

기존 관행에 얹는다. 새로 만드는 것은 이 문서(대장) 하나뿐이다.

### 2.1 판정 기준 (후보 필터)

한 문장 기준: **"이 결함이 브라우저를 모는 코드라면 프로젝트와 무관하게 재현되는가?"** 예면 후보다.

| 후보다 | 후보가 아니다 |
|---|---|
| 드라이버 옵션 조립·전달, headless 결정, 타임아웃 설정 | 쇼핑몰별 셀렉터·파싱 로직 |
| 클릭 가로채기·대기 전략·오버레이 대응 | `jbg_*` 테이블 스키마 자체 |
| 실패가 예외가 아닌 정상 반환값으로 흡수되는 자리 | 트레이 앱·WinSW·설치 배포 스크립트 |
| 프로필/락/동시성 등 프로세스 간 자원 경합 | UI 문구, 번역, 릴리스 노트 |
| 설정·스키마의 정본이 두 곳에 복제된 자리 | 코드 의존을 전제로만 성립하는 개선안 |
| 운영 중 실제로 발생한 장애의 재발 방지책 | 이미 상대가 대응한 것 (대장에서 확인) |

후보가 아닌 것은 **대장에 올리지 않는다.** 후보인데 상대가 이미 대응했으면 **판정만 남기고 닫는다**
(재조사 방지가 대장의 두 번째 목적이다).

### 2.2 기록 위치와 형식

기록은 **한 곳**이다 — 이 문서의 §3 대장. 그 외에는 기존 그릇에 한 줄씩만 남긴다.

| 시점 | 어디에 | 무엇을 |
|---|---|---|
| 이쪽에서 변경이 커밋될 때 | 해당 버전의 `CHANGELOG.md` `### Notes` | 한 줄. 예: "safeClick 도입은 상대측에도 적용 검토 대상으로 전파됨(대장 P1-5)" |
| 같은 시점 | 해당 항목의 `DEVLOG.md` `#### 메모` | 한 줄. 판단 근거·후속만 |
| 같은 시점 | 이 문서 §3 대장 | 행 1개 추가 (또는 기존 행의 상태 갱신) |
| 사례가 서술을 요구할 때만 | 수신측 저장소에 사례 문서 1개 | 첫 사례가 이 형태다 (§3 OPS-2026-05-30 행) |

`### Notes` 는 이 프로젝트의 CHANGELOG 가 이미 "영향 범위·발행 여부"를 적는 데 쓰는 로컬 확장
슬롯이므로 새 섹션을 만들지 않는다. 커밋 메시지는 **관행대로 `v<x.y.z>: 제목`** 을 유지하고,
제목이나 본문에 `(전파 P1-N)` 처럼 대장 번호를 넣어 검색 가능하게 한다.
`Co-Authored-By:` trailer 는 붙인다.

> **규칙: 공개 산출물에는 대장 번호만 쓴다.** `CHANGELOG.md`·`DEVLOG.md`·커밋 메시지는 전부
> PUBLIC 이다. 거기에 **상대 저장소 이름을 쓰지 않는다** — 번호와 이름이 한 문장에서 만나는
> 순간 대장 전체가 역매핑 가능해지고, 좌표를 비공개로 내린 의미가 사라진다. "상대측"·"수신측"
> 같은 일반 표현을 쓰고, 어느 저장소인지는 수신측 사본이 안다.
>
> 이 규칙은 **실제로 어긴 뒤에 세운 것이다.** v0.13.6 의 `### Notes` 한 줄이 대장 번호와
> 저장소 이름을 함께 적었다(2026-08-02 에 정정).

### 2.3 검토 결과 처리

수신측이 내리는 판정은 셋뿐이다. 어느 쪽이든 **대장 행의 `판정`·`상태` 컬럼 갱신으로 끝난다.**

| 결과 | 대장 표기 | 추가로 남기는 것 |
|---|---|---|
| 적용 | 판정 `전파` / 상태 `닫힘(적용)` | 수신측 CHANGELOG 항목에 출처 표기. 코드가 착지한 시점에만 |
| 해당 없음 | 판정 `해당없음` / 상태 `닫힘` | 왜 성립하지 않는지 한 줄 (근거 위치는 수신측 사본에) |
| 이미 대응됨 | 판정 `이미대응` / 상태 `닫힘` | 재조사 방지가 핵심이므로 판정 사유만 남긴다 |
| 보류 | 판정 유지 / 상태 `보류` | 재검토 조건을 한 줄로 (날짜가 아니라 **조건**으로 적는다) |

`보류` 에 날짜를 적지 않는 이유: 한 사람이 유지하므로 기한은 지켜지지 않고, 조건("프로필 재사용
도입 이후", "본체 저장소를 로컬에 확보한 뒤")이 실제 트리거로 작동한다.

---

## 3. 전파 대장 (Ledger)

`판정` 값: `전파`(상대 검토 필요) / `해당없음` / `이미대응` / `역방향`(상대가 우위 — 이쪽이 가져올 것)
`상태` 값: `열림` / `보류` / `닫힘`

**수신측 대응 지점은 이 표에 적지 않는다.** 대장 번호가 곧 키이며, 실제 파일·줄번호 매핑은
수신측 사본이 갖는다. 아래 `근거` 컬럼은 이 저장소 자신의 코드 위치이므로 그대로 적는다.

> **근거 컬럼은 "지금 그 판단을 다시 찾을 수 있는 위치"다** (2026-08-07 규칙화). 결함이 발견된
> 당시 위치가 아니라 **현재 그 사실이 사는 곳**을 적는다. P1-9 가 그 이유다 — 근거가 통합 이전의
> `ExportService`·`data.sql` 로 남아 있어, 통합이 끝난 뒤에는 그 좌표를 따라가도 "여러 곳에서
> 갈렸다"는 사실도 "한 곳으로 모았다"는 사실도 나오지 않았다. 항목을 닫을 때 근거도 함께 옮긴다.
>
> **번호 접두사.** `OPS-<일자>` 운영 장애 사례 · `P<n>-<m>` 조사 배치에서 나온 결함 · `D-<n>`
> 매핑 중 나온 파생 관찰 · `GAP-<n>` **결함이 아니라 부재**(아직 만들지 않은 것).
> `P` 뒤의 숫자는 **배치 일련번호일 뿐 이쪽 개발 Phase 와 대응하지 않는다** — `P4` 는 Phase 4A/4B
> (엔진 판정 게이트)와 무관한, 2026-08-07 의 버전 소급 등재 배치다.

| 일자 | jangbogo 변경 | 근거 (이 저장소) | 판정 | 상태 |
|---|---|---|---|---|
| 2026-07-28 | **OPS-2026-05-30** 운영 장애 사례 인계 (감지 부재 계열) | 운영 로그 (배포본 보존분) | 전파 | 열림 |
| 2026-07-28 | **P1-1** `new ChromeDriver()` 가 조립한 ChromeOptions 를 폐기 | `src/.../svc/util/WebDriverManager.java` | 이미대응 | 닫힘 |
| 2026-07-28 | **P1-2** headless 조건 반전 | `src/.../svc/util/WebDriverManager.java` | 해당없음 | 닫힘 |
| 2026-07-28 | **P1-3** 비-Spring-bean 에 `@Autowired` 라 설정이 영원히 미반영 | `src/.../svc/util/WebDriverManager.java` (수동 생성 5곳) | 이미대응 | 닫힘 |
| 2026-07-28 | **D-1** (P1-3 파생 관찰) | 이 대장 P1-3 조사 | 전파 | 열림 |
| 2026-07-28 | **P1-4** `signin()` 이 하드코딩 `return false` | `src/.../svc/mall/Ssg.java` | 해당없음 | 닫힘 |
| 2026-07-28 | **D-2** (P1-4 파생 관찰) | 이 대장 P1-4 조사 | 전파 | 열림 |
| 2026-07-28 | **P1-5** `safeClick` 헬퍼 도입 후 click 지점 교체 (2026-05-30 오버레이 대응) | `src/.../svc/util/ClickUtil.java` + 호출부 `svc/mall/{Ssg,Emart,Oasis,Hanaro}.java` | 역방향 | 닫힘 (이식 완료, 2026-08-07 확인) |
| 2026-07-28 | **D-3** (P1-5 파생 관찰) | 이 대장 P1-5 조사 | 전파 | 열림 |
| 2026-07-28 | **P1-6** 죽은 로컬 포함 빌드 선언 제거 (+ `CLAUDE.md` 동반 삭제) | `settings.gradle`, `CLAUDE.md`(§외부 의존) | 해당없음 | 닫힘 (내부 완료, 2026-08-07 확인) |
| 2026-07-28 | **P1-7** `jbg_collect_log` 자동 생성 여부 검증 | `src/main/resources/schema.sql`, `src/.../dao/SchemaMigrator.java` | 역방향 | 닫힘 (이식 완료, 2026-08-07 확인) |
| 2026-07-28 | **P1-8** 프로필 디렉터리 단위 락 — JVM 로컬 집합은 두 번째 프로세스를 못 막는다 | `src/.../svc/JangBoGoManager.java` | 역방향 | 보류 (조건: 프로필 재사용 도입 커밋과 **같은 커밋**) |
| 2026-07-28 | **P1-9** `seq`→몰 id 해석이 여러 곳에서 갈림 | `src/.../svc/mall/MallRegistry.java` (통합처. 갈려 있던 자리는 이 클래스 javadoc 의 "통합 전 seq=1 의 매핑" 표에 남겼다) | 해당없음 | 닫힘 (내부 완료, 2026-08-07 확인) |
| 2026-08-02 | **P3-1** Edge 분기 페이지 로드 타임아웃 부재 — Chrome 만 60초로 낮추고 Edge 는 기본 300초 잔존 (v0.13.6 수정) | `src/.../svc/util/WebDriverManager.java` `buildEdgeOptions()` | 전파 | 열림 |
| 2026-08-07 | **P4-1** 기동 실패가 남긴 고아 브라우저가 프로필 락을 쥐어 이후 재시도가 전부 막힘 (v0.16.1 수정) | `src/.../svc/util/WebDriverManager.java` `killOrphanProfileChrome()` | 전파 | 열림 |
| 2026-08-07 | **P4-2** 드라이버 옵션의 User-Agent 하드코딩 — UA 는 덮어써도 클라이언트 힌트는 실제 버전을 내보내 자기모순이 됨 (v0.15.7 제거) | `src/.../svc/util/WebDriverManager.java` `buildChromeOptions()` | 전파 | 열림 |
| 2026-08-07 | **P4-3** 프로필 인자에 `--` 를 빠뜨리면 최신 chromedriver 가 **조용히 무시**하고 매번 새 임시 프로필을 염 (v0.15.5) | `src/.../svc/util/WebDriverManager.java` (프로필 오버로드), `src/test/.../WebDriverOptionsTest.java` | 전파 | 열림 |
| 2026-08-07 | **P4-4** 브라우저 프로세스를 직접 띄우고 회수할 때의 함정 3종 (표준입출력 상속 · `waitFor` 오해 · `quit()` 이 attach 한 브라우저를 안 닫음) (v0.15.5·v0.15.6) | `src/.../svc/util/NativeChromeLoginLauncher.java`, `src/.../svc/util/WebDriverManager.java` `closeAttachedBrowser()` | 전파 | 열림 |
| 2026-08-07 | **P4-5** 디버깅 포트를 고정 번호로 열면 **남의 브라우저에 붙어** 살아 있는 인증 쿠키를 뜨게 됨 (v0.15.6, 고정 번호 거부) | `src/.../svc/util/NativeChromeLoginLauncher.java` (`ANY_DEBUG_PORT`) | 전파 | 열림 |
| 2026-08-07 | **P4-6** 프로필 락과 동시 실행 제한기의 **획득 순서** — 제한기 바깥 / 프로필 락 안쪽 (v0.15.10 규칙화) | `src/.../svc/util/MallProfileLock.java` 클래스 javadoc | 전파 | 열림 |
| 2026-08-07 | **GAP-1** 발송측 배송 상태 부재 — 무엇이 미도달인지 DB 어디에도 없다 (§3.4) | `src/.../svc/MallSchedulerService.java` `processFtpUpload()`, `src/.../svc/util/FtpPendingQueue.java`, `jbg_export_config.last_ftp_upload_time` | 해당없음 | 열림 (내부, 미구현) |

### 3.1 대장이 말하는 것

- Phase 1 의 9건 중 **상대측에 새로 올릴 전파 항목은 0건이었다.** 새로 열린 것은 매핑 과정에서
  나온 파생 관찰 3건(D-1·D-2·D-3)이다. 관찰 내용과 확인 방법은 수신측 사본에만 둔다.
- 9건 중 실질이 남는 P1-5·P1-7·P1-8 은 전부 **역방향** — 상대가 이미 해결한 것을 이쪽이
  내려받는 쪽이다. Phase 1 은 실질적으로 "상대 → 이쪽 이식"이었다.
- P3-1 은 그 뒤 처음으로 열린 정방향 항목이다. 타임아웃을 브라우저 분기마다 따로 설정하는
  구조는 프로젝트와 무관하게 재현되므로 §2.1 필터를 통과한다.
- **(2026-08-07) Phase 1 의 실질 3건 중 2건(P1-5·P1-7)은 이미 이쪽에 착지해 있었다.** 즉 Phase 1 의
  "상대 → 이쪽 이식"은 P1-8(보류) 하나만 남았다. 내부 2건(P1-6·P1-9)도 끝나 있었다. **네 건 모두
  코드는 끝났는데 대장만 열린 채였다** — 대장이 스스로 "열림 9건 전부 수신측 대기"로 읽히게 만든
  원인이 이것이다. 상태줄의 열린 건수는 이 문서의 유일한 운용 지표이므로(§5), 갱신을 빠뜨리면
  지표가 아니라 소음이 된다.
- **정방향은 이제 7건(P3-1 + P4-1~P4-6)이다.** Phase 1 의 "정방향 0건"은 조사 범위가 드라이버 옵션
  한 파일이었기 때문이지 이쪽에 보낼 것이 없어서가 아니었다. §3.3 참조.

### 3.2 Phase 1 수정 순서 주의 (내부, 전파와 무관하나 대장에 남긴다)

P1-1·P1-2·P1-3 은 서로를 은폐하고 있었다. 당시 순 관측 동작은 "항상 headed" 인데, P1-1(options
전달)만 먼저 고치면 P1-2·P1-3 이 남은 상태에서 "항상 headless" 로 뒤집힌다. **순서는 P1-3 →
P1-2 → P1-1** 로 고정하고 커밋마다 브라우저가 실제로 뜨는지 육안 확인했다.

### 3.3 P4 — v0.13.7 ~ v0.16.1 소급 등재 (2026-08-07)

**이 구간의 18개 버전이 대장에 한 줄도 없었다.** §5 는 "push(=버전) 시점에 §2.1 필터를 한 번
통과시킨다"고 정하고 있는데 그 절차가 이 구간 내내 돌지 않았다. 원인은 명백하다 — 필터를 돌리는
자리가 사람의 습관에만 있고 릴리스 절차 어디에도 걸려 있지 않다. 이 등재가 그 공백을 메운다.

**전부 옮기지 않았다.** 18개 버전에서 6건만 골랐다. 기준은 §2.1 한 문장이며, **고른 이유와 고르지
않은 이유를 둘 다 남긴다** — 재조사 방지가 이 대장의 두 번째 목적이기 때문이다(§2.1).

| 번호 | 출처 | 왜 후보인가 (한 줄) |
|---|---|---|
| P4-1 | v0.16.1 | **실패가 스스로를 굳힌다.** 드라이버는 브라우저를 띄운 *뒤에* 세션 핸드셰이크를 하므로, 핸드셰이크가 깨지면 `quit()` 으로 닿을 수 없는 브라우저가 프로필 락을 쥔 채 남고 이후 재시도가 전부 "이미 사용 중"으로 막힌다. §2.1 "프로세스 간 자원 경합" + "실제 발생한 장애의 재발 방지책" 두 줄에 동시에 걸린다 |
| P4-2 | v0.15.7 | UA 문자열만 덮어써도 **클라이언트 힌트는 실제 버전을 그대로 낸다.** 자기를 124 라 주장하며 힌트는 150 을 내는 자기모순이 되고, 값이 묵을수록 벌어진다. UA 를 옵션에 박는 코드는 어느 프로젝트에서나 같은 결과가 된다 |
| P4-3 | v0.15.5 | **무음 실패다.** 접두사 하나가 빠지면 최신 드라이버가 인자를 조용히 버리고 매번 새 임시 프로필을 연다 — 실패가 아니라 "로그인 안 된 상태로 성공"이라 로그로는 잡히지 않는다 |
| P4-4 | v0.15.5·v0.15.6 | 브라우저를 프로세스로 직접 띄우는 코드가 공통으로 밟는 함정이다. 표준입출력을 물려주면 **부모가 먼저 끝나지 못하고**(실측 3시간 13분 점유), `waitFor()` 는 창 수명이 아니며, `quit()` 은 붙은(attach) 브라우저를 닫지 않아 로그인된 창이 남는다 |
| P4-5 | v0.15.6 | **보안 사고 형태가 일반적이다.** 디버깅 포트를 고정 번호로 열면 그 포트를 이미 쓰고 있던 다른 브라우저에 붙어 **남의 살아 있는 인증 쿠키를 뜬다.** 포트는 브라우저가 고르게 하고 실제 값을 되읽는 것이 유일하게 안전한 형태다 |
| P4-6 | v0.15.10 | 프로세스 경계 락과 JVM 내 동시 실행 제한기를 함께 쓰면 **획득 순서가 곧 성능 결함**이 된다. 락을 바깥에 두면 세마포어 대기 내내 프로세스 경계 락을 놀리면서 쥔다. 둘 다 가진 코드라면 프로젝트와 무관하게 성립한다 |

**등재하지 않은 것과 이유** (다시 훑지 않기 위해 남긴다):

| 대상 | 왜 아닌가 |
|---|---|
| v0.13.7~v0.13.9 이마트 영수증 바코드·serial 복구 | 특정 몰의 DOM 개편 대응. §2.1 "쇼핑몰별 셀렉터·파싱 로직" 정면 |
| v0.13.10 배포 ZIP 라이선스 고지 | §2.1 "설치 배포 스크립트" 정면. 의무의 내용도 배포 형태에 따라 달라 그대로 옮길 수 없다 |
| v0.14.0 보안 하드닝 4건 (dev 엔드포인트 · 하드코딩 키 · SQL 조립 · actuator) | 브라우저 자동화 공통 표면이 아니다. 하드코딩 키는 **이 저장소가 PUBLIC 이라는 전제**에서 나온 것이라 비공개 수신측에는 같은 형태로 성립하지 않는다 |
| v0.15.0~v0.15.4 · v0.15.8 · v0.15.9 · v0.16.0 세션 프로필·캡처 기능 일체 | 이 프로젝트의 기능 설계 자체다. 코드 의존 없이 옮길 수 있는 단위가 아니며(§1 폐기 사유와 같은 이유), 그 안에서 **단위로 떼어낼 수 있는 일반 결함만** 위 6건으로 뽑았다 |
| P1-7 재확인분(`SchemaMigrator` 의 schema.sql 단일 선언) | 이미 P1-7 로 등재돼 있다. 중복 등재는 대장을 못 믿게 만든다 |

### 3.4 GAP-1 — 발송측 배송 상태가 없다 (미결, 이번에는 등재만)

**대장은 "배송 상태"라는 말을 쓰지만 이 프로젝트에 그런 상태는 없다.** 확인한 것은 다음이다.

| 지금 남는 것 | 어디에 | 무엇을 알 수 없나 |
|---|---|---|
| 마지막 FTP 전송 시각 **하나** | `jbg_export_config.last_ftp_upload_time` (단일 레코드) | 전역 시각 하나라 **어느 몰·어느 주문**이 그 전송에 실렸는지 알 수 없다 |
| 보류 파일 자체 | `{savePath}/pending/` 디렉터리 | 파일이 곧 상태다. **DB 에는 아무 흔적도 없다** — 어느 주문이 어느 파일에 실려 보류 중인지 조회할 방법이 없다 |
| 폐기 경고 로그 | 로그 파일 | 상한(기본 50건 / 14일) 초과분은 **경고 한 줄만 남기고 사라진다.** 로그가 순환하면 그 주문이 미도달이라는 사실 자체가 없어진다 |

**왜 지금 등재하는가.** 도달(ACK) 확인은 수신측 구현이 있어야 성립하지만, **발송측 절반은 전적으로
이쪽에서만 만들 수 있다.** 그런데 그 절반이 통째로 비어 있다는 사실이 대장 어디에도 없어, 열린
항목을 훑어도 "배송 상태는 수신측 대기"로만 읽힌다. 그 오독을 막는 것이 이 행의 일이다.

**필요한 것** (구현 순서가 아니라 요건이다):

1. **전송 단위에 식별자를 준다.** 지금 파일명이 사실상의 식별자인데(`FtpPendingQueue` 가 이름을
   바꾸지 않는 이유도 그것이다), 파일명은 재시도 시 `_r1` 이 붙고 폐기되면 사라진다. 재시도·폐기를
   넘어 유지되는 키가 있어야 나머지가 전부 성립한다.
2. **주문 ↔ 전송 단위의 대응을 DB 에 남긴다.** 내보내기가 **증분**이라(그 회차 신규 주문만 담는다)
   이 대응이 없으면 미도달분을 사후에 재구성할 수 없다. 지금은 파일을 열어 봐야만 알 수 있고,
   폐기된 뒤에는 그마저 없다.
3. **상태를 값으로 남긴다** — 최소 `생성 / 전송함 / 보류 / 폐기`. 특히 **폐기는 로그가 아니라
   레코드여야 한다.** 폐기를 로그로만 남기는 것은 `FtpPendingQueue` 자신이 "조용히 버리면 지금
   고치려는 결함과 같은 종류"라고 적어 둔 바로 그 형태의 잔재다.
4. **도달(ACK)은 이 위에 얹는다.** 위 셋이 있으면 수신측 확인이 붙는 자리는 상태 하나가 늘어나는
   것으로 끝난다. 없으면 ACK 를 받아도 **무엇에 대한 ACK 인지 붙일 곳이 없다.**

**이번에는 등재만 한다.** 이 항목은 스키마(전송 단위 테이블)와 전송 경로(스케줄러·보류 큐)를 함께
건드리므로, 대장 정리와 한 커밋에 섞으면 둘 다 제대로 검토할 수 없게 된다. 구현은 별도 작업으로
둔다 — **부재를 적어 두는 것과 메우는 것을 분리하는 것이 이 대장의 원래 방식이다**(§2.3 의 `보류`
가 같은 형태다).

---

## 4. 역방향도 같은 대장에서 다룬다

**같은 대장, 별도 표 없음.** 판정 컬럼의 `역방향` 값으로 구분한다.

이유는 실측이다 — Phase 1 의 9건 중 실질이 남은 3건이 전부 역방향이었다. 방향별로 표를 나눴다면
같은 항목(P1-5 `safeClick`)이 두 표에 중복 등재되거나, 방향을 잘못 잡은 채로 한 표에만 남았을
것이다. `jangbogo 변경` 컬럼은 "이쪽에서 관측된 결함"이고, 저쪽이 우위면 그 행이 곧 이식 대상이
된다. 컬럼 의미는 그대로 두고 판정만 뒤집으면 되므로 표를 나눌 이유가 없다.

역방향 항목의 상태 `닫힘` 조건은 이쪽 코드에 반영된 시점이며, 그때 해당 버전의 `### Notes` 에
"상대측에서 이식(대장 P1-N)" 한 줄을 남긴다.

> **P1-5·P1-7 은 이 규칙대로 닫히지 않았다** (2026-08-07 소급 확인). 코드는 진작 착지했는데 그때
> `### Notes` 한 줄도, 대장 갱신도 없었다. 지난 CHANGELOG 를 고쳐 쓰지는 않는다 — 그 시점에
> 하지 않은 판단을 한 것처럼 만드는 것이라, **닫힘 근거는 대장 행의 `근거` 컬럼이 대신 진다.**
> 규칙 자체는 유지한다. 다만 이 두 건이 보여 준 것은, `### Notes` 한 줄이 **커밋과 같은 시점에**
> 붙지 않으면 그 뒤로는 아무도 붙이지 않는다는 것이다.

---

## 5. 운영 규칙

| 질문 | 답 |
|---|---|
| 언제 검토하는가 | **push(=버전) 시점.** `CLAUDE.md` 가 이미 push 마다 CHANGELOG+DEVLOG 갱신을 의무로 두므로, 그 자리에서 §2.1 필터를 한 번 통과시키고 대장 행을 쓴다. 별도 주기 점검·캘린더 없음 |
| 수신측은 언제 보는가 | **세션 시작 시.** 수신측 저장소는 세션 축으로 운영되므로, 그쪽 사본의 열린 항목을 세션 백로그로 옮기는 것이 유일한 운반 수단이다 |
| 누가 | 단독 유지. 리뷰어·승인자 없음 |
| 미검토 항목은 어떻게 드러나는가 | §3 대장의 `상태 = 열림` 행. 이 문서 상단 상태줄에 열린 건수를 적어 둔다 |
| 릴리스를 막는가 | 막지 않는다. 전파는 검토 의무이지 릴리스 게이트가 아니다 |
| 도입하지 않는 것 | GitHub 이슈·라벨·PR 템플릿을 전파 추적 수단으로 쓰지 않는다. 양쪽 다 이슈 실사용 0 이고, 단일 유지자·비공개 수신자 구조에서 이슈 기반 절차는 첫 주에 사문화된다 |

> **이 규칙은 실제로 지켜지지 않았다** (2026-08-07 확인). v0.13.7~v0.16.1 **18개 버전 동안 대장에
> 한 줄도 쓰이지 않았고**(§3.3), 같은 기간에 이미 끝난 항목 4건의 상태도 열린 채로 남아 있었다.
> 원인은 이 표의 첫 줄이 가리키는 자리 — "push 시점에 §2.1 필터를 통과시킨다" — 가 **사람의 습관
> 외에 아무 데도 걸려 있지 않다**는 것이다. `CLAUDE.md` 의 push 절차는 CHANGELOG·DEVLOG 갱신만
> 의무로 두고 대장은 언급하지 않는다.
>
> 새 도구를 도입하지 않는 방침(§2)은 그대로 두되, **다음에 push 절차를 손댈 때 이 대장 한 줄을
> 그 절차에 넣는 것**이 이 공백에 대한 답이다. 넣지 않으면 같은 누락이 반복되며, 그때는 소급
> 등재가 다시 18개 버전어치의 CHANGELOG 재독으로 돌아온다.

### 5.1 이 문서를 PUBLIC 저장소에 두는 것에 대한 판단 — **2026-08-02 재검토 완료**

초판은 "상대경로+줄번호만 쓰고 커밋" 을 선택하고 사례가 3건을 넘으면 재검토하기로 했다.
**재검토 결과 그 기준은 충분하지 않았다.** 문제는 경로의 형태(절대/상대)가 아니라 **무엇을
가리키는가**였다 — 상대경로라도 비공개 저장소의 모듈 배치·클래스명·줄번호를 적으면 그건
내부 지도이고, 거기에 "미확인" 결함을 붙이면 좌표가 딸린 약점 목록이 된다.

**그래서 이렇게 나눴다.**

| 공개(이 문서) | 비공개(수신측 사본) |
|---|---|
| 대장 번호, 일자, 이쪽 변경 요약 | 수신측을 가리키는 모든 것 |
| 이 저장소 자신의 코드 위치 | 파생 관찰(D-N)의 실제 내용과 확인 방법 |
| 판정·상태, 절차와 운영 규칙 | |

운용 가치는 번호와 상태만으로 유지된다 — "무엇이 열려 있는가"를 push 시점에 확인하는 것이
이 대장의 목적이고, 그 답에 수신측 좌표는 필요 없다.

**단, 이 익명화는 앞으로만 유효하다.** 이 저장소의 커밋 이력과 일부 기존 문서 항목에는 상대
저장소 이름과 과거의 로컬 절대경로가 이미 들어 있다(2026-07-28 의 죽은 포함 빌드 선언 제거를
설명하는 대목이 대표적이다). 즉 **여기서 이름을 지운다고 과거 노출이 없어지지는 않는다.**
이 나눔이 하는 일은 (1) 앞으로 좌표·결함 목록이 더 쌓이지 않게 하고, (2) 대장이 비공개
저장소의 내부 지도로 자라지 않게 막는 것이다. 그 이상을 주장하지 않는다.

#### 히스토리 재작성 — **검토 후 하지 않기로 종결 (2026-08-04)**

공개된 이력을 되감을지 실제로 조사하고 결론을 냈다. **하지 않는다.** 재론을 막기 위해 근거를
남긴다.

조사한 것:

| 항목 | 값 |
|---|---|
| 전체 커밋 | 110개 |
| 이름·경로가 들어간 커밋 | 12개 (커밋 **메시지** 1건 포함) |
| 최초 노출 위치 | 이력 초반 → 재작성 시 이후 SHA 전부 변경 |
| 태그 / 릴리스 | 5개 / 5건 (전부 재지정 대상) |
| 포크 / 스타 | **0 / 0** |

**하지 않기로 한 이유는 위험이 커서가 아니다.** 포크가 0이라 "남의 사본이 깨진다"는 문제는
아예 없었다. 결정적이었던 것은 **효과가 보장되지 않는다**는 점이다 — force-push 후에도 도달
불가 객체는 GitHub 이 GC 할 때까지 SHA 직접 접근으로 남는다. 완전히 지우려면 GitHub 지원에
GC 를 따로 요청해야 한다. 즉 110개 커밋의 SHA 를 전부 바꾸고도 목적을 못 이룰 수 있다.

노출된 것의 성질도 함께 봤다 — **비공개 저장소 이름과 개발 PC 의 프로젝트 경로**이며,
자격증명·내부 코드·개인정보는 아니다. 그 대가로 치를 것과 균형이 맞지 않는다.

**대신 앞으로의 노출을 막는 쪽에 걸었다.** §2.2 의 "공개 산출물에는 대장 번호만" 규칙이 그것이고,
현재 트리는 이미 정리했다. 이 판단을 뒤집으려면 **노출 내용이 달라졌을 때**여야 한다 —
자격증명이나 코드가 이력에 들어간 것이 확인되면 그때는 GC 요청까지 포함해 다시 판단한다.

세션 인계용 계획서 2건도 같은 판단으로 **정본을 비공개 저장소로 옮기고 이 저장소에서는
`.gitignore` 처리했다.** 세션 인계 문서는 성질상 운영 환경 세부와 미완 작업이 섞이므로,
공개본을 따로 관리하는 것보다 정본을 비공개에 두는 편이 유지 비용이 낮다.

---

## 6. 상호 참조

- 첫 사례 본문과 수신함, 그리고 대장의 수신측 좌표 매핑: **수신측 저장소의 `doc/`** (비공개)
- 이 저장소의 작업 이력: `DEVLOG.md` · `CHANGELOG.md`
