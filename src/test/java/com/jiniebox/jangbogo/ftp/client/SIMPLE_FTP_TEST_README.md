# SimpleFtpTlsUploadTest - 바로 실행 가능한 FTP TLS 테스트

Main 함수로 바로 실행할 수 있는 FTP over TLS 텍스트 파일 업로드 테스트입니다.

---

## 🚀 30초 만에 시작하기

### 1. 파일 열기
```
src/test/java/com/jiniebox/jangbogo/SimpleFtpTlsUploadTest.java
```

### 2. 실행
- `main()` 메서드에서 **우클릭**
- **"Run 'SimpleFtpTlsUploadTest.main()'"** 선택

### 3. 완료! ✅
콘솔에서 FTP TLS 연결 및 파일 업로드 과정을 실시간으로 확인할 수 있습니다.

**별도 설정 없이 바로 실행 가능합니다!** (공개 테스트 서버 사용)

---

## ⚙️ 설정 파일 구조

### 우선순위: 환경변수 > 프로퍼티 파일 > 기본값

#### 1. 프로퍼티 파일 (이미 생성됨!)
```
src/test/resources/ftp-test.properties
```

**현재 설정값:**
```properties
ftp.host=ftp.dlptest.com
ftp.port=21
ftp.user=dlpuser
ftp.password=rNrKYTX9g7z3RgJRmxWuGHbeu
```

#### 2. 환경 변수 (선택사항)
환경 변수를 설정하면 프로퍼티 파일보다 우선 적용됩니다:

```batch
set FTP_HOST=my-ftp-server.com
set FTP_PORT=21
set FTP_USER=myuser
set FTP_PASS=mypass
```

#### 3. 기본값 (폴백)
설정이 없으면 자동으로 공개 테스트 서버 사용

---

## 📊 실행 흐름

```
SimpleFtpTlsUploadTest.java
    ↓
1. 설정 로드 시도
    ├─ 환경 변수 확인
    ├─ src/test/resources/ftp-test.properties 읽기
    └─ 없으면 기본값 사용 (dlptest.com)
    ↓
2. FTP 서버 정보 출력
    └─ 각 값의 출처 표시 (환경변수/프로퍼티/기본값)
    ↓
3. FTPS 클라이언트 생성
    ↓
4. TLS 연결 → 로그인 → 파일 업로드 → 삭제
    ↓
5. 결과 출력 ✅
```

---

## 💻 예상 출력

```
═══════════════════════════════════════════════════════════
  FTP over TLS (FTPS) 텍스트 파일 업로드 테스트
═══════════════════════════════════════════════════════════

📋 설정 파일 로드 중...
  ✅ 프로퍼티 파일 로드: src/test/resources/ftp-test.properties

🔧 FTP 서버 정보 구성 중...
  - Host: ftp.dlptest.com (출처: 프로퍼티)
  - Port: 21 (출처: 프로퍼티)
  - User: dlpuser (출처: 프로퍼티)
  - Pass: rNr*** (출처: 프로퍼티)

🔧 FTPS 클라이언트 생성 중...
  ⚠️  자체 서명 인증서 허용 (테스트 전용)
  ✅ 클라이언트 생성 완료

🌐 FTP 서버 연결 중...
  - 연결 대상: ftp.dlptest.com:21
  - 응답 코드: 220
  - 응답 메시지: 220 DLP Test FTP Server
  ✅ 서버 연결 성공

🔐 TLS 보안 채널 설정 중...
  - PBSZ 0 실행 완료
  - PROT P 실행 완료 (데이터 채널 암호화)
  ✅ TLS 보안 채널 설정 완료

🔑 FTP 서버 로그인 중...
  - 사용자: dlpuser
  ✅ 로그인 성공

⚙️  FTP 전송 모드 설정 중...
  - 파일 타입: BINARY
  - 전송 모드: PASSIVE
  ✅ 전송 모드 설정 완료

📁 현재 디렉터리 확인 중...
  - 현재 위치: /

📂 파일 목록 조회 중...
  - 파일/폴더 개수: 2

📝 업로드할 테스트 파일 생성 중...
  - 파일명: jangbogo_test_20251105_145530.txt
  - 파일 크기: 512 bytes
  ✅ 테스트 파일 생성 완료

📤 파일 업로드 중...
  - 업로드 대상: jangbogo_test_20251105_145530.txt
  ✅ 파일 업로드 성공!

🔍 업로드된 파일 확인 중...
  - 파일명: jangbogo_test_20251105_145530.txt
  - 크기: 512 bytes
  ✅ 파일 확인 완료

🗑️  테스트 파일 삭제 중...
  ✅ 파일 삭제 완료

═══════════════════════════════════════════════════════════
  ✅ 모든 테스트 완료!
═══════════════════════════════════════════════════════════

테스트 결과 요약:
  ✅ FTP TLS 연결
  ✅ 로그인 인증
  ✅ 디렉터리 목록 조회
  ✅ 텍스트 파일 업로드
  ✅ 파일 확인
  ✅ 파일 삭제

FTP TLS 연결이 정상적으로 작동합니다! 🎉

🔌 FTP 연결 종료 중...
  ✅ 연결 종료 완료

프로그램을 종료합니다.
```

---

## 🔧 설정 변경

### 다른 FTP 서버 사용하기

`src/test/resources/ftp-test.properties` 파일 편집:

```properties
ftp.host=your-server.com
ftp.port=21
ftp.user=your_username
ftp.password=your_password
```

저장 후 main() 재실행!

---

## 💡 주요 기능

### 1. 자동 설정 로드
```java
// main() 메서드 내에서:
Properties props = new Properties();
FileInputStream fis = new FileInputStream("src/test/resources/ftp-test.properties");
props.load(fis);

String ftpHost = props.getProperty("ftp.host", "기본값");
```

### 2. 우선순위 시스템
```java
// 환경 변수가 있으면 우선 사용
String ftpHost = System.getenv("FTP_HOST");
if (ftpHost == null || ftpHost.isEmpty()) {
    // 없으면 프로퍼티 파일에서 읽기
    ftpHost = props.getProperty("ftp.host", "ftp.dlptest.com");
}
```

### 3. 출처 표시
각 설정값이 어디서 왔는지 콘솔에 표시:
- `(출처: 환경변수)` - 환경 변수에서 로드
- `(출처: 프로퍼티)` - ftp-test.properties에서 로드
- `(출처: 기본값)` - 하드코딩된 기본값

---

## 📦 프로젝트 파일 구조

```
jangbogo/
├── src/test/
│   ├── java/com/jiniebox/jangbogo/
│   │   └── SimpleFtpTlsUploadTest.java    ⭐ Main 실행
│   │
│   └── resources/
│       ├── ftp-test.properties            ✅ 바로 사용 가능!
│       └── ftp-test.properties.example    (백업/참고용)
│
├── SIMPLE_FTP_TEST_README.md              (이 문서)
├── FTP_TEST_QUICK_START.md
├── run-ftp-test.bat                       (실행 스크립트)
├── run-ftp-test.ps1
└── run-ftp-test.sh
```

---

## ✨ 특징

### SimpleFtpTlsUploadTest의 장점

✅ **즉시 실행 가능**: 설정 파일이 이미 공개 테스트 서버로 설정됨
✅ **모든 로직이 main 안에**: 메서드 분리 없이 순차적 실행
✅ **유연한 설정**: 환경변수 > 프로퍼티 > 기본값 3단계 폴백
✅ **상세한 출력**: 각 단계마다 진행 상황 표시
✅ **자동 정리**: 업로드한 테스트 파일 자동 삭제
✅ **에러 가이드**: 오류 발생 시 해결 방법 제시

---

## 🎯 사용 시나리오

### 시나리오 1: 기본 테스트 (설정 변경 없음)
```
1. IDE에서 SimpleFtpTlsUploadTest.java 열기
2. main() 우클릭 → Run
3. 공개 서버(dlptest.com)로 자동 연결 및 테스트
```

### 시나리오 2: 커스텀 서버 테스트
```
1. src/test/resources/ftp-test.properties 편집
2. ftp.host, ftp.user, ftp.password 수정
3. main() 실행
```

### 시나리오 3: 임시 서버 테스트 (환경변수)
```
1. set FTP_HOST=temp-server.com
2. set FTP_USER=tempuser
3. set FTP_PASS=temppass
4. main() 실행 (환경변수가 우선 적용됨)
```

---

## 🔐 보안

✅ **ftp-test.properties는 .gitignore에 추가됨** (Git 커밋 안 됨)
✅ 비밀번호는 콘솔에 마스킹되어 출력 (`rNr***`)
⚠️ 자체 서명 인증서 허용은 테스트 전용 (프로덕션에서 제거 필요)

---

## 📞 문의

- **Email**: kiunsea@gmail.com
- **GitHub**: https://github.com/kiunsea/jangbogo/issues
- **Website**: https://www.omnibuscode.com

---

**Copyright © 2025 jiniebox.com**

