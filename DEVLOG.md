# DevLog: Jangbogo 작업 이력

## 개요

이 DEVLOG는 프로젝트의 작업 이력을 기록합니다.

**작업 기록 형식**: 각 작업은 `YYYY-MM-DD HH:MM` 형식의 일자로 기록됩니다.

---

## 주요 변경사항

### [2026-08-30 05:48] 우리 CI 아티팩트가 계정 저장 한도를 먹어 다른 저장소를 3주간 멈춰 세웠다

**작업 개요**

`build.yml` 에 아티팩트 정리 스텝을 넣고, `release.yml` 의 중복 업로드를 제거하고, 쌓여 있던 228벌 7,637MB 를 전량 삭제했다. 런타임 코드는 건드리지 않았다. v0.22.1.

**발단 — 아픈 곳과 원인이 다른 저장소였다**

`doribox-studio` 는 2026-08-07 이후 모든 Build 에서 아티팩트 업로드가 실패하고 있었다:

```
Failed to CreateArtifact: Artifact storage quota has been hit
```

그런데 그 저장소의 아티팩트는 3,702MB 였고, 전부 지운 직후에도 같은 에러가 났다. 원인이 그쪽에 없었기 때문이다 — **GitHub Actions 의 저장 한도는 저장소가 아니라 계정 단위 500MB** 이고, 계정 합계 7,640MB 중 **7,637MB 가 이 저장소**였다.

즉 이 저장소는 자기는 멀쩡한 채(2026-08-26 까지 업로드 성공) 남을 멈춰 세우고 있었다. 그리고 그쪽은 업로드에 `continue-on-error: true` 가 붙어 있어 Build 가 계속 초록이었고, **3주간 아무도 몰랐다.**

**왜 쌓였나 — retention-days 는 quota 를 지켜 주지 않는다**

`build.yml` 의 두 업로드에는 `retention-days: 7` 이 붙어 있다. 그래서 정리 스텝을 따로 두지 않았다. 그런데 실측하니 이랬다:

| 이름 | 벌 | 크기 | 출처 |
|---|---|---|---|
| `jangbogo-jar` | 107 | 7,112MB (1벌 ≈ 66MB) | `build.yml` · retention-days: 7 |
| `test-results` | 116 | 12MB | `build.yml` · retention-days: 7 |
| `jangbogo-v*` 5종 | 5 | 509MB (각 ≈ 102MB) | `release.yml` · retention-days: 90 |

만료 184벌 6,046MB / 미만료 44벌 1,591MB, 기간 2025-11-15 ~ 2026-08-26.

`retention-days: 7` 인데 2025-11 자가 남아 있다. **만료돼도 GitHub 의 GC 가 실제로 걷어갈 때까지 목록에 남아 quota 를 계속 잡는다.** 그래서 만료분만 지우는 것으로는 해결되지 않는다 — 미만료 1,591MB 만으로도 한도의 3배다.

**버전 태그 아티팩트는 Release 자산의 완전한 사본이었다**

`release.yml` 은 같은 zip 을 두 번 올리고 있었다. `softprops/action-gh-release` 가 Release 자산으로 올린 **직후** `upload-artifact` 가 한 번 더 올린다. Releases API 로 확인하니 v0.7.0·v0.8.0·v0.10.2·v0.18.1·v0.22.0 전부 `Jangbogo-v<버전>.zip` 이 Release 자산으로 실재했다. 받는 곳은 Release 페이지이지 아티팩트 목록이 아니므로, 이 스텝은 처음부터 아무도 쓰지 않는 사본이었다. **Release 자산은 저장 quota 를 먹지 않고 아티팩트는 먹는다.** 릴리스마다 102MB 가 90일씩 쌓이고 있었다.

**상세 내용**

1. **삭제** — 228벌 전량. 삭제 후 `total_count` = 0 확인. 버전 태그 5벌도 위 근거로 잃는 것이 없다.
2. **`.github/scripts/prune-artifacts.sh`** — 앞·뒤 두 스텝이 공유하는 단일 구현. `doribox-studio` 가 같은 문제를 고쳐 CI 로 검증한 설계(`4df6cc5`)를 가져왔고, 이 저장소 사정에 맞춰 셋을 바꿨다.
3. **`build.yml`** — 사전 정리(`all`)를 업로드 앞에, 수렴 가드(`keep-newest`)를 뒤에. `permissions: contents: read` + `actions: write` 추가.
4. **`release.yml`** — `Upload artifacts` 스텝 제거(주석으로 근거를 남겼다).
5. **`WorkflowArtifactHygieneTest`**(9건) 신규.

**가져오면서 바꿔야 했던 것**

- **`shell: bash` 를 명시해야 한다.** 이 job 은 `runs-on: windows-latest` 라 기본 셸이 PowerShell 이다(참조 쪽은 ubuntu 라 필요 없었다). 빠뜨리면 PowerShell 이 `.sh` 를 해석하려 들어 스텝이 죽는데, `continue-on-error` 때문에 초록으로 넘어간다 — 정리가 통째로 멈춘 것을 아무도 모르게 된다. 그래서 테스트가 이 형태를 본다.
- **`permissions:` 블록이 아예 없었다.** 아티팩트 삭제에 `actions: write` 가 필요한데, `permissions` 를 명시하는 순간 **나열하지 않은 스코프는 `none` 이 된다.** 그동안 암묵적으로 쓰던 `contents: read` 를 함께 적지 않으면 체크아웃이 죽는다.
- **업로드에는 `continue-on-error` 를 붙이지 않았다.** 참조 쪽은 붙어 있고, 정확히 그것 때문에 3주간 조용히 실패했다. 정리 스텝에만 붙인다 — 정리는 편의 기능이고, quota 로 못 올리면 빨갛게 죽는 편이 낫다.

**가드를 실제로 물게 만드는 데 한 번 실패했다**

테스트를 다 쓰고 워크플로에 변이를 두 개 넣었다 — (1) 사전 정리 스텝에서 `shell: bash` 제거, (2) 사전 정리 인자에서 `jangbogo-jar` 제거. **하나만 걸렸다.**

이름 검사가 앞·뒤 정리 스텝의 인자를 **합쳐서** 보고 있었기 때문이다. 뒤쪽 `keep-newest` 에 `jangbogo-jar` 가 남아 있으니 합집합에는 들어 있었다. 그런데 뒤쪽은 이미 올리고 난 다음이라 이번 회차의 공간을 비우지 못한다 — **즉 합집합으로 보는 순간 이 가드는 정확히 이번 사태의 형태를 통과시킨다.** 모드별로 나눠 보도록 고치고 다시 넣으니 두 건 다 걸렸다.

이 저장소가 반복해서 겪은 "가드는 있는데 아무것도 안 지킨다" 와 같은 형태다. 이번엔 커밋 전에 잡았다.

**검증 결과 — 지연을 각오했는데 즉시 풀렸다**

`doribox-studio` 실측을 근거로 **삭제 직후에는 업로드가 여전히 실패할 것**으로 보고 있었다. GitHub 의 usage 재계산이 6~12시간 지연되고(에러 문구가 그렇게 말한다), 그쪽은 3,702MB 를 지운 직후 아티팩트 0개 상태로 돌렸는데도 같은 quota 에러가 났기 때문이다. 그래서 이 커밋 직후의 CI 가 빨갛게 떨어지더라도 회귀가 아니라고 미리 적어 뒀다.

**그런데 걸리지 않았다.** 삭제 약 1시간 뒤의 첫 Build(run `33274611218`)에서 업로드 두 개가 모두 성공했고, 그 run 의 아티팩트를 API 로 직접 세어 확인했다:

```
gh api "repos/kiunsea/jangbogo/actions/runs/33274611218/artifacts" -q '.total_count'
→ 2   (jangbogo-jar 69,048,384B · test-results 243,111B, 둘 다 expired=false)
```

이것이 진짜 해소 증거다. **스텝이 success 로 찍히는 것은 증거가 아니다** — `continue-on-error` 가 붙은 스텝은 실패해도 success 로 찍힌다(이 저장소의 업로드에는 붙이지 않았지만, 정리 스텝에는 붙어 있다).

지연이 왜 이번엔 없었는지는 모른다. 지운 양이 두 배였다는 것 말고 짚을 근거가 없어서 **"지연은 없다" 로 일반화하지 않는다** — 다음에 같은 일을 겪으면 여전히 6~12시간을 각오하는 편이 맞다.

**그 유보가 곧 옳았던 것으로 드러났다.** 같은 계정의 `doribox-studio` 를 같은 날 돌려 보니(run `33275743688`, 2026-08-29 21:22Z — 삭제 47분 뒤) **여전히 quota 에러였다.**

```
##[error]Failed to CreateArtifact: Artifact storage quota has been hit.
         Unable to upload any new artifacts. Usage is recalculated every 6-12 hours.
```

즉 **지연은 실재하고, 저장소마다 다르게 걷힌다.** 20분 뒤에 이미 올라간 곳(지운 당사자인 이 저장소)과 47분 뒤에도 막힌 곳이 같은 계정에 공존했다. 지운 저장소 쪽이 먼저 풀리는 것으로 보이지만 근거가 이 한 쌍뿐이라 그 이상은 주장하지 않는다. **"우리 쪽이 됐으니 계정이 풀렸다" 로 읽으면 안 된다.**

**`actions: write` 가 실제로 부여되는지도 확인했다**

저장소의 `default_workflow_permissions` 는 `read` 다. 워크플로에 `permissions:` 로 그보다 높은 스코프를 적어도 부여되지 않는다면 정리는 403 으로 아무것도 못 지우면서 `continue-on-error` 때문에 초록으로 넘어간다 — 이 설계에서 가장 조용한 실패 자리다. 그래서 job 로그의 `Set up job` 이 찍는 실제 권한을 봤다:

```
##[group]GITHUB_TOKEN Permissions
Actions: write
Contents: read
Metadata: read
```

저장소 기본값이 `read` 여도 워크플로가 명시한 스코프는 그대로 부여된다.

**삭제 경로까지 확인했다 — 두 번째 회차에서**

첫 회차의 정리 스텝은 앞·뒤 모두 `대상 0 벌` 이었다. 직전에 228벌을 전량 지워 남은 것이 없었기 때문이다. **초록이지만 아무것도 안 한 상태**라 그것만으로는 스크립트가 실제로 DELETE 를 부르는지 알 수 없었다 — 이 저장소가 반복해서 겪은 "가드는 있는데 아무것도 안 지킨다" 가 정확히 이 자리다.

두 번째 회차(run `33274848861`)에서 밟혔다:

```
pruned(all) jangbogo-jar artifact 9721161868
jangbogo-jar: mode=all 대상 1 벌
pruned(all) test-results artifact 9721162056
test-results: mode=all 대상 1 벌
jangbogo-jar: mode=keep-newest 대상 0 벌
test-results: mode=keep-newest 대상 0 벌
```

앞쪽 `all` 이 직전 회차의 두 벌을 실제로 지웠고, 뒤쪽 `keep-newest` 는 설계대로 무동작이었다(사전 정리 덕에 이 회차 산출물만 남아 있으므로). 저장소 전체 아티팩트를 다시 세니 **이 회차의 2벌뿐**이다.

즉 정상 상태는 "가장 최근 Build 한 벌치"(약 66MB)로 고정된다. 계정 한도 500MB 의 13% 다.

---

### [2026-08-25 18:35] 태그를 밀고 나서야 릴리스가 깨져 있는 걸 알았다

**작업 개요**

`createJre` 가 Gradle 9 에서 죽던 것을 고치고, 그 경로를 CI 가 매 푸시에 밟도록 `build.yml` 에 넣었다. 버전은 v0.22.0 그대로다(릴리스가 발행되기 전에 실패했으므로 태그만 다시 만든다).

**발단 — v0.22.0 태그를 밀었더니 릴리스 워크플로가 실패했다**

```
> Task :createJre FAILED
Execution failed for task ':createJre'.
> Could not find method exec() for arguments [...] on task ':createJre' of type org.gradle.api.DefaultTask.
```

Gradle 9 에서 `Project.exec()` 가 제거됐다. 태스크 액션(`doLast`) 안의 `exec { }` 가 그것이다. 대체재는 주입받는 `ExecOperations` 이고, Groovy 빌드 스크립트에서는 인터페이스 하나를 두고 `objects.newInstance` 로 받아 온다.

**언제부터 깨져 있었나 — 두 판 내내**

래퍼를 8.14.3 → 9.6.1 로 올린 것은 `ac46e85`(dependabot PR #4)다. 그 뒤로 v0.21.0 · v0.21.1 · v0.22.0 세 판을 밀었고 **CI 는 매번 초록이었다.**

`build.yml` 과 `ci.yml` 은 `clean build` 만 돈다. `createJre` 와 `packageDist` 는 `release.yml` 에서만 돈다. 그래서 배포 경로가 깨져도 **태그를 밀기 전까지 아무도 모른다.**

이 저장소가 반복해서 겪은 형태 그대로다 — "가드는 살아 있는데 돌지 않아서 초록". 다만 이번엔 가드가 아니라 배포 경로 자체가 안 돌고 있었다.

**왜 CI 에 넣었나**

고치는 것만으로는 다음 범프에서 같은 일이 반복된다. dependabot 은 앞으로도 래퍼를 올릴 것이고, 그 PR 의 CI 는 여전히 초록일 것이다.

**태그를 미는 순간은 되돌리기가 가장 비싼 시점이다.** 원격 태그를 지우고 다시 만들어야 하고, 그 사이 누군가 그 태그를 받아 갔으면 더 번거로워진다. 그 전에 매 푸시가 같은 명령(`bootJar createJre packageDist`)을 밟도록 `build.yml` 에 넣었다. 이번 판의 CI 가 그 스텝을 실제로 통과하는 것으로 검증된다.

**로컬에서 먼저 확인했다**

`./gradlew createJre` 단독으로 통과했고, 이어서 릴리스 워크플로와 같은 순서(`bootJar createJre packageDist`)를 돌려 ZIP 까지 만들어졌다 — 산출물 검사도 통과했다(엔트리 297개, 개인 데이터 없음).

`clean` 은 붙이지 않았다. 로컬 `build/` 아래에는 배포본 인스턴스와 그 DB 가 섞여 있을 수 있고, 그것이 `build.gradle` 의 clean 가드가 있는 이유다. CI 는 매번 빈 작업공간이라 `clean` 을 그대로 둔다.

**남은 것**

`jlink` 가 `--compress=2` 를 두고 "deprecated" 경고를 낸다. 지금은 동작하므로 건드리지 않았다. 그리고 이 빌드 전체가 `Deprecated Gradle features were used ... incompatible with Gradle 10` 을 낸다 — `buildDir` 등이 남아 있다. Gradle 10 으로 올라갈 때 한 번에 볼 일이고, 그때는 이제 CI 가 배포 경로까지 밟으므로 태그 전에 드러난다.

---

### [2026-08-25 16:40] 경고는 떴는데 그 경고를 따를 방법이 없었다 (v0.22.0)

**작업 개요**

`PasswordEncryptor` 의 공개된 기본 키·IV 를 설치본별 발급으로 밀어냈다. 기존 암호문은 기동 시 새 키로 옮긴다. 테스트 929 → 947건.

**발단 — 남겨 둔 별건이었다**

v0.21.0·v0.21.1 을 하는 내내 기동 로그에 이 줄이 있었다.

> `PasswordEncryptor: 암호화 키가 기본값이다. 이 값은 공개 저장소의 소스에 들어 있으므로 저장된 비밀번호가 보호되지 않는다. -Djangbogo.crypto.key=<키> 또는 환경변수 JANGBOGO_CRYPTO_KEY 로 지정할 것.`

지키는 것은 `jbg_export_config.ftp_pass` 한 칸이다(몰 계정은 `StringEncrypter` + `jbg_mall` 의 몰별 키라 무관하다). 루프백 FTP 계정이라 실해가 작아 "배포 절차에 키 지정 단계를 넣을지" 정도의 판단 사항으로 남겨 뒀었다.

**조사하다 나온 것 — 해법이 실행 불가능했다**

배포 경로를 따라가 보니 경고가 안내하는 해법에 닿을 방법이 없었다.

- `PasswordEncryptor.overrideOf` 는 System property → 환경변수만 본다.
- 서비스 등록 파일(`jangbogo-service.xml`)은 **`-D` 를 적지 말라고 명시**한다. `install.bat` 이 설치할 때마다 `/service/arguments` 노드를 통째로 새로 쓰기 때문이다 — 적어 둔 `-D` 는 그 순간 사라지고, "켠 줄 알았는데 꺼진 채로 도는" 상태가 된다.
- 그래서 그 파일은 설정을 `config\application.yml` 에 적으라고 안내한다. 그런데 `PasswordEncryptor` 는 yml 을 못 본다.

**이건 이미 한 번 고친 결함과 같은 모양이다.** `SessionProfilePropertyBridge` 의 javadoc 이 이렇게 적어 두었다 — *"그 결과 배포본에서는 마스터 킬스위치를 켤 방법 자체가 없었다."* 같은 문장이 암호화 키에도 그대로 성립했다.

**왜 문서만 고치지 않았나**

선택지는 둘이었다. 배선을 고치고 문서에 키 지정 단계를 넣는 것, 그리고 거기에 더해 자동 발급까지 하는 것.

앞의 것은 저위험이지만 **운영자가 문서를 읽지 않으면 아무것도 바뀌지 않는다.** 그건 방금 두 판에 걸쳐 고친 "경고만 남기고 그대로 진행" 과 같은 방식이다. 경고가 이미 1년 가까이 떠 있었는데 아무도 조치하지 않았다는 사실 자체가, 경고에 기대는 설계가 여기서 작동하지 않는다는 증거다.

`PasswordEncryptor` 소스의 주석도 이미 그 방향을 적어 두고 있었다 — *"재암호화 마이그레이션은 별도 과제다. 그때 이 기본값 경로를 지운다."*

**순서 문제 — 어느 쪽을 먼저 해도 창이 남는다**

키를 발급하고 기존 암호문을 옮기는 두 작업 사이에 프로세스가 죽으면 어떻게 되는가.

- 키 파일을 먼저 쓰고 재암호화가 안 끝나면: 다음 기동은 새 키로 도는데 저장된 값은 기본키로 잠겨 있다.
- 재암호화를 먼저 하고 키 파일을 못 쓰면: 다음 기동은 기본키로 도는데 저장된 값은 새 키로 잠겨 있다.

**어느 순서를 잡아도 창이 남는다.** 그래서 순서로 풀지 않고 `decrypt` 에 되읽기를 넣었다 — 활성 키로 못 읽으면 기본키로 한 번 더 본다. 그러면 어느 쪽 키로 잠겨 있든 열리므로 순서를 마음 놓고 정할 수 있고, 이전이 끊겨도 다음 기동이 다시 시도한다.

**왕복 검증을 넣은 이유**

새로 잠근 값을 검증 없이 덮어쓰면, 잘못 잠긴 값이 **기본키로도 활성 키로도 안 열리는** 상태가 된다. 그때는 위의 되읽기도 못 구한다. 그래서 다시 잠근 뒤 활성 키로 풀어 원래 값과 대조하고, 어긋나면 기존 값을 그대로 둔다.

**updateConfig 를 쓰지 않았다**

이전은 `ftp_pass` 한 칸만 건드려야 한다. `updateConfig` 는 전체 필드를 덮어쓰고, 특히 `publicKey` 는 null 이면 빈 값으로 덮여 **FTP 암호화가 통째로 깨진다** — v0.20.0 의 CHANGELOG 에 그 함정이 그대로 적혀 있다. 좁은 갱신 메서드를 따로 뒀고, 가드가 이 자리에 `updateConfig(` 가 다시 들어오는 것을 감시한다.

**작업 중 걸린 것 둘**

`Properties.store(Writer, comments)` 는 주석의 ASCII 밖 문자를 전부 유니코드 이스케이프로 바꾼다. 키 파일 머리말에 한글 경고를 넣었더니 사람이 읽을 수 없는 줄이 됐다 — 테스트가 잡았다. 머리말을 직접 쓰는 것으로 바꿨다.

그리고 그 사실을 코드 주석에 적으면서 역슬래시-u 표기를 그대로 썼더니 컴파일이 깨졌다. **자바는 주석 안의 유니코드 이스케이프도 해석한다.**

**실제로 띄워 봤다**

단위 테스트만으로는 이 기능이 살아 있다고 말할 수 없다. `spring.factories` 한 줄과 기동 호출 한 줄이 빠져도 테스트는 전부 초록이기 때문이다. 그래서 두 번 띄웠다.

- 1회차: `이 설치본 전용 암호화 키를 발급했습니다: ...\config\jangbogo-crypto.key`, 기본값 경고 0건.
- 2회차: 발급 0건(기존 키 사용), 경고 0건, 키 지문 동일.

**남은 것**

키 파일은 DB 와 같은 장비에 있다. 파일시스템을 통째로 가져가는 공격은 막지 못한다. 막는 것은 소스 공개와 DB 단독 유출 둘뿐이고, 문서에도 그렇게만 적었다. 그 이상이 필요하면 OS 키 저장소나 사람이 입력하는 마스터 비밀번호로 가야 하는데, 서비스로 무인 기동하는 이 프로그램의 성격과 맞지 않는다.

---

### [2026-08-25 12:10] 보류 큐를 둘이 훑게 해 놓고 잠그지 않았다 (v0.21.1)

**작업 개요**

v0.21.0 리뷰에서 넘겨 둔 두 건을 고쳤다. 보류 큐 재전송에 파일 락을 걸고, 재전송용 암호문 임시본을 큐 밖(`pending/.staging/`)으로 뺐다. 테스트 924 → 929건.

**발단 — 고치면서 만든 구멍이다**

v0.21.0 에서 자동 수집 경로에도 재전송(`drain`)을 물렸다. 스케줄러를 꺼 두고 수동 자동수집만 쓰는 설정에서 보류분이 영영 나가지 못하는 것을 막으려던 것인데, 그 순간 **보류 디렉터리를 훑는 주체가 둘이 됐다.** 잠금은 넣지 않았다.

리뷰에서 PLAUSIBLE 로 올라왔고, 유실이나 평문 노출로는 이어지지 않아 v0.21.0 머지는 막지 않았다. 이번에 이어서 고친다.

**구멍 1 — 두 회차가 같은 목록을 훑는다**

운영자가 대시보드에서 자동수집을 실행하는 동안 주기 수집이 발화하면, 두 `drain` 이 같은 `pending/` 을 각자 `listPending()` 해서 **같은 파일 목록**을 얻는다. 둘 다 올리면 같은 증분이 수신측에 두 번 도달한다.

수신측 중복 검사(serial 비교)가 걸러내기는 한다. 그러나 그건 완화지 방어가 아니다 — v0.20.1 이 바로 그 중복 검사가 표기 하나 때문에 뚫린 판이었다. 그리고 뒤늦은 `file.delete()` 는 이미 사라진 파일에 대해 "보류 파일 삭제에 실패했습니다" 를 남긴다. 진짜 문제를 찾을 때 이런 헛경고가 가장 비싸다.

`pending/.drain.lock` 에 파일 락을 건다. **기다리지 않고 물러난다** — 어차피 다른 쪽이 같은 목록을 보내고 있고, 기다리면 HTTP 요청 스레드가 FTP 타임아웃만큼 붙잡힌다.

**왜 MallProfileLock 을 쓰지 않았나**

장치는 똑같다. `tryAcquire(Path lockRoot, String name)` 은 루트와 키를 받으므로 그대로 쓸 수 있었다. 쓰지 않은 이유는 하나다 — 그 클래스는 실패할 때 **"프로필 …이 다른 프로세스에 잡혀 있다"** 로 남긴다. FTP 보류 큐에는 프로필이라는 것이 없으므로, 그 로그를 읽는 사람은 엉뚱한 데를 뒤진다.

이 저장소가 반복해서 데인 것이 정확히 그것이다. v0.20.1 의 발단은 "답이 로그에 이미 적혀 있었는데 다른 것을 가리키고 있었다" 였다. 재사용의 이득보다 로그가 거짓말하는 비용이 크다고 봤다. 락 본문은 25줄이다.

**구멍 2 — 임시본이 짐인 척한다**

재전송할 때 암호화를 새로 하면 임시본이 생긴다. 그 자리를 `file.getAbsolutePath() + ".encrypted"` 로 잡았으므로 **보류 디렉터리 안에** 떨어졌다.

업로드와 `finally` 사이에서 프로세스가 죽으면 임시본이 남는다. 다음 회차의 목록은 이름 오름차순이라 `[X.json, X.json.encrypted]` 가 된다. 앞의 것을 처리하면서 같은 이름의 임시본을 덮어썼다가 성공 후 지우므로, **뒤의 항목은 방금 사라진 파일**이다. 업로드가 실패하고 `drain` 은 "여기서 중단합니다" 로 남은 보류분을 통째로 다음 회차로 미룬다.

자기 치유되기는 한다. 다만 보류가 쌓여 있을수록 한 회차에 한 건씩만 나가게 되므로, 밀린 것을 따라잡아야 할 때 정확히 못 따라잡는다.

임시본을 `pending/.staging/` 으로 뺐다. 디렉터리라 파일 목록에 걸리지 않고, 회차 시작마다 비운다. 원본은 보류 디렉터리에 그대로 있으므로 지워도 잃는 것이 없다. 비울 때 조용히 하지 않는다 — 거기 무언가 남아 있다는 것 자체가 "지난 회차가 정리 전에 끝났다" 는 진단 재료다.

**함께 나온 것 — 점 파일은 짐이 아니다**

락 파일을 `pending/` 안에 두기로 하자마자 `listPending()` 이 그것을 짐으로 본다는 것이 드러났다. 걸러내지 않으면 **락 파일을 수신측에 올린다.** 점으로 시작하는 이름은 큐 자신의 살림으로 규정했다. 내보내기 파일명은 `jangbogo_orders_`·`jangbogo_status_`·`purchase_` 로 시작하므로 이 규칙과 겹치지 않는다.

**함께 나온 것 — 적재 충돌은 포기할 일이 아니다**

`resolveTarget` 으로 이름을 고르고 `Files.move` 하는 사이에 다른 회차가 같은 이름을 차지할 수 있다. 그때 `move` 가 `FileAlreadyExists` 로 터지는데, 예전 코드에서 그것은 곧 "실패분을 원위치에 남깁니다" 였다 — **파일은 살지만 큐에 들어가지 못해 재시도되지 않는다.** 이름만 다시 고르면 되는 일이므로 최대 5번 다시 시도한다.

**가드를 돌연변이로 확인했다**

락을 통째로 걷어내니 겹침 테스트와 락 파일 테스트가 붉었다. 정리를 막으니 임시본 테스트가 붉었다. 각각 정확히 그 자리를 지목했다.

그 과정에서 커밋하지 않은 편집을 `git checkout <file>` 로 되돌리다 그 파일의 작업분을 통째로 날렸다. 돌연변이는 되돌릴 방법을 정해 두고 시작할 일이다 — 이번엔 편집 도구로 되돌렸다.

---

### [2026-08-25 07:20] 암호화를 켜 뒀는데 평문으로 내려보내고 있었다 (v0.21.0)

**작업 개요**

FTP 전송 경로 셋 모두에서 "암호화 실패 → 평문 업로드 진행" 분기를 걷어냈다. 판정을 `FtpEncryptionGate` 한 곳으로 모으고, 못 보낸 것은 보류 큐로 넘긴다. 자동 수집 경로에 없던 큐 배선도 함께 물렸다. 테스트 904 → 924건.

**발단 — 세 자리가 똑같이 틀려 있었다**

`ftp_encrypt_enabled=1` 로 암호화를 요구했는데, 암호화가 실패하거나 공개키가 없으면 코드가 조용히 평문으로 내렸다.

```java
if (encryptSuccess) { fileToUpload = encryptedFilePath; fileEncrypted = true; }
else { logger.warn("... 암호화 실패 - 평문 업로드 진행"); }   // ← 평문이 나간다
...
} else { logger.warn("FTP 암호화가 활성화되어 있으나 Public Key가 없습니다. 평문으로 업로드합니다."); }
```

`AdminController` 의 자동 수집(약 1337행)·수동 내보내기(약 1881행), `MallSchedulerService.processFtpUpload`(약 1123행). 세 자리가 같은 분기를 각자 들고 있었다는 것이 곧 원인이다 — 규칙을 세 번 적으면 세 번 다 같게 유지할 방법이 없다.

**왜 경고로는 부족한가**

응답이 성공이라 보고했다. `autoFtpUploaded: true` / `autoFtpEncrypted: false`. 운영자가 보는 화면에는 "업로드 완료" 가 뜨고, 강등 사실은 로그 한 줄로만 남아 수십 줄에 묻힌다. **요구가 지켜졌는지를 물어볼 자리가 어디에도 없었다.**

채널 자체도 평문 FTP 다. 수신측 내장 FTP 서버에 `SslConfiguration` 이 없다(jiniebox PR #5). 그래서 강등은 곧 노출이고, 실리는 것은 주문번호·상품명·구매일자다. 발견 시점의 `ftp_address` 는 `127.0.0.1:2121` 루프백이라 장비 밖으로 나가지 않았지만, 그건 설정이 그랬을 뿐이고 코드는 원격 주소에도 똑같이 동작한다.

**고친 방향 — 판정을 한 곳으로 모았다**

`FtpEncryptionGate` 가 무엇이 회선에 실리는지를 혼자 정한다.

- 암호화가 꺼져 있으면 평문으로 보낸다. **운영자의 명시적 선택이라 막지 않는다.** 이것까지 막으면 기능이 죽는다.
- 켜져 있는데 공개키가 없거나 암호화에 실패하면 **거절한다.** 업로드를 부르지 않는다.
- 이미 `.encrypted` 면 다시 암호화하지 않는다. 두 겹을 씌우면 수신측이 풀지 못한다.

거절은 조용하지 않다. 사유 문장이 응답(`autoFtpError` / `ftpError`)과 로그에 실패로 실린다.

**암호화 수행자를 주입할 수 있게 열어 둔 이유**

실제 RSA 는 정상 동작한다 — Hybrid(AES-256 본문 + AES키를 RSA-2048로), 산출물은 `.encrypted`, 키 짝도 맞다(수신측 `committed/purchase_20260822_101536.json.encrypted` 가 복호화된 실물 증거가 있다). 그래서 **실패 경로는 주입 없이는 만들어 볼 수 없다.** 테스트할 수 없는 채로 두었던 것이 이 결함이 오래 남은 이유다. 관문의 실패 경로 전부가 주입된 수행자로 재현된다.

**문제 2 — 어느 경로가 큐를 쓰고 어느 경로가 안 쓰는지**

먼저 훑었다. `FtpPendingQueue` 는 v0.11.0(2026-07-29)부터 있었는데 **스케줄 수집 경로에만** 물려 있었다. 자동 수집(`POST /malls/auto-collect`)과 수동 내보내기(`POST /export/orders`)는 큐를 아예 부르지 않았다.

2026-08-24 실측의 유실은 자동 수집 경로 형태와 일치한다 — 업로드가 `Connection refused` 로 실패한 뒤 `finally` 가 암호화 임시파일과 평문 원본을 모두 지웠고, `pending/` 은 생성조차 되지 않았다. 내보내기가 증분이라 그 회차 주문은 수신측에 영원히 도달하지 못한다.

자동 수집 경로에 `drain` + `enqueue` 를 물렸다. 스케줄러를 꺼 두고 수동 자동수집만 쓰는 설정에서 보류분이 영영 나가지 못하던 것도 이걸로 함께 풀린다.

**재전송도 관문을 통과시켰다**

큐에 평문이 들어갈 수 있다는 것을 놓치면 안 된다. 암호화가 안 돼서 못 보낸 회차분이 바로 그것이다. 재전송이 관문을 건너뛰면 **그 평문이 다음 회차에 그대로 나가** 고친 것이 한 회차만 유효해진다. 두 경로의 `drain` 모두 같은 관문을 통과시킨다.

**수동 내보내기는 일부러 큐에 넣지 않는다**

이 경로의 산출물은 사용자가 저장을 요청해 응답으로 경로(`filePath`)까지 받은 파일이다. 큐로 옮기면 **응답이 알려준 그 자리에서 파일이 사라진다.** 증분도 아니라 다음 회차에 전체 내보내기가 통째로 재전송된다. 파일은 저장 경로에 그대로 남으므로 유실도 없다 — 거절되는 것은 전송뿐이다.

**가드를 두 겹으로 뒀다**

`FtpTransferRefusalWiringTest` 는 실행이 아니라 소스 형태를 본다. 관문 클래스와 그 단위테스트만으로는 아무 것도 보증되지 않기 때문이다 — 세 경로 중 하나라도 예전처럼 직접 암호화하면 관문 테스트는 전부 초록인 채로 평문이 계속 나간다.

그리고 **가드 자체를 돌연변이로 검증했다.** 처음 쓴 재전송 가드는 `body.substring(body.indexOf("pendingQueue.drain("))` 로 그 뒤를 통째로 봤는데, 재전송 람다에서 관문을 통째로 걷어냈는데도 뒤쪽 본 전송의 `gate.prepare(` 가 걸려 초록이었다. **가드가 아무 것도 지키지 못하는 상태였다.** 괄호 균형을 세어 호출 인자만 잘라 내도록 고친 뒤 같은 돌연변이가 붉게 잡히는 것을 확인했고, 수동 내보내기 경로의 `isRefused()` 를 없애는 돌연변이도 그 경로를 정확히 지목해 잡았다.

**남긴 판단 사항**

기동 로그의 `PasswordEncryptor: 암호화 키가 기본값이다` 경고는 이 PR 범위 밖으로 뒀다. 이 키가 지키는 것은 `jbg_export_config.ftp_pass` 다(몰 계정은 `StringEncrypter` + `jbg_mall` 의 몰별 키를 쓰므로 무관하다). 지금은 루프백 FTP 계정이라 실해가 작지만, 운영 배포 절차에 `-Djangbogo.crypto.key=` 지정 단계를 넣을지는 사람이 판단할 일이다.

---

### [2026-08-22 22:49] 오아시스 주문번호에 꼬리표가 붙은 채 나가고 있었다 (v0.20.1)

**작업 개요**

오아시스 주문번호 칸의 새 표기(`주문번호 : 00-…`)를 파서가 따라가지 못해 꼬리표가 `serial` 로 저장·전송되던 것을 고쳤다. `OrderSerialNormalizer` 를 두고 파서와 DAO(저장·중복 조회) 양쪽이 같은 규칙을 쓴다. 테스트 891 → 904건.

**발단 — 수신측에서 대조하다 나왔다**

수신측 운영 DB 의 주문 테이블에서 오아시스 주문 4건이 같은 구매일에 두 행씩 있었다. 한쪽은 수신측 자체 수집분(`00-…`), 다른 쪽은 이쪽 FTP 전송분(`주문번호 : 00-…`). 수신측 중복 검사는 문자열을 그대로 견주므로 둘을 다른 주문으로 봤고, 그 주문의 아이템은 "같은 날·같은 이름" 으로 기존 행에 매칭돼 수량이 두 배가 됐다.

이쪽 SQLite 를 보니 오아시스 주문 8행 전부 꼬리표째였다. 그리고 이 사실은 이미 로그에 찍혀 있었다 — v0.19.7 DEVLOG 의 발단 인용에 `주문 시리얼: 주문번호 : 00-…-…-…` 이 그대로 있다. 그때는 값을 가리는 일을 하느라 형식이 이상하다는 것을 읽지 못했다. **형식만 남기는 마스킹이 바로 이 질문("형식이 깨졌는가")에 답하려고 있는 것인데, 답이 적혀 있는데도 묻지 않았다.**

**원인 — 괄호만 벗겼다**

`Oasis.parseOrderSummary` 는 `(2026-0729-1234)` 형태를 전제로 첫 글자와 끝 글자를 잘랐다. 2026-08 실측의 칸은 `주문번호 : 00-…` 형태라 괄호가 없고, 그러면 아무것도 안 벗기고 그대로 `serial` 로 삼는다. 수신측 계약의 `serial` 은 주문번호 그 자체인데 꼬리표가 딸려 나갔다.

**왜 파서만 고치면 안 되는가**

파서만 고치면 다음 회차에 깨끗한 값이 나온다. 그런데 중복 조회(`getOrder`)가 `serial_num=?` 문자열 일치라, 깨끗한 값은 저장된 꼬리표 값과 안 맞는다. 오아시스 수집기는 첫 쪽 전체(6건)를 매 회차 다시 읽으므로 **이번엔 이쪽 DB 에서 6건이 한 번 더 쌓이고**, 그 6건은 새 주문으로 간주돼 수신측으로 다시 나간다. 한쪽만 고치면 중복을 옮겨 놓을 뿐이다.

그래서 DAO 가 저장할 때 정규화하고, 조회는 구매일자로만 좁힌 뒤 저장된 값도 정규화해 자바에서 견준다. 하루치 주문은 몇 건이라 비용이 없고, 주문번호가 SQL 에 들어가지 않으니 이어 붙일 자리도 사라진다. `SecurityHardeningTest` 가 못 박아 두었던 `serial_num=?` 형태는 이 이유로 새 형태(구매일자 바인딩 + `OrderSerialNormalizer.normalize(rset.getString("serial_num"))`)로 바꿨다 — 가드는 설계를 따라가야지 설계가 가드를 따라가면 안 된다.

**정규화는 셋만 한다**

앞뒤 공백, 앞에 붙은 `주문번호 :` 꼬리표(콜론·공백·전각 무관), 값 전체를 감싼 괄호. 값 안쪽은 건드리지 않는다 — 하이픈과 자릿수는 주문번호의 일부이고, 영수증 번호(SSG·이마트·하나로)는 이 함수를 지나도 같아야 한다. 괄호는 "감싼" 경우만 벗긴다 — 예전처럼 괄호가 어디든 있으면 앞뒤 한 글자씩 자르면 `주문번호 : (00-…)` 에서 엉뚱한 글자를 잘라 낸다.

**기존 8행은 그대로 둔다**

비교가 정규화되므로 두지 않아도 중복은 나지 않는다. 표기만 맞추고 싶으면 SQLite 한 줄(CHANGELOG 참조)이고, 사람이 보고 돌린다. 기동 시 자동으로 고치는 길(v0.20.0 의 경로 되잡기 같은)도 있었으나 8행짜리 1회성 정리를 위해 부팅 경로에 코드를 더하지 않았다.

**검증**

- `OasisParserTest` 13건 / `OrderSerialNormalizerTest` 6건 / `JbgOrderSerialNormalizationTest` 5건 — 마지막은 `@TempDir` 의 실제 SQLite 에 꼬리표째 넣은 행을 깨끗한 값으로 찾고, 깨끗한 행을 꼬리표째 들어온 값으로 찾고, 두 INSERT 가 모두 벗겨 저장하는지 밟는다.
- 전체 891 → 904건 통과 (프로브 제외). `SecurityHardeningTest`·`PurchaseDataLogMaskingTest`·`CollectorStampWiringTest` 포함.
- 실계정·실사이트·기준선 DB 에 닿지 않았다. 기준선 SQLite 는 읽기 전용으로 열어 8행을 확인했을 뿐이다.

---

### [2026-08-22 11:13] 이관한 DB 가 이전 장비의 경로를 들고 왔다 (v0.20.0)

**작업 개요**

기동 시 `save_path` 가 이 장비의 것인지 판정하고, 다른 장비 좌표면 되잡아 DB 에서 파일을 다시 만드는 `ExportPathMigrationService` 를 추가했다.

**발단 — 장비 이관 중에 드러났다**

이 장비로 jangbogo 를 옮기고 설정을 훑다가 `jbg_export_config` 에서 이 값을 봤다.

```
save_path  C:\Users\11A\Documents\jangbogo_exports
```

이 장비의 사용자는 `KIUNSEA` 다. `11A` 는 이전 장비의 계정이고, `C:\Users\11A` 는 존재하지 않는다.

**무엇이 문제였나 — 실패하지 않는 것이 문제였다**

없는 경로면 저장이 실패할 것 같지만 그렇지 않다. `ExportService` 는 저장 직전에 `mkdirs()` 를 부른다 (316·337·361·515·638행). 그래서 **이 장비에 없던 사용자 이름으로 된 트리를 새로 만들고, 거기에 내보낸 파일을 쌓는다.** 오류도 경고도 나지 않는다.

발견 시점에 `C:\Users\11A` 가 아직 없었던 것은 순전히 운이었다. `mall_account.yml` 이 아직 넘어오지 않아 자동수집이 한 번도 성공하지 못한 상태였을 뿐이다. **첫 성공이 곧 첫 사고**가 되는 자리였다.

이관 작업자가 이걸 알아채려면 파일이 어디에 생겼는지 직접 뒤져야 한다. 그런데 뒤질 이유가 없으므로 아무도 뒤지지 않는다. 이 저장소가 반복해서 만나 온 형태다 — **조용히 성공하는 실패.**

**어디까지 단정할 것인가**

되잡는 판정을 넓게 잡으면 사람이 정한 설정을 앱이 되돌리게 된다. 그래서 다른 장비 좌표라고 단정하는 근거를 둘로 제한했다.

1. **루트가 없다** — `D:\...` 인데 이 장비에 D 드라이브가 없으면 이 경로는 성립할 수 없다.
2. **남의 홈이다** — 홈의 부모(`C:\Users`) 아래인데 내 홈 아래가 아니면 다른 계정의 프로필이다.

둘 다 아니면서 없는 경로는 `MISSING` 으로 두고 **건드리지 않는다.** 작업자가 일부러 아직 만들지 않은 경로를 지정해 두었을 수 있고, 그 경우 `mkdirs()` 가 만드는 것이 옳은 동작이다.

**복원을 어떻게 정의할 것인가**

이관 작업자가 이전 장비에서 내보낸 파일을 들고 오지 않았을 수 있다. 그런데 내보낸 파일은 **SQLite 의 구매내역에서 파생된 산출물**이다. DB 만 넘어왔으면 새 경로에 그대로 다시 만들 수 있다. 그래서 파일 이동이 아니라 `exportAllOrders` 재생성을 택했다 — 작업자에게 아무것도 요구하지 않는다.

재생성이 실패해도 경로 교체는 유효하게 뒀다. 다음 수집분부터는 올바른 자리에 쌓인다.

**빠지기 쉬운 자리 — updateConfig 는 전체를 덮어쓴다**

`JbgExportConfigDataAccessObject.updateConfig` 는 9개 필드를 모두 다시 쓴다. `ftpPass` 는 null 을 넘기면 기존 암호문을 유지하는 처리가 DAO 안에 있지만, **`publicKey` 는 null 이면 빈 값으로 덮어써 FTP 암호화가 깨진다.** 경로 하나 바꾸려다 암호화를 끄는 셈이 된다. 그래서 읽은 값을 그대로 되돌려 준다.

**어디에 끼울 것인가**

`StartupTasks` 의 스키마 보정 직후에 뒀다. 수집 가드(`jangbogo.startup.collect.enabled`) 밖인 것은 스키마와 같은 이유다 — 수집을 꺼 두어도 사람이 관리 화면에서 직접 내보낼 수 있고, 그 자리도 같은 `save_path` 를 쓴다.

1회 수집보다 **먼저** 도는 것이 핵심이다. 수집이 한 번이라도 성공하면 잘못된 경로가 이미 만들어져 있다.

**곁가지 — 구분자가 박혀 있었다**

기존 `AdminController` 에 이미 기본 경로를 만드는 코드가 있었는데 `userHome + "\\Documents\\..."` 로 구분자를 박아 두고 있었다. Windows 밖에서는 경로가 아니라 한 덩어리 이름이 된다. 새 코드는 `Paths.get` 으로 잇고, Windows 는 내문서 아래·그 외 OS 는 홈 바로 아래로 갈랐다.

**검증**

마침 이 장비의 `save_path` 가 실제 이관 상태여서 살아 있는 시험이 됐다.

```
 다른 장비에서 옮겨온 내보내기 경로를 발견했습니다
  이전 장비 경로 : C:\Users\11A\Documents\jangbogo_exports
  이 장비 경로   : C:\Users\KIUNSEA\Documents\jangbogo_exports
  DB 에서 재생성 완료 : ...\jangbogo_exports\jangbogo_orders_20260822_105459.json
```

`C:\Users\11A` 는 생성되지 않았다. `ftp_pass`(24자)와 `public_key` 도 그대로 남았다.

---

### [2026-08-12 08:30] 아이디는 가리면서 구매 이력은 그대로 남기고 있었다 (v0.19.7)

**작업 개요**

로그에 평문으로 남던 개인 데이터를 전부 가렸다 — 세션 ID, 주문번호, 구매일자, 매장명, 상품명, 그리고 계정 아이디 4곳. 감시 범위도 운영 소스 전체로 넓혔다. 테스트 876 → 891건.

**발단 — 지켜보다 봤다**

오아시스 실계정 수집을 모니터로 따라가는 중에 로그가 이렇게 흘렀다.

```
로그인 성공: a (Session ID: 5289AF23…)
주문 시리얼: 주문번호 : 00-…-…-…
주문 처리 중 - serial: …, datetime: 20260809, mallname: 오아시스마켓
```

의도해서 찾은 것이 아니라 **다른 일을 하다 눈에 들어왔다.** 그 점이 이 결함의 성질을 말해 준다 — 아무도 로그를 보안 대상으로 보지 않는다.

**무엇이 이상했나 — 절반만 지키고 있었다**

이 저장소는 세션 쿠키를 개수만, 암호화 키를 길이만 남긴다. v0.18.7 은 계정 아이디까지 가렸다. 그런데 **구매 데이터는 대상이 아니었다.** 즉 "아이디는 가리고 구매 이력은 그대로 남기는" 상태였고, <b>정작 이 프로그램이 다루는 데이터의 본체는 후자다.</b>

`Emart` 의 복구 로그가 그 어긋남을 압축해서 보여 준다 — 주석이 *"값은 남기지 않는다"* 라고 적어 놓고 바로 아래에서 매장명을 찍고 있었다.

**가리는 방식을 둘로 나눴다**

값의 종류에 따라 <b>진단에 남아야 하는 것이 다르다.</b>

| 값 | 방식 | 남는 것 | 왜 |
|---|---|---|---|
| 주문번호·구매일자 | `LogMask.shape` | 숫자를 `N` 으로 — 형식만 | 사람이 묻는 질문이 "값이 무엇인가" 가 아니라 **"형식이 깨졌는가"** 다. 구매일자가 8자리가 아니어서 스킵된 주문이 실제로 있었다 |
| 매장명·상품명 | `LogMask.name` | 앞 한 글자 + 길이 | 숫자가 거의 없어 `shape` 로는 아무것도 가려지지 않는다 |
| **세션 ID** | **제거** | 아무것도 | 살아 있는 자격증명이고, 진단에 필요한 경우가 애초에 없다 |

세션용 함수를 `LogMask` 에 <b>일부러 두지 않았다</b> — 함수가 있으면 언젠가 쓰인다.

이름 형태는 `AccountIdMasker.mask` 에 **위임**했다. 같은 형태를 두 벌 구현하면 한쪽만 고쳐지고 그때 어느 쪽이 맞는지 아무도 모른다. 쓰임이 달라 클래스는 나누되 형태의 정본은 하나다.

**가드가 목록에 적어 둔 채로 새고 있었다**

`AccountIdLogMaskingTest` 는 `svc` 아래만 봤고, 자기 javadoc 에 *"svc 밖에도 같은 형태가 남아 있다. 전부 이 작업의 배정 범위 밖이라 손대지 않았고, 고쳐지는 대로 넓혀야 한다"* 고 적어 두고 있었다. 거기 적힌 네 자리 중 하나가 바로 이번에 발견한 `AdminController` 다.

**적어 두는 것이 고치는 것을 대신하고 있었다.** 목록은 정직했지만, 정직한 목록이 있다는 사실 자체가 그 자리를 "알려진 것" 으로 만들어 방치를 정당화했다.

그래서 적어 둔 자리를 **전부 고치고** 스캔 경로를 `src/main/java` 전체로 넓혔다. 목록은 비웠다 — 남겨 두면 다음 사람이 "여기는 원래 예외" 로 읽는다.

**넓히자마자 네 자리가 더 걸렸다**

이름 목록에 `adminId`·`currentAdminId`·`ftpUser`·`ftpId` 를 넣었더니 관리자 아이디 2곳·FTP 사용자명 1곳·FTP 아이디 1곳이 즉시 잡혔다. **목록 기반 감시의 한계가 그대로 드러난 자리다** — 이름을 모르면 못 잡고, 그동안은 초록이다.

*그리고 내가 그 함정에 한 번 빠졌다.* javadoc 에 "적어 두었던 자리를 전부 고쳤다" 고 먼저 적어 놓고 실제로는 두 파일을 손대지 않은 상태였다. 가드를 돌려 보고 나서야 드러났다 — 문서가 코드보다 앞서간 것이고, 이 항목이 경계하는 형태 그 자체다.

**형태 검사만으로는 부족하다**

`PurchaseDataLogMaskingTest` 는 "로그 호출이 마스킹을 거치는가" 라는 형태를 본다. 그런데 `shape` 가 입력을 그대로 돌려주도록 바뀌어도 <b>그 검사는 초록이다.</b> 그래서 `LogMaskTest` 가 값으로 못 박는다 — 숫자가 한 글자도 남지 않는지, 마스킹이 항등함수가 아닌지.

무력화 확인도 했다. `Oasis` 한 자리를 원래대로 되돌리자 정확히 짚었다.

```
구매 데이터가 로그 인자로 그대로 실린다(LogMask 로 감쌀 것):
  [com/jiniebox/jangbogo/svc/mall/Oasis.java → serial]
```

**알면서 남긴 한계**

- 감시는 **이름 목록** 기반이다. 새 이름으로 값을 실으면 잡지 못한다. 이번에 그것을 두 번 겪었으므로(넓히자마자 4건) 목록이 완전하다고 볼 근거가 없다.
- `mallName` 은 파일마다 뜻이 달라 전역 감시를 걸 수 없다 — 러너에서는 `jbg_mall.name`(앱이 정한 몰 이름, 개인정보 아님)이고 `Emart`·`ExportService` 에서는 실제 매장명이다. 그래서 그쪽만 자리별로 못 박았다.
- **이미 쌓인 로그 파일은 그대로다.** 이 변경은 앞으로 찍힐 줄에만 적용된다. `logs/` 는 gitignore 라 저장소 유출은 아니지만, 기존 파일을 남에게 보낼 일이 있으면 여전히 손으로 확인해야 한다.

### [2026-08-12 01:20] 오아시스 — 재고 나서 "고치지 않기" 를 골랐다 (v0.19.6)

**작업 개요**

오아시스에 하나로·SSG 와 같은 순서를 밟았다. 결론이 달랐다 — `CollectPeriod` 를 붙이지 않고, 대신 첫 쪽만 읽는 사실을 드러내는 감시를 넣었다. 테스트 868 → 876건.

**1차 실측 — 기간 UI 가 아예 없다**

| 측정 | 결과 |
|---|---|
| 날짜 입력칸 | 없음 (`accountDepositor` 는 환불 계좌주, 정규식 `to` 에 걸린 오탐) |
| 연/월 드롭다운 | 없음 (`refundBank` 하나, 숨김) |
| 기간 탭 | 없음 ("전체삭제"·"전체 카테고리" 가 `전체` 에 걸린 오탐) |
| 안내 | **"최근 주문하신 N년 이내의 내역만 보입니다"** |

**붙일 자리가 없다.** 사이트가 기간을 사람에게 열어 주지 않고 대신 기본으로 N<b>년</b>치를 준다. SSG 가 최근 1<b>개월</b>에 묶여 있던 것과 처지가 정반대다. 여기에 기간 개념을 만들어 넣으면 그게 바로 이 저장소가 계속 거절해 온 추측이다.

*사용자 지시는 "오아시스도 같은 방식으로" 였다.* 방식(실측 우선)을 지킨 결과가 "여기엔 적용하지 않는다" 였다. 같은 방식이 같은 결론을 뜻하지는 않는다.

**대신 드러난 것 — 첫 쪽만 읽는다**

```
P4. 쪽 넘김 수단
     - [class*='paging'] : 1개
     - a[class*='next'] : 1개
P5. 주문 행 수
     - 진입 직후 : 6행 / 스크롤 후 : 6행 (변화 없음)
```

무한 스크롤이 아니라 쪽 나눔이고, 수집기는 첫 쪽만 읽는다. 여기서 **내가 틀린 추론을 했다.** "저장분 8건 > 한 쪽 6행이니 이미 2쪽 이상" 이라고 사용자에게 말했다.

**2차 실측이 그 추론을 깼다**

```
P4-1. 쪽 넘김 블록의 속
     컨테이너: <div class="paging-wrap">
       <ul>  글자=1
       <li>  글자=1
       <b>   글자=1
```

쪽이 **하나뿐**이고 이동할 `<a>` 가 **하나도 없다.** 1차의 `a[class*='next'] : 1개` 는 블록 밖의 오탐이었다(P1·P2 의 오탐과 같은 종류). 저장분 8건은 시간이 지나며 목록이 바뀐 누적분이지 2쪽의 증거가 아니었다.

*개수만 세는 측정은 답을 주지 않는다.* 1차가 준 것은 "paging 이라는 클래스가 1개" 뿐이었고, 그것으로 순회를 짰다면 존재하지도 않는 링크를 클릭하는 코드가 됐다. **속을 떠야 알 수 있는 것이 있다.**

**그래서 순회를 쓰지 않았다**

2쪽이 어떻게 생겼는지 볼 기회가 없었다 — 링크인지 스크립트인지, 현재 쪽을 무엇으로 표시하는지, 마지막 쪽에서 무엇이 달라지는지 전부 미측정이다.

여기서 짜면 **틀렸을 때의 실패 모양이 최악**이다. 셀렉터가 어긋나면 조용히 첫 쪽만 읽고 <b>성공으로 기록된다</b> — 지금과 동작이 완전히 같으면서 "쪽을 넘긴다" 는 거짓 인상만 남는다. 하나로에서 "등록만 하고 한 번도 실행되지 않은 수집기" 로, SSG 에서 "1개월인 줄 몰랐던 프리셋" 으로 이미 두 번 치른 값이다.

**고치는 대신 드러낸다.** 쪽 이동 링크가 하나라도 보이면 경고가 뜬다. 판정 근거는 실측값이다 — 지금 0개이므로 0보다 크면 늘어난 것이고, **정상 상태에서 오탐이 나지 않는다.** 매 회차 뜨는 경고는 곧 무시당하고, 그러면 정작 쪽이 늘어난 날 아무도 보지 않는다.

그리고 그 경고가 뜨는 날이 **2쪽을 실측할 수 있는 첫 순간**이다. 지금은 실측 자체가 불가능하다.

**함께 넣은 것**

- 블록 자체가 사라지면 그것도 경고한다 — 화면이 개편되면 이 감시는 그 순간부터 아무것도 보지 못하는데, 조용히 그렇게 되면 안 된다.
- 감시가 수집을 깨뜨리지 않는다. 드라이버가 흔들려도 삼키고 로그만 남긴다 — 멈추는 쪽으로 틀리는 것이 더 나쁘다.
- 호출 자리를 소스에서 세는 배선 가드. 지워 보고 red 를 확인했다.

**실계정 확인 (2026-08-12 07:39) — 감시가 조용했다**

오아시스를 한 회차 돌렸다. 6건 수집, 전부 중복, 부분 실패 0.

```
Oasis 수집 완료 - 6 건
전체 수집 완료 - 총 6 건 (부분 실패 0 건, 건너뜀 0 건)
신규 주문: 0개
```

**쪽 감시는 한 줄도 남기지 않았다.** 세 문구(`여러 쪽이다`·`블록을 찾지 못했다`·`쪽 수 확인 실패`)를 로그에서 직접 찾아 확인했다.

*침묵이 애매하지 않다는 점이 중요하다.* 이 감시는 루프 끝에서 <b>무조건</b> 호출되고, 블록을 못 찾거나 예외가 나면 각각 다른 문구를 남긴다. 그래서 아무것도 없다는 것은 <b>블록을 찾았고 + 링크가 0개였고 + 예외도 없었다</b>는 뜻이다. "돌지 않아서 조용한" 경우와 구분된다.

**덤으로 열려 있던 물음 하나가 닫혔다.** 프로브는 <b>사람이 연 브라우저</b>에 붙어 6행·1쪽·링크 0을 봤고, 수집기는 <b>자동 로그인한 자기 세션</b>으로 같은 화면을 봤다(6건 수집, 링크 0). 둘이 다른 화면을 받을 가능성이 원리상 있었는데, 이번 회차가 같다는 것을 보였다.

*곁가지.* `부모 요소 내에서 자식 요소를 찾을 수 없습니다` 가 반복해서 찍히는데, `price` 를 찾는 선택적 조회의 catch 로그다. `price` 는 러너가 애초에 저장하지 않으므로(`name`/`qty` 만) 버려지는 데이터가 아니다 — 소음일 뿐이다.

**남은 것**

- 쪽 넘김은 미구현이다. 경고가 뜨는 날 `OasisPeriodProbe` 로 2쪽을 실측하고 그때 구현한다.
- "최근 N년" 의 N 은 여전히 모른다(프로브가 숫자를 가린다). 기본 구간이 넓어 급하지 않다.
- **저장된 오아시스 주문 8건은 아직 `collector` 가 비어 있다.** v0.19.0 이전 행이고, 이번 회차도 전부 중복이라 새겨지지 않았다(중복이면 저장을 건너뛰므로 의도된 동작이다). 오아시스는 `CollectPeriod` 를 쓰지 않아 지금은 무해하지만, 나중에 쓰게 되면 그 수집기는 첫 회차에 기본 범위를 한 번 훑는다.
- **사이트가 보여 준 6건과 저장된 8건이 다르다.** 2건은 사이트의 "최근 N년" 창에서 이미 밀려난 것으로 보인다 — 이미 수집해 둔 것이라 손해는 없지만, 수집 전에 밀려나면 그대로 잃는다는 뜻이기도 하다. 쪽 넘김이 필요해지는 날의 근거가 하나 더 생겼다.

### [2026-08-12 00:50] 실사이트가 답했다 — 15건이 묻혀 있었다 (v0.19.5)

**작업 개요**

v0.19.4 를 실계정으로 검증했다. 날짜 주입 스크립트가 SSG 페이지에서 죽는 것을 잡아 고쳤고, 다시 돌려 **SSG 15건·품목 91개**를 수집했다. 테스트 867 → 868건.

**1차 — 구간은 맞았고 스크립트가 죽었다**

```
SSG 조회 구간 2024-08-12 ~ 2026-08-12          ← 계산은 의도대로
SSG 수집 실패 - 단계: navigatePurchased
  JavascriptException: javascript error: Event is not a constructor
```

`new Event('change', {bubbles:true})` 가 터졌다. 브라우저가 낡아서가 아니다 — **이 페이지가 전역 `Event` 를 자기 변수로 덮어썼다.** 옛 스크립트에 흔한 형태이고, 그 순간 표준 생성자는 이 문서 안에서 존재하지 않는 것이 된다.

**문법은 옳은데 이 사이트에서만 죽는다.** 단위테스트로는 원리상 드러나지 않는 종류이고, 실사이트를 밟아야만 나온다. 전역에 기대지 않는 `document.createEvent` 로 바꿨다. 폐기 예정 API 지만 <b>페이지가 무엇을 덮어쓰든 영향받지 않는다</b>는 점이 여기서는 결정적이다. 소스에 `new Event(` 가 다시 들어오면 빨개지는 가드를 함께 넣었다.

*격리는 제대로 돌았다.* SSG 가 죽어도 Emart 는 17건을 정상 수집했고, `step=navigatePurchased` 와 스크린샷이 남았다(v0.8.0 진단 장치). 부분 실패 1건으로 집계됐다.

**2차 — 브레이커가 내 버그를 세고 막았다**

```
SSG 수집 건너뜀 (연속 실패 1회 — 백오프 중) - 재시도 예정: 2026-08-12T12:34
```

12시간이다. 실제 사이트 장애가 아니라 방금 고친 버그의 흔적인데, 브레이커는 그것을 구분하지 못한다 — 구분하지 않는 것이 옳다.

**DB 를 건드리지 않고 풀었다.** `CollectBreakerPolicy.positiveProperty` 의 javadoc 이 *"임계는 안전장치라 화면에 노출하지 않는다. 개발·시험용으로 시스템 프로퍼티만 연다"* 라고 적고 있다 — 정확히 이 상황을 위해 열어 둔 손잡이다. 백오프 상한을 1분으로 낮춰 **그 실행에만** 적용했다.

`bootRun` 은 `-D` 를 앱 JVM 으로 넘기지 않아(포크된 JVM 이 상속하지 않는다 — 프로브 대상 지정에서 같은 함정을 이미 겪었다) 빌드된 JAR 로 띄웠다. `config/jbg_config.yml` 도 DB 도 손대지 않았고, 앱을 다시 켜면 기본값으로 돌아간다.

**3차 — 답이 나왔다**

```
SSG 조회 구간 2024-08-12 ~ 2026-08-12
SSG 온라인 구매내역 조회
SSG 수집 완료 - 15 건
전체 수집 완료 - 총 32 건 (부분 실패 0 건, 건너뜀 0 건)
신규 주문: 15개, 신규 아이템: 91개
```

**되돌림 경고가 뜨지 않았다** — 사이트가 2년 구간을 그대로 받았다는 뜻이다. "최근 N개월까지 제공" 안내가 걱정거리였는데, 적어도 2년 요청을 거부하지는 않는다.

**가장 값진 숫자 하나.** 저장된 SSG 주문의 최신 구매일이 **`2026-06-01`** 이다. 실행일이 8월 12일이니 **2개월 이상 전**이고, 예전의 1개월 고정 창으로는 <b>원리상 볼 수 없었다.</b> 결함이 이론이 아니라 실제로 데이터를 잃고 있었다는 증거다. 두 회차 연속 0건이던 그 자리다.

**함께 확인된 것**

- `jbg_order.collector='SSG'` 로 15건이 새겨졌다 — 다음 회차는 2년치가 아니라 `2026-06-01` 부터 조회한다
- 브레이커 연속 실패가 0으로 초기화됐고, **`last_nonempty_time` 이 사상 처음 채워졌다.** SSG 는 더 이상 "한 번도 데이터를 받은 적 없는" 수집기가 아니다 — v0.18.7 이 7일 뒤 띄우려던 `NO_DATA` 경보도 이제 뜰 이유가 없다
- 재조회 바닥은 두 수집기 모두 비어 있다(저장 실패 0건이니 정확하다). v0.19.1 코드가 프로덕션에서 규칙대로 돌았다
- FTP 는 이번에 상태 파일이 아니라 **실제 주문 파일**(`jangbogo_orders_*.encrypted`)을 올렸다. 서버는 `127.0.0.1:2121` 로컬이다

**교훈으로 남길 것**

실사이트를 밟기 전까지 이 결함은 **테스트 867건 전부 초록**이었다. 프로브로 셀렉터와 날짜 형식을 실측했는데도, *"그 값을 어떻게 넣을 것인가"* 의 실행 환경까지는 재지 못했다. 실측이 DOM 의 <b>모양</b>은 알려 줘도 <b>그 페이지의 자바스크립트 전역이 어떻게 오염돼 있는가</b>는 알려 주지 않는다.

**남은 것**

- `Oasis` 는 여전히 조회 기간을 지정하지 않는다(사이트 기본값). 같은 순서가 필요하다.
- "최근 N개월까지 제공" 의 N 은 여전히 모른다. 2년 요청이 통과했으므로 급하지 않지만, `split` 을 켤 근거는 아직 없다.

### [2026-08-11 22:40] SSG 도 되돌아가지 않고 있었다 (v0.19.4)

**작업 개요**

SSG 조회 구간을 최근 1개월 고정에서 `CollectPeriod` 기반으로 바꿨다. 실측용 프로브(`SsgPeriodProbe`)를 함께 넣었다. 테스트 854 → 867건.

**발단 — 실계정 수집이 답을 반쯤만 줬다**

사용자 지시로 실계정 수집을 돌려 SSG 를 확인했다. 로그가 결론을 줬다.

```
SSG 로그인 판정: signedIn=true (보이는 logoutBtn=true)
SSG 온라인 구매내역 조회
SSG 온라인 구매내역 없음 (안내 행) — 해당기간내에 온라인몰에서 구매하신 내역이 없습니다.
```

셀렉터는 멀쩡했다. 로그인하고 페이지에 도달해 **사이트가 준 "구매내역 없음" 안내를 읽어냈다.** 같은 브라우저·같은 자격증명으로 Emart 는 17건을 정상 수집해 대조군까지 맞았다.

그런데 문구에 "**해당기간내에**" 가 있었다. 코드를 보니 이랬다.

```java
ClickUtil.safeClick(driver, By.xpath("//label[@for='sf_m3']")); // 단기간 조회 (1개월전부터 지금까지)
```

**확인된 것은 "최근 1개월 온라인 구매가 없다" 였지 "온라인 구매가 없다" 가 아니었다.** 그리고 시작점이 언제나 오늘 기준 1개월 전이라 **되돌아가지 않는다** — 한 달 넘게 앱을 안 쓰면 그 구간은 다음 회차에도 범위 밖이다. v0.19.1 에서 고친 워터마크 결함과 같은 계열이고, `CollectPeriod` 를 쓰는 것은 하나로뿐이었다.

**바로 고치지 않았다 — 잴 것이 있었다**

계산하는 쪽은 하나로에 이미 있다. 모르는 것은 **계산한 구간을 화면에 넣는 법**이었고, 저장소에도 문서에도 기록이 없었다.

추측하면 안 되는 근거가 코드 안에 있었다. **`sf_m3` 인데 뜻은 1개월이다.** 이름에서 뜻을 읽을 수 없다는 증거다. 여기서 `sf_m6` 을 "6개월이겠지" 하고 넣으면 엉뚱한 구간을 조회하고, 그 결과는 0건이거나 조용히 모자란 목록이라 실패로 드러나지 않는다.

게다가 그 시점에 SSG 는 5분 전에 로그인한 상태라 **6시간 규칙**에 걸려 자동 로그인을 한 번 더 할 수 없었다.

그래서 v0.19.0 이 하나로에서 한 순서를 그대로 밟았다 — **프로브 → 실측 → 구현.** 프로브는 사람이 순정 Chrome 에 로그인하면 CDP 로 밖에서 붙어 폼만 읽으므로 자격증명을 다루지 않고, 자동 로그인이 한 번도 더 일어나지 않아 6시간 규칙과도 무관하다.

**실측이 답을 줬다**

```
id=_d_sch_start_dt  name=schDateFr  type=text  값형태=NNNN-NN-NN  maxlength=10  readonly=false
id=_d_sch_end_dt    name=schDateTo  type=text  값형태=NNNN-NN-NN  maxlength=10  readonly=false
```

**날짜를 직접 넣는 칸이 있었다.** 표기는 `yyyy-MM-dd`, `readonly` 도 아니다. 프리셋을 누를 이유가 사라졌다.

프리셋은 `sf_m`·`sf_m2`·`sf_m3`·`sf_m4`·`sf_m2_01~03`·`sf_r`(최소 조회 기간)·`sf_r2`(최대 조회 기간) 로 일곱 종이었다. 어느 것이 몇 개월인지는 여전히 모르지만 **알 필요가 없어졌다.**

**값을 JS 로 넣는 이유.** 이 칸에는 달력 위젯이 붙어 있을 수 있고, 그러면 `sendKeys` 가 위젯을 띄워 뒤이은 조회 클릭을 가로챈다. 값을 직접 넣고 `change` 를 쏘면 위젯을 건드리지 않는다.

**넣은 값이 남았는지 확인한다**

이 사이트는 조건이 틀려도 **예외가 아니라 빈 목록**을 준다. 사이트가 날짜를 되돌리면(프리셋이 이기거나 제공 범위를 넘어 잘리면) 그 회차는 '정상인 0건' 으로 굳는다 — 이번에 겪은 것이 정확히 그 형태였다. 그래서 조회 뒤 칸을 다시 읽어 어긋나면 경고를 남긴다.

**어긋나도 멈추지는 않는다.** 사이트가 제공 범위에 맞춰 잘라 준 것일 수 있고 그때 결과는 여전히 쓸모 있다. 멈추는 쪽으로 틀리면 사람이 알아채기 전까지 아무것도 모이지 않는다.

숫자만 뽑아 견주는 것도 이유가 있다 — 사이트가 표기를 다듬는 것(점↔하이픈)까지 어긋남으로 세면 정상 회차마다 경고가 뜨고, 그러면 경고가 무시당하기 시작한다.

**모르는 것은 켜지 않았다**

안내 문구에 "최근 N개월까지 제공" 이 있었으나 프로브가 숫자를 가렸다(개인정보 규칙). 그래서 `CollectPeriod.split` 의 구간 쪼개기는 켜지 않았다 — 하나로에서 상한을 모른다고 `maxDays=0` 으로 둔 것과 같은 판단이다.

**가드를 무력화해 red 를 봤다**

날짜 넣는 두 줄을 지우고 프리셋 클릭을 되돌렸더니 정확히 짚었다.

```
1개월 프리셋을 아직 누른다 — 계산한 구간이 무시될 수 있다.
```

`sf_m3` 이 javadoc 에 <b>역사로</b> 남아 있는데도 초록이었던 것도 확인했다 — 가드가 주석을 걷어내고 보기 때문이다.

**곁가지 — 레지스트리의 이름 중복을 없앴다**

`MallRegistry` 가 `"SSG"` 를 문자열로 다시 적고 있었다. 이 값은 `jbg_order.collector` 로 저장되고 조회 시작일을 유도하는 키다. 두 곳에 적으면 한쪽만 고쳐져도 컴파일은 통과하고, 그때 기준일이 늘 비어 매 회차 기본 범위를 훑는다. `Ssg.COLLECTOR` 로 모았다(`HanaroOffline` 이 같은 이유로 같은 형태다).

**남은 것**

- 실사이트에서 아직 돌려 보지 않았다. SSG 는 6시간 규칙이 풀린 뒤에 확인해야 한다.
- `Oasis` 는 여전히 조회 기간을 지정하지 않는다(사이트 기본값). 같은 실측이 필요하다.
- 첫 회차는 2년치를 요청하는데, 사이트 제공 범위를 넘으면 무엇을 주는지 모른다. 위의 되돌림 경고가 그때 드러내 준다.

### [2026-08-11 20:15] 로그인 화면만 다른 판을 싣고 있었다 (v0.19.3)

**작업 개요**

Bootstrap CDN 버전을 `5.3.3` 으로 통일했다(fragments 둘이 `5.3.2` 였다). 화면 간 자산 판이 갈리면 빨개지는 가드를 함께 넣었다. 테스트 852 → 854건.

**배경**

전수 확인(v0.19.2) 중에 곁가지로 발견해 보고만 해 두었던 것을 사용자가 고치라고 했다.

| 화면 | Bootstrap |
|---|---|
| `fragments/header.html` (CSS) | 5.3.2 |
| `fragments/footer.html` (JS) | 5.3.2 |
| `signin.html` (CSS·JS) | **5.3.3** |

**원인은 또 같은 모양이었다**

`signin.html` 은 `layout:decorate` 를 쓰지 않는 **독립 페이지**라 자기 `<head>` 를 들고 있다. 나머지 다섯 장은 레이아웃을 통해 fragments 를 싣는다. 그래서 "어느 판을 쓰는가" 가 두 곳에 살고, 한쪽만 올라갔다. v0.19.2 의 몰 주소와 **구조가 똑같다** — 이번 세션에서 세 번째로 같은 형태를 만났다.

**왜 그냥 두면 안 되나**

판이 갈려도 **화면은 대체로 멀쩡히 그려진다.** CSS 클래스도 컴포넌트 API 도 패치 버전 사이에서는 거의 그대로다. 그래서 아무도 모른다. 문제는 두 판 사이에서 바뀐 것 하나를 밟는 날인데, 그때 **한쪽 화면에서만** 깨진다. 화면별로 다른 자산을 싣고 있다는 사실을 모르면 원인 후보에 CDN 버전이 아예 오르지 않는다.

**가드가 숫자를 박지 않는 이유**

"5.3.3 이어야 한다" 로 짜면 판을 올릴 때마다 이 테스트가 함께 빨개진다. 그러면 사람은 테스트를 고치는 김에 통과시키는 쪽으로 움직이고, 결국 가드가 형식이 된다. **강제할 것은 "전부 같아야 한다" 하나**이고, 어느 판이 옳은지는 사람이 정한다. 패키지별로 판이 하나인지만 본다.

`bootstrap-icons` 도 같은 규칙에 자동으로 걸린다(지금은 `1.11.3` 로 이미 일치). 앞으로 CDN 패키지를 하나 더 실어도 규칙을 다시 쓸 필요가 없다.

**red 를 확인했다**

`header.html` 만 구 판으로 되돌렸더니 어긋난 화면 이름까지 짚어 줬다.

```
화면마다 bootstrap 의 판이 다르다 — 한쪽 화면에서만 깨지는 결함이 되고,
원인을 CDN 버전까지 되짚어야 한다:
  {5.3.3=[fragments\footer.html, signin.html], 5.3.2=[fragments\header.html]}
```

대조군도 넣었다. 정규식이 한 건도 못 찾으면 검사가 **빈 반복으로 통과**한다 — 판이 제각각이어도 초록인 상태다.

**곁가지 — 구 사이트용 잔재를 걷어냈다**

전수 확인에서 드러난 두 가지를 사용자 지시로 지웠다.

- `src/test/resources/test_mall_account.yml` — 하나로 **실계정 아이디·비밀번호가 평문으로** 들어 있었다. `.gitignore` 에 걸려 있었고 git 이력에 올라간 적이 없어(확인함) 공개 저장소 유출은 없었다. 커밋된 `.example` 도 자리표시자뿐이었다. 추적되지 않던 파일이라 되돌릴 수 없는 삭제였다.
- `src/test/java/com/jiniebox/jangbogo/mall/HanaroTest.java` — 구 사이트(`nonghyupmall.com`)를 단계별로 훑던 **수동 실습용 클래스**다. `@Test` 가 하나도 없어 CI 는 한 번도 돌린 적이 없고, 위 파일의 유일한 사용처였다. 대상 사이트가 사라졌으므로 실행해도 의미가 없다.

**지워도 잃는 것이 없는지 먼저 봤다.** `Hanaro` 클래스를 `@Deprecated` 로 남긴 이유는 "다시 수집할 날의 출발점" 이었는데, 그 출발점을 실제로 붙잡고 있는 것은 변환 규칙을 고정하는 `HanaroParserTest` 다. 그것은 그대로 있다. 지운 쪽은 브라우저를 손으로 몰아 보던 스크래치패드이고, 추적되던 파일이라 필요하면 이력에서 꺼낼 수 있다.

**남긴 것.** `.gitignore` 의 `test_mall_account.yml` 항목은 그대로 뒀다 — 누가 `.example` 을 다시 복사해도 커밋되지 않는다. 값이 없는 그 `.example` 자체도 남겼다. 하나로 전용이 아니라 `hanaro`·`ssg`·`oasis` 세 몰의 서식이라 다음 하네스가 쓸 수 있다. **다만 지금은 읽는 코드가 없다** — 그 사실을 여기 적어 둔다.

`config/mall_account.yml` 은 **건드리지 않았다.** 같은 형태(추적 안 됨 + ignored)로 걸려 나왔지만 성격이 정반대다 — `MallAccountYmlService` 가 가리키는 <b>앱의 현역 계정 저장소</b>이고, 지우면 연결해 둔 몰 계정이 전부 끊긴다. `RepositoryHygieneTest`·`DistributionHygieneTest` 가 이미 커밋·배포 포함을 막고 있다.

### [2026-08-11 19:05] 화면은 아직 옛 사이트를 가리키고 있었다 (v0.19.2)

**작업 개요**

메인 화면 쇼핑몰 목록의 하나로마트 URL 을 `nonghyupmall.com` → `nhhanaro.co.kr` 로 고쳤다. 어긋남을 감시하는 가드를 함께 넣었다. 테스트 849 → 852건.

**배경 — 사용자가 화면에서 발견했다**

v0.19.0 에서 하나로 수집 대상을 새 사이트로 옮기며 수집기·레지스트리·검증 수집기·세션 착지 지점을 전부 따라가게 했다. 그런데 **사용자가 실제로 보는 화면은 옮기지 않았다.** 사용자가 알려 줬다.

이 실패의 성질이 v0.19.1 과 같다. 링크를 누른 사람은 이 앱이 더 이상 수집하지 않는 사이트에 착지하고, 거기서 구매내역을 보며 "여기 있는데 왜 안 모이나" 를 묻게 된다. **아무것도 실패시키지 않는다 — 화면만 조용히 거짓말한다.**

**원인 — 같은 사실이 두 곳에 산다**

쇼핑몰 카드는 주소를 `index.html` 에 **직접 적어** 둔다. 같은 사실이 `MallRegistry` 에도 있다. 이 저장소가 반복해서 대가를 치른 형태이고(`SsgSessionCollector.LOGIN_MARKER` 사본을 없앤 것이 같은 이유였다), 이번에도 한쪽만 고쳐졌다.

**고른 것 — 중복을 없애는 대신 어긋나면 빨개지게 했다**

카드를 모델에서 그리도록 바꾸면 중복 자체가 사라진다. 그런데 그것은 정적 HTML 로 짜인 카드 세 개를 전부 다시 짜는 일이고, 이 요청은 주소 한 줄이다. **증명할 수 없는 화면 개편을 한 줄짜리 수정의 부수효과로 끼워 넣지 않는다** — v0.19.1 에서 중복 검사와 날짜 파싱 순서를 건드리지 않은 것과 같은 판단이다.

대신 `MallSiteLinkTest` 가 화면의 안내 주소와 등록된 몰의 호스트를 비교한다. 비교를 **호스트로만** 하는 이유가 있다 — 화면은 사람이 들어갈 첫 화면을, 레지스트리는 로그인 지점을 담아 경로가 다르다(오아시스가 `/login` 이다). 경로까지 맞추라고 하면 정상 상태에서 빨개진다.

**가드가 초록인지 붉은지 확인했다**

주소를 구 값으로 되돌려 두 검사가 실제로 빨개지는 것을 봤다.

```
등록된 수집기가 쓰지 않는 사이트를 화면이 안내한다: www.nonghyupmall.com
  (등록된 것: [www.nhhanaro.co.kr, www.oasis.co.kr, www.ssg.com])
```

대조군도 함께 넣었다. 정규식이 한 건도 못 찾으면 위 검사들이 **빈 집합끼리 비교하거나 빈 반복으로 통과**한다 — 화면이 통째로 잘못돼도 초록인 상태다. 찾은 주소 행의 개수를 등록된 몰 수로 고정했다.

**전수 확인 — 다른 화면에도 남아 있는가**

사용자 요청으로 화면 전부를 훑었다. **더 없다.**

| 대상 | 결과 |
|---|---|
| `templates/` 8장 (`dev`·`fragments` 포함) | 몰 주소를 안내하는 곳은 `index.html` 뿐. 세 줄 모두 현재 수집 대상과 일치 |
| `static/js/jangbogo.js` | 몰 주소 없음 |
| 컨트롤러·DTO 가 만들어 내보내는 문자열 | 몰 주소 없음 |
| 대시보드 주소(`localhost:8282`) | `application.yml` 의 `server.port` 와 문서·스크립트 93곳이 전부 일치 |
| 사용자 문서(`doc/user`, `README`) | 몰 주소를 아예 싣지 않는다 |

**확인하면서 검사를 넓혔다.** 처음 가드는 `index.html` 한 장만 봤다. "지금 한 장뿐" 을 검사에 박아 두면 **다음에 생기는 화면은 감시 밖**이고, 이 가드가 막으려는 실패가 거기서 그대로 재현된다. `templates/` 아래 모든 `.html` 을 훑도록 바꿨다.

**남은 `nonghyupmall.com` 참조는 전부 의도된 것이다** — `@Deprecated Hanaro` 클래스(다시 수집할 날의 출발점), `HanaroSiteProbe` 의 온라인 화면 프로브, 이 가드 자신, 그리고 이력 문서. 화면에는 하나도 없다.

**남은 것**

- 중복 자체는 그대로다. 몰을 하나 추가하면 여전히 `MallRegistry` 와 `index.html` 두 곳을 손대야 하고, 가드는 어긋남을 알려 줄 뿐 막지는 못한다. 카드를 모델 기반으로 바꾸는 것은 별건이다.
- `mall/HanaroTest` 는 구 사이트를 향한 **수동 실습용 클래스**다(`@Test` 가 하나도 없어 CI 는 돌리지 않는다). 대상이 사라졌으므로 실행하면 실패하거나 엉뚱한 화면을 본다. `Hanaro` 클래스처럼 남길지 지울지는 정하지 않았다.

### [2026-08-11 18:10] 자기교정의 숨은 전제 — 실패한 날짜가 가장 늦어야만 성립했다 (v0.19.1)

**작업 개요**

부분 저장 실패가 조회 기준일을 넘겨 그 구간을 영구히 봉인하던 결함을 고쳤다. 함께 `MallRegistry.HANARO.loginSignals` 를 실측값으로 채웠다. 테스트 816 → 849건.

**배경 — 어제 만든 것 안에 있었다**

v0.19.0 에서 `CollectPeriod` 를 만들며 "기준일을 저장하지 않고 유도한다" 를 골랐고, 그 근거를 javadoc 에 적었다 — *저장이 실패하면 기준일도 자동으로 뒤로 남아 다음 회차가 다시 가져온다*.

**그 문장에는 전제가 하나 숨어 있었다: 실패한 주문이 그 회차에서 가장 늦은 날짜여야 한다.**

`MallOrderUpdaterRunner` 는 주문 하나가 깨지면 그것만 롤백하고 루프를 계속한다(v0.8.0 이후 의도된 격리다). 그런데 같은 회차의 **더 늦은 주문이 커밋되면** `MAX(date_time)` 이 실패한 날짜를 지나간다. 다음 회차의 시작일이 그 뒤가 되고, 시작점은 앞으로만 가므로 되돌아올 방법이 없다. **유도가 막으려던 바로 그 실패가 유도 안에서 일어난다.**

**red 를 먼저 봤다**

회차가 남긴 DB 상태를 그대로 만들어 놓고(이른 날짜는 없고 늦은 날짜만 커밋) 다음 조회 구간을 물었다.

```
저장하지 못한 20260601 을 지나 20260610 부터 조회한다 — 그 구간은 영영 다시 조회되지 않는다.
```

**고른 것 — 앞서 나가는 기준일이 아니라 뒤로 당기는 바닥**

두 갈래를 놓고 골랐다.

| 안 | 방식 | 버린 이유 |
|---|---|---|
| 실패 시점에서 루프 중단 | 날짜순 정렬 후 첫 실패에서 멈춘다 | 스키마 변경이 없어 깔끔하지만, **영구히 저장 불가한 주문 하나가 그 뒤의 모든 수집을 막는다.** 원래 결함보다 나쁘다 |
| 미저장 날짜를 적어 시작일을 되돌린다 | 실패한 가장 이른 날짜를 기록 | **채택.** 막히지 않고, 값이 틀려도 손해가 겹쳐 가져오는 쪽이다 |

핵심은 **저장하는 값의 방향이 반대**라는 것이다. `CollectPeriod` 가 경계한 것은 *앞서 나가는* 기준일이고, 여기 적는 것은 *뒤로 당기는* 바닥이다. 방향이 반대라 실패 모양도 반대다 — 낡거나 틀리면 겹쳐 조회할 뿐, 구간을 봉인하지 않는다. 이 클래스가 일관되게 고르는 방향("덜 가져오는 쪽보다 겹쳐 가져오는 쪽이 안전하다") 그대로다.

**언제 지우는가가 실제로 어려웠다**

바닥을 지우는 조건을 `isCompleted()`(SUCCESS + EMPTY)로 잡았다가 되물렸다. **0건은 "정말 안 샀다" 와 "셀렉터가 깨졌다" 를 구분하지 못한다** — v0.18.7 이 `EMPTY` 를 `SUCCESS` 와 따로 기록하기로 한 이유가 정확히 그것이다. 뒤쪽인데 지우면 그 순간 구멍이 봉인되고, 그것은 이 수정이 막으려는 실패 그 자체다.

그래서 **실제로 주문을 받아 온 회차에서만** 지운다. 대가는 0건이 이어지는 동안 조금 더 이른 날짜부터 조회하는 것뿐이다. 실패·건너뜀 회차는 아예 손대지 않는다 — 자기 구간을 한 번도 훑지 못했으므로 지울 근거도 새로 적을 근거도 없다.

**가드가 green-by-construction 이었다**

배선 가드를 "러너 파일에 `saveRetryFrom(` 이 들어 있는가" 로 짰다. 그 상태에서 **회차 끝의 호출 한 줄을 지워 봤더니 그대로 초록이었다** — 적는 코드는 파일에 있고 도달만 하지 않는 상태를, 파일 단위 검사는 구분하지 못한다. 이 저장소가 반복해서 겪은 형태라 `run()` 본문에서 한 번 더 세도록 고쳤고, 다시 무력화해 붉어지는 것을 확인했다.

양쪽을 각각 무력화해 red 를 확인한 것도 같은 이유다. 적기만 하고 읽지 않으면 컬럼에 값만 쌓이고, 읽기만 하고 적지 않으면 바닥이 늘 비어 있다 — **어느 쪽이든 단위테스트는 전부 초록인데 실제로는 무동작이다.**

**건드리지 않은 것**

- **주문 하나가 깨져도 루프를 계속하는 격리.** 이번 결함의 무대이지만 원인이 아니다. 여기서 멈추게 하면 v0.8.0 이 제거한 "한 건이 전체를 막는" 형태가 되살아난다.
- **`decideStatus` 의 판정식.** 저장 실패를 바닥으로 적는 것과 그 회차를 FAIL 로 볼지는 별개다. 함께 바꾸면 회귀 원인을 가릴 수 없다.
- **중복 검사와 날짜 파싱의 순서.** 날짜를 먼저 파싱하면 실패 기록이 한 자리 늘지만, 형식이 깨진 중복 주문의 집계가 미묘하게 달라진다. 공용 러너에서 증명할 수 없는 변화를 부수효과로 끼워 넣지 않는다.

**곁가지 — 하나로 만료 판정 신호를 채웠다**

실측 근거가 생겼다: 주소 마커 `nahh_70021`(로그인 화면의 CMS 페이지 코드), 셀렉터 `input[type=password]`.

셀렉터를 id 가 아니라 타입으로 잡은 데는 이유가 있다. 이 사이트는 **사람이 치는 칸과 따로 숨은 폼**(`userId`/`userPw`)을 둔다. 관측은 `isDisplayed()` 로만 세므로 숨은 칸을 잡으면 **선언은 돼 있는데 판정은 죽은** 상태가 된다. 마커가 거래내역 주소에 걸리지 않는 것도 테스트로 고정했다 — 걸리면 멀쩡한 수집이 매 회차 만료로 멈춘다.

**인증 쿠키 이름은 계속 비워 뒀다. 그것이 실측 결과다.** 로그인·로그아웃 상태의 쿠키 이름이 같았다(`SCOUTER`/`NSESSIONID`/`NAHH_SSID`). 셋 다 로그인 전에 발급되므로 하나라도 넣으면 첫 화면만 열어도 판정이 통과하고, 사람이 로그인을 건너뛴 미인증 스냅샷이 저장된다 — 이 검사가 막으려는 상황이 정확히 그것이다. **아는 이름을 적는 쪽이 검사를 무력화한다.** SSG 가 `JSESSIONID`·`FSID` 를 뺀 것과 같은 판단이고, 하나로는 그 조건이 전부에 걸린 경우다. 이름이 아니라 값으로 갈라야 하는데 값 기반 판정은 아직 없다.

**남은 것**

- 바닥을 읽는 수집기는 아직 `HanaroOffline` 하나다. 다른 기간 조회형 수집기가 생기면 같은 두 줄을 붙여야 하고, 그것을 강제하는 장치는 없다.
- 하나로는 세션 주입 경로가 없어 `loginSignals` 가 지금은 쓰이지 않는다. 선언은 그 경로가 켜지는 날을 위한 것이다.

### [2026-08-11 13:40] 하나로 오프라인 수집기 — 대상이 옮겨 간 것을 따라갔다 (v0.19.0)

**작업 개요**

하나로마트 오프라인 거래내역 수집기(`HanaroOffline`)를 신설하고 `MallRegistry.HANARO` 에 둘째 수집기로 등록했다. 함께 기간 조회 정책(`CollectPeriod`)과 수집기별 워터마크(`jbg_order.collector`)를 넣었다. 실계정으로 거래 4건·품목 73개 수집을 확인했다. 테스트 738 → 815건.

**배경**

하나로 수집이 계속 0건이었다. 로그인은 성공했고 "영수증 목록 개수: 0" 이었다 — 로그만으로는 실제로 구매가 없었던 것인지 셀렉터가 어긋난 것인지 구분할 수 없었다. 사용자 확인으로 원인이 드러났다: **하나로가 서비스를 개편해 오프라인 거래내역을 `nhhanaro.co.kr` 로 분리했다.** 기존 코드는 `nonghyupmall.com` 만 탐색하므로 오프라인을 영영 못 가져온다. 셀렉터 버그가 아니라 대상이 사라진 것이다.

**상세 내용**

*추측 대신 실측.* 새 사이트의 DOM 을 아무도 기록해 두지 않았다. 그래서 먼저 프로브(`HanaroSiteProbe`)를 만들어 **사람이 로그인한 브라우저에 밖에서 붙어** 화면 구조만 떴다. 이 저장소는 PUBLIC 이므로 프로브는 값을 옮기지 않고 형태만 옮긴다(`DomShapeReport` — `2099-12-31` 은 `NNNN-NN-NN` 이 되고 상품명은 `가×7` 이 된다). 그 경계는 테스트로 고정돼 있다.

*실측으로 확정한 것.* 프레임 없음 / 표 하나 / 거래 행(td 6칸)과 품목 행(colspan 1칸)이 **번갈아** 옴 / 품목은 `ul` 하나 = 품목 하나에 `li` 여섯 개(두 거래에서 120÷20 과 66÷11 로 비율이 정확히 6) / 조회는 **같은 주소로 POST**(`st_dt`·`ed_dt`) / 주문번호 없음.

*가장 값진 측정 하나.* 품목 행은 접혀 있어 `getText()` 로는 빈칸이지만 `textContent` 에는 6084자가 들어 있었다. **품목이 처음부터 DOM 에 있다**는 뜻이고, 그래서 수집기는 행마다 클릭하지 않는다.

*프로브가 스스로 만든 실패.* 초기 판정식이 "값이 든 칸이 둘 이상인 행" 이라 **달력 위젯의 요일 줄**(일·월·화…)에 걸려, 거래 목록이 비어 있는데도 '결과 행 확인' 으로 끝났다. 막으려던 실패를 판정식이 재현한 것이다. 머리글로 표를 특정하도록 고치고, **머리글 필터를 일부러 무력화해 red 를 확인**했다.

*실계정에서만 드러난 것 셋.* ① 로그인 폼의 `id=userId`/`userPw` 는 **숨은** 입력이었다 — 거기 타이핑하면 매 회차 `signin` 에서 죽는다. ② 이 사이트는 속 주소로 바로 들어가면 스크립트 없는 조각을 준다 — `?id=` CMS 메뉴 코드가 붙어야 온전히 렌더된다. ③ 로그인 성공을 `alert` 으로 알리는데, 열려 있으면 그 뒤의 모든 WebDriver 호출이 터진다. **로그인에 성공하고도 그 알림 때문에 죽었다.** 셋 다 단위테스트로는 원리상 드러날 수 없었고, 지금은 ③만 목으로 재현해 고정했다.

*조용한 실패를 막은 자리들.* 페이지 순회 상한에 닿으면 경고를 남긴다(조용히 자르면 못 가져온 구간이 워터마크 전진으로 봉인된다). 날짜 형식은 실측하지 못해 `yyyyMMdd` 로 넣되 0건이면 `yyyy-MM-dd` 로 한 번 더 시도한다(형식이 틀려도 이 사이트는 예외가 아니라 빈 목록을 준다). 금액이 비면 날짜만으로 serial 을 만들지 않는다 — 그러면 그날 거래 전체가 한 건으로 접힌다(`Emart` 가 같은 이유로 금지한 형태다).

*배선 가드.* 수집기를 만들고 파서 테스트를 붙이면 전부 초록이지만 `MallRegistry` 에 넣지 않으면 한 번도 실행되지 않는다. 등록·이름 유일성·워터마크 유도·미실측 자리 비움을 각각 고정했고, 대조군으로 판별식이 살아 있는지도 함께 본다. 첫 판에서 가드 둘이 **green-by-construction** 이었던 것을 적대적 검증이 잡아냈다.

*온라인은 개조하지 않고 등록에서 뺐다.* 처음 계획은 기존 `Hanaro` 를 온라인 주문내역용으로 개조하는 것이었다. 그런데 파서를 쓰려고 실측을 준비하던 중 **사용자가 그 서비스를 이용한 적이 없고 앞으로도 이용하지 않는다**고 확인해 줬다(2026-08-11). 개조할 대상이 없는 것이다.

그렇다고 등록만 남겨 두면 손해가 분명하다 — 매 회차 안 쓰는 사이트에 실계정 로그인을 한 번 더 하고, 늘 0건이라 **"정상인 0건" 과 "깨진 셀렉터" 를 가릴 수 없게** 만든다. 이 프로젝트가 하나로에서 겪은 문제가 정확히 그 형태였다. 그래서 `MallRegistry.HANARO` 의 수집기를 오프라인 하나로 줄이고, **계정 검증 수집기와 세션 착지 지점도 함께** 옮겼다. 검증 수집기를 예전 이름으로 두면 폴백이 첫 수집기를 집어 <b>우연히</b> 맞는데, 그때는 선언과 실제가 어긋난 채 조용히 굴러간다.

클래스는 지우지 않고 `@Deprecated` 로 남겼다. 다시 수집해야 할 날이 오면 로그인 흐름과 영수증 파싱 규칙이 출발점이 되고, `HanaroParserTest` 가 그 변환 규칙을 계속 고정한다. 다시 등록할 때 **그 주소에 실제로 데이터가 있는지부터 확인하라**는 경고를 javadoc 에 적어 뒀다.

*남은 것.* 부분 저장 실패가 워터마크를 넘길 수 있는 결함은 공용 러너를 건드려야 해서 별건으로 남긴다. `loginSignals` 는 실측 근거(주소 마커 `nahh_70021`, 보이는 비밀번호 칸)가 생겼으나 아직 비워 뒀다.

### [2026-08-10 17:40] 건강도 사각지대 — 경보가 가장 필요한 수집기가 경보를 못 받았다 (v0.18.7)

#### 발단

사용자가 즉시 수집을 돌리고 "실행이 올바르지 못한 쇼핑몰이 있다" 고 했다. 실행 기록을 훑었다.

| 몰 | 수집기 | 결과 |
|---|---|---|
| seq=1 | SSG | 0건 — 사이트가 "해당 기간 내 온라인 구매내역 없음" 안내를 줌 |
| seq=1 | Emart | 17건 → 신규 2건 (구 템플릿 바코드 부재를 폴백으로 복구까지) |
| seq=2 | Oasis | 6건 → 신규 1건 |
| seq=3 | Hanaro | 0건 — 로그인 성공, "영수증 목록 개수: 0" |

수집이 실패한 몰은 없었다. 예외도 셀렉터 오류도 없다. 그런데 브레이커 표를 보니 **SSG 와 Hanaro 는 `last_nonempty_time = 0`** — 한 번도 데이터를 받은 적이 없다. 그리고 대시보드 건강도는 **둘 다 '정상'** 이었다.

#### 원인 — 폴백이 시계를 리셋한다

`CollectHealthPolicy.judge` 124행:

```java
long sinceData = now - (lastNonEmptyTime > 0 ? lastNonEmptyTime : lastSuccessTime);
```

데이터를 받은 적이 없으면 `lastSuccessTime` 으로 폴백하는데, **그 값은 0건 수집마다 갱신된다.** `sinceData` 가 항상 0 에 가까워 `NO_DATA` 가 영원히 발화하지 않는다.

**셀렉터가 처음부터 어긋나 한 번도 데이터를 못 받은 수집기 — 즉 경보가 가장 필요한 경우 — 가 정확히 경보를 받지 못하는 구조였다.** 이 프로젝트가 반복해서 싸워 온 '조용한 실패' 이고, 사용자가 하나로의 이상을 화면으로 알 수 없었던 이유다.

#### 고친 것

"얼마나 오래 빈손인가" 의 기준 시각을 바로잡았다. 받은 적이 있으면 마지막 수신 시각부터(현행 유지), **없으면 처음 성공한 시각부터** 잰다. 그 값이 없어서 `jbg_collect_breaker.first_success_time` 을 신설했다 — `schema.sql` 선언만으로 `SchemaMigrator` 가 자동 ALTER 한다.

DAO 는 **시그니처를 바꾸지 않았다.** "첫 성공" 은 "값이 아직 비어 있을 때 들어온 성공 시각" 이라 호출부가 알 필요가 없는 사실이고, UPSERT 의 `CASE` 가 스스로 판정한다. 덕분에 `MallOrderUpdater` 는 손대지 않았다.

`judge` 는 오버로드를 두지 않고 시그니처를 바꿨다. 눈먼 5인자 버전을 남기면 다음 호출부가 그것을 집어 사각지대가 되살아난다.

#### 건드리지 않은 것

- `MallOrderUpdaterRunner.decideStatus` 의 "신규 주문 0 은 실패가 아니다" — 수집을 따라잡은 뒤의 재실행은 전부 중복으로 걸러지는 것이 정상이고, FAIL 로 바꾸면 그 시점부터 모든 주기 실행이 영구히 실패로 기록돼 대시보드가 반대로 거짓말을 한다.
- `NO_DATA` 가 브레이커를 트립시키지 않는 것 — 장바구니를 두 달 안 쓰는 사용자를 장애로 처리하면 안 된다.
- 기간 임계 — 갓 연결한 몰이 첫 회차부터 빨개지면 경보가 무시당하기 시작한다.

#### 알면서 받아들인 것 — 경보는 7일 뒤에 뜬다

기존 행은 `first_success_time = 0` 이라 **다음 성공 회차에 그 시점 시각으로 채워지고**, 거기서부터 7일(720분 × 14)을 센다. 즉 이 변경이 나간다고 내일 화면이 빨개지지 않는다. 과거 사실을 지어내지 않기로 했으므로 설계상 불가피하다. 앞당기려면 `jbg_collect_log` 의 최초 성공 시각으로 backfill 하는 1회성 작업이 필요하고, 그것은 사용자 DB 를 쓰는 일이라 승인 사항이다.

#### 곁가지 — 계정 아이디가 로그에 평문으로 남고 있었다

조사 중 발견했다. 수집 시작 로그가 로그인 아이디를 그대로 찍는다. 세션 쿠키·암호화 키는 "값은 기록하지 않는다" 를 철저히 지키는데 계정 아이디는 새고 있었고, `SECURITY.md` 가 "로그를 이슈에 붙이지 말라" 고 안내하는 근거이기도 하다.

마스킹은 복원 불가가 설계다 — 별표를 길이만큼 찍으면 마스크가 길이를 흘리고, 해시는 아이디의 후보 공간이 좁아 사전 대입으로 되돌아온다. 앞 한 글자 + 고정 폭 별표 + 글자 수로 뭉갠다.

**남은 것**: 컨트롤러·인터셉터 쪽에 같은 형태가 여럿 남아 있다. 특히 로그인 성공 로그가 **아이디와 살아 있는 세션 ID 를 한 줄에** 찍는데, 이는 `SECURITY.md` 의 "세션 값은 로그 어디에도 싣지 않는다" 를 정면으로 깬다. 이번 가드는 `svc` 트리만 보므로 그 자리들을 잡지 못하며, 그 한계를 테스트 문서에 명시했다.


### [2026-08-07 23:20] 배치 스크립트 — 주석 영문화 + CRLF (v0.18.6)

#### 두 번 틀리고 세 번째에 맞췄다

v0.18.4 → v0.18.5 → v0.18.6 으로 같은 파일을 세 번 고쳤다. 원인이 하나가 아니라 셋이었고, 매번 하나씩만 잡아 "고쳤다" 고 보고했다. 기록해 둘 값이 있는 실패다.

| 시도 | 진단 | 실제 |
|---|---|---|
| v0.18.4 | (문제 인지 못함) | 애초에 깨진 채로 커밋 |
| v0.18.5 | `chcp` 가 한글 뒤에 있다 | 맞지만 **부분적**. 옮겨도 여전히 깨졌다 |
| v0.18.6 | 주석의 한글 + LF 줄바꿈 | 이번에 실제로 cmd 에 태워 확인 |

#### 원인 1 — chcp 는 출력을 고칠 뿐 파싱을 보장하지 않는다

`chcp 65001` 을 맨 앞으로 옮겼는데도 이렇게 깨졌다.

```
'불가능한' is not recognized as an internal or external command
'수집과' is not recognized as an internal or external command
```

둘 다 REM 줄 **안에 있던 낱말**이다. 주석의 일부가 명령으로 실행된 것이다. `chcp` 는 콘솔이 바이트를 어떻게 **표시**할지를 정할 뿐, cmd 가 배치 파일을 어떻게 **디코딩해 파싱**할지를 완전히 통제하지 못한다.

그래서 규칙을 좁혔다 — **주석은 ASCII 로만 쓴다.** 주석은 사용자에게 보이지 않으니 한글일 이유가 없고, 긴 설명은 `bat/README.md` 로 옮기면 된다. `echo` 는 막지 않는다: 그 바이트는 파싱 대상이 아니라 그대로 출력되고 `chcp 65001` 아래에서 정상 표시된다(실측).

#### 원인 2 — LF 줄바꿈

주석을 영문으로 바꿨는데 이번엔 **영문 낱말**이 깨졌다.

```
'aunches' is not recognized...          ("launches" 의 뒤쪽)
'gnized..."). chcp 65001 is' is not recognized...
```

cmd 는 CRLF 를 전제한다. LF 만 있으면 줄 경계가 어긋나 낱말 중간이 잘린 채 명령이 된다. `.gitattributes` 가 `*.bat` 을 `eol=crlf` 로 정해 두지만 **파일을 직접 쓰면 그 경로를 거치지 않는다.** 이 세션에서 두 번 그랬다.

**이 결함의 가장 나쁜 점은 증상이 인코딩 문제처럼 보인다는 것이다.** 한글이 깨져 보이니 인코딩을 의심하게 되고, 원인은 줄바꿈에 있다.

#### 이번에 바꾼 검증 방식

앞선 두 번은 문자열 검사와 파일 바이트만 보고 "고쳤다" 고 했다. 이번에는 **실제로 cmd 에 태웠다** — 실행 직전에 `goto` 로 빠지는 사본을 만들어 네 스크립트를 전부 돌리고, `is not recognized` 가 0건인지 셌다. 프로브는 지웠다(트리 무흔적).

가드도 둘 늘렸다(주석 ASCII·줄바꿈 CRLF). 둘 다 판별부를 직접 두들기는 대조군을 함께 넣었다.


### [2026-08-07 22:30] 배치 스크립트 코드페이지 순서 (v0.18.5)

#### 무슨 일이 있었나

v0.18.4 로 다시 쓴 `test_run.bat` 이 실행되지 않았다.

```
'쭊'은(는) 내부 또는 외부 명령...
'/d'은(는) 내부 또는 외부 명령...
```

#### 원인 — 주석이 명령을 망가뜨렸다

cmd 는 배치 파일을 **한 줄씩 읽어 가며 실행하고, 각 줄을 그 시점의 코드페이지로 해석한다.** 이 저장소의 `.bat` 은 UTF-8 로 저장되는데(`.gitattributes` 는 줄바꿈만 정한다) 한국어 Windows 의 기본 코드페이지는 CP949 다.

CP949 는 2바이트 문자셋이다. UTF-8 한글(3바이트)을 CP949 로 읽으면 lead 바이트가 다음 바이트를 짝으로 삼키는데, 그 짝이 어긋나면 **줄 끝의 개행까지 두 번째 바이트로 먹는다.** 그러면 다음 줄이 앞줄에 붙는다.

v0.18.4 가 `chcp 65001` **앞에** 한글 주석 블록을 20줄 넣었다. 그 구간이 CP949 로 읽히면서 정렬이 어긋났고, `cd /d "%~dp0\.."` 의 `cd` 가 앞 주석에 먹혀 `/d` 만 명령으로 남았다.

기존 파일들도 `chcp` 앞에 한글이 있었지만(2행 REM) 짧아서 우연히 정렬이 맞았다. 즉 **잠복해 있던 결함이고, 주석을 늘린 순간 터진 것**이다.

#### 고친 것

네 스크립트 모두 `chcp 65001` 을 `@echo off` 바로 다음, **모든 비-ASCII 문자보다 앞**으로 옮겼다. 그 위 안내 주석은 영문으로 적었다 — 그 줄 자체가 코드페이지 이전에 읽히기 때문이다.

`clean_build.bat`·`build_package.bat` 도 같은 상태였다. 지금 동작한다고 안전한 것이 아니라 **주석 한 줄 차이로 깨지는 상태**였고, 가드가 그것을 찾아냈다.

#### 검증

문자열 검사로 끝내지 않고 **실제로 cmd 에 태워 확인했다.** 실행 직전에 `goto` 로 빠지는 사본을 만들어 네 스크립트를 모두 돌렸다 — 한글 출력·경로 이동·jar 자동 선택·기본 인자가 전부 정상이었고, 프로브는 지웠다(트리 무흔적).

가드도 추가했다: 첫 비-ASCII 바이트가 `chcp 65001` 보다 앞이면 실패한다. 이 검사가 위 두 기존 스크립트의 잠재 결함을 찾아낸 것이다.


### [2026-08-07 21:40] 개발 스크립트 역할 분리 + build/ 직접 삭제 제거 (v0.18.4)

#### 작업 개요

`test_run.bat` 과 "패키징된 jar 실행" 이 같은 것인지 묻는 질문에서 출발했다. 읽어 보니 같지 않았고, 더 나쁜 것이 나왔다 — **`test_run.bat` 이 v0.18.3 의 clean 가드를 우회하고 있었다.** 두 스크립트의 역할을 나누고 그 경로를 없앴다. 테스트 708 → **711**.

#### 배경 — 가드가 있는데 우회되고 있었다

v0.18.3 이 `clean` 태스크 자체에 가드를 걸어 `build/` 아래에 앱 DB 가 있으면 멈추게 했다. 그런데 `test_run.bat` 은 `gradlew clean` 뒤에 이렇게 한 번 더 지우고 있었다.

```
set CACHE_DIRS=build bin .gradle
rmdir /s /q "%%D"
```

`rmdir` 은 Gradle 을 거치지 않으므로 가드가 아무 일도 하지 않는다. 이 프로젝트는 배포 패키지를 `build/distributions` 아래에 풀어 그 자리에서 실행하는 관행이 있고 그 인스턴스는 자기 `db/` 를 그 안에 만든다 — 즉 그 한 줄에 실제 구매 내역이 사라진다. 실제로 사라졌고, `rmdir` 도 Gradle 의 delete 도 휴지통을 거치지 않아 복구 수단이 없었다.

순서상 `gradlew clean` 이 먼저라 가드가 막으면 `ERRORLEVEL` 로 스크립트가 종료되긴 한다. 하지만 그건 **우연히 막히는 것**이지 설계가 아니다.

#### 역할 분리

- **`test_run.bat`** — 소스에서 띄운다(`bootRun`, 핫리로드). **아무것도 지우지 않는다.** 개발 반복에 전체 삭제가 필요할 이유가 없고, 클린 빌드는 `clean_build.bat` 이 따로 있다.
- **`run_jar.bat`(신설)** — 빌드된 jar 을 그대로 띄운다. `bootRun` 과 jar 실행은 클래스패스 순서·리소스 로딩(파일 vs JAR 엔트리)·`spring.config.import` 상대 경로 해석이 달라 소스에서는 되는데 jar 에서 안 되는 경우가 생긴다. 배포되는 것은 jar 이므로 확인하는 자리가 필요하다.

둘 다 **기동 자동수집을 기본으로 끈다.** `application.yml` 이 `true` 라 끄지 않으면 띄우는 즉시 실계정에 로그인하고, 개발 중 반복 실행하면 그것이 곧 차단 패턴이다.

#### 이번에도 거짓 GREEN 을 만났다

`BuildScriptHygieneTest` 를 넣고 **음성 프로브**를 돌렸다 — `test_run.bat` 에 `rmdir /s /q build` 를 일부러 넣고 실행. **초록이 나왔다.** `--rerun-tasks` 로 다시 돌리자 빨개졌다.

원인은 `.bat` 이 `test` 태스크의 입력으로 선언돼 있지 않아 Gradle 이 UP-TO-DATE 로 넘긴 것이다. 즉 **파일을 고쳐도 검사가 아예 실행되지 않는다.** 이 세션에서 세 번째로 만난 같은 형태다(만료 감지 호출자 0건 / 배포 가드 판별식 무력화 / 뮤테이션 하네스의 UP-TO-DATE).

그래서 `bat/`·`packaging/` 의 스크립트와 `build.gradle`·`.gitignore` 를 `test` 의 입력으로 선언했다. 이제 `--rerun-tasks` 없이도 감지된다(주입 → 실패, 원복 → 통과로 실측). `build/distributions` 를 입력으로 선언했다가 되돌린 앞선 시도와 달리 이쪽은 안전하다 — 사람이 쓰는 소스 디렉터리라 어떤 태스크의 출력도 아니어서 암묵적 의존이 생기지 않는다.

#### 가드가 스스로 만든 함정

`BuildScriptHygieneTest` 를 처음 돌렸을 때 실패한 것은 명령이 아니라 **주석**이었다. 새 `test_run.bat` 헤더에 "예전에는 여기서 gradlew clean 을 돌렸다" 고 적어 둔 설명 문구가 걸렸다. 그대로 두면 통과시키는 방법이 두 가지가 되는데(명령을 지우거나, 설명을 지우거나) 뒤쪽은 위험은 그대로인 채 근거만 사라진다. 판별부가 주석을 걷어내게 고치고, 그 동작 자체를 대조군으로 고정했다.


### [2026-08-07 21:05] clean 가드 + 만료 일시중단 키 세분화 + 가드 전수 점검 (v0.18.3)

#### 작업 개요

`clean_build.bat` 실행으로 **배포본 인스턴스와 그 DB 가 사라졌다.** 규칙으로 막고 있던 것을 코드로 옮기고, 함께 미뤄 뒀던 승인 불필요 항목들을 처리했다. 테스트 668 → **708**(통과 693 · 스킵 15).

#### 사고 — 규칙은 잊힌다

`clean` 은 `build/` 를 통째로 지운다. 그런데 이 프로젝트는 배포 패키지를 `build/distributions` 아래에 풀어 그 자리에서 실행하는 관행이 있고, 그 인스턴스는 자기 `db/` 를 그 안에 만든다. 즉 **지워도 되는 산출물과 지우면 안 되는 구매 내역이 같은 폴더 아래 섞여 있다.**

"clean 을 쓰지 마라" 는 규칙은 있었지만 지켜지지 않았다. 게다가 `clean` 을 부르는 경로가 스크립트 셋(`clean_build`·`build_package`·`test_run`)에 직접 실행까지 있어, 호출부마다 막으면 반드시 빠지는 자리가 생긴다. 그래서 **태스크 자체**에 걸었다. 두 방향 모두 실측했다 — 앱 DB 를 두면 정확히 막고, 빌드가 만드는 `test-db` 만 있으면 막지 않는다.

가드 메시지에 근본 해결책(배포본을 저장소 밖으로)을 넣었다. 지금 구조가 유지되는 한 이 가드는 증상 억제일 뿐이다.

#### 되돌린 시도 — 캐시 구멍을 막으려다 빌드를 깼다

검증에서 새 발견이 있었다. `DistributionHygieneTest` 의 실물 ZIP 검사가 `build/distributions` 를 읽는데 그 디렉터리가 테스트 입력으로 선언돼 있지 않아 **캐시 키에 안 들어간다.** ZIP 이 없던 시점의 결과가 복원되면 새 ZIP 이 생긴 뒤에도 그 층은 '스킵' 인 채 초록이다 — "배포본에 개인 데이터가 없다" 를 확인한 적 없는 초록이다.

`inputs.dir` 로 선언해 봤더니 Gradle 이 **암묵적 의존으로 `build` 를 실패시켰다**(`test` 가 `distZip`·`bootDistZip` 의 출력을 읽는 셈이 된다). 의존을 명시하면 단위 테스트가 배포 아카이브 생성을 기다리게 되어 성격이 바뀐다. 그래서 되돌리고, 그 구멍에 기대지 않기로 했다 — 배포물을 실제로 막는 것은 `packageDist` 자신의 산출물 검사이고, 그것은 아카이브를 만드는 태스크 안에서 돌므로 "아카이브는 새로 만들어졌는데 검사만 건너뛴" 상태가 될 수 없다. 시도와 되돌린 이유를 그 자리에 주석으로 남겼다.

#### 가드 전수 점검 — 이 세션의 반복 주제

소스/파일 형태를 보는 가드 14개를 점검해 **판별부를 무력화해도 초록인 자리 3곳**을 찾아 대조군을 넣었다(배포 ZIP 스캔·서비스 XML 개수 세기·주석 제거기). 이번에는 논리 판정에서 멈추지 않고 **실제로 판별부를 죽여** 예상한 테스트만 빨개지는지 확인했고, 새로 넣은 진입 지점 시험까지 두 방향으로 두들겼다.

#### 만료 일시중단 키

몰 단위 → `몰+수집기`. 통합회원에서 SSG 세션만 죽어도 이마트까지 멈추던 것을 고쳤다. 키 생성이 거는 쪽·보는 쪽·푸는 쪽 세 군데에 각각 적혀 있어 한쪽만 바뀌면 **건 자리와 보는 자리가 다른 키**가 되는 구조였다 — 어느 쪽으로 어긋나든 로그에 흔적이 없다. 한 곳으로 모았다.

기존 검사는 맵 상태와 소스 문자열만 재고 있어 "한쪽이 죽어도 다른 쪽은 계속 수집된다" 를 증명하지 못했다. 회차가 실제로 시작되는지 세는 시험을 새로 넣었다.

#### 남긴 것

`RepositoryHygieneTest` 의 추적 파일 수집기 시험이 전체 실행 3회 중 1회 실패했다(단독 실행과 이후 2회는 통과). `git ls-files` 가 다른 도구와 인덱스를 두고 경합한 것으로 보이나 재현하지 못했다. 플래키 가드는 그 자체가 이 세션이 고쳐 온 문제와 같은 종류이므로 다음에 다시 나오면 원인을 잡아야 한다.

---

### [2026-08-07 19:10] 개인 데이터 유출 재발 방지 가드 + 공개 기록 (v0.18.2)

#### 작업 개요

"배포본 DB 에 실데이터가 실려서는 안 된다" 는 지적에서 출발해 배포 경로를 검증하다가, **배포 경로는 깨끗한데 저장소 이력이 그렇지 않다**는 것을 발견했다. 재발 방지 가드를 넣고 공개 기록을 남겼다. 테스트 660 → **668**.

#### 확인한 것

- **배포 ZIP 은 안전하다.** v0.18.1 산출물 300 엔트리를 전수 확인했고 `db/`·`exports/`·`logs/`·`config/` 가 하나도 없다. `packageDist` 가 담을 대상을 열거식으로 지정하기 때문에 구조적으로도 그렇다. 배포본을 푼 폴더에 DB 가 보이는 것은 **그 자리에서 앱을 실행해 런타임이 만든 것**이다.
- **그러나 개발용 DB 가 과거에 이 PUBLIC 저장소에 커밋된 적이 있다.** 좌표·노출 범위·미조치 사항은 비공개 폴더의 사건 기록에 남겼다(공개 문서에는 넣지 않는다 — 아래 참조).
- **쇼핑몰 계정 파일은 한 번도 커밋된 적이 없다.** `exports/`·`logs/`·스크린샷도 이력에 없다.

#### 왜 기존 가드가 못 잡았나 — 이번 국면의 핵심

`RepositoryHygieneTest` 는 이미 있었고 `.gitignore` 규칙이 지워지지 않았는지 감시하고 있었다. 그런데 **git 은 이미 추적 중인 파일에는 무시 규칙을 적용하지 않는다.** 규칙을 넣기 전에 한 번이라도 `add` 된 파일은 `.gitignore` 를 아무리 고쳐도 계속 따라온다. 즉 "규칙이 있다" 는 "실제로 추적되지 않는다" 를 보증하지 않는데, **그 간극을 보는 검사가 아무도 없었다.**

음성 프로브로 그 사실을 실측했다 — 합성 DB 를 `git add -f` 로 스테이징하면 `git check-ignore` 는 "무시됨" 이라 답하는데도 새 가드는 잡는다. 무시 규칙 층이 원리적으로 못 보는 자리를 덮는다는 증거다.

#### 검증이 잡은 것 — 가드가 죽은 채 초록이었다

배포 산출물 가드의 판별식을 `return false` 로 무력화했는데 **테스트 5건이 전부 통과했다.** 소스 형태 검사는 빌드 스크립트 문자열만 보고, 실물 검사는 "금지 항목 0건" 이라는 같은 결론에 도달하기 때문이다. 판별식이 통째로 죽은 채 릴리스가 나갈 수 있었다.

이 세션에서 두 번째로 만난 같은 형태다(만료 감지가 테스트 25건 초록인 채 호출자 0건이었던 것). 판별식을 직접 두들기는 대조군을 추가했고, **가드를 넣을 때는 그 가드를 죽여 보는 절차를 함께 밟는다**는 것이 이번 교훈이다.

#### 공개 수위 판단

공개 문서(`SECURITY.md`)에는 **사건의 사실과 교훈만** 담고 커밋 해시·날짜·행 수·자격증명 관련 서술은 넣지 않았다. 공개 문서의 목적은 "이런 실수를 하지 마라" 이지 "여기서 데이터를 찾아라" 가 아니다. 특히 자격증명이 아직 유효한지에 대한 서술은 그 자체가 공격 유인이 된다.

작업 중 `.gitignore` 와 테스트 주석에 좌표가 들어간 것을 발견해 걷어냈다 — 이 저장소의 "주석에 실측 사실을 적는다" 는 스타일과 공개 수위가 충돌한 지점이고, 날짜가 없어도 '왜' 는 온전히 남는다.

#### 함께 한 것

`config/backup/` 이 파일명 패턴으로만 막혀 있어 다른 이름은 `-f` 없이 스테이징되는 것을 프로브로 확인하고 폴더 전체로 넓혔다. `CONTRIBUTING.md` 가 안내하던 `git add .` 도 제거했다 — 이 습관 하나가 위 사고의 가장 흔한 경로다.

---

### [2026-08-07 14:20] 프로필 락 배선 + 즉시수집 응답 모순 제거 (v0.18.1)

#### 작업 개요

A~D 트랙의 마지막 항목이다. `MallProfileLock` 은 클래스와 테스트가 완성돼 있는데 **프로덕션 호출자가 0건**이었고, 그 사이 `killOrphanProfileChrome` 호출 지점이 1곳에서 3곳으로 늘어 위험만 넓어져 있었다. 테스트 633 → **660**(통과 645 · 스킵 15).

#### 배경 — 내가 만든 구멍이 커져 있었다

v0.16.1 에서 고아 브라우저 정리를 넣을 때 전제는 "지금 이 프로필을 쓰는 활성 캡처가 없다" 였다. 그것은 **이 JVM 안에서만** 참이다. 서비스로 도는 인스턴스와 사용자가 직접 띄운 인스턴스는 다른 프로세스이므로, 한쪽이 사람이 방금 로그인해 둔 세션을 다른 쪽이 죽일 수 있다. v0.17.0 이 수집 경로에도 같은 호출을 추가하면서 노출 면이 넓어졌다.

#### 상세

- **락이 정리보다 앞이다** — 뒤면 죽인 다음에 잠그는 꼴이다. 캡처는 락 획득이 정리보다 14줄 앞이고, 못 잡으면 정리·기동 어느 것도 도달하지 않는다. 수집은 정리 전체가 try-with-resources 안에 있다. 기동 실패 경로의 정리도 "락을 쥐었을 때만" 으로 막았다.
- **해제 경로 8개** — 정상 완료·취소·창 만료·유휴 회수·리퍼·shutdown·기동 실패·락 획득 실패. 하나라도 빠지면 락이 남아 다음 캡처가 영구히 막힌다. 해제를 한 메서드로 모으고, **활성 상태를 비우는 유일한 지점이 그 해제 바로 다음 줄**이 되게 했다 — 락을 놓지 않고 상태만 버리는 경로가 문법적으로 존재할 수 없다.
- **락을 못 잡으면 죽이지 않는다** — 캡처는 `PROFILE_LOCKED`(`ALREADY_ACTIVE` 와 나눴다. 저쪽은 "마치거나 취소하세요" 가 통하지만 이쪽은 이 화면에 마칠 창이 없다), 수집은 `SKIPPED`. 브레이커는 건드리지 않는다.
- **`UNAVAILABLE`(잠글 수 없는 환경)은 막지 않는다** — 막을 근거가 없다는 게이트의 명시적 판단을 따랐다.

#### 응답 모순 — 조사해 보니 예상보다 넓었다

세션 만료 뒤 스케줄 등록 예외만 문제인 줄 알았는데, 흐름을 표로 그려 보니 **수집에 성공한 몰(`processed`)까지** 같은 경로로 `skipped` 에 담기고 있었다. `processed` 는 화면의 보호 필터에 없어서, **수집에 성공한 몰이 '연결 끊김' 으로 그려져 사용자가 멀쩡한 연결을 다시 맺고 있었다.**

회차 상태를 하나만 들고 `finally` 에서 한 번 분류하도록 바꿨다. 분기 조건은 한 글자도 안 바꿨다 — 담는 지점만 옮겼다. 스케줄 등록 실패는 수집 결과와 **다른 사건**이므로 앞선 판정을 덮지 않고 `scheduleFailed` 로 따로 나간다.

#### 알면서 받아들인 것

킬스위치 OFF 에서도 예전과 다른 곳이 **둘**이다 — (1) 즉시수집이 자격 없음으로 건너뛸 때 계정 상태를 안 누름(v0.18.0), (2) 수집 단계를 통과한 뒤 예외가 나면 `skipped` 가 아니라 `scheduleFailed` 로 감. 둘 다 되돌리면 진짜 버그가 되살아나므로 그대로 뒀다.

---

### [2026-08-07 12:40] 세션을 수집 자격으로 인정 + 만료 감지 배선 (v0.18.0)

#### 작업 개요

v0.17.0 이 주입 경로를 배선했지만 **스케줄러가 그 앞에서 막고 있었다.** 자격 판정을 통합해 세션만으로도 수집 대상이 되게 하고, 세션이 죽었을 때 그것을 인지해 멈추는 만료 감지를 붙였다. 몰별 옵트인을 켜는 화면도 처음 만들었다. 테스트 542 → **633**(통과 618 · 스킵 15).

#### 배경 — 판정이 두 벌로 갈려 있었다

읽어 보니 자격 판정은 스케줄러와 즉시수집에 **각각** 있었고 이미 어긋나 있었다. 스케줄러는 `account_status != 1` 이면 물러나는데 즉시수집은 그 값을 보지 않았고, 대신 건너뛸 때마다 `jaDao.update(seq, 0, null, null)` 로 계정 상태를 0 으로 눌렀다. 즉 **스케줄러만 고쳤다면 즉시수집 한 번에 원복될 구조**였다. 5-19 가 수집 게이트를 통합한 것과 같은 이유로 판정을 `JangBoGoManager` 진입부로 모았다.

부수적으로 그 로그가 "암호화 키/IV 초기화 및 account_status=0 설정 완료" 라고 찍지만 `update` 가 null 인자를 SET 절에 붙이지 않아 **키/IV 는 실제로 지워지지 않는다**는 것도 드러났다.

#### 이번 국면에서 가장 값진 발견 — 초록은 배선을 보증하지 않는다

3차 통합 검증이 잡아냈다. 만료 감지가 **테스트 25건 초록인데 프로덕션 호출자가 0건**이었다. `SessionExpiryDetector.observe` 를 부르는 코드도, `MallCollectOutcome.sessionExpired(...)` 를 만드는 코드도 없어서 스케줄러·컨트롤러의 만료 분기는 도달 불가 코드였고 일시중단·재개가 전부 무동작이었다. **사용자에게는 아무 일도 일어나지 않는 상태**다.

이 프로젝트가 지금 고치고 있는 결함이 정확히 같은 형태다 — v0.16.0 이 캡처·저장을 만들었는데 주입 호출자가 0건이었던 것. 그래서 4차에서 배선을 완성하면서 **각 홉의 호출자가 실재하는지 소스에서 세는 가드**(`SessionExpiryWiringTest`)를 함께 넣었다. 부품 → 승격 → 소비 세 홉을 각각 감시한다.

#### 상세

- **자격 판정 통합** — `qualify(mall, accountSupplier, trigger)` 가 `CollectEligibility{seqMall, Route{CREDENTIALS,SESSION,NONE}, account, blocker}` 를 돌려주고, `collect` 는 그 판정 없이는 부를 수 없다(판정에 실린 seq 가 다르면 거부 — 반복문에서 앞 회차 값이 남아 넘어가는 형태 차단). 사유 문구는 수집 로그·화면에 그대로 나가므로 예전 것을 글자 그대로 옮겼다. 세션 자격 판정은 **호출부가 이미 조회한 행의 컬럼만 읽어** 새 DB 왕복을 만들지 않는다.
- **자격증명 우선, 세션은 폴백** — 킬스위치가 꺼지면 세션 분기가 항상 false 라 판정이 예전과 동일해지고(수용 기준 10), 세션이 대체하지 못한 자리(통합회원의 이마트)가 자격증명 없이 돌지 않는다.
- **만료 승격은 타입으로** — `SessionCollector.SkipCause{NO_SESSION, NOTHING_APPLIED, SESSION_EXPIRED}` 를 두고 문자열 비교를 피했다(문구를 고치는 순간 조용히 깨진다). 승격 지점은 수집기별 결과와 신규 주문 목록을 **둘 다** 들고 있는 유일한 자리인 `MallOrderUpdaterRunner` 다.
- **승격에 건 제약** — 이번 회차에 다른 수집기가 주문을 가져왔으면 승격하지 않는다. 승격하면 만료 결과의 `newOrderSeqs` 가 비어 스케줄러가 내보내기 앞에서 물러나 **방금 저장한 주문이 영영 빠지고**, 몰 단위 일시중단이라 멀쩡한 수집기까지 멈춘다.
- **검증이 추가로 잡은 것 2건** — (1) 만료 승격이 `getName()`·`recordCollectorOutcomes()` **뒤**에 있어 둘 중 하나가 던지면 만료 사실이 유실되고 '0건 수집 성공' 이 된다(죽은 세션을 매 주기 두드리게 된다). 순수 계산을 DB 접촉 앞으로 옮겼다. (2) SKIPPED 전체를 "브레이커 건너뜀" 으로 찍어, 이제 그 통에 함께 들어오는 세션 만료를 읽은 사람이 브레이커부터 뒤지게 된다.

#### 알면서 받아들인 것

- **일시중단 키가 몰 단위(seq)다.** 통합회원에서 SSG 세션만 죽어도 이마트까지 멈춘다. 승격 제약으로 최악(내보내기 누락)은 막았지만 올바른 키는 `seq+수집기` 일 수 있다.
- **만료 판정 축이 넓어졌다** — '주소' 에서 '주소 OR 로그인 요소 보임' 으로. 오탐이 나면 멀쩡한 세션이 매 회차 만료로 읽혀 몰이 멈추는데, 이는 만료를 놓치는 쪽보다 나쁘다. **킬스위치를 실제로 켜기 전 실사이트 확인이 필요하다.**

---

### [2026-08-07 09:10] 5-9′ 주입 수집기 배선 + 게이트 정합 + SQL 주입 제거 (v0.17.0)

#### 작업 개요

5-15 실측이 끝난 뒤 "남은 것이 무엇인가" 를 계획서 정본·코드 실태·출하 이력 세 갈래로 **서로 독립적으로** 조사하고, 후보 50건을 코드로 하나씩 반증 검증했다(병합 43 → 확정 40, 기각 3). 그 결과를 두 배치로 구현했다. 테스트 434 → **542**, 전부 통과.

#### 배경 — 계획서대로 5-9′ 를 바로 시작했으면 안 됐다

조사에서 아무도 눈치채지 못한 모순이 나왔다. 게이트(`SessionProfileGate`)는 **프로필 디렉터리 모델**로 판정한다 — `session_profile_name` 이 비면 `PROFILE_MISSING`, `session_profile_owner` 로 OS 계정 대조. 그런데 v0.16.0 이 실제로 만든 것은 **스냅샷 모델**(암호문 파일 + DB 의 키/IV)이고, 저장소 전체에서 그 두 컬럼을 **쓰는 코드가 0건**이었다. 캡처가 커밋하는 것은 `session_snapshot_key`·`iv`·`last_login` 셋뿐이다.

즉 주입 수집기를 완벽하게 짜 넣고 몰 옵트인을 켜도 **코드가 한 줄도 돌기 전에 게이트가 전량 SKIPPED 시킨다.** 계획서가 "프로필 발급을 계속하면 저절로 해소된다" 고 적어 둔 서술은 현재 코드에서 성립하지 않았다. 그래서 주입 배선보다 이것을 먼저 닫았다.

#### 상세

- **게이트 정합** — 캡처가 저장 직후 name/owner/status 를 커밋한다. 이름은 `start` 가 프로필 디렉터리를 만들 때 쓴 값을 `ActiveCapture` 에 실어 나른다(재계산하면 게이트는 통과하는데 수집이 빈 프로필을 연다). owner 는 게이트에 넘어가는 것과 같은 `System.getProperty("user.name")` 을 쓴다. status 는 READY 만 — 만료는 갱신 주체가 없어 값이 굳으므로 `last_login` 에서 파생 계산한다. 신원 커밋 실패는 `PROFILE_MARK_FAILED` 로 나눴다(스냅샷만 들어간 상태를 '성공' 으로 알리면 이후 수집이 조용히 전량 SKIPPED 된다).
- **5-9′ 주입 수집기(ssg 파일럿)** — 설계 병목이던 `CollectorSpec(name, BiFunction<String,String,MallSession>)` 은 건드리지 않고 옆에 `SessionCollectorSpec(name, replaces, Function<String,SessionCollector>)` 를 뒀다. 합치면 세션 경로가 쓰지도 않을 비밀번호를 인자로 들고 다니고, 그러려면 호출부가 먼저 복호화해야 해서 목적이 무너진다. `replaces` 로 자리를 **대체**한다(덧붙이면 비밀번호 로그인이 남는다). Emart 는 도메인·폼이 다르고 그쪽 세션이 이 스냅샷에 있다는 근거가 없어 자격증명 경로로 남겼다 — 근거 없이 대체하면 조용히 0건이 된다.
- **SQL 주입 두 형태** — (1) `getOrder` 가 수집한 영수증 번호를 그대로 조립(중복 방지 조회로 매 수집마다 호출), (2) 따옴표 없는 숫자 자리. 특히 방어라고 있던 `replaceAll("[^0-9]","")` 는 거르는 게 아니라 **깎는** 것이라 `1a1` → `11` 로 남의 몰 행을 열어 줬다. SQL 이 안 깨지고 대상만 조용히 바뀌어 로그로 알 수 없는 형태다.
- **가드가 초록이었던 이유** — 기존 가드는 따옴표 조립만 봤고, `.java` 로 끝나는 파일만 걸러 **추적 중이던 `.bak` 을 통째로 건너뛰었다.** "전수 스캔" 이라는 주장이 실제로는 전수가 아니었다. 두 사각지대를 닫고 백업 파일 추적 자체를 막는 가드를 신설했다.

#### 배선 순서에서 지킨 것

주입은 `load` → 드라이버 → **대상 도메인 1회 선방문** → `inject` → 회원 페이지 순이다. `about:blank` 상태 주입은 조용히 무시되므로 선방문을 빼면 실패가 드러나지 않는다. `load` 를 드라이버보다 앞으로 당긴 것은 무인 수집에서 '아직 캡처 안 함' 이 가장 흔한 경우라, 그대로 두면 매 회차 브라우저를 띄웠다 아무것도 못 하고 닫기 때문이다.

#### 남은 것

실사이트 왕복은 미검증이다. 이번 배치가 보증하는 것은 "게이트가 꺼져 있을 때 아무것도 바뀌지 않는다" 뿐이다(수용 기준 10 — 실행 계획이 통합 전과 동일하고 DB 조회도 늘지 않음을 코드로 확인). 세션 수명 측정(5-17)은 실계정 다일 관측이라 승인 대기다.

---

### [2026-08-07 04:25] 5-15 실측 후속 — 고아 브라우저 정리 + 기동 실패 로깅 (v0.16.1)

#### 작업 개요

v0.16.0 캡처의 첫 실측에서 [브라우저로 로그인] 이 매번 실패했다. 연쇄였다 — 첫 기동 실패(`SessionNotCreatedException`)가 남긴 고아 chrome.exe 가 프로필 락을 쥔 채 쌓였고(실측 8개), 이후 모든 시도가 "user data directory is already in use" 로 막혔다. 고아를 수동 정리한 뒤 재시도한 캡처는 한 번에 성공했고 저장 검증(암호문 파일 생성·DB `session_snapshot_key/iv` 채움·`last_login` 갱신·로그 값 미노출)까지 통과했다. 같은 연쇄가 반복되지 않도록 두 가지를 고쳤다. 테스트 432 → **434**, 전부 통과.

#### 배경 — 원인이 아니라 원인의 은폐가 문제였다

첫 실패의 catch 가 예외 클래스명만 찍어(`세션 캡처 시작 실패(SessionNotCreatedException)`) 근본 원인이 로그 어디에도 없었다. 값 노출 방지를 기동 실패에까지 적용한 과잉이다 — 기동은 로그인 전이라 메시지에 세션 값이 섞일 수 없다. 격리 재현 프로브(`CaptureLaunchDiagProbe`, 실계정·로그인 없이 기동만 재현)로 기동 경로 자체는 정상임을 확인해 남은 용의자인 락 잔류를 특정했다.

#### 상세

- **`WebDriverManager.killOrphanProfileChrome(profileDir)`** — 명령줄에 `--user-data-dir=<프로필>` 이 있는 chrome.exe 만 PowerShell(CIM)로 골라 강제 종료한다. Windows 의 `ProcessHandle` 은 다른 프로세스의 명령줄을 주지 않아 CIM 조회를 쓴다. 캡처 프로필 디렉터리는 캡처 브라우저만 쓰므로, 활성 캡처가 없을 때 그 경로를 문 크롬은 전부 고아다 — 개인 Chrome 은 이 인자를 갖지 않아 건드리지 않는다. 최선 노력이라 어떤 실패도 밖으로 던지지 않는다.
- **`SessionCaptureService.start()` 두 지점 배선** — (1) 기동 전 예방 정리: 강제 종료·크래시가 남긴 락 잔류를 스스로 치우고 시작한다, (2) 실패 후 정리: 핸드셰이크 실패는 `driver=null` 인 채 브라우저만 남겨 `quitQuietly` 가 닿지 못하던 정확한 구멍을 막는다.
- **기동 실패 로깅** — 메시지 첫 줄 warn + 전체 스택 debug. 캡처·저장 실패의 클래스명-만 로깅은 유지한다(로그인 후 단계라 보수적으로 남겨 둔다).
- **테스트 2개 추가** — 예방 정리가 기동보다 먼저 도는지(InOrder), 실패 시 정리가 닿고 활성이 남지 않아 재시도가 막히지 않는지.

---

### [2026-08-06 16:49] 5-15·5-16 — 세션 캡처 + 암호화 저장 (v0.16.0)

#### 작업 개요

사람이 브라우저에서 직접 로그인해 만든 세션을 CDP `Network.getAllCookies` 로 떠서 암호화 저장하는 앞단을 구현했다. 캡처 경로는 ADR-0002 로 확정된 후보 (a) — Selenium 이 프로필을 쓰며 브라우저를 띄우고 마스킹을 건 뒤 사람이 그 창에서 로그인한다(디버깅 포트를 열지 않는다). 이미 있던 `SessionSnapshot`·`SessionTransfer`·`WebDriverManager` 마스킹을 재사용하고, 남아 있던 **오케스트레이션(비동기 start/status/capture/cancel)과 암호화 저장**을 새로 만들었다. 킬스위치(`jangbogo.session-profile.enabled`, 기본 OFF) 뒤에 두었고 몰별 옵트인은 켜지 않았으므로 수집 동작은 그대로다. 테스트 388 → **432**, 전부 통과.

#### 배경 — 계획서가 코드와 어긋난 지점을 착수 전에 잡았다

정본 계획서 5-16 행이 *"`StartupTasks` 마이그레이션이 필요하다(`migrateCollectLogSchema` 선례)"* 라고 적고 있었는데, 그 선례 메서드는 Phase 3-10 에서 이미 제거됐고 지금은 `SchemaMigrator.ensureMigrated()` 가 `schema.sql` 을 읽어 없는 컬럼을 자동 ALTER 한다. 즉 `schema.sql` 에 컬럼 2개를 선언하는 것으로 끝이고 `StartupTasks` 는 손댈 필요가 없었다. 실제 코드로 확인해 계획서를 정정했다.

#### 상세

- **5-16 저장 (`svc/util/SessionSnapshotStore`)** — 몰 계정 키를 재사용하지 않고 저장마다 새 키/IV 생성(계정 재연결이 그 키를 회전시킨다). JSON→UTF-8→base64 로 ASCII 고정 후 암호화(문자열 암호기가 charset 미명시라 비-ASCII 쿠키가 복호화 시점에 깨지는 것을 막는다). 암호문은 프로필 루트 아래 파일, 키/IV 는 DB 전용 컬럼 2개에 분리. 스냅샷 값은 넓은 SELECT 에도 안 넣고 로그·응답·`toString` 에도 안 낸다.
- **5-15 캡처 (`svc/SessionCaptureService` + `ctrl/SessionCaptureController`)** — 비동기 + 폴링. 막힘·실패는 값(enum). 한 번에 하나만 활성이고 수집 동시 실행 리스는 잡지 않는다(사람 페이스라 수집을 굶긴다). 창 상한은 관리자 세션 30분 잔여(`AuthInterceptor` 가 `LOGIN_TIME` 기준으로 잰다) 아래로 캡한다.
- **DB** — `jbg_mall` 에 `session_snapshot_key`·`session_snapshot_iv`. `update()`(null 을 SET 에서 빼는 함정)를 피해 전용 read/write. `MallSessionProfileFieldsTest` 패턴의 경계 테스트로 쓴 것을 그대로 읽는지 확인.
- **UI** — ‘계정연결’ 모달에 브라우저 로그인 진입점(‘로그인을 마쳤습니다’ 버튼 + 상태 폴링).

#### 적대검증이 잡은 것 (3렌즈 → 6건, 회귀 렌즈는 빈 배열)

착수 산출을 그대로 두지 않고 회귀·조용한 실패·계약경계·유출 렌즈로 검토한 뒤 file:line 으로 직접 재확인해 실재 결함만 고쳤다.

1. **(HIGH) 고아 브라우저·영구 잠김** — 폴링이 끊기면 살아 있는 세션을 쥔 브라우저가 남고 이후 `start` 가 영구 `ALREADY_ACTIVE` 였다. 데몬 리퍼(유휴 2분·창 만료 회수) + `start` 진입 시 방치 캡처 선회수 + `@PreDestroy` 종료 정리로 닫았다.
2. **(MED) 재캡처 데이터 손실** — 암호문 파일을 키 커밋 전에 덮어써, 키 쓰기가 일시 실패하면 이전 세션이 영구 파손됐다. 임시 파일 → 키 커밋 → 원자적 교체로 바꿨다.
3. **(MED) 모달 닫기·이탈 시 미취소** — `hidden.bs.modal`·`pagehide`(sendBeacon)에서 진행 중이면 취소하도록 했다.
4. **(LOW) `status` seqMall 미대조 + JS 좀비 폴링** — 서버는 seqMall 불일치를 `NO_ACTIVE_CAPTURE` 로, JS 는 폴링 세대 가드로 막았다.
5. **(LOW) `load` 실패 로그에 seqMall 부재** — ‘미캡처’와 ‘파손’ 구분되게 seqMall(비밀 아님)을 남겼다.
6. **(MED) 락 잡은 채 블로킹 호출** — 단일 관리자·로컬 실행 전제 + Selenium 타임아웃 하에 위험이 낮아 클래스 javadoc 에 트레이드오프를 기록하고 수용했다.

#### 남은 것

주입 기반 수집(5-9′) 배선·몰별 옵트인·실계정 캡처 실측(5-15)·세션 수명 측정(5-17)은 다음 단계다. 실측은 킬스위치 ON + 사람이 실제 로그인해야 하므로 별도로 진행한다.

### [2026-08-06 00:20] 프로필 락 획득 순서의 근거를 코드로 옮겼다 (v0.15.10)

#### 작업 개요

`MallProfileLock` 클래스 주석에 획득 순서 규칙 한 항목을 추가했다. 코드 동작은 바뀌지 않는다.

#### 배경 — 근거가 사라질 뻔했다

세션 인계 계획서(REV1)가 두 판본으로 갈라져 있어 어느 쪽이 최신인지 확정했다. 8차 세션 판이 정본이고 다른 하나는 6차 세션에서 멈춘 구판이었다 — 현재 HEAD 및 `build.gradle` 버전과의 일치, 인용된 커밋 8개의 실재 여부, 구판이 "결정 대기"로 적어 둔 항목이 이미 실행됐다는 점으로 확정했다.

구판을 지우기 전에 정보 손실을 검사했더니 **딱 한 문장이 신판에 없었다.**

> 획득 순서는 제한기 바깥 / 프로필 락 안쪽(락을 바깥에 두면 세마포어 대기 중 프로세스 경계 락을 놀리면서 쥔다)

*결정*은 신판에 남아 있었지만 *이유*가 빠져 있었다. 저장소 전체를 훑어도 이 근거는 없었다. `MallProfileLock` javadoc 이 "파일 락이라야 프로세스 경계를 넘는다"고 적고 있으나 그것은 이 락의 **존재 이유**이지 **획득 순서**의 근거가 아니다.

#### 왜 코드에 남기나

세 가지가 겹쳤다.

1. **아직 살아 있는 제약이다.** `MallProfileLock` 은 만들어만 두고 수집 경로에 배선하지 않았다(Phase 5-3 재검토). 배선하는 세션이 정확히 이 순서를 알아야 한다.
2. **복구 경로가 없었다.** 구판은 `.gitignore` 배제라 커밋 이력이 없고, 신판이 놓인 자리는 git 저장소가 아니다. 그냥 지웠으면 되돌릴 방법이 없었다.
3. **계획서에는 백업이 없다.** 그래서 결정 옆(계획서)과 코드 옆(주석) 양쪽에 남겼다. 배선하는 사람은 계획서를 안 열어도 이 파일은 연다.

#### 상세 내용

- `MallProfileLock` 클래스 주석 `규칙` 목록에 네 번째 항목을 추가했다. `spotlessApply` 가 줄바꿈을 재정렬했고 `compileJava` 통과.
- 구판 계획서는 이관을 확인한 뒤 삭제했다 — 두 곳에서 근거 문장이 각 1건씩 확인된 뒤에만 삭제가 실행되도록 걸었다.
- 부수로 신판 계획서의 커밋 표에서 버전 표기 오류 하나를 정정했다. `7214fe7` 은 버전 bump 가 없는 커밋인데 0.15.6 으로 적혀 있었다 — 커밋 8개의 `build.gradle` 을 전부 확인해 "bump 가 있는 커밋만 값을 적는" 표의 규칙을 특정한 뒤 정정했다.

### [2026-08-05 08:40] 5-19 수집 통로 통합 — 우회를 규약이 아니라 컴파일로 막았다 (v0.15.9)

#### 작업 개요

제한기를 수집 경로에 배선하고 즉시수집을 스케줄러와 같은 통로로 합쳤다. 착수 전에 결정 3건을 받았다 — 공통 통로는 `updateItemsAndGetNewSeqs` 자리, 막힌 즉시수집은 계정을 끊지 않음, 즉시수집 대기 상한은 수 초.

#### 실측이 계획서를 두 곳 정정했다

계획서는 `AdminController` 가 **6곳**에서 계정을 끊는다고 적었는데 실제로는 **7곳**이었다. 다만 게이트와 관련되는 건 하나뿐이다 — 포괄 `catch (Exception perMallEx)` 가 수집에서 나온 모든 예외를 받아 끊는다. 그래서 "막힌 것을 예외로 던지면 안 된다" 가 설계 제약이 됐다.

더 중요한 것은 **`updateItems` 였다.** 호출처가 0인 채로 게이트·제한기를 거치지 않고 곧바로 수집으로 들어가는 두 번째 진입점이 열려 있었다. 내가 새 메서드 javadoc 에 "유일한 통로" 라고 적은 뒤에야 확인했고, 적어 놓은 주장이 거짓임을 알고 지웠다. 결정 3건에 없던 항목이라 따로 보고했다.

#### 순서를 지키려고 자격증명을 공급자로 받았다

게이트를 공통 통로로 들여오면 문제가 하나 생긴다. 스케줄러는 게이트를 **복호화보다 앞**에서 보는데, 공통 통로는 이미 복호화된 아이디·비밀번호를 받는 자리다. 그대로 옮기면 "게이트에 막히는 회차에서는 로그인이 발생하지 않는다(복호화보다 앞)" 는 기록된 성질이 깨진다.

그래서 자격증명을 값이 아니라 `MallCredentialSupplier` 로 받았다. 순서가 **게이트 → 제한기 → 복호화** 로 고정되고, 막히면 람다가 호출되지 않는다. 5-18 이 실행 컨텍스트에 쓴 것과 같은 패턴이다. 성질이 유지되는 정도가 아니라 두 경로 모두에서 강해졌다 — 이제 제한기에 막혀도 비밀번호가 오르지 않는다.

#### 적대적 검증이 네 가지를 잡았다 (지적 21건 중 8건 생존)

세 렌즈(회귀·조용한 실패·계약 경계)로 보고 각 지적을 반박 시도로 걸렀다. 살아남은 것 중 셋은 **세 렌즈가 각각 도달한 같은 결함**이었다.

1. **단계 이름을 게이트로 박아 뒀다.** `saveSkipLog` 가 `step_name` 을 `session-profile-gate` 리터럴로 쓰는데, 이제 브라우저 자리 부족·중복 실행도 그 통로로 들어온다. 세션 프로필을 켠 적 없는 사용자의 수집 로그 화면 단계 필터에 그 이름이 뜨고, 사유 칸에는 "다른 수집이 브라우저를 쓰고 있다" 가 들어가 서로 모순된다. → 사유마다 다른 단계 이름을 쓰게 고쳤다(게이트 차단은 기존 값 유지).
2. **`executeNow=false` 에서 자격증명 검증이 통째로 사라졌다.** 복호화를 람다로 옮기면서 즉시 실행하지 않는 요청에서는 아예 돌지 않게 됐다. 예전에는 복호화가 깨진 계정을 그 자리에서 알려 주고 연결을 끊었는데, 이제 '주기적 수집 활성화' 성공으로 보고되고 사용자는 다음 주기가 실패할 때까지 모른다. **복호화를 미룬 목적은 막힐 회차에서 비밀번호를 올리지 않는 것이지 확인을 그만두는 것이 아니었다.** → 검증을 되돌렸다.
3. **`isCollecting` 선검사에 걸린 몰이 여전히 `skipped` 로 갔다.** 서버는 계정을 끊지 않는데 프런트가 `skipped` 원소마다 버튼을 '계정연결' 로 되돌린다. 같은 조건이 `collect` 안에서 잡히면 `blocked` 로 가서 안 건드린다 — 한 조건이 두 결과를 내고 있었다. → `blocked` 로 보냈다.
4. **즉시수집이 막힌 사유가 DB 에 남지 않았다.** 스케줄러는 `SKIPPED` 로 남기는데 즉시수집은 응답 JSON 뿐이고, 그 응답을 읽는 프런트 코드도 없다. 같은 통로로 합치는 것이 목적인데 사유가 남는 자리는 갈려 있었다. → 수집 로그에 남긴다.

반증 패스는 부수로 **`saveSkipLog` 주석 자체가 틀렸음**을 밝혔다. "FAIL 로 적으면 브레이커가 연속 실패로 세어 몰을 차단한다" 고 적혀 있는데, 브레이커의 입력은 `jbg_collect_breaker` 뿐이고 수집기가 실제로 돈 회차에만 갱신된다. 직접 확인한 뒤 정정했다 — 남의 결론을 검증 없이 옮기지 않는다.

#### 알면서 남긴 것

- **게이트 판정이 사전점검 뒤로 밀렸다.** 게이트에도 막히고 자격증명도 깨진 몰은 예전의 `SKIPPED` 대신 `FAIL` 로 남는다. 두 조건이 겹치는 교집합뿐이고, 그 상태의 몰은 어느 순서에서도 수집이 불가능하다. 오히려 조치 가능한 사유("yml 에 계정 정보 없음")가 드러난다.
- **`connectToMall`(계정연결)은 여전히 제한기 밖에서 브라우저를 띄운다.** 이번 단계의 범위는 수집 경로다. 제한기 javadoc 의 "이 JVM 안의 브라우저 총 개수" 라는 보장과 실제 적용 범위가 어긋난 채로 남는다.
- **`MallProfileLock` 은 배선하지 않았다.** 계획서에 `[재검토]` 로 표시돼 있고, 5-7b 분석이 "지금 배선해도 보호할 프로필이 없다" 로 결론지었다. 획득 순서(제한기 바깥 / 프로필 락 안쪽)는 코드 구조로 자리만 비워 뒀다.

#### 검증

`spotlessApply test` 로 **388건, 실패 0**(직전 367건). `clean` 은 쓰지 않았다. 새 테스트는 전부 브라우저·네트워크·DB 없이 돈다.

경계 쪽은 새로 만들 필요가 없었다. 5-4 때 만든 `MallSessionProfileFieldsTest` 가 실제 SQLite 에 대고 `getMall` 의 게이트 키 5개를 검사하는데, 즉시수집 경로의 새 게이트가 같은 메서드·같은 키를 읽으므로 그 보호를 그대로 물려받는다.

### [2026-08-05 06:40] 5-18 게이트 단축평가 — 호출부가 아니라 게이트 안에서 고쳤다 (v0.15.8)

#### 작업 개요

(a) 가 통과했으므로 지시대로 5-18 로 넘어왔다. 옵트인하지 않은 몰이 매 회차 실행 컨텍스트를 탐지하던 문제다. 수용 기준 10 의 기존 위반.

#### 무엇이 문제였나

게이트 자신은 이미 올바르다 — `evaluate` 첫 줄이 `if (!profileApplies) return PROCEED;` 다. 그런데 호출부가 이렇게 부른다.

```java
SessionProfileGate.evaluate(
    SessionProfilePolicy.appliesTo(...),
    ExecutionContextDetector.detect(),   // ← 자바가 호출 전에 평가한다
    ...);
```

자바는 인자를 전부 평가한 뒤 호출하므로, **첫 줄에서 통과할 몰에서도 탐지가 돈다.** 그 탐지는 `tasklist` 를 띄우고 최대 5초를 기다린다.

#### 왜 호출부가 아니라 게이트를 고쳤나

계획서는 "호출부 단축평가"라고 적었고 그렇게 고칠 수도 있었다. 그런데 이 결함의 형태 자체가 **"규약을 호출부가 지켜야 해서 어긋난 것"** 이다 — 같은 판정을 하는 `StartupTasks.blockedBySessionProfile` 은 `appliesTo` 를 먼저 보고 일찍 반환해 올바른데, 스케줄러 쪽만 어긋나 있었다.

호출부만 고치면 규약은 그대로 남고 세 번째 호출부에서 다시 깨진다. 그래서 인자 타입을 `Supplier<ExecutionContext>` 로 바꿔 **단축평가를 게이트 안으로 옮겼다.** 이제 호출부가 실수할 자리가 없다.

#### 검사되는 계약으로 만들었다

테스트 2건을 붙였다 — 적용 대상이 아니면 공급자를 **호출조차 하지 않는다**, 대상이면 **정확히 한 번** 호출한다. 값으로 받던 때는 이 테스트를 쓸 수조차 없었다(호출부에서 이미 평가돼 버리므로). 총 367건, 실패 0.

옵트인한 몰의 판정 결과는 그대로다. 달라지는 것은 옵트인하지 않은 몰에서 탐지가 아예 일어나지 않는 것뿐이다.

### [2026-08-05 06:10] UA 하드코딩이 유일한 차이였다 — 제거 후 12축 전부 일치 (v0.15.7)

#### 작업 개요

"(a) 의 실패 요인이 UA 하드코딩 때문인지 확인하고, 맞으면 제거해 올바른 크롬 버전을 취하라"는 지시. 확인했고, 맞았고, 제거했다.

#### 확인 — 무엇이 갈리는가

한 축(`navigator.webdriver`)만으로는 "표식이 없다"를 믿을 수 없어 **12축**으로 나란히 쟀다.
`webdriver` · `ua` · `uaTail` · `platform` · `langs` · `plugins` · `cores` · `chromeKeys` · `brands` · `uaFull` · `webgl` · `perm`.

| 축 | 순정 | 후보 (a) — 수정 전 |
|---|---|---|
| `ua` | `Chrome/150.0.0.0` | `Chrome/124.0.6367.91` |
| `uaTail` | `Safari/537.36` | `Safari/537.3` |
| `uaFull` | `150.0.7871.187` | 빈 값 |

**3축이 갈렸고 셋 다 `buildChromeOptions` 의 UA 한 줄에서 나왔다.** 나머지 9축은 이미 일치했다.

`uaTail` 이 흥미롭다 — 박아 둔 UA 는 끝이 `Safari/537.3` 으로, 정상값 `537.36` 과 다르다. 즉 그 문자열은 **버전이 낡았을 뿐 아니라 형태 자체가 어긋나 있었다.** `uaFull` 이 빈 값인 것도 같은 뿌리다. UA 문자열을 덮어써도 클라이언트 힌트는 실제 버전을 그대로 내므로, 사이트는 자기모순을 그냥 본다.

#### 제거 — 무엇으로 대체했나

**아무것도 지정하지 않는 것으로.** Chrome 이 내는 값이 언제나 옳고, 업데이트를 따라가는 유지보수도 사라진다. 버전을 읽어 UA 문자열을 조립하는 방안도 있었지만, 플랫폼 토큰까지 정확히 재현해야 하고 그 자체가 새 드리프트 원천이 된다.

박아 둘 이유가 있었는지도 확인했다 — **그 값을 읽는 코드가 저장소에 없고, 도입 커밋(초기 대규모 리팩토링)에도 근거가 없다.** 그럴듯한 동기 하나(헤드리스 노출 은폐)는 직접 재서 배제했다: `--headless=new` 는 `Chrome/150.0.0.0` 을 내고 `HeadlessChrome` 를 드러내지 않는다.

#### 결과

| 항목 | 결과 |
|---|---|
| 지문 12축 (순정 vs 후보 a) | **전부 일치** |
| headless UA | `Chrome/150.0.0.0` (누출 없음) |

프로브가 이 상태를 계약으로 고정한다 — 한 축이라도 갈리거나 headless 가 스스로를 드러내면 실패한다. ADR-0002 의 캡처 경로를 **후보 (a) 로 확정**했다.

#### 이번 변경은 휴면이 아니다

지금까지의 5-15 작업은 부르는 호출자가 없어 런타임 동작이 그대로였지만, 이것은 **모든 수집기가 보내는 UA 를 바꾼다.** 몰이 UA 에 따라 다른 마크업을 주는지는 실제 수집 실행으로만 확인되므로, 그 검증은 승인 대상으로 남는다. 롤백은 한 줄이다.

### [2026-08-05 05:20] 후보 (a) 권고 철회 — UA 일관성 측정 (버전 변경 없음)

#### 배경

앞 항목에서 후보 (a)(Selenium+마스킹)를 권했다. 사용자가 **"(a)는 이전 세션 테스트에서 계속 실패하지 않았어?"** 라고 물어 기록을 확인했고, 확인 과정에서 **권고 자체가 근거가 약했다**는 것이 드러났다.

#### 기록 확인 — 무엇이 실패했었나

로그인 경로가 오래 실패한 이력은 있다. 다만 **봇 차단이 아니었다.** Phase 0A 실측이 배포본 로그에서 이마트 프로모션 배너가 로그인 버튼을 덮어 `ElementClickInterceptedException` 이 난 순간을 찾아 확정했고, SSG 빈 결과 행 파싱 실패·수집기 간 실패 전파도 같은 시기 원인이었다. 전부 **자동화가 셀렉터로 직접 로그인하던 경로**의 문제다.

그런데 후보 (a) 는 사람이 타이핑하는 경로라 그 실패들과 같은 것이 아니다. **문제는 (a) 가 이 저장소에서 한 번도 시도된 적이 없다는 것이다** — 6차 세션의 사람 로그인은 순정 Chrome(T3)과 Playwright(PW-1)였지 Selenium 이 아니었다. 즉 내 권고는 측정이 아니라 추론이었다.

#### 그래서 잴 수 있는 축을 하나 더 쟀다 (실계정 불필요)

| 팔 | UA 문자열 | 클라이언트 힌트 |
|---|---|---|
| **[대조군]** 순정 Chrome | `Chrome/150.0.0.0` | `Chromium/150 Google Chrome/150` |
| **[후보 a]** Selenium + 마스킹 | **`Chrome/124.0.6367.91`** | `Chromium/150 Google Chrome/150` |

`buildChromeOptions` 가 UA 문자열을 고정하는데 클라이언트 힌트는 실제 버전을 낸다. **(a) 는 자기를 26개 메이저 버전 다르게 주장하는 브라우저를 사이트에 건넨다.** 사이트가 이미 받는 두 값이 서로 어긋나므로 정교한 분석이 필요 없다.

T2 의 "지문 동등성 PASS" 와 모순되지 않는다 — T2 가 잰 네 축에 UA 쌍이 없었다. 다른 것을 잰 것이다.

#### 결론

**권고 철회. 미결로 둔다.** (a) 를 고르려면 UA 하드코딩 수정이 선행 조건인데, 그 UA 는 기존 수집기 전부가 쓰고 있어 회귀 위험이 따로 있다. 어느 쪽이 실제 몰의 봇 방어를 통과하는지는 실사이트 없이 알 수 없다. ADR-0002 갱신.

#### 배운 것

- **"계속 실패했다"는 기억은 원인까지 같이 기억되지 않는다.** 실패 이력은 실재했지만 원인은 봇 차단이 아니라 UI 드리프트였다. 기록을 열어 보지 않았으면 잘못된 이유로 결론이 뒤집혔을 것이다.
- **내가 방금 쓴 방법론을 내가 어겼다.** 측정으로 (b) 의 전제를 깬 직후, (a) 는 재지도 않고 권했다.

### [2026-08-05 04:30] 5-15 세션 캡처 — 전제 실측 + 캡처 경로 기반 (v0.15.6)

#### 작업 개요

5-15(세션 캡처)에 착수하면서 계획서가 "유력하나 미측정"으로 남긴 캡처 경로부터 쟀다. 실계정 없이 로컬 페이지만으로 재는 방법을 찾았고, **전제 하나가 깨졌다.** 판정 기록은 `doc/developer/adr/ADR-0002-session-capture-path.md`.

#### 착수 전에 정정한 것 — 캡처 시점

계획서는 "사람이 로그인하면 CDP 로 세션을 떠서 저장한다"고 적었고, 6차 세션의 런처는 **창이 닫힌 것**(프로필 락 해제)을 완료 신호로 삼았다. 그런데 ADR-0001 이 확인한 대로 인증 쿠키가 세션 스코프라 **창이 닫히면 사라진다.** 닫힌 뒤에는 뜰 것이 없다.

⇒ 캡처는 브라우저가 **살아 있는 동안** 해야 한다. 락 해제는 정리 신호다. 이 어긋남은 조용하다 — 락 해제 뒤에 뜨면 매번 빈 스냅샷이 나오는데 아무 예외도 나지 않는다.

#### 측정 결과

| 팔 | `navigator.webdriver` |
|---|---|
| **[대조군]** 순정 chrome.exe, 포트 없음 | `false` |
| 순정 chrome.exe + 디버깅 포트 | **`true`** |
| 붙은 뒤 마스킹 주입 | `false` |
| **[양성 대조군]** Selenium 기본 | `true` |
| **[후보 a]** Selenium + 마스킹 | `false` |

**`--remote-debugging-port` 를 여는 것 자체가 표식을 켠다.** 후보 (b) 의 근거였던 "브라우저 자체는 순정"이 사람이 로그인하는 그 시점에는 성립하지 않는다. 대조군이 없었으면 "런처가 뭘 잘못했나"로 갈 뻔했다.

그 밖에: 캡처·주입 왕복 성립 / `quit()` 후에도 사람의 창은 살아남음 / `closeAttachedBrowser` 로 닫힘 / 브라우저가 도는 동안 프로필 사용 중 판정 참.

#### 상세 내용

- `WebDriverManager.attachToRunningChrome` · `closeAttachedBrowser` · `applyStealth` public 승격
- `SessionSnapshot` · `SessionTransfer`(신규) — 뜨고 넣는 최소 단위. **평문 디스크 경로는 두지 않았다**(5-16 이 암호화와 함께 가져온다)
- `NativeChromeLoginLauncher` 디버깅 포트 지원 — 고정 번호 거부, 이미 열린 프로필 거부, 포트 기록 2줄 규칙
- 테스트 29건 추가(총 363건, 실패 0). 개발 트리 DB md5 불변

#### 적대적 검증에서 배운 것

구현 뒤 3렌즈(회귀·조용한실패·계약경계)와 반증 패스를 돌렸다. 32건 중 REFUTED 1·OVERSTATED 4, 나머지 확인. 값진 것 둘:

- **내 프로브가 '재지 못함'을 '재서 false'로 접고 있었다.** 보고를 못 받은 축을 `false` 로 읽어, 재지도 않은 것에 대해 확신에 찬 문장을 산출물에 남길 수 있는 상태였다. 6차 세션에서 네 번 반복된 "측정이 깨졌는데 결론은 나오는" 형태 그대로다. 3상태로 갈랐다.
- **적대적 검증도 틀린다 — 측정이 갈랐다.** 검증이 "Windows Chrome 은 `lockfile` 을 만들지 않으므로 새 가드가 죽는다"를 HIGH 로 보고했다. 근거는 "쉬고 있는 프로필 14개에 파일이 없다"였는데, 실제로 띄워 보니 도는 동안에는 있고 배타적으로 잠겨 있으며 종료 시 지워진다. **쉬고 있을 때 없는 것과 만들지 않는 것은 다르다.** 지금은 프로브가 그 전제를 매번 함께 잰다.

### [2026-08-05 02:30] Phase 4A 종결 — 엔진 판정(T1~T4) 실측 + 세션 프로필 경로 (v0.15.5)

#### 작업 개요

브라우저를 실제로 띄워 Phase 4A 의 네 측정을 끝냈다. 판정 기록은 `doc/developer/adr/ADR-0001-session-profile-reuse-t3.md` 에 남겼다. 코드로는 세션 프로필로 브라우저를 여는 경로(휴면)와 사람 로그인용 순정 Chrome 런처를 추가했다.

#### 배경

"사람이 1회 로그인해 둔 프로필을 자동 수집이 재사용한다"는 전략을 세웠고, Phase 4A 는 그것을 **엔진 질문**으로 규정했다 — Selenium 단독으로 되는가, Playwright(Phase 5B)가 필요한가.

#### 측정 결과

| 측정 | 결과 |
|---|---|
| T1 자동화 표식 제거 | **PASS** — 실제 chrome.exe 명령줄에서 `enable-automation`·`test-type` 소거 확인 |
| T2 지문 동등성 | **PASS** — 순정과 완전 일치 |
| T4 chromedriver 기동 | 문제 없음 — 150 이 `--remote-debugging-port=0` 을 스스로 붙인다 |
| T3 세션 재사용 | **FAIL (ssg·oasis 모두)** — 그러나 엔진 탓이 아니다 |

**대조군이 원인을 갈랐다.** Selenium 을 빼고 순정 Chrome 으로 같은 프로필을 열어도 결과가 동일했다. 자동화가 걸러진 것이 아니라 **프로필에 유효한 세션이 없다.** 캡처해 보니 ssg 의 인증 쿠키가 전부 세션 스코프였고, 세션 쿠키는 어떤 Chrome 설정으로도 재시작을 넘지 못했다(영속 쿠키는 살아남는 것을 함께 확인한 양성 대조군).

**대신 세션 이관이 성립한다.** 같은 로그인 한 번으로 방식을 갈랐다 — 프로필 재사용은 엔진과 무관하게 실패했고, 살아 있는 브라우저에서 쿠키를 떠 새 브라우저에 주입하는 방식은 성공했다. 그 방식은 Selenium 만으로 된다(CDP `Network.getAllCookies` 캡처 + `setCookies` 주입 왕복 실측). **⇒ Phase 5 의 전략을 "프로필 재사용"에서 "세션 이관"으로 바꾼다.**

#### 상세 내용

- `WebDriverManager.getWebDriver(browserName, profileDir)` 오버로드 — 프로필이 `null` 이면 조립 결과가 기존과 완전히 같다. 프로필이 있을 때만 `--user-data-dir`·`--profile-directory`·`excludeSwitches`·바이너리 핀·headless 강제 off·CDP 마스킹이 붙는다.
- `NativeChromeLoginLauncher`(신규) — 사람 로그인용 순정 chrome.exe 기동. 창 닫힘 판정은 `Process.waitFor()` 가 아니라 프로필 락 해제로 한다.
- 프로브 4종(`@Tag("probe")`) + `test` 태스크의 태그 제외 + 프로브 프로퍼티를 테스트 JVM 까지 전달.
- 테스트 29건 추가(총 333건, 실패 0). 개발 트리 DB md5 불변.

#### Playwright 의존성 처리 — 커밋을 둘로 나눴다

판정 배제 절차로 넣었던 `PlaywrightSessionProbe` 와 그 `testImplementation` 은 근거가 사라져 제거 대상이었지만, 미커밋 상태에서 그냥 지우면 **git 이력에도 남지 않아** 측정 수단이 통째로 사라진다. 그래서 커밋을 둘로 나눴다.

1. `6f59357` — 프로브를 포함해 커밋한다. PW-2·PW-3 측정이 이력에 남는다.
2. 이 커밋 — 의존성과 프로브를 제거한다. `main` 최종 상태가 수용 기준 5 를 지킨다.

재현이 필요하면 `git show 6f59357` 로 복원할 수 있다. 두 커밋을 함께 push 하므로 CI 는 최종 상태 하나만 검증한다 — 중간 커밋에서 Playwright 를 내려받는 비용은 들지 않는다.

#### 이번 측정에서 배운 것

- **대조군 없이는 FAIL 의 원인을 못 가른다.** 첫 판에는 Selenium 결과만 보고 "봇 탐지"로 갈 뻔했다.
- **양성 대조군이 없으면 '없다'는 결과를 믿을 수 없다.** 세션 쿠키가 안 남았다는 결과는 영속 쿠키가 남았다는 사실이 함께 있어야 의미가 있다.
- **관측 지점을 틀리면 맞는 실험도 틀린 답을 준다.** 세션 쿠키 생존을 디스크로 재려 했으나, 복원이 일어난다면 그것은 브라우저 메모리에 있다.
- **측정 실패를 데이터로 섞지 말 것.** 쿠키 파일 읽기 실패를 '새 쿠키 1개'로 세는 바람에 확신에 찬 오답이 한 번 나왔다.

### [2026-08-04 04:00] C-2 히스토리 재작성 — **하지 않기로 종결** (docs, 버전 변경 없음)

#### 결론

공개 이력에서 비공개 저장소 이름과 개발 PC 경로를 지우는 재작성을 **하지 않는다.** 조사까지 마치고 내린 결론이라 근거를 대장 §5.1 에 남겼다. 재론 방지가 목적이다.

#### 조사한 것

| 항목 | 값 |
|---|---|
| 전체 커밋 | 110개 |
| 이름·경로 포함 커밋 | 12개 (커밋 메시지 1건 포함 — `doribox 의존성 임시 해제`) |
| 최초 노출 | 이력 초반 → 이후 SHA 전부 변경 |
| 태그 / 릴리스 | 5개 / 5건 |
| 포크 / 스타 | **0 / 0** |

#### 판단이 뒤집힌 지점

착수 전 내 우려는 "모든 clone·fork 가 깨진다" 였는데 **포크가 0이라 그 문제는 아예 없었다.** 즉 위험은 예상보다 작았다.

그런데 반대쪽에서 더 결정적인 것이 나왔다 — **재작성해도 GitHub 에서 지워진다는 보장이 없다.** force-push 후에도 도달 불가 객체는 GC 전까지 SHA 직접 접근으로 남고, 완전 삭제는 GitHub 지원에 별도 요청해야 한다. 110개 커밋의 SHA 를 전부 바꾸고도 목적을 못 이룰 수 있다는 뜻이다.

노출 내용이 **저장소 이름과 프로젝트 경로**뿐이고 자격증명·코드·개인정보가 아니라는 점까지 놓고 보면 균형이 맞지 않는다.

#### 남긴 것

- 백업 번들을 떠 두고 시험 사본에서 검증하려 했으나 `git filter-branch` 가 권한 정책에 막혀 실행하지 못했다. 우회하지 않고 사용자에게 선택지를 제시했고, ③(하지 않음)으로 결정됐다.
- 뒤집을 조건을 명시해 뒀다 — **자격증명이나 코드가 이력에 들어간 것이 확인되면** 그때는 GC 요청까지 포함해 다시 판단한다.
- 앞으로의 노출은 §2.2 의 "공개 산출물에는 대장 번호만" 규칙이 막는다. 현재 트리는 이미 정리됐다.

### [2026-08-04 03:20] v0.15.4 게이트 실측 — **배선이 죽어 있었다** (사용자 승인)

#### 첫 실행에서 게이트가 걸리지 않았다

킬스위치를 켜고 옵트인 ON·프로필 이름 없음으로 돌렸다. `PROFILE_MISSING` 으로 막혀야 하는데 **그대로 통과해 실계정 로그인까지 갔다.**

원인은 로그의 SQL 한 줄에 있었다.

```
SELECT seq, id, name, details, encrypt_key, encrypt_iv, account_status,
       last_signin_time, auto_collect, collect_interval_minutes FROM jbg_mall WHERE seq=1
```

`session_profile_*` 이 없다. `mall.get("session_profile_enabled")` 가 항상 null → 옵트인 판정이 영원히 false → `appliesTo(false)` → PROCEED. **컬럼을 선언하고 정책을 만들고 게이트를 배선하고도, 값을 실어 나르는 한 구간이 끊겨 전체가 죽어 있었다.**

#### 왜 단위테스트가 못 잡았나

게이트 테스트는 `SessionProfileGate.evaluate(...)` 를 **직접** 호출했다. 그래서 판정 로직은 완벽히 검증됐지만, **그 함수에 값을 넣어 주는 경로**는 시야 밖이었다. 순수 함수 테스트와 실제 배선 사이에는 이런 틈이 있고, 이번 것이 정확히 거기 빠졌다.

메꾸는 방법은 순수 함수 테스트를 늘리는 게 아니라 **경계를 건너는 테스트**다. `MallSessionProfileFieldsTest` 는 DAO 를 실제 SQLite 파일에 대고 호출해 게이트가 읽는 키가 결과에 담기는지 본다.

#### 대가

**실계정 SSG 로그인 1회가 의도치 않게 발생했다.** 게이트가 자격증명 복호화보다 앞에 있으므로 막히면 로그인이 없어야 했는데, 막히지 않아 그대로 수집까지 갔다(17건 수집, 기존 17건 중복 판정, 신규 0건). 프로브 DB 사본을 쓰고 있어 배포본 원본은 무접촉이다.

#### 고친 뒤 실측 — 3종 전부 확인

| 조건 | 결과 | 로그인 |
|---|---|---|
| 옵트인 ON, 프로필 이름 없음 | `PROFILE_MISSING` | 없음 |
| 프로필 이름 있음, 소유자 불일치 | `PROFILE_OWNER_MISMATCH` | 없음 |
| 소유자 일치, 컨텍스트 세션 0 강제 | `REQUIRES_USER_SESSION` | 없음 |

셋 다 ChromeDriver 기동조차 없었다. `SKIPPED` 행이 `step_name=session-profile-gate` 와 사유 문구를 달고 `jbg_collect_log` 에 남았다.

**5-5 도 같은 실행에서 확인됐다** — `세션 프로필 몰인데 세션 0 이라 스케줄을 등록하지 않는다` → `복원할 개별 쇼핑몰 스케줄 없음`.

#### 도달할 수 없는 하나

`PROFILE_LOCKED` 는 아직 발생시킬 수 없다. `MallProfileLock` 은 만들었지만 **수집 경로에 락 획득을 배선하지 않았다.** 락은 브라우저 세션을 여는 자리에 붙어야 하고, 그 자리는 5-7(경로 A 배선)에서 생긴다. 지금은 `fromLockOutcome` 이 정의만 돼 있다.

#### 곁가지 — WAL 파일을 빼고 복사했다

프로브 DB 사본을 만들 때 `.db` 만 복사해 컬럼이 없는 것처럼 보였다. 마이그레이션 결과가 아직 `-wal` 에 있었기 때문이다. WAL 모드 SQLite 는 세 파일을 함께 복사하거나 체크포인트를 먼저 해야 한다. 이전 회차들은 앱을 정지한 뒤 복사해 우연히 문제가 없었다.

#### 메모

- 테스트 310 → **314건**(실패 0). 배포본·개발 트리 DB md5 둘 다 불변.

### [2026-08-03 19:30] v0.15.3 Phase 5-4·5-5 — 게이트 배선

#### 배선의 전제는 "꺼져 있으면 아무것도 바꾸지 않는다"

여기서부터는 기존 수집 경로를 건드린다. 그래서 첫 번째 계약을 이렇게 잡았다 — **마스터 킬스위치가 꺼졌거나 그 몰이 옵트인하지 않았으면 게이트는 무조건 통과한다.** 검증되지 않은 Phase 5 경로가 멀쩡히 돌던 수집을 망가뜨리면 안 된다.

`SessionProfileGate.evaluate` 의 첫 줄이 그 판정이고, 테스트가 그것을 고정한다.

#### SKIPPED 로 적은 이유 — FAIL 이면 브레이커가 몰을 차단한다

게이트에 막힌 것을 `FAIL` 로 기록하면 `CollectBreakerPolicy` 가 연속 실패로 세어 그 몰을 자동 차단한다. 그런데 **막은 것은 우리 쪽 조건이지 사이트가 아니다.** 세션 0 에서 돌고 있다는 이유로 몰이 차단되면, 나중에 조건을 갖춰도 브레이커부터 풀어야 한다.

`SKIPPED` 는 성공·실패 집계 양쪽에서 빠진다(3-3 에서 그렇게 정해 뒀다). 이 자리에 정확히 맞는다.

#### 5-5 를 등록 시점에 둔 이유

세션 0 에서 옵트인 몰의 스케줄을 등록해 두면 매 회차가 게이트에 막혀 `SKIPPED` 만 쌓인다. 주기가 720분이니 12시간마다 같은 줄이 반복되고, 그러면 **로그가 무의미해져 정작 사람이 봐야 할 때 눈에 띄지 않는다.**

등록 자체를 건너뛰고 기동 시 한 번만 남긴다.

#### 판단이 갈린 두 곳

- **소유자가 기록돼 있지 않으면 검사하지 않는다.** 소유자를 적기 전에 만든 프로필을 '미기록' 만으로 막으면 멀쩡한 것을 못 쓰게 된다. 기록돼 있을 때만 대조한다.
- **잠글 수 없는 환경(`UNAVAILABLE`)은 막지 않는다.** 막을 근거가 없다. `MallProfileLock` 이 경고를 남기는 것으로 족하다.

#### 아직 확인하지 못한 것

게이트가 **실제로 거는지**는 킬스위치를 켜고 수집을 돌려야 확인된다. 지금 검증된 것은 판정 로직과 "꺼져 있으면 통과" 계약뿐이다. 켜는 순간 실계정 수집이 발생하므로 승인이 필요하다.

#### 메모

- 테스트 299 → **310건**(실패 0). DB 무접촉.

### [2026-08-03 18:50] v0.15.2 Phase 5-1 — 실행 컨텍스트 판별 (코드 부분)

#### 이 판별이 없으면 생기는 증상

세션 0 에는 데스크톱이 없다. 거기서 headed 브라우저를 띄우면 창이 그려지지 않는다. 문제는 그게 **깔끔한 실패로 나타나지 않는다**는 것이다 — 수집이 로그인 화면에서 멈춘 채 타임아웃되고, 로그에는 "타임아웃" 만 남는다. 프로필이 잘못됐는지, 세션이 만료됐는지, 애초에 띄울 수 없는 자리인지 구분할 수 없다.

그래서 **먼저 판별하고, 세션 0 이면 시도하지 않고 그 사실을 기록**하는 쪽으로 잡았다.

#### 런처 배선이 필요했던 이유

`JangbogoLauncher.filterModeArguments` 가 `--service` 를 Spring 에 넘기기 전에 걸러낸다. 그래서 컨텍스트 안의 코드는 자기가 서비스로 떴는지 알 방법이 없었다. `launchServiceMode` 에서 `jangbogo.launch.mode=service` 를 심어 그 사실을 넘긴다.

#### 판단 순서와, 모를 때의 태도

재정의 → 세션 번호 → 휴리스틱 3종 순이다. 두 가지를 의도적으로 정했다.

- **신호가 엇갈리면 `UNKNOWN`** 이다. `Console` 인데 머신 계정이면 둘 중 하나가 틀린 것이고, 한쪽을 임의로 우선하면 틀린 확신이 된다.
- **`UNKNOWN` 은 브라우저를 띄울 수 있다고 보지 않는다.** 모를 때 띄우면 위의 증상이 그대로 재현된다. `canRunHeadedBrowser()` 가 `INTERACTIVE` 에서만 true 다.

#### 실측으로 확인한 것 / 못 한 것

`tasklist` 출력 형식을 이 머신에서 실제로 받아 파서와 대조했다.

```
"java.exe","29408","RDP-Tcp#29","2","99,292 K"
```

칸이 5개, 세션 번호는 4번째, **메모리 칸에 쉼표가 들어간다** — `split(",")` 로는 칸이 밀린다는 것이 여기서 확인된다. 이 줄을 회귀 고정용 테스트로 넣었다. 세션 이름(`RDP-Tcp#29`)만으로 내린 판정도 세션 번호와 같은 결론(INTERACTIVE)이라 두 신호가 어긋나지 않았다.

**못 한 것**: 서비스로 떴을 때 `tasklist` 가 정말 0 을 돌려주는지. 서비스 등록이 필요하다. 파서와 판단 로직은 검증됐고 남은 것은 그 한 값의 확인이므로, 서비스를 등록할 때 함께 보면 된다.

#### 메모

- 테스트 285 → **299건**(실패 0). DB 무접촉. 기능은 여전히 꺼져 있다.

### [2026-08-03 18:10] v0.15.1 Phase 5-11 — 브라우저 동시성 제한

#### 락과 겹치지 않는다

같은 "동시 실행 제어"처럼 보이지만 지키는 대상이 다르다.

| | 지키는 것 | 경계 |
|---|---|---|
| `MallProfileLock` (5-3) | 프로필 **하나** | 프로세스 **사이** |
| `BrowserConcurrencyLimiter` (5-11) | 브라우저 **총 개수** | **JVM 안** |

프로필이 서로 달라도 Chrome 을 여럿 띄우면 메모리·CPU 가 무너진다. 그리고 같은 시각에 여러 몰로 나가는 트래픽은 그 자체가 눈에 띄는 패턴이다. 그래서 프로필 락을 통과해도 이 제한에 걸릴 수 있고, 반대도 성립한다.

#### 기다리는 정책을 락과 반대로 잡았다

프로필 락은 **기다리지 않는다** — 붙잡고 있는 쪽이 다른 프로세스라 언제 끝날지 알 수 없다.

이 제한은 **기다린다.** 붙잡고 있는 것이 같은 JVM 의 수집이고, 실측상 한 회차가 약 2분 30초라는 것을 안다. 여기서 즉시 포기하면 그 몰은 **다음 주기까지 통째로 건너뛴다** — 주기가 720분이므로 반나절을 잃는다. 몇 분 기다려 차례를 받는 편이 낫다.

대기 상한은 300초다. 앞선 수집을 기다리기엔 넉넉하고, 멈춘 수집에 무한정 매달리지는 않는다. 넘기면 `SKIPPED/BROWSER_BUSY` 로 물러난다.

공정(fair) 세마포어를 쓴다. 아니면 한 몰이 계속 밀릴 수 있다.

#### 테스트에서 실제로 확인한 것

"기다린다"가 말뿐이 아님을 멀티스레드 테스트로 고정했다 — 앞선 수집이 끝나면 대기하던 쪽이 자리를 받는다. 이게 성립하지 않으면 기다리는 설계 자체가 무의미하다.

실패한 임대권을 닫아도, 같은 임대권을 두 번 닫아도 자리가 늘지 않는 것도 함께 고정했다. 세마포어에서 없던 permit 이 생기는 것이 가장 흔한 사고다.

#### 메모

- 테스트 275 → **285건**(실패 0). DB 무접촉. 기능은 여전히 꺼져 있다.
- 테스트를 `svc.util` 패키지에 뒀다. 생성자를 public 으로 열면 인스턴스가 여럿 생길 수 있고, 그러면 "총량 제한"이 총량 제한이 아니게 된다.

### [2026-08-03 17:30] v0.15.0 Phase 5 선행 코드 — 상태·스위치·락

#### 범위를 이렇게 자른 이유

Phase 5 는 14건이고 그중 다수가 실계정·브라우저·사람의 로그인을 요구한다(5-4·5-6·5-7·5-9·5-10·5-13). 승인 없이 진행할 수 있는 것은 **상태를 둘 자리, 안전장치, 그리고 브라우저 없이 검증되는 원시 기능** 셋이다. 이번에 그 셋을 넣었다.

| 항목 | 넣은 것 |
|---|---|
| 5-2 | `jbg_mall` 세션 프로필 컬럼 5개 |
| 5-12 | `SessionProfilePolicy` — 마스터 킬스위치 |
| 5-14 | 프로필 루트 정책 (`.gitignore` 는 앞서 처리) |
| 5-3 | `MallProfileLock` — 프로필 단위 파일 락 |

#### 5-2 는 계획서보다 작아졌다

계획서는 `JbgMallDataAccessObject.ensureSessionProfileColumns()` 를 새로 만들라고 적고 있었다(`ensureAutoCollectColumns` 패턴 복제). **이제 불필요하다** — Phase 3-10 의 `SchemaMigrator` 가 `schema.sql` 을 단일 선언처로 삼아 없는 컬럼을 자동 ALTER 한다. 컬럼을 자바 코드에 다시 적으면 두 목록이 어긋날 자리를 새로 만드는 것이다.

`schema.sql` 에 5줄 더한 것으로 끝났고, 실제 DB 에서 `PRAGMA table_info` 로 5개가 붙은 것을 확인했다.

#### 스위치를 기능보다 먼저 만든 이유

프로필 재사용은 수집 경로 전체를 바꾼다 — 로그인 화면을 밟지 않고, 사람이 쓰던 프로필로 브라우저를 띄우고, 락과 소유자 검사가 붙는다. 하나라도 어긋나면 수집이 통째로 멈춘다.

그래서 **코드를 다 넣어 두고도 배포본은 예전처럼 도는 상태**를 유지할 수 있어야 한다. 마스터 스위치가 꺼져 있으면 몰별 옵트인이 켜져 있어도 아무 일도 하지 않는다. 문제가 생기면 롤백이 아니라 스위치 하나로 되돌린다.

#### 락 설계에서 갈린 판단 셋

- **비차단** — 기다리면 스케줄러가 다음 회차까지 통째로 막힌다. 수집은 나중에 다시 돌면 된다.
- **키는 프로필 이름** — 몰 seq 가 아니다. 같은 계정을 쓰는 몰이 실재하므로(ssg/emart) seq 로 잠그면 그 경우를 놓친다.
- **락 파일을 지우지 않는다** — 지우면 다른 프로세스가 이미 연 핸들과 새로 만든 파일이 갈라져 락이 무의미해진다. 빈 파일 하나가 남는 비용이 그 위험보다 싸다.

`UNAVAILABLE` 을 따로 둔 것도 같은 결이다. 파일 락이 지원되지 않는 위치가 있을 수 있는데, 못 잠근다고 수집을 멈출 이유는 없다. 다만 조용히 넘기지도 않는다.

#### 남은 Phase 5 (전부 승인 또는 사람의 조작이 필요하다)

5-1(실행 컨텍스트 판별) · 5-4(수집 게이트 4종) · 5-5(스케줄 등록 조건) · 5-6(`--login` 런처 모드) · 5-7(경로 A 배선) · 5-9(`ProfileMallCollector`) · 5-10(만료 감지) · 5-11(브라우저 동시성 세마포어) · 5-13(UI).

이 중 **5-11 은 순수 코드라 승인 없이 가능하다** — 다음 차례로 적합하다. 나머지는 브라우저를 띄우거나 사람이 로그인해야 검증된다.

#### 메모

- 테스트 260 → **275건**(실패 0). DB 무접촉.
- 기능은 꺼져 있다. 이번 릴리스로 동작이 바뀌는 것은 없다.

### [2026-08-03 12:30] v0.14.0 A·B 목록 — 잔여 정리 3건 + 보안 하드닝 4건

#### 조사가 계획서를 정정한 것 — B-3 은 절반이 이미 고쳐져 있었다

계획서 §7.4 는 SQL 주입 지점으로 `JbgItemDataAccessObject` / `JbgOrderDataAccessObject` 의 INSERT 를 지목했다. **실제로 확인해 보니 수집기가 쓰는 경로(`addWithConnection`)는 order·item 양쪽 다 이미 PreparedStatement 였다.** 문자열 조립로 남아 있던 것은 호출되지 않는 `add(...)` 오버로드였다.

같은 INSERT 를 두 벌 두면 한쪽만 고쳐진다는 것을 그대로 보여 준 사례다. 구현을 `addWithConnection` 하나로 모았다.

**다만 조립 자체는 계획서가 말한 2곳이 아니라 5개 DAO 약 19곳에 퍼져 있다.** 이번에 처리한 것은 외부 입력(수집한 상품명·매장명·영수증 번호)이 들어오는 경로다. 나머지(`jbg_export_config`, `jbg_mall` 의 UPDATE 계열)는 사용자 본인이 폼에 넣는 값이라 노출 성격이 다르고, `encrypt_key`·`ftp_pass` 같은 자격증명 컬럼을 건드리므로 **별도 패스로 미룬다.**

#### B-1 은 "고칠 수 없어서" 절반만 고쳤다

키를 바꾸면 기존 DB 의 암호문을 복호화할 수 없다. 그래서 **기본값은 그대로 두고 덮어쓸 수만 있게** 했다. 기본값을 쓰면 기동 시 경고가 뜬다. 테스트가 양쪽을 다 고정한다 — 재정의가 먹는 것과, 재정의가 없을 때 기존 데이터가 계속 열리는 것.

이건 해결이 아니라 **완화**다. 진짜 해결은 재암호화 마이그레이션이고 그건 별도 과제다.

#### B-2 는 동작이 바뀐다

`dev` 패키지 빈 4개가 `@Profile("dev")` 가 됐다. **개발 중 `/dev/*` 를 쓰려면 이제 `--spring.profiles.active=dev` 로 띄워야 한다.** 그래서 patch 가 아니라 minor 를 올렸다.

#### 메모

- 테스트 252 → **260건**(실패 0). DB 무접촉.
- A-3(`loadCollectorHealth`)은 JS 라 단위테스트를 붙이지 못했다. 이 프로젝트에 JS 테스트 장치가 없고, 3줄 고치자고 러너를 들이는 것은 균형이 맞지 않는다. 실패 분기를 `clearCollectorHealth()` 한 곳으로 모아 육안 검토가 가능하게 하는 선에서 그쳤다.

### [2026-08-03 11:40] 미결 4 **해소** — 미커밋 문서 3건 처리 (docs, 버전 변경 없음)

#### 결정과 처리

PUBLIC 저장소 노출 관점으로 3건을 전수 분석한 뒤 **(A)안**으로 처리했다.

| 문서 | 처리 |
|---|---|
| `doc/CROSS-PROJECT-PROPAGATION.md` | 공개 안전하게 고쳐 쓰고 **커밋** |
| `doc/PLAN-2026-07-28-REV1-decisions-applied.md` | 정본을 비공개 저장소로 이관 + `.gitignore` |
| `doc/PLAN-2026-07-28-playwright-antibot.md` | 위와 같음 |

전파 대장만 커밋하는 이유는 성격이 다르기 때문이다 — 대장은 push 시점마다 갱신하는 **운용
문서**라 저장소에 있어야 절차가 성립하는 반면, 계획서 2건은 세션 인계용이라 비공개에서도
똑같이 기능한다. `doc/PLAN-*.md` 패턴으로 무시해 앞으로 만들 계획서도 기본적으로 걸리게 했다.

#### 대장에서 걷어낸 것

초판은 "상대경로+줄번호만 쓰면 안전하다"는 전제였는데 **그게 틀렸다.** 문제는 경로의
형태(절대/상대)가 아니라 **무엇을 가리키는가**다 — 상대경로라도 비공개 저장소의 모듈 배치·
클래스명·줄번호를 적으면 내부 지도이고, 거기에 "미확인" 결함을 붙이면 좌표가 딸린 약점 목록이
된다. 그래서 수신측을 가리키는 것과 파생 관찰(D-N)의 내용을 전부 수신측 사본으로 내리고,
공개본에는 대장 번호·판정·상태와 이쪽 코드 위치만 남겼다.

#### 재감사가 잡아낸 것 — **익명화 전제가 이미 깨져 있었다**

고쳐 쓴 문서를 세 관점(비공개 저장소 / 개인정보·호스트 / 법무·전략)으로 독립 재감사했다.
파일 자체는 깨끗했으나 **더 중요한 것이 걸렸다.**

`CHANGELOG.md:123` 과 `DEVLOG.md:225` — **오늘 v0.13.6 에서 내가 쓴 두 줄**이 대장 번호와
상대 저장소 이름을 한 문장에 묶고 있었다. 좌표를 비공개로 내려도 그 한 줄이 있으면 대장 전체가
역매핑된다. 두 줄을 정정하고, §2.2 에 **"공개 산출물에는 대장 번호만, 상대 이름 금지"** 규칙을
명문화했다. 규칙을 어긴 사례가 먼저 있었다는 사실도 함께 적었다.

같은 재감사가 **내가 새로 만든 노출**도 잡았다. "무엇을 지웠는지" 설명하는 §5.1 문단이 실계정
운용·전략 문서 존재·미수정 취약점 목록 보유를 한 문장으로 자인하고 있었다(같은 문구가
`.gitignore` 주석에도 복제돼 있었다). 일반화했다.

§1 의 라이선스 사유도 뺐다. 미결 1(v0.13.10)에서 **그 서술이 결합을 전제한 낡은 것**이라고
이미 결론이 났다 — 폐기 결정을 지탱하는 것은 재현 불가능한 빌드와 기여도 0 두 가지다. 빼는
쪽이 안전할 뿐 아니라 더 정확하다.

#### 남긴 것과 그 이유

`CHANGELOG.md:528` 과 `DEVLOG.md:1780` 에는 상대 저장소 이름이 남아 있다. 과거 작업의 이력
기록이고, 커밋 이력에는 어차피 원문이 남아 있어 현재 트리만 고쳐도 되돌려지지 않는다.
**이 익명화는 앞으로만 유효하다** — 그 한계를 §5.1 에 명시했다. 공개 이력 재작성은 이미
배포된 저장소를 깨뜨리는 조치라 자동으로 하지 않는다.

#### 메모

- 코드 변경 없음. 테스트 250건 유지, DB 무접촉.
- 계획서 사본은 이관 후 원본과 md5 대조로 확인했다. 이관 대상 저장소에서는 커밋하지 않았다.

### [2026-08-02 17:45] v0.13.10 미결 1 **해소** — jlink 유지 + 라이선스 고지 보강

#### 결정: jlink 오프라인 ZIP 배포를 유지한다

계획서의 "AGPL 대응 소스 의무와 직결" 이라는 프레이밍은 **낡은 것이었다.** 그 표현은 *비공개 저장소의 코드가 PUBLIC 릴리스 ZIP 에 들어가는* 상황을 전제한 것인데, 외부 패키지 import 방침이 폐기되면서 그 경로 자체가 사라졌다.

조사 결과 **jlink 는 라이선스 충돌을 만들지 않는다.** OpenJDK 는 GPLv2 + Classpath Exception 이고 CE 가 링크를 명시적으로 허용한다. ZIP 한 개에 동봉하는 것은 AGPL 제5조의 aggregate 이지 결합 저작물이 아니다. 그리고 `jre/legal/` 은 jlink 가 이미 복사해 넣고 있었다.

**폐지의 진짜 논거는 크기(ZIP 103 MB 중 JRE 52 MB)와 유지보수이지 법무가 아니다.** `download-jre.ps1` 이 이미 Adoptium 폴백을 갖고 있어 jlink 를 빼도 잃는 것은 오프라인 설치 능력 하나뿐이다. 그 판단은 사용자가 "유지 + 보강"으로 내렸다.

#### 정작 위반이던 것 — jlink 와 무관했다

**배포 ZIP 루트에 `LICENSE` 도 `NOTICE` 도 없었다.** `packageDist` 의 include 목록에서 빠져 있었다. AGPL 제4조는 사본과 함께 라이선스 전문을 전달할 것을 요구하고, 정작 이 저장소의 `NOTICE` 51행이 "배포 시에는 본 프로젝트의 LICENSE 와 함께 이 NOTICE 를 포함해 주십시오"라고 적어 두고 있었다. **여러 버전이 그 상태로 나갔다.**

#### 넣은 것

| 파일 | 근거 |
|---|---|
| `LICENSE` | AGPL 제4조 — 사본과 함께 라이선스 전문 전달 |
| `NOTICE` | 제3자 구성요소 고지 |
| `CORRESPONDING-SOURCE.txt` (생성) | AGPL 제6조 — 대응 소스를 찾을 수 있는 명확한 안내 |

안내문은 세 구성요소를 모두 다룬다 — 앱 본체(저장소·릴리스 태그 + **제13조** 네트워크 상호작용 안내), 번들 JRE(Temurin 소스 취득처 + `jre/legal` 위치), 제3자 라이브러리(`BOOT-INF/lib` + `NOTICE`).

#### 버전을 두 번 적지 않는다

안내문의 앱 버전과 JRE 버전은 빌드 시점에 채운다. 3-8(WinSW XML)과 같은 규칙이되, 방식이 다르다 — **`ReplaceTokens` 를 쓸 수 없었다.** 그 필터는 토큰 맵을 **설정 시점**에 굳히는데, JRE 버전은 `createJre` 가 실행돼야 생기는 `build/jre/release` 안에 있다. 그래서 `generateSourceNotice` 태스크를 따로 두고 `doLast` 에서 읽어 치환한다.

**CI 는 `setup-java` 가 주는 JDK 로 빌드하므로 로컬과 JRE 버전이 다를 수 있다.** 숫자를 손으로 적으면 반드시 어긋나고, 그건 소스 취득처를 잘못 알리는 것이 된다. 읽기에 실패하면 빌드를 실패시킨다.

#### 실물 검증

`packageDist` 산출 ZIP 을 풀어 확인했다 — 루트에 세 파일이 있고, `CORRESPONDING-SOURCE.txt` 의 버전이 `app=0.13.10` / `jre=21.0.11` 로 치환돼 있다. 첫 시도에서는 토큰 설명 문장 자체가 치환돼 "0.13.9 / 21.0.11 토큰만 있습니다" 라는 엉뚱한 줄이 나왔고, 그 문장에서 토큰을 빼는 것으로 고쳤다.

#### 손대지 않은 것

AGPL 제13조는 **수정본을 네트워크로 운영하는 쪽**에 걸리는 의무다. 수정 없이 배포되는 이 산출물은 저장소 주소를 알리는 것으로 충분하므로 UI 푸터에 소스 링크를 넣는 작업은 하지 않았다. 안내문에 그 구분을 적어 뒀다.

#### 메모

- 테스트 244 → **250건**(실패 0). DB 무접촉.

### [2026-08-02 16:55] v0.13.9 B-3 **해소** — 신 템플릿 영수증 수집 복구 (사용자 승인)

#### 작업 개요

A/B/C 폴백을 구현하고 실계정 수집으로 값까지 확인했다. **17건 수집 → 17건 저장. 유실 0건.** 판단 대기 B-3 은 이것으로 해소한다.

#### 경로 C 를 저장에서 뺀 이유 — 설계를 바꾼 발견

구현 직전에 저장 경로를 확인한 것이 결정적이었다. 중복 판정이 `serial_num` + `date_time` **조합**이다(`JbgOrderDataAccessObject.getOrder:378-385`). 그래서 본문 날짜만으로 `serial` 없이 저장하면 **같은 날 산 영수증이 한 건으로 뭉개진다** — 첫 건만 남고 나머지는 "기존 주문"으로 스킵된다. 저장 이력에 같은 날짜 3건이 실재하므로 가상의 위험이 아니었다.

같은 이유로 **경로 B 도 꼬리(매장·POS·거래번호)가 하나도 없으면 만들지 않는다.** 날짜만으로는 구분이 안 된다. 틀린 키로 저장해 데이터를 잃는 것보다 버리는 편이 낫다. 이 규칙을 단위테스트로 고정했다.

경로 C 는 진단으로만 남긴다 — "날짜는 있는데 식별자가 없다"와 "아무것도 없다"를 구분하기 위해서다.

#### 실측 결과 — 경로 B 가 답이었다

```
구 템플릿 바코드 부재 — B(필드 조합) 로 복구했다. serial 자릿수=20
```

미인식 2건 모두 경로 B(`biz_date`+`pos_str_code`+`pos_no`+`tran_no`)로 복구됐다. **A2 는 실패했다** — `#barcode-no`·`#ern-barcode` 에는 바코드 형식(앞 8자리 날짜)을 만족하는 값이 없었다. 이름만 보고 A2 를 우선순위에 둔 것은 맞았지만, 실제로 일한 것은 B 다.

| 검증 항목 | 결과 |
|---|---|
| `jbg_order` | 15 → **17건** |
| `jbg_item` | 117 → **127건** |
| 스킵된 주문 | **0개** (이전 2개) |
| 기존 주문 중복 판정 | 15개 — 재수집이 중복을 만들지 않는다 |
| `(serial_num, date_time)` 중복 그룹 | **0건** |
| serial 비어 있는 주문 | **0건** |
| 앞 8자리 ≠ `date_time` | **0건** |
| 아이템 0개인 주문 | **0건** |

serial 자릿수는 구 템플릿 26자리 / 신 템플릿 조립본 20자리로 다르지만, 둘 다 앞 8자리가 `YYYYMMDD` 라는 규약을 지킨다.

#### 고친 것 — 첫 회차 로그의 무의미한 비교

첫 실행 로그에 `A2·B 일치=true` 가 찍혔는데 **이건 아무것도 알려주지 않는 값이었다.** 경로 B 를 채택하면 채택값과 B 가 같은 것은 당연하다. 물어야 할 것은 "A2 와 B 가 서로 같은가"이고, 그게 다르면 채택 우선순위가 틀렸을 때 재수집 시 serial 이 흔들려 중복 주문이 생긴다. `describeRecoveryAgreement` 로 분리하고 테스트를 붙였다.

#### 남은 것

- **배포본 DB 에는 아직 15건이 들어 있다.** 복구는 프로브 사본에서 검증했고 사본은 삭제했다. 배포본에서 수집을 한 번 돌리면 그때 17건이 된다 — 중복 판정이 검증됐으므로 안전하다.
- A2 가 왜 실패했는지(요소가 비었는지, 형식이 다른지)는 확인하지 않았다. B 가 동작하므로 급하지 않다. 실패 시 진단(`복구 경로 판독`)이 이제 그 답을 남긴다.

#### 메모

- 테스트 230 → **244건**(실패 0). 배포본·개발 트리 DB md5 둘 다 불변.

### [2026-08-02 03:45] v0.13.8 B-3 후속 2차 — **해결 경로 확정** (사용자 승인)

#### 작업 개요

1차 프로브가 "`#barcodeTargetRec` 이 없다"까지 확정했으므로, **그 자리에 무엇이 있는지**와 **본문에서 날짜를 얻을 수 있는지**를 한 번의 수집으로 함께 떴다. 실계정 1회 수집이 발생하므로 승인을 받고 착수했다.

#### 실측 결과 — 미인식 영수증은 **다른 템플릿**이다

두 건의 팝업 구조가 동일했다.

```
태그 137개 [div=48, a=22, script=18, span=13, input=8, li=6, button=4, ...]
id 21종 [barcode-no, biz_date, cmd_frm, eapp-common-container, emoneyIcon,
         ern-barcode, ern-container, ern-header, ern-skip-navi, ern-wrap, ff,
         fridgeCnt, gift_issue_gbn, menu-dim, momkidsIcon, pos_no,
         pos_str_code, quick-refund, recConfirmButton, tot_sale_amt, tran_no]
```

`ern-` 접두 id 가 한 벌 있다(`ern-wrap` / `ern-container` / `ern-header` / `ern-barcode` / `ern-skip-navi`). **구 템플릿의 `#barcodeTargetRec` 을 쓰지 않는 새 영수증 화면이다.** 이마트가 영수증 페이지를 개편했고, 같은 계정의 같은 회차 안에서 구·신 템플릿이 섞여 내려온다.

#### 해결 경로가 둘 확인됐다 — 이제 진단이 아니라 수정 대상이다

**경로 A — 신 템플릿의 바코드 요소.** `#barcode-no` 와 `#ern-barcode` 가 있다. 셀렉터를 구·신 양쪽으로 두면 된다.

**경로 B — 필드가 개별 id 로 분해돼 있다.** `biz_date`(영업일자) · `pos_str_code`(매장코드) · `pos_no`(POS 번호) · `tran_no`(거래번호). **이것이 바로 영수증 바코드의 구성요소다** — 기존 바코드가 "앞 8자리 구매일자 + 매장·POS 번호" 형태였던 것과 일치한다. 바코드 문자열을 못 읽어도 이 넷으로 `serial` 을 재구성하고 `biz_date` 에서 `datetime` 을 얻을 수 있다.

**본문 경로도 열려 있다.** 날짜 패턴 판정 결과 `YYYY-MM-DD=1(줄10)`, `HH:MM=1(줄10)` — 본문 10번째 줄에 `YYYY-MM-DD HH:MM` 형태의 거래일시가 **정확히 한 건** 있다. 매치가 1건이므로 라벨을 보지 않아도 모호함이 없다. 두 영수증의 본문 줄 수는 각각 50줄·43줄로 달랐지만 날짜 위치는 둘 다 10줄째로 같았다.

#### 다음 (수정 착수 시)

우선순위는 **경로 A → 경로 B 폴백**이다. `#barcode-no`/`#ern-barcode` 를 먼저 보고, 비어 있으면 `biz_date`+`pos_str_code`+`pos_no`+`tran_no` 로 조립하고, 그것도 없으면 본문 10줄째 날짜로 `datetime` 만이라도 살린다. 세 단계 모두 실패해야 지금처럼 버린다.

다만 **각 요소에 실제로 무엇이 들어 있는지는 아직 확인하지 못했다** — 프로브가 구조만 뜨고 값은 담지 않기 때문이다. 수정 착수 시 값 형식 확인을 위해 실계정 수집이 한 번 더 필요하다.

#### 메모

- 테스트 221 → **230건**(실패 0). 배포본·개발 트리 DB md5 둘 다 불변.
- 1차와 같은 방식으로 배포본 DB 사본에만 주기를 넣어 기동 수집을 태웠고, 수집 후 사본을 삭제하고 원본 DB 로 재기동했다.
- 프로브는 값을 담지 않는다 — id 의 숫자는 `N` 으로 가리고(주문번호가 섞일 수 있다), 본문은 날짜 **형식과 위치**만 보고한다. 본문 줄을 그대로 싣지 않는 이유는 숫자를 가려도 같은 줄의 상품명이 남기 때문이다.
- 수집 도중 배포본 로그가 회전됐다(`jangbogo.log` 가 03:33 부터 시작). 이전 회차 기록은 회전된 파일에 있다.

### [2026-08-01 22:30] v0.13.7 B-3 후속 — 바코드 미인식 영수증 구조 프로브 (사용자 승인)

#### 작업 개요

미인식 2건의 원인을 "canvas/svg/이미지 렌더링"과 "그 영수증에 바코드가 없음"으로 가르기 위한 진단. **실계정 1회 수집이 발생하므로 사용자 승인을 받고 착수했다.**

#### 왜 DOM 덤프가 아니라 구조 프로브인가

계획서의 다음 단계는 "그 팝업의 실제 DOM 을 뜨는 것"이었으나, 영수증 DOM 을 통째로 덤프하면 상품명·금액·구매일시가 로그에 남는다. 이 저장소는 PUBLIC 이고 배포본 로그는 사용자 PC 에 쌓인다.

답해야 할 질문은 좁다 — **"바코드가 그려지는가, 아니면 없는가"**. 그 답에는 태그 이름과 개수면 충분하다. 그래서 텍스트는 내용이 아니라 길이만 센다. 이걸로 못 가르면 그때 전체 덤프로 승격한다.

#### 무엇을 어떻게 읽는가

바코드를 못 읽었을 때만, `#barcodeTargetRec` 을 두 시점(읽은 직후 / +1.5초)에 떠서 비교한다. 두 시점을 보는 이유는 세 번째 가설 때문이다 — 기존 `delayTime(2000)` 이 짧아 아직 안 그려졌을 가능성. 두 요약이 다르면 렌더링이 늦은 것이다.

판독법:

| 관측 | 결론 |
|---|---|
| `canvas` / `svg` / `img` 가 잡힘 | 바코드는 **그려진다**. 텍스트 추출로는 영원히 못 읽는다 |
| 자손 0개 + 텍스트 0자 | 그 영수증에 바코드가 **없다** |
| `#barcodeTargetRec 없음` | 셀렉터나 팝업 전환이 어긋났다 |
| 두 시점 요약이 다름 | 렌더링이 늦다 — 대기를 늘리면 된다 |

Selenium 읽기(`probeBarcodeContainer`)와 판정 조립(`describeBarcodeContainer` / `tagCensus`)을 분리해 후자에 단위테스트를 붙였다. 프로브는 어떤 예외도 밖으로 내보내지 않는다 — 진단이 수집을 깨뜨리면 원래 결함보다 나쁘다.

#### 실측 결과 — **원인 확정. 세 가설이 전부 틀렸다**

v0.13.7 배포본으로 seq=1 을 1회 수집했다. 17건 수집 / 15건 저장으로 이전과 동일하게 재현됐고, 미인식 2건의 프로브 출력이 **두 시점 모두 동일**했다.

```
후보 div=0개, 구조=[읽은 시점] #barcodeTargetRec 없음 / [+1.5s] #barcodeTargetRec 없음
```

| 가설 | 판정 |
|---|---|
| canvas/svg/이미지로 렌더링된다 | **아니다.** 컨테이너 자체가 없으므로 자손을 논할 수 없다 |
| 그 영수증에만 바코드가 없다(컨테이너는 빈 채로 있다) | **아니다.** 빈 컨테이너가 아니라 **요소 부재**다 |
| 대기 2초가 짧아 아직 안 그려졌다 | **아니다.** +1.5초 뒤에도 없다 |

**확정된 것: 그 두 영수증 팝업에는 `#barcodeTargetRec` 이라는 ID 가 존재하지 않는다.** 같은 회차의 나머지 15건은 같은 셀렉터로 정상 인식됐으므로 셀렉터 오타나 팝업 전환 실패가 아니다. **같은 사이트가 영수증에 따라 다른 DOM 을 내려주고 있다.**

팝업이 안 열린 것도 아니다 — 바로 뒤에서 `<pre>` 를 찾아 `parseReceipt()` 가 성공하고 그 결과가 `resJsonArr` 에 들어간다. **본문은 있는데 바코드 컨테이너만 없다.**

#### 기각된 곁가지 가설 — "모바일 영수증 유효기간 30일"

저장된 15건의 `date_time` 분포를 확인했다. **가장 오래된 것은 수집일 기준 50일이 넘는데 바코드가 정상 인식됐다.** 유효기간 만료로 바코드 섹션이 빠진다는 설명은 성립하지 않는다.

다만 저장분의 최신 건과 수집일 사이에 **일주일 넘는 공백**이 있다. 유실 2건이 그 구간의 최근 영수증이라면 이마트가 영수증 템플릿을 개편했다는 뜻이고, 그렇다면 앞으로 수집분이 계속 유실된다. **추정이며 미확인이다** — 유실 건은 날짜 자체를 바코드에서 얻으므로 현재 코드로는 날짜를 알 수 없다.

(구체적 구매일자는 개인정보이므로 이 저장소에 적지 않는다. 확인 방법만 남긴다 — 배포본 DB 에서 `SELECT MIN(date_time), MAX(date_time) FROM jbg_order`.)

#### 다음 단계 (실계정 재접근이 필요하므로 승인 대상)

1. **팝업의 id/구조 census** — `#barcodeTargetRec` 대신 무엇이 있는지. body 직계 자식 태그와 id 속성명만 모으면 개인정보 없이 대체 컨테이너를 찾을 수 있다.
2. **본문에서 날짜 추출 가능성** — `<pre>` 본문에 구매일시가 텍스트로 있다면 `serial`·`datetime` 을 바코드에 의존하지 않아도 된다. 그러면 이 결함은 진단이 아니라 **해결**된다. 현재 픽스처에는 날짜 라인이 없어 로컬로는 확인 불가.

#### 메모

- 테스트 210 → **221건**(실패 0). 개발 트리 기준선 DB md5 불변.
- 재빌드는 `clean` 없이 `bootJar createJre packageDist` 만 돌렸고, 그 전에 배포본 `db/*.db` 를 스크래치패드로 백업했다(md5 `ab1cdae1804277e7343412d7d20948f1`).
- **수집 트리거에 관리자 로그인이 필요한데 비밀번호 입력은 할 수 없다.** 대신 배포본 DB 를 스크래치패드로 복사해 사본에만 `collect_interval_minutes=720` 을 넣고, `-Djangbogo.localdb.url` 로 그 사본을 가리켜 기동 수집을 태웠다. **원본 배포본 DB 는 md5 불변**이고 사용자 설정(주기 0)도 그대로다. 수집 후 프로브 사본은 삭제했다.
- 프로브 실행이 끝난 뒤 앱을 원본 DB 로 재기동해 원래 상태(8282 대기, 수집 미실행)로 되돌렸다.

### [2026-08-01 18:45] v0.13.6 앱 기동 없이 되는 잔여 정리 — Edge 타임아웃·clean_build.bat 드리프트

#### 작업 개요

Phase 3 종결 후 "승인 불필요"로 분류된 잔여 정리 2건. 앱 기동·실계정·기준선 DB 무접촉. 테스트 206 → **210건**(실패 0), 기준선 DB md5 불변.

#### Edge 분기 페이지 로드 타임아웃 (`WebDriverManager`)

B-2 수정(v0.13.4)이 Chrome 분기만 60초로 낮췄고, Edge 분기는 `new EdgeDriver()` 로 옵션 없이 기동되어 Selenium 기본값 300초가 남아 있었다. `buildEdgeOptions()` 를 분리해 Chrome 과 같은 상수·같은 재정의 프로퍼티를 공유한다. headless·user-agent 는 넣지 않았다 — Edge 분기는 그 두 설정을 읽어 온 적이 없는 예비 경로다.

#### clean_build.bat 버전 드리프트

52·55·56행이 `jangbogo-0.5.0.jar` 를 참조했다(8개 버전 방치). 3-8(WinSW XML)의 토큰 치환은 `packageDist` 가 파일을 **복사하는 시점**에 필터가 도는 방식이라, 저장소에서 직접 실행되는 이 파일에는 성립하지 않는다. `install.bat` 이 이미 쓰는 실행 시점 와일드카드 탐지로 통일했다. plain JAR 은 `build.gradle` 에서 꺼져 있어 bootJar 산출물 하나만 잡힌다.

#### 메모

- Edge 타임아웃 부재는 전파 대장 후보(드라이버 옵션 조립·전달) — 대장 P3-1 등재. 수신측 확인 여부는 대장에서 추적한다.
- `index.html` 의 `loadCollectorHealth` 실패 분기(박스 미정리)는 이번에 손대지 않았다 — 1회 호출이라 무해하고, JS 테스트 장치가 없어 단위테스트 의무를 충족할 수 없으며, 주기 갱신을 붙일 때 함께 고치는 계획("남은 후속 1건")이 이미 있다.

### [2026-08-01 15:20] v0.13.5 실전 수집 검증 — B-2·B-3 수정 효과 확인

#### 작업 개요

배포본을 다시 만들어 seq=1(SSG+Emart)만 수집했다. **코드 변경 없음, 검증 기록이다.** 이번 세션의 세 수정이 전부 실사이트에서 효과를 냈다.

Oasis·Hanaro 는 제외했다 — 이번 수정은 전부 Emart 경로라 불필요한 실계정 로그인이다.

#### B-2 타임아웃 — 6분이 사라졌다

| | v0.13.2 (수정 전) | v0.13.5 (수정 후) |
|---|---|---|
| Emart 소요 | 04:10:57 → 04:16:54 (**5분 57초**) | 15:17:10 → 15:19:37 (**2분 27초**) |
| 결과 | `FAIL` TimeoutException | **`SUCCESS` 17건** |

**이번엔 트레이더스가 응답했다.** 저장 결과에 트레이더스 지점 주문 2건이 들어 있다. 즉 지난번 무응답은 **일시적**이었다 — 계정에 트레이더스 이력이 없어서가 아니었다. 60초 타임아웃은 이번에 발동할 일이 없었지만, 발동해야 할 때 1분에 끊긴다는 보장은 남았다.

#### B-2 격리 — 이게 실질적 성과다

이마트 **13건** + 트레이더스 **2건** = 15건이 저장됐다. 수정 전 구조였다면 T 가 죽는 순간 **이마트 13건이 통째로 버려졌을** 자리다. 지난 실행에서 실제로 그 손실이 있었는지는 확정할 수 없지만, 구조상 있었다면 그랬다.

#### B-3 바코드 — 미인식이 재현됐고 원인이 확정됐다

```
영수증 바코드를 읽지 못했다 — serial·datetime 없이 수집되어 저장 단계에서 버려진다.
mall=이마트 <지점명>, 후보 div=0개     ← 2건
```

**수집 17건 → 저장 15건.** 정확히 2건이 미인식이고, 계획서의 "17건 중 2건(12%)"과 완전히 일치한다. 같은 현상이 재현된 것이다.

원인도 좁혀졌다 — **`후보 div=0개`**. `#barcodeTargetRec` 안에 div 가 하나도 없다. 내가 나열한 세 결함 중 **1번(조용한 유실)** 이 실제 원인이었다. 2번(크래시)·3번(위치 가정)은 아직 발현되지 않은 잠재 결함이다.

수정 전에는 `size() > 1` 조건에서 걸러져 **아무 기록도 남지 않았다.** 이제 어느 점포의 영수증인지까지 로그에 나온다.

#### v0.13.3 오류 메시지 요약도 확인

`jbg_collect_log.error_message` 길이가 **0·5·10자**다. 지난 실행에서 1,293자였던 그 칸이다.

#### 브레이커·하트비트 재확인

| 수집기 | 연속실패 | 마지막 성공 | 마지막 데이터 |
|---|---|---|---|
| SSG | 0 | 기록됨 | **없음** (EMPTY) |
| Emart | 0 | 기록됨 | 기록됨 (SUCCESS) |

EMPTY 가 `last_nonempty_time` 을 밀지 않는 규칙이 또 확인됐다.

#### 정정 — Claude-in-Chrome 은 loopback 에 닿을 수 있다

이 세션에서 다섯 번 시도하고 "자동화 탭의 사설망 차단, 뚫을 수 없는 환경 제약"이라고 결론지었는데 **틀렸다.**

`switch_browser` 를 쓰면 확장이 설치된 모든 Chrome 에 연결 요청을 보내고, 사용자가 Chrome 안에서 직접 'Connect' 를 누른다. 그렇게 연결한 브라우저에서는 `127.0.0.1:8282` 가 정상 로드되고 스크린샷·클릭까지 전부 됐다. 애초에 **페어링이 안 된 상태**였던 것이지 정책 차단이 아니었다.

`list_connected_browsers` 가 브라우저를 하나 보고했기 때문에 "연결돼 있다"고 판단한 것이 오류였다. 목록에 뜨는 것과 자동화가 페어링된 것은 다르다. **다음에 같은 증상(외부 사이트는 되는데 localhost 만 error page)을 보면 `switch_browser` 부터 시도할 것.**

#### 실수 — clean 으로 배포본 DB 를 지웠다

재빌드할 때 `clean bootJar createJre packageDist` 를 그대로 돌렸고, `clean` 이 `build/` 를 통째로 지우면서 **배포본 폴더의 DB 가 함께 삭제**됐다. 사용자가 직접 연결한 쇼핑몰 계정 3개와 Oasis 수집분 6건이 사라졌다.

배포본 폴더가 `build/` 아래라는 걸 알고 있었으면서 DB 를 먼저 복사하지 않았다. **재빌드 전에 `db/jangbogo-dev.db` 를 백업하고, `clean` 없이 `bootJar packageDist` 만 돌릴 것.** 이후 실행에서는 백업을 먼저 떴다.

기준선 DB 는 무사하다 — 배포본은 별도 폴더다.

#### 남은 것 — B-3 후속

**미인식 2건은 여전히 버려진다.** 이번 수정은 "왜 버려졌는지 보이게" 했을 뿐 건지지는 못한다. `후보 div=0개` 라는 사실이 나왔으니 다음은 그 영수증 팝업의 **실제 DOM 을 덤프**해 바코드가 어떤 구조로 렌더링되는지(canvas/svg/이미지인지, 아니면 그 영수증만 바코드가 없는지) 보는 것이다. 실계정 접근이 한 번 더 필요하다.

---

### [2026-08-01 05:50] 판단 대기 B-3 — 영수증 바코드 미인식 (v0.13.5)

#### 작업 개요

계획서는 "영수증 바코드 미인식 2건(17건 중 12%). 원인 지점은 `divElements.size() > 1` 로 좁혀져 있음"이라고 적었다. 그 지점이 맞았고, **결함이 하나가 아니라 셋이 겹쳐 있었다.**

#### 문제의 코드

```java
List<WebElement> divElements = driver.findElements(By.cssSelector("#barcodeTargetRec div"));
if (divElements.size() > 1) {
  receiptBarcode = divElements.get(divElements.size() - 1).getText();  // 마지막 것을 무조건
}
...
if (receiptBarcode != null) {
  detailJo.put("serial", receiptBarcode);
  detailJo.put("datetime", receiptBarcode.substring(0, 8));
}
```

**1. 조용한 유실.** div 가 1개 이하면 바코드가 `null` 이 된다. 그 영수증은 `serial`·`datetime` 없이 `resJsonArr` 에 들어가고, 저장 단계에서 `MallOrderUpdaterRunner` 가 datetime 없는 주문을 `skippedOrders` 로 세고 건너뛴다. **어디에도 "바코드를 못 읽었다"는 기록이 남지 않는다.** 미인식 2건이 조용했던 이유다.

**2. 크래시 위험.** 마지막 div 의 텍스트가 비었거나 8자 미만이면 `substring(0, 8)` 이 `StringIndexOutOfBoundsException` 을 던진다. `""` 는 `null` 이 아니므로 가드를 통과한다. **영수증 하나 때문에 Emart 수집 전체가 무너진다.** 아직 안 터졌을 뿐 미인식보다 위험하다.

**3. 위치 가정.** "마지막 div" 는 렌더링 구조에 대한 가정이다. 바코드 뒤에 라벨이나 래퍼 div 가 하나 붙으면 **엉뚱한 값이 serial 로 저장된다.** 이건 미인식보다 나쁘다 — 틀린 데이터가 조용히 들어간다.

#### 고친 방식 — 용도를 판정 기준으로 삼는다

바코드가 무엇인지 따로 정의하지 않고, **호출측이 그 값으로 무엇을 하는지**를 기준으로 삼았다. 앞 8자리를 `YYYYMMDD` 로 쓰므로 그 용도가 성립하는지만 보면 된다.

- 공백 제거 후 8자 이상
- 앞 8자가 전부 숫자
- 그 8자가 실제 날짜 범위(월 01~12, 일 01~31)

뒤에서부터 훑되 이 조건을 통과한 첫 값을 쓴다. 위치 가정 대신 형태 검증이다.

날짜 범위까지 보는 이유는 휴대폰 번호(`01012345678`)나 금액 같은 긴 숫자가 바코드로 잘못 잡히는 것을 막기 위해서다. 실제로 그런 후보가 같은 컨테이너에 있을 수 있다.

**못 찾으면 추측하지 않고 `null` 을 돌려준다.** 틀린 serial 로 저장하는 것보다 낫다. 대신 그때 경고를 남겨 조용한 유실을 없앴다.

#### 왜 픽스처 테스트가 아니라 순수 함수인가

Phase 2-8 의 `EmartReceiptParserTest` 는 영수증 **본문 텍스트**를 픽스처로 검증한다. 바코드는 본문이 아니라 DOM 의 별도 컨테이너라 그 픽스처에 없다.

실사이트 HTML 을 캡처할 수 없는 것은 판단 대기 8 때와 같다. 그래서 추출 로직을 `List<String>` 을 받는 순수 함수로 떼어 냈다 — 셀렉터가 맞는지는 여전히 실사이트에서만 알 수 있지만, **후보 텍스트가 주어졌을 때 무엇을 고르는가**는 완전히 검증된다. 위 세 결함은 전부 그 판정 로직에 있었다.

#### 남은 것

**미인식 2건이 위 셋 중 어느 경우였는지는 확정할 수 없다.** 그때의 DOM 이 남아 있지 않다. 다만 세 경우 모두 이제는 로그에 후보 div 개수와 함께 남으므로, 다음에 재현되면 바로 구분된다.

#### 검증

자동 테스트 **206건** (실패 0). 신규 10건은 브라우저를 띄우지 않는다. 기준선 DB md5 `909dfe…073ac` 불변.

---

### [2026-08-01 05:20] 판단 대기 B-2 — Emart 6분 타임아웃 규명과 수정 (v0.13.4)

#### 규명 — 6분의 정체는 "설정한 적 없는 기본값"

실패 시점 capabilities 덤프에 답이 있었다.

```
timeouts: {implicit: 0, pageLoad: 300000, script: 30000}
```

`pageLoad: 300000ms` = **정확히 5분**, Selenium 기본값이다. `WebDriverManager` 는 타임아웃을 한 번도 설정하지 않는다.

타임라인이 딱 맞는다.

| 시각 | 사건 |
|---|---|
| 04:10:57 | Emart 수집 시작, ChromeDriver 기동 |
| 04:11:05 | `data-body = mypage` — 로그인 성공 |
| 04:11:08 | `logout();` 버튼 확인 — signin 종료 |
| ~04:11:53 | 이마트(E) 처리 후 트레이더스(T) 로 `get` |
| **04:16:53** | +300초 → `TimeoutException` |

실패 커맨드도 `get {url=...jornalGbn=T}` 였다. **6분 = 준비 45초 + 기본 타임아웃 300초.**

#### 스크린샷이 원인을 한 겹 더 좁혔다

v0.8.0 의 실패 스크린샷 기능이 여기서 값을 했다. 실패 순간 화면은 **이마트 탭 페이지를 완전히 렌더링한 채** 멈춰 있었다. 트레이더스 탭으로 넘어가지 못했다.

T 문서는 도착했는데 하위 리소스만 걸린 거라면 트레이더스 탭이 활성화된 화면이 보였을 것이다. 그렇지 않았으므로 **T 요청 자체가 응답을 받지 못했다** — 서버 무응답이거나 차단이다.

**다만 왜 무응답인지는 규명하지 못했다.** 트레이더스 이용 이력이 없어서인지, 차단인지, 일시적인지 구분하려면 재현이 필요하다. 아래 수정은 원인과 무관하게 피해를 줄이는 것이다.

#### 조사 중 더 큰 문제를 찾았다

`Emart.navigateReceipt` 의 구조가 이랬다.

```java
resJsonArr.addAll(this.inspectReceipts(...));   // 이마트(E) 수집 → 누적
driver.navigate().to("...jornalGbn=T");         // ← 여기서 예외
...
return resJsonArr;                              // ← 도달 못 함
```

**T 가 죽으면 이미 모아 둔 E 결과가 통째로 버려진다.** `resJsonArr` 이 반환되지 못하고 예외가 올라가 `collectFrom` 이 FAIL 로 처리하며 빈 배열을 돌려주기 때문이다.

`MallOrderUpdater` 는 SSG·Emart 사이에 부분 실패 격리를 이미 적용해 뒀다. **같은 원칙이 한 겹 아래에는 빠져 있었다.** 6분 지연보다 이쪽이 실질적으로 더 나쁘다 — 시간은 늦어질 뿐이지만 이건 데이터가 사라진다.

#### 고친 것 둘

**A. 페이지 로드 타임아웃 60초 명시.** `ChromeOptions.setPageLoadTimeout` 으로 옵션에 박으므로 기존 `buildChromeOptions` seam 으로 테스트된다. 시스템 프로퍼티로 올릴 수 있게 두되 화면에는 노출하지 않는다 — 안전장치다.

**B. E·T 격리.** 각각을 감싸 실패를 모으고, **둘 다 실패했을 때만** 첫 실패를 올린다. 한쪽이라도 완주하면 그 결과는 살린다.

A 만 했으면 6분이 1분으로 줄 뿐 E 결과가 버려지는 건 그대로였다.

#### 남긴 한계 — 숨기지 않고 적어 둔다

한쪽만 실패한 경우 그 사실은 **애플리케이션 로그에만** 남는다. `jbg_collect_log` 에는 수집기(Emart) 단위 한 행으로만 기록되므로, E 는 성공하고 T 만 죽는 상태가 계속돼도 수집기 수준에서는 SUCCESS 로 보인다.

E·T 를 따로 세려면 별도 수집기로 분리해야 하는데 둘은 로그인 세션을 공유한다 — 분리하면 로그인이 두 번 일어나고, 그건 이 계획이 줄이려는 바로 그 트래픽이다. 3-4 의 "조용한 실패를 만들지 않는다" 원칙에 비추면 아쉬운 지점이지만, 대안의 비용이 더 크다고 판단해 **로그 노출까지만** 한다.

#### Edge 경로는 손대지 않았다

`getWebDriver` 의 Edge 분기는 `new EdgeDriver()` 로 옵션 없이 만든다 — 타임아웃도 기본값 300초 그대로다. 운영 설정이 `default_web_driver: chrome` 이고 Edge 경로는 실사용 흔적이 없어 범위에 넣지 않았다. Edge 를 쓰게 되면 같은 처리가 필요하다.

#### 검증

자동 테스트 **196건** (실패 0). 신규 6건은 브라우저를 띄우지 않는다 — 옵션 객체 검사와 Mockito 로 세운 WebDriver 다. 기준선 DB md5 `909dfe…073ac` 불변.

---

### [2026-08-01 04:40] 실전 수집 검증 + 오류 메시지 오염 수정 (v0.13.3)

#### 작업 개요

사용자가 배포본에 쇼핑몰 3개를 연결하고 수집을 실행했다. **이번 세션에 만든 코드가 처음으로 실사이트를 탔다.** 전부 의도대로 동작했고, 그 과정에서 결함 하나를 찾아 고쳤다.

#### 실전 결과 — 세 가지 상태가 한 번씩 다 나왔다

`jbg_collect_log` 7행. 수집기별 행 + 실행 단위 집계 행 구조가 그대로 찍혔다.

| 몰 | collector | status | 주문/아이템 |
|---|---|---|---|
| 1 | SSG | `EMPTY` | 0 / 0 |
| 1 | Emart | `FAIL` (navigateReceipt) | 0 / 0 |
| 1 | *(집계)* | SUCCESS | 0 / 0 |
| 2 | Oasis | `SUCCESS` | 0 / 0 |
| 2 | *(집계)* | SUCCESS | **6 / 35** |
| 3 | Hanaro | `EMPTY` | 0 / 0 |
| 3 | *(집계)* | SUCCESS | 0 / 0 |

**SUCCESS·FAIL·EMPTY 가 전부 실전에서 한 번씩 나왔다.** 3-4 이전 구조였다면 SSG·Hanaro 의 0건이 SUCCESS 로 뭉뚱그려졌을 자리다.

#### 하트비트 — 설계의 핵심이 그대로 확인됐다

`jbg_collect_breaker` 4행이 자동 생성됐다.

| 수집기 | 연속실패 | 마지막 성공 | 마지막 **데이터** |
|---|---|---|---|
| SSG | 0 | 04:10 | **없음** |
| Emart | 1 | 없음 | 없음 |
| Oasis | 0 | 04:28 | **04:28** |
| Hanaro | 0 | 04:33 | **없음** |

3-4 에서 "이 한 줄이 설계의 핵심"이라고 적었던 것 — **EMPTY 는 `last_success_time` 만 갱신하고 `last_nonempty_time` 은 건드리지 않는다** — 이 SSG·Hanaro 행으로 실증됐다. 이 상태가 주기의 14배(720분 기준 7일) 이어지면 `NO_DATA` 경보가 뜬다.

브레이커는 트립하지 않았다. Emart 연속실패 1회, 임계 3회. 정상이다.

대시보드 요약도 의도대로 전체 2 / 성공 1 / 실패 1 이 나왔다 — 집계 행이 빠지고 수집기 단위로 세어졌으며 EMPTY 는 제외됐다.

**3-7 의 전제도 실환경에서 확인됐다.** 드라이버 capabilities 에 `unhandledPromptBehavior: dismiss and notify` 가 그대로 찍혀 있다. 그 기본값을 근거로 alert 처리 순서를 바꿨던 것이 맞았다.

#### 찾은 결함 — 요약 칸에 1,293자

Emart 실패행을 보다 발견했다.

```
브레이커 last_reason 길이 = 1293 자
jbg_collect_log.error_message 길이 = 1293 자
jbg_collect_log.error_detail  길이 = 15314 자
```

Selenium 예외의 `getMessage()` 가 `Build info` / `System info` / `Driver info` / `Command` / `Capabilities` / `Session ID` 를 통째로 달고 온다. 그 값이 **"사람이 읽을 사유" 칸과 화면에 표시되는 요약 칸**에 그대로 들어갔다.

3-7 에서 alert 문구에는 `oneLine()`(한 줄 접기 + 300자 컷)을 적용해 놓고 **이 경로에는 안 넣었다.** 같은 문제를 한쪽만 막아 둔 셈이다.

#### 고친 방식 — 잘라내기 전에 걷어낸다

글자 수로만 자르면 잘린 자리에 진단 블록 앞머리가 남는다. 그래서 순서를 뒀다.

1. Selenium 정형 문구(`Build info:` 등) **이후를 버린다**
2. 남은 줄바꿈·연속 공백을 한 칸으로 접는다
3. 300자에 맞춘다

`ErrorSummary` 로 빼서 네 곳에 적용했다 — `CollectStep.wrap`(모든 수집 실패의 발원지), `MallOrderUpdaterRunner` 2곳, `MallSchedulerService` 1곳. `CollectStep` 의 사설 `oneLine` 은 이걸로 대체했다.

전체 스택은 `error_detail` 이 그대로 갖는다. **진단 정보를 버리는 게 아니라 요약 칸을 요약답게 만드는 것**이다.

#### 관찰만 하고 조치하지 않은 것

- **Emart `navigateReceipt` TimeoutException** — 04:10:57 → 04:16:54, **약 6분** 매달렸다 실패했다. 판단 대기 B-2 와 같은 지점이다(거기선 `StaleElementReferenceException`). 실패 자체보다 **6분 점유**가 눈에 띈다. 실계정 재현이 필요해 별건으로 둔다
- **SSG·Hanaro 0건** — 설계상 한 회차로는 정상인지 셀렉터 고장인지 구분할 수 없다. 며칠 이어지면 `NO_DATA` 가 알려 준다. 그게 3-4 의 답이다
- Oasis 6건 35아이템 정상 — 파서 경로는 건재하다

#### 검증

자동 테스트 **190건** (실패 0). 신규 10건은 실전에서 관측된 형태의 Selenium 메시지를 그대로 입력으로 쓴다. 기준선 DB md5 `909dfe…073ac` 불변.

---

### [2026-08-01 03:50] 미검증 UI 2건 해소 — 배포본 실앱에서 확인

#### 작업 개요

3-5(수집기 건강도 배너)와 판단 대기 10(마지막 저장·전송 시각)의 화면을 실제 앱에서 확인했다. **코드 변경 없음.** 둘 다 의도대로 동작한다.

#### 왜 그동안 못 봤나 — 그리고 어떻게 뚫었나

앱 기동이 실계정 로그인을 유발한다는 이유로 미뤄 왔다. 그런데 **배포본은 사정이 다르다.**

`application.yml` 은 `startup.collect.enabled: true` 지만, `runInitialCollection` 은 `auto_collect=1` 이고 주기가 설정된 몰만 수집한다. `data.sql` 시드는 `auto_collect` 를 넣지 않아 스키마 기본값 **0** 이고 자격증명도 비어 있다.

⇒ **새 폴더에 푼 배포본은 계정이 하나도 연결돼 있지 않아 수집이 일어나지 않는다.** 로그가 그대로 말한다.

```
장보고 애플리케이션 시작 - 1회 수집 실행 중
1회 수집 대상 쇼핑몰 없음
```

배포본은 자기 폴더의 `db/jangbogo-dev.db` 를 쓰므로 기준선 DB 에도 닿지 않는다. **실계정 로그인 0회, 기준선 md5 불변.**

> **단, 조건이 있다.** 기존 DB 가 있는 폴더에 풀거나 개발 트리 DB 를 복사해 넣으면 `auto_collect=1`·720분 설정이 살아나 기동 즉시 실계정 로그인이 돈다. **"새 폴더"가 안전의 전제다.**

#### 브라우저 자동화가 막혀 역할을 나눴다

인앱 패널은 화면이 표시되지 않아 픽셀 합성이 안 됐고, Claude-in-Chrome 확장 탭은 **loopback 주소에 접근하지 못했다** — 4회 시도(탭 2개) 동안 서버 로그에 요청이 한 건도 안 들어왔다. 같은 탭에서 `https://example.com` 은 정상 로드되므로 연결·제어권 문제가 아니라 **자동화 탭의 사설망 차단**이다.

우회 수단이 없지는 않았지만(공개 DNS 로 127.0.0.1 을 가리키는 도메인 등) 안전장치를 우회하는 짓이라 하지 않았다.

**대신 역할을 나눴다** — 사용자가 화면을 보고, 이쪽은 DB 를 심고 서버 로그로 교차 확인했다. 결과적으로 빈틈이 없었다.

#### 검증 방법 — 예측한 뒤 대조했다

배포본 DB 에 수집기 4개를 서로 다른 상태로 심고, `CollectHealthPolicy` 를 손으로 적용해 **화면에 무엇이 떠야 하는지 먼저 예측**한 다음 실제 화면과 대조했다.

| 수집기 | 마지막 성공 | 마지막 데이터 | 예측 | 실제 화면 |
|---|---|---|---|---|
| Emart | 3h 전 | 3h 전 | OK → **숨김** | 표시 안 됨 ✓ |
| SSG | 50h 전 | 50h 전 | TRIPPED | `자동 차단됨` ✓ |
| Oasis | 40h 전 | 40h 전 | STALE | `수집 지연` + "마지막 성공 이후 40시간 경과 (주기 720분)" ✓ |
| Hanaro | 1h 전 | **300h 전** | NO_DATA | `0건 지속` + "300시간째 0건 — 셀렉터 확인 필요" ✓ |

4개 중 3개만 뜨고 정상인 Emart 가 숨겨지는 것이 설계 의도다. 문구·주기·경과 시간까지 정책 계산과 일치했다.

마지막 저장·전송 시각도 시드값(5h 전 / 4h 전)과 정확히 맞았다 — `마지막 저장: 2026-07-31 22:30` / `마지막 전송: 23:30`.

서버 로그로 API 호출도 확인했다. 페이지 로드 한 번에 `/`, `/malls/getinfo`, `/api/collect-logs/summary`, `/api/collect-health` 넷이 모두 인증 성공으로 처리됐다.

#### 앞서 하네스로 확인해 둔 것

브라우저가 막혀 있는 동안 실제 템플릿을 띄워 두 함수를 직접 구동했고, 실앱 확인 전에 이미 통과해 있었다 — 값 있음/없음, `exportConfig` null 안전, OK 숨김, 4가지 배지·색상, API 실패·JSON 깨짐, 그리고 **XSS 이스케이프**(`<img src=x onerror=...>` 주입이 `&lt;img...&gt;` 로 무력화, img 태그 0개).

정적으로도 확인했다 — `#last_outbound`·`#collector_health` 실재, 두 함수 최상위 선언, 호출부가 `DOMContentLoaded` 안의 **비동기** XHR(`sendGet` → `open(..., true)`)이라 호출 시점에 이미 전역에 있다.

#### 덤 — SchemaMigrator 실전 검증

배포본이 아니라 앞선 사본 실행에서, 레거시 DB 를 상대로 마이그레이터가 처음 돌았다. **없는 것만 정확히** 채웠다.

```
컬럼 추가: jbg_export_config.last_ftp_upload_time
컬럼 추가: jbg_collect_log.collector
+ jbg_collect_breaker 테이블 생성
```

기존 컬럼(`auto_collect`·`qty`·FTP 6종)은 건드리지 않았다. 3-10 이 단위테스트뿐 아니라 실 DB 에서도 의도대로 동작한다.

#### 발견한 잠재 이슈 — 폴링 추가 시 고칠 것

`loadCollectorHealth` 는 API 실패 시 `return` 만 하고 기존 내용을 지우지 않는다. 지금은 `DOMContentLoaded` 에서 **1회만** 호출되므로 무해하다(초기 상태가 `display:none` 이라 실패해도 아무것도 안 뜬다 — 이것도 확인했다).

그러나 주기 갱신을 붙이면 **일시적 API 실패 때 이전 경보가 화면에 남는다.** 폴링을 추가할 때 실패 분기에서 박스를 비우도록 함께 고쳐야 한다.

#### 정리 상태

확인 후 심어 둔 데이터를 전부 지웠다 — 브레이커 0행, 주기 0, 내보내기 시각 0. 배포본은 계정 0개·주문 0건의 초기 상태이고, 기준선 DB md5 `909dfe…073ac` 는 이번에도 불변이다.

---

### [2026-07-31 16:00] 미결 3 종결 — session_profile_* 컬럼 ALTER 시점

#### 작업 개요

**코드 변경 없음.** 이미 결정된 사항을 한 곳에 모아 닫는 기록이다. Phase 5 착수 시 같은 질문을 다시 하지 않기 위해서다.

#### 왜 코드 작업이 없나

미결 3 은 "`jbg_mall` 의 `session_profile_*` 5개 컬럼 ALTER 시점 (SQLite 특성상 편도)"였다. 착수 전에 확인해 보니 구성요소 셋이 전부 이미 결정돼 있었다.

| 구성요소 | 상태 |
|---|---|
| 컬럼명 | **확정** — 새 질문 1 에서 `session_profile_enabled` / `_name` / `_status` / `_last_login` / `_owner` 채택 |
| "편도" 전제 | **틀렸음이 확인됨** (아래) |
| ALTER 시점 | **결정됨** — 차단 질문 해소 라운드의 결정 4: "3-10 통합만 먼저, 컬럼은 Phase 5" |

`grep session_profile` 결과 코드·스키마 어디에도 없다. 지금 컬럼을 넣는 것은 **결정 4를 뒤집는 일**이고, 그 결정 이후 새로 바뀐 사실이 없다(3-10 완료는 결정 당시 이미 전제였다). 사용자 확인을 거쳐 **해소로 닫는다.**

#### 전제 정정 — "SQLite 특성상 편도"는 낡았다

이 문구가 미결 3 을 오래 붙잡아 둔 원인이다. `ALTER TABLE ... DROP COLUMN` 은 **SQLite 3.35.0(2021)부터 지원**되고, `sqlite-jdbc 3.45.3.0` 이 싣고 있는 엔진은 **3.45.3** 이다.

임시 DB 로 실측했다 — plain nullable 컬럼을 ADD 한 뒤 DROP 했고 기존 행이 보존됐다. **인덱스·PK·UNIQUE 를 걸지 않는 한 되돌릴 수 있다.**

그래서 이 결정은 원래 생각했던 것만큼 무겁지 않다. 잘못 넣어도, 늦게 넣어도 복구된다.

#### Phase 5 착수 시 해야 할 일 — 5줄

3-10 통합 덕분에 남은 작업이 이것뿐이다.

```sql
-- schema.sql 의 jbg_mall 에 추가
session_profile_enabled INTEGER DEFAULT 0,
session_profile_name TEXT,
session_profile_status TEXT,
session_profile_last_login INTEGER DEFAULT 0,
session_profile_owner TEXT,
```

`SchemaMigrator` 가 `schema.sql` 을 읽어 기존 DB 에도 자동 반영한다. 자바 코드에 컬럼 목록을 따로 적을 필요가 없고, `addableByAlter()` 가 위 다섯을 전부 안전한 컬럼으로 판정한다(PK·UNIQUE·기본값 없는 NOT NULL 이 아니다).

DAO 접근자(`ensureSessionProfileColumns` 같은 것)는 **만들지 않는다.** 그 패턴이 3-10 에서 걷어낸 부채다.

#### 남은 미결

1(release.yml jlink AGPL)과 4(문서 커밋 / PUBLIC↔PRIVATE) 둘이다. 2와 3은 해소됐다.

---

### [2026-07-31 15:20] 판단 대기 10 — 내보내기·전송 시각 기록 (v0.13.2)

#### 작업 개요

계획서는 "`last_export_time` 이 FTP 전송 경로에서 **갱신되지 않는다** (11번 내보내고도 `none`)"로 적었다. 조사해 보니 갱신만 안 되는 게 아니었다.

#### 실제 상태 — 완전한 죽은 컬럼

`grep last_export_time` 결과가 두 줄이었다.

| 위치 | 내용 |
|---|---|
| `schema.sql:58` | 컬럼 선언 |
| `JbgExportConfigDAO:196` | 3-10 에서 내가 단 주석 |

**자바 코드 어디에서도 읽지도 쓰지도 않는다.** 설정 조회 SELECT 목록에도 빠져 있고, 화면에도 없다. "11번 내보내고도 none" 은 이상 현상이 아니라 **당연한 결과**였다. 아무도 쓰지 않는 컬럼이니까.

즉 이 건은 "FTP 경로만 누락"이 아니라 **처음부터 구현되지 않은 기능**이었다. 3-11(죽은 드라이버 경로 필드)과 같은 부류인데, 그쪽과 달리 **없애는 게 아니라 채우는 쪽이 맞다** — "내보내기가 마지막으로 언제 됐나"는 실제로 필요한 정보이고, 대체할 수단이 없다.

#### 왜 시각을 둘로 나눴나

`last_export_time` 하나로 로컬 저장과 FTP 전송을 함께 세려다 그만뒀다. 두 경로는 각각의 설정 플래그로 켜고 끌 수 있다.

| 설정 | 경로 |
|---|---|
| `auto_save_enabled` | 로컬 파일 저장 |
| `save_to_jiniebox` | FTP 전송 |

**한쪽만 켜 둔 구성이 정상이다.** 하나의 시각으로 뭉치면 "파일은 저장되는데 전송만 죽은" 상태를 구분할 수 없다 — 그게 바로 이 항목이 잡으려던 문제인데 정작 그걸 못 보게 된다. `last_ftp_upload_time` 을 따로 뒀다.

3-5 에서 `last_success_time` 과 `last_nonempty_time` 을 나눈 것과 같은 판단이다.

#### 기록 지점을 어디에 둘 것인가

로컬 저장은 호출 지점이 셋(`AdminController` 자동수집·수동 저장, `MallSchedulerService` 스케줄)이다. 셋에 각각 한 줄씩 넣는 대신 **파일을 실제로 쓴 `ExportService` 메서드 안**에 뒀다. 저장 경로가 늘어나도 기록이 빠지지 않는다.

FTP 전송은 서비스 밖(컨트롤러·스케줄러)에서 일어나므로 성공 직후 호출측이 부른다. 네 지점이다 — 자동수집, 수동 저장, 스케줄 수집, 그리고 **보류 큐 재전송**.

마지막 것을 빠뜨리기 쉬운데 넣었다. 재전송도 실제 도달이다. 빼면 밀린 것만 나가는 기간 동안 마지막 전송 시각이 멈춰 보인다.

FTP 전송 준비용 임시 파일(`exportToJinieboxFileBySeqList`, `createEmptyStatusFile`)은 로컬 저장으로 세지 않는다. 올린 뒤 지우는 파일이라 "사용자가 보관하는 내보내기"가 아니다.

#### 쓰기만 하면 다시 죽은 컬럼이 된다

값을 채워도 조회 SELECT 에 없으면 화면에 못 나오고, 결국 같은 상태로 돌아간다. 그래서 세 가지를 한 커밋에 묶었다.

1. 성공 시 기록
2. `getConfig()` SELECT 와 기본값 JSON 에 두 키 추가
3. 대시보드 '구매정보 저장 옵션' 카드에 표시

테스트가 2번(`configCarriesBothTimestamps`)을 못 박는다.

#### 검증과 그 한계

- 자동 테스트 **180건** (실패 0). `@TempDir` 의 새 SQLite 파일을 쓴다. 기준선 DB md5 `909dfe…073ac` 불변
- **화면은 런타임 검증을 못 했다.** 3-5 의 건강도 배너와 같은 이유다(앱 기동 = 실계정 로그인). `renderLastOutbound` 는 AJAX 응답 핸들러에서 호출되므로 선언이 뒤쪽 `<script>` 블록에 있어도 그 시점엔 전역에 올라와 있다 — 논리상 맞지만 **브라우저에서 확인한 것은 아니다.** 다음에 앱을 띄울 때 3-5 배너와 함께 볼 항목이다

---

### [2026-07-31 14:30] Phase 3-12 — seq→몰 매핑 단일 레지스트리 (v0.13.1)

#### 작업 개요

Phase 3 의 마지막 항목. `seq` 로 분기하는 하드코딩 사슬 세 개를 `MallRegistry` 하나로 모았다. **동작은 그대로다.**

#### 왜 이게 §9-2 답변을 기다려야 했나

계정 연결은 seq=1 자격증명을 `eapp.emart.com` 으로 보내고, 수집은 같은 값을 거기에 더해 `member.ssg.com` 으로도 보낸다. **두 경로가 서로 다른 로그인 폼에 같은 아이디·비밀번호를 제출한다.** 그 상태에서 매핑을 아무렇게나 통일하면 엉뚱한 사이트에 자격증명을 반복 제출하게 되는데, 그것이 바로 이 계획이 줄이려는 리스크다.

사용자 확인으로 **seq=1 자격증명은 SSG 통합회원 하나이고 두 사이트 모두에서 인증에 성공한다**는 것이 확정되어 통합이 가능해졌다.

#### 통합 전 상태

| 경로 | seq=1 → |
|---|---|
| `data.sql` 시드 | `id='ssg'` |
| `JangBoGoManager.getMallSession` | `new Emart` 하나만 |
| `MallOrderUpdater.collectItems` | `Ssg` + `Emart` 둘 |
| `ExportService.getMallIdFromSeq` | `"emart"` |

몰을 하나 추가하려면 네 곳을 손대야 했고, **한 곳을 빠뜨려도 컴파일은 통과한다.** 그게 이 부채의 성질이다.

#### 바꾸지 않기로 한 것 둘 — 그리고 그 이유

리팩터링에서 제일 위험한 것은 "이왕 손대는 김에"다. 두 가지를 의도적으로 그대로 뒀다.

**1. 내보내기 `mall_id` 를 `emart` 에서 `ssg` 로 통일하지 않았다.**

수신측 `getSeqById` 를 읽어 확인했다. 자기 `jbg_mall.id` 로 먼저 찾고, 못 찾으면 폴백 스위치를 타는데 그 폴백이 `emart` 와 `ssg` 를 **둘 다 seq 1 로** 받는다. 즉 어느 쪽을 보내도 결과는 같다.

그런데 1차 조회는 **수신측 자기 DB 의 시드**에 달려 있고, 그 시드는 저장소에 없다(스키마 덤프에 데이터가 빠져 있다). 확인할 수 없는 것을 리팩터링의 부수효과로 바꿀 이유가 없다. DEVLOG 부록 8 의 "현행 유지" 결정도 그대로 둔다.

대신 **두 값이 다르다는 사실을 레지스트리 한 곳에 적어 뒀다.** `mallId()` 는 jangbogo 의 `jbg_mall.id`, `exportId()` 는 jiniebox 페이로드의 `mall_id` 다. 흩어져 있던 것을 한곳에 모으는 것이 이 작업이지, 값을 바꾸는 것이 아니다.

**2. 계정 연결 검증 수집기를 바꾸지 않았다.**

seq=1 은 여전히 `Emart` 로 검증한다. §9-2 확인상 `SSG` 로 바꿔도 인증은 성공하지만, **검증 로그인이 향하는 사이트를 바꾸는 것은 리팩터링이 낼 변화가 아니다.**

검증을 수집기 하나로만 하는 것도 유지했다. 연결 한 번에 두 사이트로 로그인하면 이 프로젝트가 줄이려는 반복 로그인을 스스로 늘리게 된다. 테스트가 이 성질을 못 박는다.

#### coupang 을 넣지 않은 이유

`Coupang` 은 컴파일만 되는 껍데기이고 `data.sql`·DB·화면 어디에도 배선되어 있지 않다. 레지스트리에 넣으면 **"지원되는 몰"로 보이게 된다.** Phase 4B(법무 게이트) 통과 후에 추가한다. 테스트가 지금 넣는 것을 막는다.

#### 부산물 — 레지스트리와 시드의 정합성 감시

`mallIdsMatchTheDatabaseSeed` 가 레지스트리의 `mallId` 와 `data.sql` 시드를 대조한다. 둘이 갈라지면 계정 연결·수집·내보내기가 서로 다른 몰을 가리키게 되는데, 통합 전에는 그걸 잡을 방법이 없었다.

#### 작업 중 실수 1건

셸 작업 디렉터리가 앞선 조사(수신측 코드 확인) 때문에 다른 프로젝트에 머물러 있었고, 그 상태로 `./gradlew compileJava` 를 돌려 **엉뚱한 프로젝트가 빌드된 것을 성공으로 읽었다.** 그 직후 grep 결과에 이 저장소에 없는 경로가 섞여 나온 것으로 알아챘다. 절대경로로 `cd` 한 뒤 다시 돌리니 실제로는 import 누락 5건으로 실패하고 있었다. 고치고 재검증했다.

교훈은 상대경로 `./gradlew` 를 믿지 말라는 것이다. 앞으로 빌드·테스트는 `cd` 를 명시한다.

#### 검증

- 자동 테스트 **173건** (실패 0). 신규 10건은 수집기를 만들기만 하고 `getItems()` 는 부르지 않아 브라우저를 띄우지 않는다
- `grep "seqMallInt == \|new Ssg(\|new Emart(\|new Oasis(\|new Hanaro("` → `MallRegistry` 외 **0건**
- 기준선 DB md5 `909dfe…073ac` 불변. 앱 기동 없음

#### Phase 3 종결

12개 항목 전부 완료다.

---

### [2026-07-31 13:10] Phase 3-4 · 3-5 — 조용한 실패 드러내기 (v0.13.0)

#### 작업 개요

둘을 한 커밋으로 묶었다. **같은 문제의 두 얼굴**이기 때문이다 — 3-4 는 "0건을 성공으로 뭉뚱그리지 않는다", 3-5 는 "그 상태가 오래 이어지면 알린다". 앞의 것만 하면 기록만 늘고 아무도 안 보고, 뒤의 것만 하면 볼 데이터가 없다.

#### 3-4 의 절반은 이미 되어 있었다

계획서는 3-4 를 "성공 판정을 예외 없음 → **로그인 상태 확인** + **건수 sanity check**"로 적었다. 착수 전에 네 몰을 전수 확인했다.

| 몰 | 로그인 확인 |
|---|---|
| Ssg | `signedIn` 검사 후 `IllegalStateException` |
| Oasis | 동일 |
| Emart | 동일 |
| Hanaro | 동일 |

**네 몰 모두 이미 하고 있다**(Phase 1). 그러니 3-4 에 남아 있던 것은 건수 쪽뿐이다.

#### 남아 있던 구멍 — 예외 없이 0건

`collectFrom` 은 예외가 없으면 무조건 SUCCESS 로 기록했다. 그런데 예외 없이 0건이 나오는 경로가 실재한다.

`Ssg.onlinePurchaseList` 는 목록 `tr` 을 `findElements` 로 찾고 `size() > 0` 일 때만 루프를 돈다. 셀렉터가 어긋나면 예외가 아니라 **빈 목록**이 돌아오고, 루프가 통째로 건너뛰어지고, 0건이 조용히 반환된다. 로그인은 성공했으니 예외도 없다.

**v0.12.0 이후로는 이게 더 나빠졌다.** SUCCESS 기록이 브레이커의 연속 실패 카운트를 초기화하므로, 셀렉터가 깨지면 **브레이커가 영원히 트립하지 않는다.** 앞 작업이 만든 부작용이다.

#### 그런데 0건을 실패로 볼 수는 없다

0건은 두 가지를 뜻한다.

- **정상** — 조회 기간에 산 것이 없다
- **조용한 실패** — 셀렉터가 깨져 아무것도 못 읽었다

**한 회차만 봐서는 구분할 수 없다.** 그래서 추측하지 않기로 했다. `EMPTY` 로 구분해 기록해 두고, 그 상태가 **얼마나 오래 이어지는지**로 판단한다. 그것이 3-5 다.

`EMPTY` 는 실패가 아니므로 연속 실패 카운트는 초기화한다 — 로그인도 됐고 예외도 없었으니 사이트는 살아 있다. 대신 `last_nonempty_time` 을 **갱신하지 않는다.** 이 한 줄이 설계의 핵심이다. 0건이 그 시각을 밀어 버리면 조용한 실패를 알아챌 신호가 사라진다. 테스트로 못 박아 뒀다.

#### 3-5 — 두 가지 침묵을 구분한다

| 판정 | 뜻 | 임계 |
|---|---|---|
| `TRIPPED` | 브레이커가 열렸다 | — |
| `STALE` | **수집 자체가 안 돌고 있다** | 마지막 성공 > 주기 × 2 |
| `NO_DATA` | **돌긴 도는데 계속 빈손이다** | 마지막 실제 데이터 > 주기 × 14 (720분에서 7일) |
| `NEVER_RAN` | 아직 성공한 적이 없다 | — |

우선순위가 있다. 트립된 수집기는 당연히 STALE 해지므로 둘을 같이 보고하면 같은 사실이 두 번 세어진다.

**`NO_DATA` 로 브레이커를 트립시키지 않는 것은 의도다.** 두 달 장을 안 본 사용자를 장애로 처리하면 안 된다. 사람에게 알리기만 하고 판단은 사람이 한다.

#### 화면 — 정상일 때는 아무것도 그리지 않는다

'실행 결과 요약' 카드 안에 이상이 있는 수집기만 표시한다. 늘 떠 있는 배너는 읽히지 않게 되고, 정작 문제가 생겼을 때도 눈에 안 띈다.

#### 검증과 그 한계

- 자동 테스트 **163건** (실패 0). 정책이 순수 함수라 시각을 인자로 받아 **실제로 기다리지 않고** 일주일 뒤를 검증한다
- 상태 저장은 `@TempDir` 의 새 SQLite 파일. 기준선 DB md5 `909dfe…073ac` 불변
- **화면(JS·HTML)은 런타임 검증을 못 했다.** 앱 기동이 실계정 로그인을 유발해 이번 범위 밖이다. 서버 쪽(`/api/collect-health`)은 테스트로 덮여 있지만 브라우저에서 실제로 그려지는지는 확인하지 않았다. **다음에 앱을 띄울 때 눈으로 볼 항목이다**

#### 남은 것

판단 대기 10(`last_export_time` 이 FTP 전송 경로에서 갱신되지 않는다)은 같은 묶음이지만 이번에 손대지 않았다. 하트비트는 **수집** 쪽이고 그 건은 **내보내기** 쪽이라 데이터 경로가 다르다.

---

### [2026-07-31 11:20] Phase 3-3 · 3-9 — 수집기 서킷 브레이커와 백오프 (v0.12.0)

#### 작업 개요

둘을 한 커밋으로 묶었다. 백오프와 브레이커가 **같은 정책 객체·같은 관문**을 쓰기 때문이다. 나누면 정책 클래스를 인위적으로 쪼개게 된다.

#### 착수 전 확인에서 내 앞선 진술이 틀렸다

바로 앞 항목([2026-07-29 14:40])에서 "결과가 한 행으로 합쳐져 SSG 실패가 SUCCESS 로 기록된다"고 적었다. **틀렸다.** `recordPartialFailures`(Phase 1)가 이미 수집기별 FAIL 행을 남기고 있고, 기준선 `jbg_collect_log` seq=4 가 `Emart:navigateReceipt` FAIL 로 실재한다. 해당 항목에 정정을 달았다.

실제로 모자랐던 것은 둘이다.

1. **수집기 식별이 1급 필드가 아니다** — `step_name` 의 `"SSG:signin"` 접두사라 집계에 문자열 파싱이 필요하다.
2. **수집기별 성공이 기록되지 않는다** — 연속 실패 카운트를 **리셋할 근거가 없다.** "FAIL 행이 없으면 성공"이라는 부재 추론은 러너가 `recordPartialFailures` 전에 통째로 예외를 던지는 경로에서 깨진다.

그래서 이번 작업의 절반은 브레이커가 아니라 **셀 수 있는 기록을 만드는 일**이었다.

#### 왜 상태를 테이블에 두는가

이 결정이 설계에서 제일 중요하다. **트립은 재시작을 넘어 살아남아야 한다.** 메모리에만 두면 재기동이 차단된 수집기를 되살려 다시 두드린다 — 자동 차단의 목적이 그 순간 사라진다. 그래서 `jbg_collect_breaker` 를 만들었다. 복합 PK `(seq_mall, collector)` 에 upsert 한다.

부수 효과로 `last_success_time` 이 수집기별로 남는다. **3-5(하트비트)가 필요로 하는 바로 그 값이다.**

#### 결정 1 을 문자 그대로 구현하지 않았다 — 이유

결정은 "해당 몰 스케줄 일시중단 + 경보"였다. 실제 구현은 **수집기 진입 지점에서 건너뛰기**다. 결정과 어긋나 보이므로 근거를 남긴다.

| 문제 | 스케줄 취소 | 진입 건너뛰기 |
|---|---|---|
| 차단 단위가 수집기인데 스케줄은 몰 단위 | SSG 트립이 **멀쩡한 Emart 까지 멈춘다** | 트립한 수집기만 멈춘다 |
| 사이트를 그만 두드리는가 | 그렇다 | **그렇다** — 브라우저는 각 수집기의 `getItems()` 안에서 뜨므로 진입 전에 걸러내면 로그인 시도도 브라우저 기동도 없다 |
| 냉각 후 복귀 | 되살릴 트리거를 따로 만들어야 하고, 빠뜨리면 영구 정지 | 그냥 된다 |

목적(그만 두드리기)은 동일하게 달성하면서 부작용이 없다. `auto_collect` 는 어느 경우에도 건드리지 않는다.

#### 임계 — 횟수와 시간을 함께 본다

원안은 연속 5회였다. 720분 주기에서 2.5일이라 늦다는 지적이 있었다. 그런데 **횟수만 쓰는 한 주기가 길어지면 언제든 다시 늦어진다.** 그래서 둘 중 먼저 오는 쪽으로 트립한다.

- 연속 **3회** 실패
- 또는 실패가 끊기지 않은 채 **48시간** 경과

시간 조건이 주기와 무관한 벽시계 상한 역할을 한다.

#### 백오프와 브레이커는 서로 당긴다

백오프는 재시도를 미루고(사이트를 덜 두드림), 트립은 연속 실패 **횟수**로 걸린다. 그래서 **백오프를 걸수록 트립까지의 벽시계 시간이 늘어난다.** 720분 주기 기준:

| | 1회 | 2회 | 3회(트립) |
|---|---|---|---|
| 백오프 없음 | T+0 | T+12h | **T+24h** |
| 백오프 있음 | T+0 | T+24h | **T+48h** |

이 지연을 48시간 시간 조건이 받아 준다 — 백오프가 있어도 트립이 이틀을 넘지 않는다. 두 장치를 같이 설계해야 했던 이유다.

#### 운영 주기에서 백오프의 효과는 작다 — 그대로 받아들인다

720분 주기에서 첫 실패의 유예는 1440분이고 그것이 곧 상한이다. 즉 **사실상 "한 회차 건너뛰기"** 다. 주기를 하한(360분)까지 낮춘 경우에만 360 → 720 → 1440 단계가 생긴다.

과장하지 않고 적어 둔다. 주기가 길다는 것은 이미 충분히 느리게 두드리고 있다는 뜻이라, 백오프의 여지가 작은 것이 정상이다.

#### 요약 통계 집계 단위

수집기 행이 늘어나면 대시보드 숫자가 부풀거나(전부 세면 seq=1 정상 회차가 성공 3) 반대로 부분 실패가 사라진다(집계 행만 세면 SUCCESS 로만 보인다). 그래서 **수집기 행이 있는 회차에서는 집계 행을 빼고, 수집기 행이 없는 회차는 그대로 센다.** 기존 기록의 숫자가 이 변경으로 달라지지 않는다. `SKIPPED` 는 성공도 실패도 아니라 제외한다 — 브레이커가 막은 것을 실패로 세면 같은 사실이 두 번 계산된다.

#### 검증

- 자동 테스트 **150건** (실패 0). 정책이 순수 함수라 시각을 인자로 받으므로 **실제로 기다리지 않고** 24시간 뒤·48시간 뒤를 검증한다
- 상태 저장은 `@TempDir` 의 새 SQLite 파일. 기준선 DB md5 `909dfe…073ac` 불변
- 앱 기동 없음

#### 남은 것

- 트립된 수집기를 **화면에 드러내는 일**은 아직이다. `getTripped()` 로 목록은 뽑히지만 대시보드 배너·배지는 3-5(하트비트)와 같은 화면 작업이라 그쪽에서 함께 한다. 지금은 트립 시 `logger.error` 경보만 남는다
- `jbg_collect_log` 의 legacy `"SSG:signin"` 접두사는 기존 행에 그대로 남는다. 신규 행은 `collector` 필드를 쓴다

---

### [2026-07-29 15:30] Phase 3-10 — 스키마 선언을 schema.sql 하나로 (v0.11.8)

#### 작업 개요

마이그레이션 3경로 통합이 과제였다. 착수해 보니 **5경로**였고, 진짜 문제는 경로 수가 아니라 **선언이 두 벌 있었다는 것**이었다.

#### 계획서가 몰랐던 2경로

계획서는 `schema.sql` + `JbgMallDAO.ensureAutoCollectColumns` + `StartupTasks.migrateCollectLogSchema` 셋을 지목했다. grep 해 보니 둘이 더 있었다.

| 경로 | 내용 | 호출 지점 |
|---|---|---|
| `JbgMallDAO.ensureAutoCollectColumns` | 컬럼 2개 | 4곳 |
| `StartupTasks.migrateCollectLogSchema` | 컬럼 5개 + CREATE TABLE 복제본 | 1곳 |
| **`JbgExportConfigDAO.ensureExportConfigTable`** | 컬럼 6개 + CREATE TABLE 복제본 | **3곳** |
| **`JbgItemDAO.ensureQtyColumn`** | 컬럼 1개 | **5곳** |

호출 지점 합계 13곳. 전부 조회·저장 경로 안에 있어서 **DAO 를 한 번 쓸 때마다 컬럼 존재 확인이 돌았다.**

#### 진짜 문제 — 선언이 두 벌이고 이미 갈라져 있었다

컬럼 **8개**가 `schema.sql` 에 선언되어 있지 않고 런타임 ALTER 에만 있었다.

| 테이블 | schema.sql 에 없던 컬럼 |
|---|---|
| `jbg_mall` | `auto_collect` |
| `jbg_item` | `qty` |
| `jbg_export_config` | `save_to_jiniebox`, `ftp_address`, `ftp_id`, `ftp_pass`, `public_key`, `ftp_encrypt_enabled` |

즉 **신규 설치는 이 컬럼들이 없는 테이블을 받고**, DAO 가 처음 불릴 때 뒤늦게 메워졌다. 신규 설치와 기존 설치가 서로 다른 경로로 같은 스키마에 도달하고 있었다는 뜻이다.

그리고 복제된 DDL 은 이미 갈라져 있었다. `JbgExportConfigDAO` 가 들고 있던 `CREATE TABLE jbg_export_config` 는 `schema.sql` 의 것과 달리 **`updated_time` 과 `last_export_time` 두 컬럼이 없다.** 어느 쪽이 먼저 테이블을 만드느냐에 따라 스키마가 달라진다. `StartupTasks` 도 `jbg_collect_log` DDL 을 자바 문자열로 복제하고 있었다.

> 참고 — `last_export_time` 이 이 복제본에서 빠져 있다는 사실은 **판단 대기 10**(`last_export_time` 이 FTP 전송 경로에서 갱신되지 않는다)과 인접해 있다. 다만 같은 원인이라고 단정할 근거는 아직 없다. 그 건은 따로 본다.

#### 채택안 — schema.sql 을 유일한 선언처로 두고, 코드가 그것을 강제한다

`SchemaMigrator` 를 신설했다. 하는 일은 단순하다.

1. 클래스패스에서 `schema.sql` 을 읽는다
2. `CREATE TABLE` 선언을 테이블·컬럼 목록으로 파싱한다
3. `PRAGMA table_info` 로 실제 DB 와 대조한다
4. 없는 테이블은 **그 파일의 DDL 을 그대로** 실행하고, 없는 컬럼만 `ALTER TABLE ADD COLUMN` 한다

핵심은 **컬럼 목록을 자바 코드에 다시 적지 않는다**는 것이다. 목록이 한 벌뿐이니 어긋날 수가 없다. 통합 전에는 "자바의 목록"과 "schema.sql 의 목록"이 각자 낡았다.

파서는 SQL 전체 문법이 아니라 이 프로젝트가 실제로 쓰는 형태만 다룬다. `--` 행 주석 제거, 괄호 깊이를 세며 최상위 콤마에서만 분리(`DECIMAL(10,2)` 가 잘리지 않게), `PRIMARY KEY (...)` 같은 테이블 제약은 컬럼에서 제외.

#### ALTER 로 못 붙이는 컬럼은 손대지 않는다

SQLite 는 `ADD COLUMN` 으로 PRIMARY KEY·UNIQUE 를 거부하고, 기본값 없는 NOT NULL 도 기존 행을 채울 값이 없어 거부한다. 이런 컬럼은 **시도하지 않고 경고만 남긴다.** 테이블 재작성이 필요한 변경을 자동으로 하면 안 된다.

#### 보정 시점 두 곳

- 기동 시 `StartupTasks.onApplicationReady` — 명시적 진입점
- `CommonDataAccessObject` 생성자 — **안전망.** 웹서버는 `ApplicationReadyEvent` 보다 먼저 뜨므로 그 사이에 들어온 요청도 스키마가 보장돼야 한다

`ensureMigrated()` 는 `AtomicBoolean` 으로 JVM 당 1회를 보장한다(재진입도 이 플래그가 막는다). 그래서 생성자에 둬도 첫 호출 이후에는 플래그 한 번 읽는 비용뿐이다 — **조회마다 예외를 두 번 던지던 기존 방식보다 싸다.**

#### 결정 4 를 지켰다

`session_profile_*` 5개 컬럼은 넣지 않았다. 읽는 코드가 없는 컬럼을 미리 세우지 않는다. 통합이 끝났으므로 Phase 5 에서 `schema.sql` 에 5줄 더하면 끝난다.

같은 이유로 결정 2 의 수집기 식별 컬럼도 넣지 않았다. 그것을 쓰는 3-3 이 통합된 경로로 추가한다.

#### 검증

- 자동 테스트 **128건** (실패 0 / 스킵 15). 신규 16건은 전부 `@TempDir` 의 새 SQLite 파일을 쓴다
- 구버전 테이블 3종(v0.7.0 이전 `jbg_mall`, v0.8.0 이전 `jbg_collect_log`, FTP 컬럼 없는 `jbg_export_config`)을 만들어 보정 후 **기존 행이 남는지** 확인
- `grep "ADD COLUMN\|CREATE TABLE"` 결과 `SchemaMigrator` 외 **0건**
- 기준선 DB md5 `909dfe…073ac` 불변 — 실 DB 에 닿지 않았다

#### 남은 것

`spring.sql.init` 은 그대로 뒀다(`mode: always`, `continue-on-error: true`). 신규 설치의 선언적 DDL 경로로는 여전히 유효하고, 그것이 조용히 실패하더라도 이제 `SchemaMigrator` 가 같은 파일을 읽어 복구한다. 즉 **침묵이 더 이상 유실로 이어지지 않는다.** `data.sql` 의 시드까지 자바로 옮기는 것은 이번 범위에 넣지 않았다.

---

### [2026-07-29 14:40] 차단 질문 4건 해소 — 조사 결과 세 건 모두 전제가 바뀜

#### 작업 개요

Phase 3 의 잔여 6건 중 3건(3-3, 3-10, 3-12)이 "코드가 아니라 답변"에 막혀 있었다. 착수 전에 각 전제를 코드로 검증했더니 **세 건 모두 전제가 사실과 달랐다.** 정정한 뒤 사용자 결정을 받았다.

#### 정정 1 — 서킷 브레이커는 "재조정"이 아니라 신규 설계다

계획서는 3-3 을 "서킷 브레이커(연속 5회) — 임계 재조정 필요"로 적었다. 마치 임계 5가 코드에 있는 것처럼 읽힌다. **없다.** 수집 경로에 circuit / backoff / retry 코드가 0줄이고, `max-retry-count: 3` 은 프로덕션 소비자가 0개다(`dev/JangbogoConfigExample` 에서만 읽힌다). "연속 5회"는 원안의 제안값이었다.

720분 × 5 = **2.5일** 이라는 지적 자체는 유효하다.

#### 정정 2 — 임계보다 먼저 걸리는 문제가 있다

> **[2026-07-31 정정]** 이 절의 최초 서술이 부정확했다. "결과가 한 행으로 합쳐져 SSG 실패가 SUCCESS 로 기록된다"고 적었으나, `recordPartialFailures`(Phase 1, v0.10.3)가 **이미 수집기별 FAIL 행을 남기고 있다.** 기준선 `jbg_collect_log` seq=4 가 `step_name='Emart:navigateReceipt'` FAIL 로 실재한다. 아래는 실제 확인한 내용으로 고쳐 쓴 것이다. 하드코딩 `false` 기간에는 `recordPartialFailures` 가 없었으므로 그 시기에 대한 서술은 유효하다.

`MallOrderUpdaterRunner:325` 가 `nothingUsable ? "FAIL" : "SUCCESS"` 로 판정한다. seq=1 은 수집기가 SSG·Emart **둘**인데 이 판정은 **실행 단위 한 행**으로 남는다. 즉 SSG 가 실패해도 Emart 가 성공하면 그 **집계 행**은 SUCCESS 다.

수집기별 실패 자체는 별도 FAIL 행으로 남는다. 그러나 브레이커가 쓰기에는 두 가지가 모자란다.

1. **수집기 식별이 1급 필드가 아니다.** `step_name` 앞에 `"SSG:signin"` 처럼 접두사로 붙어 있어, 수집기별 연속 실패를 세려면 문자열을 파싱해야 한다.
2. **수집기별 성공이 전혀 기록되지 않는다.** 연속 실패 카운트를 **리셋할 근거가 없다.** "FAIL 행이 없으면 성공했겠지"라는 부재 추론은, 러너가 `recordPartialFailures` 전에 통째로 예외를 던지는 경로에서 깨진다.

⇒ 연속 실패를 몰 단위로 세면 SSG 단독 장애를 트립하지 못하고, 수집기 단위로 세려면 위 두 가지를 먼저 갖춰야 한다.

#### 정정 3 — §9-2 는 코드가 이미 답을 좁혀 놨다

seq=1 매핑이 네 곳에서 제각각이다.

| 경로 | seq=1 → |
|---|---|
| DB 시드 `data.sql:8` | `id='ssg'` / `SSG(신세계,이마트,트레이더스)` |
| 계정 연결 `JangBoGoManager:244` | `new Emart` 하나만 |
| 수집 `MallOrderUpdater:66-67` | `Ssg` + `Emart` 둘 다, 같은 자격증명 |
| 내보내기 `ExportService:507` | `'emart'` |

로그인 대상 사이트도 다르다 — `Emart.signin:102` 는 `eapp.emart.com/login/login.do`, `Ssg.signin:96` 은 `member.ssg.com/member/popup/popupLogin.ssg`. **같은 아이디/비번이 서로 다른 두 로그인 폼에 제출되고 있다.**

그런데 Phase 1 관측이 답을 거의 줬다. `Ssg.signin` 은 `mem_id`/`mem_pw` 에 자격증명을 실제로 채워 제출한 뒤 판정하는데, 하드코딩 `return false` 를 정직화하자 **`signedIn=true` 가 4회 재현**됐다. 동시에 `account_status=1` 은 emart 경로로 세팅된 값이다. 두 사이트 모두에서 인증에 성공한다는 뜻이다.

#### 정정 4 — 미결 3 의 "SQLite 특성상 편도"는 낡았다

`ALTER TABLE ... DROP COLUMN` 은 SQLite **3.35.0(2021)** 부터 지원된다. `sqlite-jdbc 3.45.3.0` 이 싣고 있는 엔진은 **3.45.3** 이다.

임시 DB 로 실측했다 — plain nullable 컬럼을 ADD 한 뒤 DROP 했고, 기존 행이 보존됐다. **인덱스·PK·UNIQUE 를 걸지 않는 한 되돌릴 수 있다.**

그리고 **3-10 은 미결 3 에 막혀 있지 않다.** 앞선 세션 요약에서 "선결"이라고 적은 것은 부정확했다. 순서는 오히려 반대다 — 통합을 먼저 하면 컬럼 추가는 선언 목록에 5줄 더하는 일이 된다.

#### 사용자 결정 4건

| # | 질문 | 결정 |
|---|---|---|
| 1 | 브레이커가 트립하면 무엇을 하나 | **해당 몰 스케줄 일시중단 + 경보.** `auto_collect` 컬럼은 건드리지 않는다 — 코드가 사용자 설정을 덮으면 재개할 때 원래 값을 복원할 근거가 사라진다. Phase 5-10(세션 만료 감지)이 이미 같은 패턴이라 두 기능이 한 가지 복귀 절차를 공유한다 |
| 2 | 연속 실패를 무엇 단위로 세나 | **수집기 단위.** 몰 단위로는 seq=1 의 한쪽 수집기 단독 장애를 잡을 수 없다. `jbg_collect_log` 에 수집기 식별이 필요하다 |
| 3 | seq=1 자격증명의 정체 | **SSG 통합회원 하나로 두 사이트가 다 된다.** 코드 관측과 일치한다 |
| 4 | `session_profile_*` 5개 컬럼 추가 시점 | **3-10 통합만 먼저, 컬럼은 Phase 5.** 읽는 코드가 없는 컬럼을 미리 세우지 않는다 |

#### 무엇이 풀렸나

- **3-3 / 3-9** — 착수 가능. 결정 1 이 동작을, 결정 2 가 집계 단위를 정했다. 임계 숫자는 이 둘이 정해진 뒤에야 의미가 생긴다
- **3-10** — 착수 가능. 결정 4 가 범위를 "마이그레이션 경로 통합"으로 한정했다
- **3-12** — 하드 블로커 해제. 결정 3 에 따라 DB 기준인 `'ssg'` 로 통일해도 자격증명이 엉뚱한 사이트로 가지 않는다

#### 결정 2 의 실행 순서 정정

결정 2 는 `jbg_collect_log` 스키마 변경을 부른다. 질문 당시에는 "3-10 통합과 같이 하는 게 맞다"고 적었으나, 결정 4 를 받고 보니 일관되지 않는다 — 결정 4 의 근거가 "읽는 코드가 없는 컬럼을 미리 세우지 않는다"인데 같은 이유가 여기에도 걸린다.

**순서를 바꾼다.** 3-10 은 컬럼을 추가하지 않고 **경로 통합만** 한다. 수집기 식별 컬럼은 그것을 쓰는 3-3 이 통합된 경로를 통해 추가한다. 통합을 먼저 하는 값이 바로 그것이다.

---

### [2026-07-29 14:05] Phase 3-11 — 죽은 드라이버 경로 필드 제거 (v0.11.7)

#### 작업 개요

계획서는 3-11 을 "실배선 **또는** 삭제"로 열어 두고 "미확인. 착수 시 먼저 실배선 여부 확인 필요"라고 적었다. 확인한 뒤 **둘로 갈라서** 처리했다.

#### 사실 확인

`WebDriverManager.java:20-21` 의 5개 필드 — `CHROME_DRIVER_ID` / `CHROME_DRIVER_PATH` / `CHROME_BINARY_PATH` / `EDGE_DRIVER_ID` / `EDGE_DRIVER_PATH`.

| 확인 항목 | 결과 |
|---|---|
| 대입 | 0회 |
| 참조 | 0회 |
| `extends WebDriverManager` | 없음 (`protected` 인데 상속자가 없다) |
| 대응 설정 키 | 없음 (`application.yml` 에는 `default_web_driver`·`browser_headless` 뿐) |

#### 갈라지는 지점 — 드라이버와 바이너리는 사정이 다르다

Gradle 캐시의 `selenium-chrome-driver-4.31.0.jar` 를 열어 상수를 직접 확인했다.

```
ChromeDriverService.CHROME_DRIVER_EXE_PROPERTY = "webdriver.chrome.driver"
```

**드라이버 경로는 필드가 없어도 이미 지정할 수 있다.** Selenium 이 이 표준 프로퍼티를 직접 읽고, 값이 있으면 Selenium Manager 의 자동 다운로드를 건너뛴다. 폐쇄망 대응은 코드 없이 기동 인자로 해결된다.

```
java -Dwebdriver.chrome.driver=D:\drivers\chromedriver.exe -jar jangbogo-x.y.z.jar
```

즉 `CHROME_DRIVER_PATH` 는 **완전 중복**이었다. 삭제로 잃는 기능이 없다.

반면 브라우저 **실행 파일**은 다르다. 같은 방식으로 `ChromeOptions` 의 상수를 확인했더니 바이너리 경로에 대응하는 표준 시스템 프로퍼티가 **없다** — `setBinary()` 로만 지정할 수 있다. 그러니 `CHROME_BINARY_PATH` 는 중복이 아니라 **한 번도 구현되지 않은 기능**이다.

그래서 이렇게 정리했다.

- `CHROME_DRIVER_PATH` 외 4개 → **삭제.** 중복이거나 아무 의미가 없다.
- 바이너리 핀 고정 → **Phase 5 로 명시 이관.** 계획서 §"이 Phase 에 넣지 않은 것"이 이미 "프로필 재사용의 `setBinary` 핀 고정과 묶이므로 Phase 5"로 적어 둔 것과 같은 판단이다. 지금 급조하면 Phase 5 에서 다시 설계하게 된다.

삭제한 자리에는 위 근거를 주석으로 남겼다. 필드만 지우면 "왜 경로 지정 통로가 없지"라는 질문이 다시 돌아온다.

#### 왜 죽은 필드가 해로웠나

`CHROME_BINARY_PATH` 라는 이름이 보이면 "설정 어딘가에 값을 넣으면 되겠구나"라고 읽게 된다. 실제로는 아무 데도 연결돼 있지 않으므로, 폐쇄망에서 막힌 사람이 **없는 통로를 찾느라 시간을 쓴다.** 반대로 실제로 동작하는 표준 프로퍼티는 코드 어디에도 적혀 있지 않았다.

#### 테스트 seam

옵션 조립을 `buildChromeOptions(boolean)` 으로 분리했다(순수 코드 이동). `getWebDriver()` 는 실제 `ChromeDriver` 를 생성하므로 브라우저 없이 검증할 수 없지만, 옵션 조립만 떼면 `ChromeOptions` 객체를 그대로 들여다볼 수 있다.

`headless` 를 설정에서 읽지 않고 인자로 받는다 — 테스트가 설정 싱글턴에 의존하지 않게 하기 위해서다.

**headless 기본값을 특히 못 박았다.** Phase 1 에서 이 조건이 반대로 되어 있어 로그인 화면을 눈으로 확인할 수 없었고, 그것이 수집 장애 진단을 오래 막았다. 설정이 없으면 headless 를 켜지 않는다는 규칙이 이제 테스트로 고정돼 있다.

---

### [2026-07-29 13:30] 판단 대기 8 — Oasis·Hanaro·SSG 파서 테스트 (v0.11.6)

#### 결정

**파싱 seam 추출 + Mockito.** 사용자 승인을 받고 착수했다.

제시한 4안과 채택/기각 사유는 아래와 같다.

| 안 | 판정 | 사유 |
|---|---|---|
| **seam 추출 + Mockito** | **채택** | 순수 코드 이동이라 회귀 위험이 가장 낮고, 실제 회귀가 나는 지점(변환 규칙)을 정확히 덮는다 |
| seam 추출 + `navigatePurchased` 전체 목킹 | 기각 | 커버리지는 넓지만 스텁 분량이 3~4배이고, 목이 실제 드라이버 의미론과 어긋나면 **통과하는 거짓 테스트**가 생긴다 |
| jsoup 순수 파서로 재작성 | 기각 | 이번 세션은 실사이트 HTML 을 캡처할 수 없다(앱 기동 = 실계정 로그인). 픽스처가 어차피 손으로 지어낸 HTML 이 되므로, 의존성 추가와 셀렉터 전면 재작성의 값을 못 한다 |
| 보류 | 기각 | 세 몰의 수집 경로에 회귀 테스트가 하나도 없는 상태가 계속된다 |

#### 무엇을 검증할 수 있고 무엇은 못 하는가 — 먼저 못 박는다

실사이트 HTML 이 없으므로 픽스처는 손으로 세운 `WebElement` 트리다. 그래서 이 테스트가 잡는 것은 **셀렉터의 정확성이 아니라 변환 규칙의 회귀**다.

- **잡는다** — serial 괄호 제거, 하이픈·점 제거, 헤더행 skip, `td` 개수 가드, 선택 필드(가격) 누락 허용, 빈 목록일 때 `null` 이 아닌 빈 배열
- **못 잡는다** — 셀렉터가 실사이트 DOM 과 맞는지. 이건 원리상 단위테스트로 알 수 없고 실사이트에서만 드러난다

이 한계를 `MallDomFixtures` javadoc 에 적어 두었다. 나중에 "테스트가 있는데 왜 수집이 깨졌나"를 묻지 않기 위해서다.

#### seam 추출 — 순수 코드 이동

| 몰 | 분리한 메서드 |
|---|---|
| `Oasis` | `parseOrderSummary(WebElement)` / `extractDetailLink(WebElement)` / `applyOrderDetail(WebDriver, JSONObject)` |
| `Ssg` | `parseOnlineOrderRow(WebElement)` / `parseOnlineOrderItems(WebDriver)` |
| `Hanaro` | `parseDetailPage` 는 이미 분리돼 있었다 — 가시성만 `private` → package-private |

로직은 한 줄도 바꾸지 않았다. 분리한 메서드에는 페이지 이동·창 전환·지연이 들어가지 않는다.

#### 잡아낸 것 — 기존 가드의 의미가 테스트로 고정됐다

SSG 의 `td.size() < 4` 가드는 Phase 1 에서 들어간 것이고, 원래 주석은 "구매 내역이 없으면 안내 행 하나가 렌더링된다"였다. 테스트로 옮기면서 경계를 양쪽에서 못 박았다 — **3개 이하는 거부하고 5개 이상은 허용한다.** 컬럼이 늘어난 것만으로 데이터 행을 버리면 수집이 조용히 0건이 되기 때문이다.

Oasis 의 serial 괄호 벗기기도 마찬가지다. `substring(1, len-1)` 을 무조건 적용하면 짧은 값이 빈 문자열이 된다. 길이 가드가 그것을 막고 있는데, 그 사실이 코드에는 적혀 있지 않았다. 테스트가 그 의도를 고정한다.

#### 개발 중 걸린 것

`when(driver.findElement(BY)).thenReturn(text("..."))` 처럼 `thenReturn` 인자 안에서 새 목을 스터빙하면 Mockito 가 중첩 스터빙으로 보고 `UnfinishedStubbingException` 을 던진다. 목 생성을 `when(...)` 밖에서 끝내도록 고쳤고, 재발 방지로 그 이유를 주석에 남겼다.

#### 테스트 수

29건 추가(Oasis 11 / Hanaro 10 / SSG 8). `HanaroTest` 는 `@Test` 가 없는 `main()` 드라이버라 Gradle 이 실행하지 않는다 — 이번에 추가한 것과 별개다.

---

### [2026-07-29 12:55] Phase 3-6 — 상태 파일 포맷 불일치 (v0.11.5)

#### 작업 개요

`buildJinieboxJsonFromOrders` 는 JSON 배열을 만드는데 `createEmptyStatusFile` 은 JSON 객체를 썼다. 같은 수신측에 최상위 구조가 다른 두 종류가 올라갔다.

#### 수신측 기대값 — 물어볼 필요가 없었다

지시는 "어느 쪽에 맞출지는 수신측 기대값을 확인할 수 없으니 판단 근거를 정리해서 물어라"였다. 확인해 보니 **수신측 코드가 로컬에 있었다.** 읽어서 확정했다.

수신측 파서는 최상위 노드가 배열인지 검사하고, 아니면 그 자리에서 검증에 실패한다. 통과한 뒤에야 주문을 하나씩 순회한다. 즉 **배열은 선택지가 아니라 하드 게이트**다. 현재의 객체 형태는 예외 없이 매번 실패한다.

방증도 있다. 수신측 파일 이동 모듈의 주석 예시 파일명이 하필 `jangbogo_status_…_ftp.json.encrypted.failed.log` 다. 상태 파일이 `failed/` 로 가는 것은 이미 관측돼 있던 사실이다.

빈 배열을 넣으면 어떻게 되는지도 끝까지 따라갔다.

| 단계 | 객체 (기존) | 빈 배열 (변경 후) |
|---|---|---|
| 파서 | 최상위 검사 실패 | 통과, 주문 0건 |
| 주문 순회 | 도달 못 함 | 루프 미진입 |
| DB | 영향 없음 | 영향 없음 |
| 알림 | — | 발송 없음 (순회 안에서 만들어지는데 순회를 안 한다) |
| 파일 | `failed/` + **복호화 평문 JSON 기록** | `committed/` |

상태 파일에는 구매 데이터가 없으므로 평문이 남아도 개인정보 노출은 아니었다. 다만 파일이 계속 쌓였다.

#### 남은 판단은 포맷이 아니라 "계속 보낼 것인가"였다

포맷은 증거로 확정됐으므로, 실제 결정이 필요한 것만 물었다. **사용자 결정: 빈 배열로 바꾸고 계속 보낸다.**

근거는 하트비트다. 이 파일의 존재 자체가 "수집은 돌았고 신규가 없었다"는 신호다. 보내지 않으면 수신측에서 그 상태와 "수집 자체가 죽었다"를 구분할 수 없다 — 판단 대기 11(배송/도달 추적 부재)과 직결된다.

#### 버려지는 필드

`status`·`timestamp`·`message` 3개가 사라진다. 손실은 사실상 없다.

- `timestamp` — 파일명 `jangbogo_status_20260729_125500_ftp.json` 에 이미 초 단위로 들어 있다.
- `status: no_new_orders` — `jangbogo_status_` 접두사 + 빈 배열로 충분히 드러난다.
- `message` — 사람이 읽는 문장이고 수신측 소비자가 없다.

페이로드 계약 `[{serial, datetime, mall_id, mallname, items:[…]}]` 에는 애초에 메타데이터를 실을 자리가 없다. 억지로 넣으려면 가짜 주문 한 건을 만들어야 하는데 그건 훨씬 나쁘다.

#### 계획서 항목 중 하나는 해당 없음

계획서 3-10 의 "기존 `failed/` 평문 적체 정리"는 **jangbogo 작업이 아니다.** `failed/` 는 수신측 디렉터리 개념이고 jangbogo 코드에는 존재하지 않는다. 지시대로 재조사하지 않았다.

---

### [2026-07-29 12:35] Phase 3-7 — 수집 실패 시 대화상자 문구·URL 유실 (v0.11.4)

#### 작업 개요

계획서는 이 건을 "`ScreenshotUtil.capture()` 가 `getScreenshotAs` 를 바로 호출해 alert 이 뜨면 실패하고 null 을 반환한다 — 스크린샷이 가장 필요한 상황에서 아무것도 안 남는다"로 적었다. 착수 전에 코드를 다시 읽었더니 **실제 손실은 스크린샷이 아니었다.**

#### 배경 — 손실은 순서에서 나온다

`WebDriverManager` 는 `unhandledPromptBehavior` capability 를 지정하지 않는다. 그래서 chromedriver 는 W3C 기본값 `"dismiss and notify"` 로 동작한다. 이 모드에서는 대화상자가 떠 있을 때 들어온 명령이 **먼저 대화상자를 닫고** 그 다음 오류를 돌려준다.

`CollectStep.wrap` 의 수집 순서는 이랬다.

```java
String url   = safe(() -> driver.getCurrentUrl());   // ← 여기서 대화상자가 닫힌다
String title = safe(() -> driver.getTitle());
String screenshot = ScreenshotUtil.capture(driver, mallName);
```

1. `getCurrentUrl()` 이 대화상자를 닫고 `UnhandledAlertException` 을 던진다.
2. `safe()` 가 그 예외를 삼킨다 → **`url = null`**.
3. 그 시점에 대화상자는 이미 사라졌으므로 `getTitle()` 과 `capture()` 는 오히려 **성공한다.**

즉 계획서가 지목한 스크린샷은 대개 정상적으로 찍히고, 대신 **실패 원인이 적혀 있는 대화상자 문구가 아무 데도 기록되지 않은 채 소멸하며 URL 까지 함께 유실된다.** 찍힌 스크린샷에도 대화상자는 없다 — 이미 닫힌 뒤다.

`ScreenshotUtil` 단독 수정으로는 이 손실을 못 막는다. 고칠 지점은 `wrap` 의 **순서**다.

#### 수정

`wrap` 의 맨 처음에 대화상자를 처리한다.

```java
String alertText = ScreenshotUtil.consumePendingAlert(driver);  // 문구 확보 + 닫기
String url   = safe(() -> driver.getCurrentUrl());              // 이제 정상 동작
String title = safe(() -> driver.getTitle());
String screenshot = ScreenshotUtil.capture(driver, mallName);
```

문구는 예외 메시지에 `(alert="…")` 로 붙어 `jbg_collect_log` 에 남는다. 여러 줄은 한 줄로 접고 300자에서 자른다.

`capture()` 에도 같은 처리를 방어적으로 넣었다. 통상은 `wrap` 이 이미 처리한 뒤라 두 번째 호출은 "대화상자 없음"으로 돌아온다. 직접 호출자와, `unhandledPromptBehavior` 가 `ignore` 인 환경·Edge 경로를 위한 것이다.

#### dismiss 인가 accept 인가

`dismiss()` 를 쓴다. 인자가 하나뿐인 `alert` 에서는 둘이 같지만, `confirm` 에서 `accept` 는 "확인"을 누르는 것이다. **진단하려다 실제 동작(주문 취소 등)을 일으켜서는 안 된다.** 테스트가 `accept()` 가 호출되지 않는 것까지 검증한다.

기존에 의도적으로 `accept()` 를 쓰는 `Ssg.signout` 은 정상 경로라 영향이 없다.

#### 테스트 격리

`ScreenshotUtil` 의 기준 폴더를 시스템 프로퍼티 `jangbogo.screenshot.dir` 로 덮어쓸 수 있게 했다(기본값 불변). `test` 태스크가 `build/test-screenshots` 로 돌린다. `jangbogo.localdb.url` 격리와 같은 방식이며, 테스트가 개발 트리의 `logs/` 에 PNG 를 남기지 않는다. 실행 후 `git status` 로 오염 없음을 확인했다.

#### 부수 확인 — Phase 3-11 선취

같은 파일을 읽다 확인했다. `WebDriverManager.java:20` 의 `CHROME_DRIVER_PATH` / `CHROME_BINARY_PATH` 는 **선언만 있고 대입도 참조도 0회**다. 3-11 은 "실배선 또는 삭제"가 아니라 사실상 삭제 건이다. 이번 커밋 범위에는 넣지 않았다.

---

### [2026-07-29 12:10] Phase 3-8 — WinSW 서비스 정의 버전 드리프트 제거 (v0.11.3)

#### 작업 개요

`packaging/winsw/jangbogo-service.xml:16` 이 `jangbogo-0.8.1.jar` 을 가리키고 있었다. 당시 앱 버전은 0.11.2 다. 버전을 고쳐 적는 대신 **버전을 적을 자리 자체를 없앴다.**

#### 배경 — 계획서의 판단을 착수 전에 재확인했고, 두 군데가 달랐다

계획서는 "배포본으로 서비스를 설치하면 존재하지 않는 JAR 를 실행하게 된다"고 적었다. 절반만 맞다.

`install.bat:90` 이 이미 설치 시점에 XML 을 동기화하고 있다. 폴더에서 `dir /b /o:-d jangbogo-*.jar` 로 실제 JAR 을 찾아 `/service/arguments` 노드를 통째로 갈아끼운다. 그래서 **`install.bat` 경로로 설치하면 0.8.1 참조는 실행되지 않는다.**

문제는 다른 경로다. `packaging/winsw/README.md` 는 `install.bat` 을 언급하지 않고 관리자 명령 프롬프트에서 `jangbogo-service.exe install` 을 직접 실행하라고 안내한다. 그 README 도 릴리스 ZIP 의 `service/` 에 들어간다. **README 절차를 그대로 따르면 동기화가 일어나지 않고, 서비스는 등록되지만 시작에 실패한다.**

게다가 드리프트 지점은 XML 하나가 아니었다.

| 위치 | 적혀 있던 값 | 실제 |
|---|---|---|
| `jangbogo-service.xml:16` | `jangbogo-0.8.1.jar` | 0.11.2 |
| `README.md` 설정 예시(2곳) | `jangbogo-0.6.0.jar` | 0.11.2 |
| `README.md` 문제 해결 | `jangbogo-0.6.0.jar` | 0.11.2 |
| `README.md` WinSW 버전 | `v3.0.0-alpha.11` | `download-winsw.ps1` 은 `v2.12.0` 을 받는다 |

버전을 다섯 군데에 적어 두면 다섯 군데가 각자 낡는다. 실제로 그렇게 됐다.

#### 채택안 — 버전을 두 번 적지 않는다

저장소의 XML 에는 `@JAR_NAME@` 토큰만 둔다. `packageDist` 가 `bootJar.archiveFileName` 으로 치환한다(`ReplaceTokens`). 앱 버전이 오르면 ZIP 안의 XML 도 같이 오른다 — 사람이 손댈 자리가 없다.

`@JAR_NAME@` 은 유효한 JAR 이름이 아니라서 **자리표시자임이 한눈에 보인다.** `jangbogo-0.8.1.jar` 처럼 그럴듯하면서 틀린 값보다 낫다. 저장소 파일을 그대로 복사해 쓰면 즉시 실패하고, 그 실패가 곧 "치환 단계를 빠뜨렸다"는 신호다.

README 에서는 버전을 전부 걷어내고 `jangbogo-x.y.z.jar` 자리표시자로 바꿨다. 수동 등록 절차에는 **3-1 JAR 이름 확인** 단계를 새로 넣었다(`dir ..\jangbogo-*.jar` 와 XML 대조).

#### install.bat 의 동기화는 그대로 둔다

두 장치는 서로를 대체하지 않는다.

- `packageDist` 치환 → **ZIP 을 만든 시점**의 정합성을 보장한다.
- `install.bat` 동기화 → **JAR 만 갈아끼우고 재설치하는 폴더**의 정합성을 보장한다.

앱만 새 JAR 로 교체한 뒤 `install.bat` 을 다시 도는 것은 실제로 있는 운용이라, 후자를 빼면 드리프트가 되살아난다. `ServiceDescriptorTest` 가 이 단계의 존재도 함께 감시한다.

#### 검증

`./gradlew packageDist` 로 실제 ZIP 을 만들어 확인했다. 앱은 기동하지 않았다.

```
service/jangbogo-service.xml:
  <arguments>-Xms256m -Xmx1024m -jar "%BASE%\..\jangbogo-0.11.2.jar" --service</arguments>
ZIP 최상단: jangbogo-0.11.2.jar
```

두 이름이 일치한다.

#### 남은 것

`bat\clean_build.bat:52,55,56` 이 `jangbogo-0.5.0.jar` 을 적고 있다. 개발용 스크립트라 배포본에 들어가지 않고 릴리스 경로에 영향이 없어 이번 범위에서 제외했다. 별건으로 정리 대상이다.

---

### [2026-07-29 09:40] Phase 3-2 — 수집 주기 하한 코드 강제 (v0.11.2)

#### 작업 개요

3순위로 판정한 Phase 3-2 를 구현했다. 720분 적용은 이미 DB 에 되어 있었고, 미착수였던 **하한 코드 강제**가 이번 작업이다.

#### 배경

주기가 짧을수록 같은 계정으로 로그인을 반복하게 되고, 그것이 곧 봇 차단을 자초하는 트래픽 프로파일이다. 그런데 하한 검증이 코드 어디에도 없었다 — 화면은 `min="0"`, 서버는 `> 0` 검사뿐이었다. 화면의 `min` 속성만 바꿔서는 API 직접 호출을 못 막는다.

실제로 수정 전 seq=1 은 **10분** 주기였다(Phase 1 에서 720분으로 변경). 이론적 위험이 아니라 이미 한 번 일어났던 상태다.

#### 하한 값 — 360분

계획서 743행이 "하한(360분)과 기본값(720분)의 관계도 함께 확정 필요"(새 질문 8)로 남겨 둔 값을 그대로 채택했다.

- 하한을 720 으로 두면 사용자가 주기를 조금도 조절할 수 없다. 720 은 **기본값**이지 **최소값**이 아니다.
- 그 절반인 360분(1일 4회)을 넘지 말아야 할 선으로 둔다.
- `0` 은 계속 유효하다 — "자동수집 안 함"을 뜻하는 기존 의미다. 따라서 거부 구간은 `0 < 값 < 360` 뿐이다.

**하한은 사용자 설정이 아니라 안전장치다.** 그래서 화면에 노출하지 않고 시스템 프로퍼티 `jangbogo.collect.min-interval-minutes` 로만 바꿀 수 있게 했다(`jangbogo.localdb.url` 과 같은 방식). 개발 중 짧은 주기로 시험하려면 `-Djangbogo.collect.min-interval-minutes=10` 으로 기동한다. 바꾸기 번거로운 것이 의도다.

#### 강제 지점 3곳

계획서 수용 기준이 "UI 와 서버 양쪽에서 하한 미만 값 저장이 거부된다"인데, 실측해 보니 **한 곳이 더 필요했다.**

| 지점 | 동작 |
|---|---|
| 화면 (`index.html`) | 저장 전에 검사하고 사유를 알린다. `min`/`placeholder` 도 함께 손봤다 |
| 서버 저장 (`AdminController`, DAO 2곳) | 거부하고 사유를 반환한다 |
| **스케줄 등록** (`MallSchedulerService`) | 경고를 남긴 뒤 하한으로 올려 스케줄한다 |

세 번째가 필요한 이유: **저장 시점에 거부해도 그 이전에 들어간 값은 DB 에 그대로 남아 있다.** seq=1 의 10분이 실제 사례다. 검증만 추가하고 끝내면 기존 값은 계속 그대로 쓰인다. 조용히 클램프하지 않고 경고를 남기는 것도 같은 이유다 — 값이 정정된 것이 아니라 런타임에 덮인 것이므로 사용자가 알아야 한다.

DAO 를 강제 지점에 넣은 것은 쓰기 경로가 `saveAutoCollectFlags` 와 `updateCollectInterval` 둘이기 때문이다. Controller 에만 두면 다른 호출자가 생겼을 때 뚫린다. Controller 검증은 사용자에게 친절한 메시지를 주기 위한 것이고, DAO 가 실제 방벽이다.

스케줄 경로는 `scheduleMall` 하나로 수렴한다(호출부 3곳: `StartupTasks`, `AdminController` 2곳). 그래서 그 안에서 한 번만 막으면 된다. 호출부가 없는 `scheduleMallImmediate` 에도 같은 규칙을 넣어 뒀다.

#### 검증

`CollectIntervalPolicyTest` 10건 — 경계값(0/1/10/359/360/720), null·음수, 클램프 동작, 시스템 프로퍼티 재정의, 잘못된 프로퍼티 값의 기본값 복귀, 거부 메시지 내용.

전체 59건 / 실패 0. 기준선 DB md5 `909dfe48…` 불변. 앱 기동·실계정 접근 0.

#### 변경 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `svc/util/CollectIntervalPolicy.java` | 신규 | 하한 정책 |
| `dao/JbgMallDataAccessObject.java` | 수정 | 쓰기 경로 2곳 강제 |
| `ctrl/AdminController.java` | 수정 | 저장 요청 거부 + 사유 반환 |
| `svc/MallSchedulerService.java` | 수정 | 스케줄 시 클램프 |
| `templates/index.html` | 수정 | 저장 전 검사 + 입력 속성 |
| `src/test/java/.../svc/CollectIntervalPolicyTest.java` | 신규 | 회귀 테스트 10건 |

#### 남는 것

- **기존 DB 값은 자동 정정되지 않는다.** 현재 seq=1·2 는 720분이라 문제 없지만, 하한 미만 값이 남아 있으면 스케줄 시 클램프될 뿐 DB 는 그대로다. 설정 화면에서 저장하면 정정된다.
- 새 질문 8 의 나머지 절반("720분이 전 몰 공통인가 몰별인가")은 여전히 열려 있다. 현재는 몰별 컬럼이므로 몰별이고, 하한만 공통이다.

---

### [2026-07-29 09:00] BASELINE 2026-07-29 — Phase 2 종결 (v0.11.1)

#### 작업 개요

Phase 2 의 잔여 기록 항목(2-4, 2-9)을 처리해 **Phase 2 를 종결**한다. 함께 판단 대기 7(jackson 스큐)을 해소하고 `CLAUDE.md` 의 깨진 참조를 정리했다.

---

#### BASELINE 2026-07-29 (2-9)

**이 시점의 값이 이후 모든 Phase 의 무회귀 판정 기준이다.**

| 항목 | 값 |
|---|---|
| `jbg_order` | **22** (Emart 15 + Oasis 7) |
| `jbg_item` | **168** (117 + 51) |
| DB md5 | `909dfe48822aea77bf4f6806a37073ac` |
| `jbg_collect_log` | 6행 (seq 1·3 은 Phase 1 수정 전 기록 — **보존 결정**) |
| 백업 | `db/backup/jangbogo-dev.db.baseline-2026-07-28` (초기화 전 주문 7 / 아이템 53) |
| 몰 상태 | seq=1 ssg, seq=2 oasis 모두 `account_status=1`·`auto_collect=1`·**720분**. seq=3 hanaro 미연결 |
| 자동화 판정 단위 | 테스트 49건 (실패 0 / 스킵 15) |

**판정 방식**: 회귀는 **실계정 재수집이 아니라 테스트 통과 여부와 위 수치 비교**로 판정한다. Emart 파서는 `EmartReceiptParserTest` 의 합성 픽스처가, FTP 전송 실패 경로는 `FtpPendingQueueTest` 가, jackson 정렬은 `YamlMapperCompatibilityTest` 가 각각 담당한다.

DB md5 는 이번 세션 내내(수집 가드 검증 → 파서 픽스처 → FTP 큐 → jackson 정렬) 한 번도 바뀌지 않았다. 모든 작업이 실계정·실 DB 접근 0 으로 수행됐다는 뜻이다.

---

#### 호스트 환경 기록 (2-4)

| 항목 | 값 |
|---|---|
| OS | Windows 11 Pro (10.0.22621) |
| 설치 Chrome | **150.0.7871.187** |
| chromedriver 캐시 최대 | **150.0.7871.124** |
| `app_bound_encrypted_key` | **존재** (Chrome `Local State`) |
| `encrypted_key` | 존재 |

**계획서 정정 2건**

1. **4A-0 은 이미 충족됐다.** 계획서는 "chromedriver 캐시 최대 148, 설치 Chrome 150 → 실행 파일 다운로드이므로 사용자 승인 필요"로 적고 있으나, 캐시에 `150.0.7871.124` 가 이미 있다. 이전 세션에서 Selenium Manager 가 받아 둔 것이다(새 질문 5 의 "자동 다운로드 허용" 결정에 따른 결과). chromedriver 는 메이저 버전으로 매칭되므로 150.0.7871.124 ↔ Chrome 150.0.7871.187 조합은 유효하다. **Phase 4A 의 T1~T4 는 다운로드 승인 없이 착수 가능하다.**
2. **`app_bound_encrypted_key` 가 존재한다.** Chrome 127+ 의 App-Bound Encryption 이 활성이라는 뜻이다. 쿠키 복호화가 이를 기록한 애플리케이션에 묶이므로 **쿠키를 꺼내 쓰는 방식은 성립하지 않는다.** Phase 5 가 쿠키 추출이 아니라 **프로필 재사용**을 택한 것이 환경적으로도 옳다는 근거가 된다.

---

#### 판단 대기 7 해소 — jackson 스큐 정렬

`build.gradle` 이 `jackson-dataformat-yaml` 만 2.15.3 으로 못박아 BOM 해석값(2.19.2)과 4개 마이너가 어긋나 있었다. 버전 선언을 제거해 정렬했고, 결과적으로 `core`/`databind`/`dataformat-yaml`/`bom` 모두 2.19.2 로 수렴했다(강제 표기 `->` 소멸).

`YamlMapperCompatibilityTest` 4건이 이를 감시한다. 프로젝트가 YAML 을 다루는 세 방식(`JangbogoConfig` 의 기본 팩토리 읽기, `MallAccountYmlService` 의 builder + `findAndRegisterModules` 왕복, `ExportService` 의 `MINIMIZE_QUOTES` 쓰기)을 각각 덮고, 모듈 버전 일치 검사를 더했다.

**가드의 실효를 실증했다** — 수정 전 상태에서 버전 검사가 `expected: <2.19> but was: <2.15>` 로 정확히 실패했다. 나머지 3건은 스큐 상태에서도 통과했는데, 이것이 그동안 드러나지 않은 이유다. 관측된 장애는 없었으므로 잠재 위험이었다.

이로써 **판단 대기 9(의존성 스큐 상시 탐지)도 함께 해소**됐다. 파일 diff 가 아니라 테스트가 감시한다.

---

#### `CLAUDE.md` 깨진 참조 정리

`CLAUDE.md` 가 미커밋 문서(미결 4)를 참조해 공개 저장소에서 깨진 링크가 돼 있었다. 참조를 제거하면서 PRIVATE 저장소명과 특정 PC 절대경로 서술도 함께 걷어냈다. 내용상 필요한 것은 "외부 패키지를 import 하지 않는다"는 방침뿐이고, 그 경위는 저장소에 없는 내부 문서에 있다는 사실만 남겼다.

---

#### Phase 2 항목 최종 현황

| # | 상태 |
|---|---|
| 2-1 | **완료** — 재정의 후 측정·기록. 부산물로 jackson 스큐 발견 |
| 2-2 | **완료** — seq=1·2 실측 (seq=3 은 `account_status=0` 미연결) |
| 2-3 | **완료(Emart)** — 마스킹 규칙 = 합성 픽스처. 나머지 몰은 2-8 과 함께 보류 |
| 2-4 | **완료** — 위 호스트 환경 기록 |
| 2-5 | **보류** — 배포 baseline 실측. 운영이 콘솔 실행으로 확정돼 우선순위가 내려갔다 |
| 2-6 | **보류** — 쿠팡 메일 샘플. Phase 4B 게이트 입력이라 4B 착수 시 함께 |
| 2-7 | **보류** — jiniebox 운영 시드 확인. §9-2 와 묶인다 |
| 2-8 | **완료(Emart)** — 파서 픽스처 8건. Oasis·Hanaro·SSG 는 순수 파서 부재로 판단 대기 8 |
| 2-9 | **완료** — 위 BASELINE |

**Phase 2 를 종결한다.** 목표였던 "수정된 코드 기준의 무회귀 판정 단위"는 확보됐다. 보류 3건(2-5·2-6·2-7)은 Phase 2 의 목표에 필수가 아니며 각각 다른 Phase 의 선행 작업으로 이관한다.

#### 변경 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `build.gradle` | 수정 | jackson 버전 선언 제거, 버전 0.11.1 |
| `src/test/java/.../svc/YamlMapperCompatibilityTest.java` | 신규 | jackson 호환성·버전 일치 감시 4건 |
| `CLAUDE.md` | 수정 | 깨진 참조 + 절대경로 서술 제거 |

---

### [2026-07-29 08:20] Phase 3-1 — FTP 전송 실패분 보류 큐 (v0.11.0)

#### 작업 개요

우선순위 1위로 판정한 Phase 3-1 을 구현했다. 조사 과정에서 같은 경로의 부수 결함 2건이 드러나 함께 고쳤고, 그 결함이 디스크에 남긴 고아 파일 6건도 정리했다.

#### 배경 — 무엇이 실제로 유실됐나

`MallSchedulerService.processFtpUpload` 의 `finally` 가 업로드 성공 여부와 **무관하게** 전송 파일을 삭제했다. 실패는 `logger.warn` 한 줄이 전부였다.

핵심은 내보내기가 **증분**이라는 점이다. `exportToJinieboxFileBySeqList(savePath, newOrderSeqs)` 는 그 회차 신규 주문만 담는다. 다음 회차는 자기 신규분만 보내므로 실패한 회차의 주문을 대신 보내주지 않는다.

**정정**: 조사 초기에 "영구 데이터 유실"로 판단했으나 과했다. 주문 자체는 `jbg_order`/`jbg_item` 에 영구 보존되고, `save_path` 에 로컬 내보내기 JSON 도 남는다(실측 확인). 실제로 잃는 것은 **jiniebox 로의 전달**과 **전달 실패 사실의 기록**이다. 후자가 본질이다 — 배송 상태가 코드 어디에도 없어 무엇이 미도달인지 알 방법이 없었다.

`save_path` 실측이 이를 뒷받침했다.

| 항목 | 수 | 의미 |
|---|---|---|
| `jangbogo_orders_*.json` (로컬 내보내기) | 11 | 정상 |
| `jangbogo_orders_*_ftp.json` (전송용) | **0** | 성공했는지 실패 후 버려졌는지 **구분 불가** |
| `jangbogo_status_*_ftp.json` (고아) | **6** | 정상이면 0 |
| `last_export_time` | `none` | 11번 내보내고도 미갱신 |

전송용 고아가 0 이라는 사실이 오히려 문제였다. **증거의 부재 자체가 결함**이다.

`jbg_export_config` 는 이 경로가 활성임을 보여줬다 — 주소·아이디·비밀번호 모두 설정, `ftp_encrypt_enabled=1`, `auto_save_enabled=1`, `save_to_jiniebox=1`. 앱이 정지 상태라 출혈 중은 아니었고, 기동하는 순간 활성화되는 상태였다.

#### 설계 판단 — 왜 파일 큐인가

대안은 `jbg_order` 에 배송 상태 컬럼을 두는 것이었다. 파일 큐를 택한 이유:

- DB 방식은 `jbg_order` ALTER 가 필요하고 미결 3(SQLite ALTER 편도)과 얽힌다.
- **수신측 jiniebox 의 멱등성이 미확인**이다. 파일 큐는 "보낸 적 없는 바이트만" 재전송하므로 중복 위험이 가장 작다.
- 기준선을 막 확보한 상태에서 변경면이 가장 좁다.

구현상 유의점 3가지:

1. **파일명을 바꾸지 않는다.** `FtpUploadUtil:68` 이 원격 파일명을 `localFile.getName()` 으로 쓴다. 이름이 바뀌면 수신측에 다른 이름으로 올라간다. 충돌할 때만 접두사·확장자 체인을 유지한 채 `_r1` 을 끼워 넣는다.
2. **첫 실패에서 중단한다.** `FtpUploadUtil` 의 연결 타임아웃이 15초라, 보류 50건이 쌓인 상태에서 FTP 가 죽어 있으면 12분 넘게 수집 사이클을 붙잡는다. 같은 서버이므로 첫 건이 실패하면 나머지도 실패한다.
3. **폐기는 반드시 경고로 남긴다.** 상한 초과분을 조용히 버리면 지금 고치려는 결함과 같은 종류가 된다.

상태 파일(`jangbogo_status_*`)은 큐에 넣지 않는다. "신규 없음" 하트비트라 뒤늦게 보내면 수신측 시각을 오도한다.

**사용자 결정**: 상한 50건 / 14일(720분 주기·2몰 기준 약 56회차분). 암호화가 꺼진 구성에서는 평문도 보류한다 — 사용자가 평문 업로드를 택한 것과 노출 수준이 같고 기간 상한이 체류를 제한한다. 암호화가 켜져 있으면(현재 설정) 암호문만 보류되고 평문 원본은 즉시 삭제되므로 평문이 적체되지 않는다.

#### 함께 고친 부수 결함 2건

1. **상태 파일 이중 생성** — `processFileExport:440` 과 `processFtpUpload:478` 이 각각 `createEmptyStatusFile` 을 호출했다. 보통은 같은 초라 파일명이 같아 한 개로 겹치지만, 초 경계를 넘으면 앞의 것이 업로드되지도 삭제되지도 않고 남았다. 디스크의 고아 6건(2026-02-27~05-21)이 그 증거다. 생성을 `processFtpUpload` 한 곳으로 모았다.
2. **`auto_save_enabled=0` 인데도 로컬 파일을 씀** — `exportOrdersBySeqList` 를 무조건 실행하고 `shouldAutoSave` 로는 로그만 감쌌다. 현재 `auto_save=1` 이라 발현되지 않았을 뿐이다.

추가로 FTP 자격증명 확인을 전송 파일 생성보다 **앞으로** 옮겼다. 보낼 수 없는 상태에서 파일을 만들었다가 지우던 순서를 뒤집은 것이다.

#### 고아 파일 정리

`jangbogo_status_*` 6건(평문 5 + 암호문 1)을 삭제했다. 내용은 `{"orders":[],"status":"no_new_orders","timestamp":"..."}` 로 구매 데이터가 없음을 확인한 뒤 지웠다. 되돌릴 수 있도록 세션 스크래치패드에 사본을 남겼다.

#### 검증

`FtpPendingQueueTest` 8건 — FTP 서버·네트워크 없이 검증한다. 업로드 수행자를 함수 인터페이스로 주입할 수 있게 만든 이유가 이것이다.

| 테스트 | 확인하는 것 |
|---|---|
| 실패분 이동 | 삭제되지 않고 `pending/` 으로, 이름·내용 보존 |
| 재전송 성공 | 보류분 제거, 오래된 것부터(FIFO) |
| 재전송 실패 | 파일 유지 + **첫 실패에서 중단** |
| 업로더 예외 | 실패로 취급하고 보류분 보존 |
| 건수 상한 | 오래된 것부터 폐기 |
| 기간 상한 | 초과분 폐기 |
| 이름 충돌 | 둘 다 보존, 접두사·확장자 유지 |
| 빈 큐 | 업로드 시도 자체를 하지 않음 |

전체 45건 / 실패 0. 기준선 DB md5 `909dfe48…` 불변. 앱 기동·실계정 접근 0.

#### 변경 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `svc/util/FtpPendingQueue.java` | 신규 | 보류 큐 |
| `svc/MallSchedulerService.java` | 수정 | 보류 큐 배선, 부수 결함 2건, 자격증명 확인 순서 |
| `src/test/java/.../svc/FtpPendingQueueTest.java` | 신규 | 회귀 테스트 8건 |
| `build.gradle` | 수정 | 버전 0.11.0 |

#### 남는 것

- **배송 상태 자체는 여전히 없다.** 보류 큐는 "전송 실패분을 잃지 않는다"까지만 보장한다. 어떤 주문이 언제 수신측에 도달했는지를 알려면 별도 상태 추적이 필요하다. Phase 3-5(하트비트)와 함께 볼 항목이다.
- `last_export_time` 이 이 경로에서 갱신되지 않는다. 판단 대기로 올린다.
- 보류 건수가 로그에만 남는다. 대시보드·`jbg_collect_log` 노출은 Phase 3-5 와 겹치므로 이번에는 하지 않았다.

---

### [2026-07-29 07:10] Phase 2 — 2-1 재정의 및 2-8 파서 픽스처 단위테스트 (v0.10.5)

#### 작업 개요

Phase 2(기준선 동결 · 파서 픽스처) 중 **2-1 을 재정의**하고 **2-8(파서 픽스처 단위테스트)을 완료**했다. 2-8 이 Phase 2 의 실질 작업이었다.

#### 2-1 재정의

원래 정의는 "`gradlew dependencies --configuration runtimeClasspath > baseline-deps.txt` — 선언 4.25.0 vs 실효 4.31.0 **스큐를 기록**" 이었다. 그런데 Phase 1 에서 `ext['selenium.version'] = '4.31.0'` 으로 **스큐 자체를 제거**했으므로 기록할 대상이 사라졌다.

**재정의**: 목적은 "스큐 기록"이 아니라 **"실효 좌표 동결 + 스큐 재발 탐지"** 다. 스큐라는 결함 유형은 없어지지 않았고, 이후 Phase 가 의존성 드리프트를 회귀로 오인하지 않으려면 실효 해석 상태를 알고 있어야 한다.

**측정 결과** (`runtimeClasspath`, 240줄)

| 항목 | 결과 |
|---|---|
| selenium 모듈 | **16개 전부 4.31.0 으로 일원화** — Phase 1 조치의 실효 확인 |
| typed DevTools 아티팩트 | `selenium-devtools-v133`·`v134`·`v135` **만** 존재. Chrome 150 용이 없다는 사실이 의존성 그래프로 확인된다 (기동 시 CDP 경고의 근거) |
| **새로 발견한 스큐** | `jackson-dataformat-yaml` 이 **2.15.3**, `jackson-core`·`jackson-databind` 는 **2.19.2** |

**jackson 스큐 상세**: `build.gradle:72` 가 `jackson-dataformat-yaml:2.15.3` 을 명시 고정하는데, Spring Boot BOM 은 나머지 jackson 모듈을 2.19.2 로 해석한다. jackson 은 모듈 버전 일치를 전제하는데 4개 마이너가 갈려 있다. 그리고 이 경로는 `JangbogoConfig`(기동 설정 로드) · `MallAccountYmlService`(계정 자격증명) · `ExportService` 가 모두 쓴다. 현재 관측된 장애는 없다.

**동결 산출물에 대한 판단**: 240줄 덤프를 파일로 커밋하는 안은 채택하지 않았다. 아무도 diff 하지 않으면 썩는 파일이 하나 느는 것뿐이고, 재생성 명령이 한 줄이라 언제든 다시 뜰 수 있다. 대신 위 측정 결과를 이 항목에 고정한다. 지속적 탐지가 필요하다면 파일 diff 가 아니라 **버전 일치를 검사하는 테스트**가 맞다 — 판단 대기로 올린다.

#### 2-8 파서 픽스처 단위테스트

**대상 선정**: `Emart.parseReceipt(String)` 하나로 좁혔다.

- `parseReceipt` 는 `public` 이고 인자가 문자열뿐이며 `new Emart(id, pass)` 는 필드 대입만 한다. **리팩터링 0 으로 즉시 테스트 가능**하고, 코드베이스에서 가장 분기가 많은 파서다.
- 반면 **Oasis · Hanaro · SSG 는 순수 파싱 함수가 없다.** `navigatePurchased(WebDriver)` / `parseDetailPage(WebDriver)` 안에서 네비게이션 · 창 전환 · DOM 추출이 뒤섞여 있다. 브라우저 없이 검증하려면 Selenium 목킹(자기 목을 검증하는 꼴이라 무회귀 가치가 낮다) 아니면 순수 파서 추출 리팩터링(기준선을 막 확보한 코드를 건드리는 실질 리스크)이 필요하다. 판단 대기로 올린다.

**픽스처 성격 — 합성으로 확정**: §9-13(픽스처 개인정보)이 미해소이고 이 저장소는 PUBLIC 이다. 실제 영수증은 상품명·수량·금액·구매일시·매장·바코드가 모두 든 구매 이력 그 자체라 부분 마스킹으로는 재식별 위험이 남는다. 실물을 마스킹하는 대신 **파서 분기를 덮도록 손으로 지은 데이터**를 쓴다. 분기 커버리지는 확보하면서 개인정보는 애초에 존재하지 않게 하는 것이 목적이다. 규칙은 `src/test/resources/fixtures/emart/README.md` 에 명문화했다. 이로써 2-3(픽스처 저장 + 마스킹 규칙 확정)의 마스킹 규칙 부분도 Emart 범위에서 확정됐다.

**작성 절차**: 단정문을 코드 독해가 아니라 **실제 파서 출력**에 근거시켰다. 픽스처를 만든 뒤 임시 덤프 테스트로 8종의 실제 결과를 먼저 관측하고, 그 값으로 단정문을 썼다. 특성화 테스트(characterization test)이므로 "옳은 값"이 아니라 "현재 값"을 고정한다.

**픽스처 8종과 덮는 분기**

| 픽스처 | 분기 |
|---|---|
| `basic-4col` | 표준 4열 (`arrSize == 4`) |
| `starred-5col` | 선행 기호가 별도 컬럼인 5열 (`arrSize >= 5`), 상품명이 인덱스 1 로 밀림 |
| `wrapped-name-first` | `combineExtraPattern01` — 상품명 줄 → 정보 줄 |
| `wrapped-data-first` | `combineExtraPattern01` — 정보 줄 → 상품명 줄 |
| `discount-row` | 단가에 `-` 가 있는 할인행 스킵 |
| `summary-rows` | `cancelKeys` 요약행 6종 + 숫자만 있는 행 + 빈 행 필터 |
| `empty-items` | 구분선 사이가 비어 있음 (예외 없이 빈 배열) |
| `short-row` | 4열 미만 행 |

**테스트가 고정한 현행 동작 2건 (결함이지만 이번에 고치지 않음)**

1. **줄번호가 상품명에 남는다** — `wrapped-name-first` 에서 결과 상품명이 `"01 테스트과자"` 다. `combineExtraPattern01` 이 줄번호를 분리하지 않고 상품명 컬럼을 통째로 옮긴다.
2. **4열 미만 행이 빈 객체로 목록에 들어간다** — `Emart.parseReceipt` 의 `itemArr.add(itemJson)`(:377)이 컬럼 수 분기 밖에 있다. 어느 분기에도 걸리지 않은 행이 `{}` 로 추가된다. **지금은 무해하다** — `MallOrderUpdaterRunner:172` 의 `item.has("name")` 가드가 걸러내므로 `jbg_item` 에는 도달하지 않는다. 다만 하류 가드에 의존하고 있다는 뜻이다.

두 건 모두 Phase 2 의 목적이 "동결"이지 "수정"이 아니므로 손대지 않았다. 테스트 주석에 `현행 동작` 으로 표시했고, 고칠 때 테스트가 깨지면 그것은 회귀가 아니라 의도된 변경이다.

**결과**: 8건 전부 통과. 브라우저·네트워크·DB 접근 0.

#### 변경 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `src/test/java/.../parser/EmartReceiptParserTest.java` | 신규 | 파서 픽스처 테스트 8건 |
| `src/test/resources/fixtures/emart/*.txt` | 신규 | 합성 픽스처 8종 |
| `src/test/resources/fixtures/emart/README.md` | 신규 | 데이터 규칙(마스킹 규칙) + 영수증 형식 |
| `build.gradle` | 수정 | 버전 0.10.5 |

#### Phase 2 항목 현황

| # | 상태 |
|---|---|
| 2-1 | **완료** (재정의 후 측정·기록) |
| 2-2 | 완료 (이전 세션 실측, seq=1·2. seq=3 하나로는 `account_status=0` 미연결) |
| 2-3 | **Emart 범위 완료** — 마스킹 규칙 확정 + 픽스처 저장. 나머지 몰은 2-8 과 함께 보류 |
| 2-8 | **Emart 완료.** Oasis·Hanaro·SSG 는 순수 파서 부재로 판단 대기 |
| 2-4·2-5·2-6·2-7·2-9 | 미착수 |

#### 판단 대기 (추가)

7. **jackson 모듈 버전 스큐** — `build.gradle:72` 의 `jackson-dataformat-yaml:2.15.3` 명시 고정을 제거해 BOM(2.19.2)에 맞출 것인가. 한 줄 삭제로 정렬되지만 기준선을 막 확보한 상태에서 런타임 의존성을 바꾸는 일이다.
8. **Oasis·Hanaro·SSG 파서 테스트 방식** — 순수 파서 추출 리팩터링 / Selenium 목킹 / 보류 중 택일.
9. **의존성 스큐 상시 탐지** — jackson·selenium 모듈 버전 일치를 검사하는 테스트를 둘 것인가.

---

### [2026-07-29 06:30] 판단 대기 B-1 해소 — 기동 수집 프로퍼티 가드 및 테스트 DB 격리 (v0.10.4)

#### 작업 개요

`[2026-07-29 00:10]` 항목의 "남은 판단 대기" 1번(`./gradlew test` 가 실계정 수집을 실행한다)을 사용자 승인 후 해소했다. 조사 과정에서 같은 뿌리의 두 번째 결함(테스트가 기준선 DB 파일을 직접 연다)이 드러나 함께 처리했다.

#### 배경

`JangbogoApplicationTests` 는 `@SpringBootTest` 로 전체 컨텍스트를 로드한다. 그러면 `StartupTasks.onApplicationReady()` 가 발화하고 `runInitialCollection()` 이 실행되는데, 이 호출은 **동기**다(`MallSchedulerService.runOneTimeCollection` → `runCollectForMall` 직접 호출). 즉 테스트가 실계정 로그인과 브라우저 수집이 끝날 때까지 블록된다. CI 는 `build.yml`(windows)·`ci.yml`(ubuntu) 양쪽에서 `./gradlew test` 를 돌리고 `gradlew build` 도 test 를 포함하므로, push 1회당 4번 시도된다.

조사 중 확인한 것:

- `ApplicationReadyEvent` 진입점은 `StartupTasks` **하나뿐**이다(전체 grep).
- `config/jbg_config.yml` 의 `auto-update-items-on-startup: true` 는 **코드에서 참조되지 않는 죽은 설정**이다.
- `JangbogoConfig` 는 Spring 프로퍼티가 아니라 `config/jbg_config.yml` 을 직접 읽는다. 따라서 이 파일로는 프로퍼티 가드를 만들 수 없어 Spring 프로퍼티를 신설했다.
- `LocalDBConnection` 이 접속 문자열을 하드코딩하고 있어 `src/test/resources/application.yml` 의 `spring.datasource.url: jdbc:sqlite::memory:` 가 **DAO 계층에는 아무 효과가 없었다**(그 설정은 `spring.sql.init` 경로에만 쓰인다).

#### 상세 내용

**1. 기동 수집 가드 — `jangbogo.startup.collect.enabled`**

`StartupTasks` 에 `@Value("${jangbogo.startup.collect.enabled:false}")` 를 두고 `runInitialCollection()` + `restoreIndividualSchedules()` 를 감쌌다. `migrateCollectLogSchema()` 와 스크린샷 정리는 가드 **밖**이다 — "앱은 띄우되 수집만 끈다" 를 해도 스키마는 최신이어야 하기 때문이다.

코드 기본값을 `false` 로 둔 것이 설계의 핵심이다. 프로퍼티 소스가 없는 컨텍스트(테스트 슬라이스, 앞으로 추가될 CI 잡, 컨텍스트만 띄우는 외부 도구)에서 자동으로 안전한 쪽으로 떨어지고, 수집을 켜는 것이 명시적 행위가 된다. `src/main/resources/application.yml` 이 `true` 를 선언하므로 콘솔 운영 동작은 바뀌지 않는다.

건너뛸 때 info 로그를 남겨 "왜 수집이 안 도나" 진단 비용을 없앴다.

**2. 테스트 DB 격리 — `jangbogo.localdb.url`**

`LocalDBConnection` 의 `DB_URL` 을 `System.getProperty("jangbogo.localdb.url", "jdbc:sqlite:./db/jangbogo-dev.db")` 로 바꾸고, `build.gradle` 의 `test` 태스크에서만 `build/test-db/jangbogo-test.db` 를 가리키게 했다. 기본값이 기존 값이라 운영 동작은 불변이고 DAO 는 한 줄도 손대지 않았다.

이것이 가드와 **독립적인 2차 방어선**이다. 가드가 어떤 이유로 실패해도 테스트가 보는 DB 에는 `jbg_mall` 이 없으므로 수집 대상이 0건이고 실계정에 도달할 수 없다.

**3. 검증 (`./gradlew test` 실측)**

| 확인 | 결과 |
|---|---|
| 기준선 DB md5 | `909dfe48822aea77bf4f6806a37073ac` — 실행 전후 동일, mtime·크기까지 불변 |
| `jbg_order` / `jbg_item` | 22 / 168 유지 |
| WAL·shm 잔여 | 없음 |
| 격리 DB | `build/test-db/jangbogo-test.db` 생성, 테이블은 `jbg_collect_log`(0행)뿐 |
| 가드 로그 | `06:27:11.704 기동 수집 비활성 … 건너뜁니다` |
| 브라우저·로그인 시도 | 0건 |
| 테스트 | 29건 / 실패 0 / 오류 0 (스킵 15 — `FtpTlsConnectionTest` `@Disabled` 14 포함) |
| 소요 | 27초 (수집이 돌면 수 분) |

#### 변경 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `boot/StartupTasks.java` | 수정 | `startupCollectEnabled` 가드 |
| `dao/LocalDBConnection.java` | 수정 | `jangbogo.localdb.url` 오버라이드 |
| `src/main/resources/application.yml` | 수정 | `jangbogo.startup.collect.enabled: true` |
| `src/test/resources/application.yml` | 수정 | 같은 키 `false` + DataSource 적용 범위 주석 |
| `build.gradle` | 수정 | 버전 0.10.4, `test` 태스크 DB 격리 |

#### 남은 판단 대기 항목

`[2026-07-29 00:10]` 항목의 1번은 해소. 2·3·4 는 그대로 남는다.

2. Emart `navigateReceipt` 의 `StaleElementReferenceException` — 간헐적 발생.
3. 영수증 바코드(`#barcodeTargetRec`) 미인식 2건 — 스킵 동작은 올바르나 원인 미조사.
4. `jbg_collect_log` 의 수정 전 기록 2행(seq 1·3) — **보존으로 결정**(2026-07-29). Phase 1 수정의 before 증거이고, 회귀 판정은 `jbg_order`/`jbg_item` 수치로 하므로 영향이 없다.

새로 기록하는 항목:

5. `dev/JangbogoConfigExample.java:97` 이 `JangbogoConfig` 로 DB 경로를 별도 조립한다. 개발 예제 클래스라 런타임 경로가 아니지만, DB 경로 출처가 두 곳이라는 사실은 남아 있다.
6. `CLAUDE.md` 가 `doc/CROSS-PROJECT-PROPAGATION.md` 를 참조하는데 이 문서는 미결 4(PUBLIC 저장소 공개 판단) 때문에 미커밋이다. 공개 저장소에서 깨진 참조가 된다.

---

### [2026-07-29 00:10] Phase 1 — 수집 장애 버그 일괄 수정 및 기준선 확보 (v0.10.3)

#### 작업 개요

`doc/PLAN-2026-07-28-REV1-decisions-applied.md` 의 Phase 1(버그 일괄 수정)을 수행하고, 종료 조건인 "수집 1회 이상 성공(기준선 확보)"을 달성. 2026-05-30 이후 수집 0건이던 상태에서 **주문 22건 / 아이템 168건**을 실제로 수집했다.

#### 배경

Phase 0A 실측으로 드러난 사실은 "봇 차단"이 아니었다. 배포본 로그(`logs/jangbogo-2026-05-30-1.log.gz`)에 이마트 프로모션 배너 `<img>` 가 로그인 버튼을 덮어 `ElementClickInterceptedException` 이 발생한 순간이 그대로 남아 있었고, 그 뒤 약 2개월간 모든 예약 수집이 `account_status가 1이 아님, 건너뜀` 으로 끝났다.

#### 상세 내용

**1. 기준 DB 결정 (착수 전 확인)**

회귀 판정 기준 DB 를 개발 트리 초기화(0/0)로 확정. `db/backup/jangbogo-dev.db.baseline-2026-07-28` 에 기존 데이터(주문 7 / 아이템 53)를 백업한 뒤 `jbg_order`·`jbg_item` 을 비우고 시작.

**2. 계획서 8개 항목 처리**

| # | 대상 | 처리 |
|---|---|---|
| 1+2 | `WebDriverManager.java` | `new ChromeDriver(options)` + headless 조건 정상화를 **같은 커밋**에서. 54만 고치면 무효였던 headless 가 처음 적용되어 "설정 없음 → headless 기동"이 되는 것을 막기 위함 |
| 3 | `WebDriverManager.java` `@Autowired` | `JangbogoConfig.getInstance()` 홀더로 교체 |
| 4 | `Ssg.java` 하드코딩 `return false` | `isSignedIn()` 으로 관측 상태 반환. 로그인 "완성"이 아니라 "정직화"까지만 |
| 5 | click 11지점 | `ClickUtil.safeClick()` 도입 |
| 6 | `settings.gradle` | 죽은 `includeBuild` 제거 |
| 7 | `jbg_collect_log` | **검증 결과 자동 생성되지 않음.** `migrateCollectLogSchema` 는 ALTER 만 하고, `spring.sql.init` 은 `continue-on-error` 로 실패를 삼켰다. `CREATE TABLE IF NOT EXISTS` 추가 |
| 8 | `ExportService.getMallIdFromSeq` | jiniebox `getSeqById()` 확인 결과 `emart`/`ssg` 둘 다 seq 1 로 매핑되어 무해 → **현행 유지** |

**3. 실행으로 발견해 추가 수정한 3건**

- **SSG 빈 결과 행 파싱 실패** — 조회 기간에 구매내역이 없으면 안내 문구 행(td 1개)이 렌더링되는데 이를 데이터 행으로 파싱해 `td[2]/p` 에서 예외. 이 예외가 seq=1 수집 전체를 중단시켜 Emart 에 도달하지 못하게 한 직접 원인이었다.
- **수집기 간 실패 격리** — seq=1 의 SSG/Emart 가 예외 격리 없이 순차 실행되어 한쪽 실패가 다른 쪽을 막았다. 격리하되 실패를 삼키지 않도록 `partialFailures` → `jbg_collect_log` FAIL 행 경로를 만들었다(v0.8.0 에서 제거한 swallow 패턴의 부활 방지).
- **성공/실패 판정 오탐** — `스킵 > 0 && 신규 == 0` 을 FAIL 로 보던 식 때문에, 17건을 정상 수집하고 15건 중복·2건 키 누락인 정상 상황이 FAIL 로 기록됐다. 데이터가 따라잡히면 상시화되는 오탐.

**4. 실행 검증 (실계정, seq=1 → 이후 seq=2 포함)**

- `ChromeDriver 기동 (headless=false)` — 옵션 전달·headless 정상 (항목 1·2·3)
- `SSG 로그인 판정: signedIn=true` — 4회 연속 재현 (항목 4)
- 이마트 로그인 통과, `data-body=mypage` — 2026-05-30 장애 지점 (항목 5)
- `jbg_collect_log 테이블 확인/생성 완료` + 실제 행 기록 (항목 7)
- **격리 로직 실전 검증**: 마지막 실행에서 Emart 가 `StaleElementReferenceException` 으로 실패했으나 SSG 결과는 보존되고 수집은 계속됐으며, 실패는 `Emart:navigateReceipt` FAIL 행으로 별도 기록됨. 수정 전이었다면 seq=1 전체가 FAIL 이고 아무 데이터도 남지 않았다.
- seq=2(오아시스)도 처음으로 수집 성공 — 7건 / 51아이템

**5. CDP 사용 가능 실증**

계획서 §[정정]의 전제를 실측 확인. typed DevTools(`selenium-devtools-vNNN`)는 Chrome 150 용 아티팩트가 없어 사용 불가지만, `ChromiumDriver.executeCdpCommand` 는 정상 동작한다. `Page.addScriptToEvaluateOnNewDocument` 로 주입한 스크립트가 실제 사이트(ssg.com)에서도 유지되고 `navigator.webdriver` 가 `undefined` 로 마스킹되는 것까지 확인했다. Phase 4A 의 T2 는 이 방식으로 진행 가능하다.

#### 변경 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `svc/util/WebDriverManager.java` | 수정 | ChromeOptions 전달, headless 조건, 설정 조회 경로 |
| `dto/JangbogoConfig.java` | 수정 | `getInstance()` 인스턴스 홀더 추가 |
| `svc/util/ClickUtil.java` | 신규 | `safeClick` 헬퍼 |
| `svc/mall/Ssg.java` | 수정 | `isSignedIn()`, 빈 결과 행 스킵, safeClick 적용 |
| `svc/mall/Emart.java` / `Hanaro.java` / `Oasis.java` | 수정 | safeClick 적용 |
| `svc/MallOrderUpdater.java` | 수정 | 수집기 격리(`collectFrom`, `partialFailures`) |
| `svc/MallOrderUpdaterRunner.java` | 수정 | 부분 실패 기록, `decideStatus` 분리 |
| `boot/StartupTasks.java` | 수정 | `ensureCollectLogTable` 추가 |
| `settings.gradle` | 수정 | 죽은 `includeBuild` 제거 |
| `build.gradle` | 수정 | `ext['selenium.version'] = '4.31.0'` 로 버전 일원화 |
| `svc/MallOrderUpdaterIsolationTest.java` | 신규 | 격리 회귀 테스트 5건 |
| `svc/MallOrderUpdaterRunnerStatusTest.java` | 신규 | 상태 판정 회귀 테스트 5건 |
| `CLAUDE.md` | 수정 | 외부 의존 항목 갱신(로컬 포함 빌드 참조 제거 반영) |

#### 운영 설정 변경 (DB)

- `jbg_mall.collect_interval_minutes` — seq=1,2 를 **720분**으로 (결정 5)
- seq=2 는 검증 중 일시적으로 `auto_collect=0` 으로 두었다가 원복 완료

#### 남은 판단 대기 항목

1. `./gradlew test` 가 실계정 수집을 실행한다 — `JangbogoApplicationTests` 가 전체 컨텍스트를 로드하면 `StartupTasks` 의 `ApplicationReadyEvent` 가 수집을 건다. CI 에서도 시도된다. 기동 수집을 프로퍼티로 가드할지 결정 필요.
2. Emart `navigateReceipt` 의 `StaleElementReferenceException` — 간헐적 발생.
3. 영수증 바코드(`#barcodeTargetRec`) 미인식 2건 — serial/date_time 이 비어 스킵된다. 스킵 동작 자체는 올바르다고 확정됐으나 원인은 미조사.
4. `jbg_collect_log` 의 수정 전 기록 2행(seq 1·3) 삭제 여부.

---

### [2026-05-01 04:00] v0.10.2 정식 릴리즈 발행 (v0.8.0 이후 첫 태그)

#### 작업 개요

v0.8.0 (2026-04-18) 이후 main 에 누적된 다섯 차례의 push (v0.9.0 / v0.9.1 / v0.10.0 / v0.10.1 / v0.10.2) 를 v0.10.2 정식 릴리즈로 묶어 발행. CLAUDE.md 의 Release 워크플로우 8~11 단계 수행.

#### 상세 내용

1. `release/RELEASE_NOTES_v0.10.2.md` 신규 작성 — v0.9.0 ~ v0.10.2 누적 변경을 "새로운 기능 / 내부 개선 / 버그·안정화 / 새 API / 호환성·업그레이드 / 설치 방법" 으로 재구성.
2. release notes 파일 commit + main push (release.yml 이 태그 push 시 해당 파일을 body 로 사용하므로 태그 푸시 전에 main 에 들어가 있어야 함).
3. `git tag v0.10.2` + `git push origin v0.10.2` 로 release.yml 트리거.
4. release.yml 자동 실행 모니터링 — `clean bootJar createJre packageDist` → `Jangbogo-v0.10.2.zip` 생성 → GitHub Release 발행.

#### 메모

- 이번 ZIP 빌드 환경(GitHub Actions runner) 에는 `packaging/winsw/jangbogo-service.exe` 가 없으므로, ZIP 의 `service/` 폴더에는 exe 가 빠진 채로 발행됨. 사용자가 `install.bat` 실행 시 v0.10.2 의 fallback (`download-winsw.ps1`) 이 자동으로 받아오므로 사용자 측 영향 없음 — 의도된 동작.
- 향후 빌드 환경에 사전 다운로드된 WinSW 를 두고 싶으면 release.yml 에 `download-winsw.ps1` 호출 단계를 추가 가능. 현재는 fallback 으로 충분하다고 판단해 단순 유지.

---

### [2026-05-01 03:30] v0.10.2 - WinSW 자동 다운로드 fallback

#### 작업 개요

배포 ZIP 의 `service\jangbogo-service.exe`(WinSW 실행파일) 가 누락되어 `install.bat` 이 즉시 종료되던 문제를 해결. `download-jre.ps1` 과 동일한 패턴으로 `download-winsw.ps1` 을 신규 추가하고, install.bat 이 누락 감지 시 자동 호출하도록 변경.

#### 배경

- `packaging/winsw/jangbogo-service.exe` 는 `.gitignore` 의 `packaging/winsw/jangbogo-service.exe` 라인에 의해 git 추적 제외. 각 빌드 환경에서 한 번 받아둬야 함.
- 빌드 환경에 해당 파일이 없으면 `packageDist` 태스크가 `from('packaging/winsw') { include '*.exe' }` 단계에서 그냥 빈 결과로 처리해 ZIP 에 exe 가 들어가지 않음 (Gradle 의 include 패턴은 매칭 0건이어도 에러를 안 냄).
- 사용자가 새 빌드 환경에서 ZIP 을 만들고 압축 풀어 install.bat 실행 시 "service\jangbogo-service.exe not found" 에러로 막힘. 실제 발생함.
- `download-jre.ps1` 이 이미 동일한 fallback 패턴을 사용하고 있어 (JRE 누락 → Temurin 자동 다운로드) 동일 패턴 적용이 자연스러움.

#### 상세 내용

**1. `download-winsw.ps1` 신규**
- `packaging/distribution/download-winsw.ps1` 추가.
- WinSW v2.12.0 (`WinSW-x64.exe`, .NET Framework 4.6.1 빌드) 을 GitHub Releases 에서 다운로드.
- 다운로드 후 `Unblock-File` 로 MOTW 제거 (Windows 가 인터넷에서 받은 exe 를 차단하면 sc 등록이 거부됨).
- TLS 1.2 강제, 이미 존재 시 skip, 실패 시 throw → install.bat 의 `errorlevel` 검사가 잡아냄.
- PowerShell 5.1 의 비-BOM UTF-8 파일 파스 한계 때문에 한글 주석 + em-dash(`—`) 조합이 파스 에러를 일으켜 영문 주석으로 작성. (`Jangbogo-Tray.ps1` 처럼 BOM 없는 한글이 통과되는 케이스도 있어 케이스별로 차이가 있음 — 안전하게 영문 사용.)

**2. `install.bat` 의 WinSW 누락 분기 변경**
- 기존: WinSW 없으면 `[ERROR] ... Please place the WinSW executable first.` 출력 후 `exit /b 1`.
- 변경: `download-winsw.ps1` 존재 시 자동 호출 → `errorlevel` 검사 → exe 생성 재확인. 다운로드 스크립트도 없거나 실패 시에만 에러로 종료.
- 기존에 WinSW exe 가 있던 사용자는 분기 자체에 진입하지 않아 동작 변동 없음.

**3. `build.gradle` 의 `packageDist`**
- `from('packaging/distribution')` 의 `include` 목록에 `'download-winsw.ps1'` 추가 → 새 ZIP 부터 자동 포함.

#### 검증

- `Parser::ParseFile` 로 `download-winsw.ps1` syntax 0 errors.
- 기존 사용자(빌드 환경에 WinSW 있음) 는 분기 미진입 → 회귀 없음.
- 신규 사용자(WinSW 없음) 는 install.bat 한 번에 다운로드 + 설치 완결.

#### 메모

- WinSW v3.x (alpha) 도 사용 가능하지만 .NET Core 의존성 부담 + production 채택 사례 부족으로 v2.12.0 채택. 향후 .NET 4.6 호환성 이슈 발생 시 재검토.
- 현재 사용자는 이번 push 가 main 에 들어간 후 ZIP 재빌드 + 압축 풀어 install.bat 재실행하면 자동 복구. 또는 즉시 한 줄 PowerShell 다운로드(이전 메시지)로도 복구 가능.

---

### [2026-04-23 17:30] v0.10.1 - 트레이 아이콘 재시작 도구 추가

#### 작업 개요

OS 재부팅 후 Windows 시스템 트레이의 NotifyIcon 새로고침 누락으로 `Jangbogo-Tray.ps1` 프로세스는 살아있지만 아이콘이 보이지 않는 상황을 위해 별도 재시작 진입점을 추가. 사용자가 바탕화면 단축아이콘 한 번으로 트레이를 안전하게 다시 띄울 수 있도록 함.

#### 배경

- v0.7.0 에서 PowerShell 트레이를 도입한 이후, 일부 환경에서 재부팅 직후 트레이 아이콘이 보이지 않는 현상이 보고됨.
- 원인은 Windows explorer.exe 의 알려진 새로고침 버그 — 프로세스 자체는 정상이지만 알림 영역에서 누락됨.
- 기존 "Jangbogo Tray" 단축아이콘을 그대로 더블클릭하면 새 인스턴스가 추가되어 좀비 프로세스가 누적될 수 있어, 명시적 재시작 경로가 필요했음.

#### 상세 내용

**1. `Jangbogo-Tray.ps1` 단일 인스턴스 보호**
- 스크립트 상단에 `param([switch]$Restart)` 추가.
- 글로벌 Mutex `Global\JangbogoTrayInstance` 획득 시도 → 이미 잡혀 있으면 안내 메시지 후 즉시 종료.
- `-Restart` 모드: `Get-CimInstance Win32_Process` 로 같은 스크립트(`Jangbogo-Tray.ps1`)를 실행 중인 powershell.exe / pwsh.exe 프로세스를 모두 찾아 `Stop-Process -Force` 후 400ms 대기 → mutex 획득 → 트레이 시작.
- "Exit Tray" 메뉴 핸들러에서 `ReleaseMutex()` + `Dispose()` 추가.

**2. `Restart-Tray.bat` 신규 (`packaging/distribution/`)**
- 관리자 권한 불필요한 일회성 배치 파일.
- `start "" /B powershell -NoProfile -ExecutionPolicy Bypass -STA -WindowStyle Hidden -File "%SCRIPT_DIR%Jangbogo-Tray.ps1" -Restart` 호출.
- 실행 후 3초 대기 후 자동 종료 (사용자가 메시지를 읽을 시간 확보).

**3. `create-shortcuts.ps1` 확장**
- 바탕화면에 "Restart Jangbogo Tray.lnk" 생성 (`-Restart` 인자 포함).
- 시작 메뉴에 동일 단축아이콘 추가.
- 출력 메시지에 사용 안내(Tip) 라인 추가.

**4. `build.gradle` 의 `packageDist` 태스크**
- `from('packaging/distribution')` 의 `include` 목록에 `'Restart-Tray.bat'` 추가 → 배포 ZIP 에 자동 포함.

**5. `사용설명서.txt` FAQ Q11 추가**
- "컴퓨터를 재부팅했더니 트레이 아이콘이 안 보여요" 질문에 3가지 해결 방법 (바탕화면 단축아이콘 / Restart-Tray.bat / 시작 메뉴) 안내.
- "서비스는 계속 동작 중이므로 데이터 수집에는 영향 없음" 명시 → 사용자 안심.

#### 검증

- PowerShell 두 스크립트(`Jangbogo-Tray.ps1`, `create-shortcuts.ps1`) AST 파스 에러 0건.
- `install.bat` 의 트레이 기동 부분(183행)은 사전에 기존 프로세스를 정리(95행)하므로 mutex 와 충돌하지 않음 — 추가 변경 불필요.

#### 메모

- 새 단축아이콘 이름은 영문 "Restart Jangbogo Tray" — 다른 단축아이콘들("Jangbogo Tray", "Jangbogo Dashboard")과 톤을 맞춤. 한글로 표기하면 일부 Windows 환경에서 단축아이콘 이름 인코딩 문제가 발생할 수 있어 영문 유지.
- 향후 주기적 watchdog (예: 5분마다 트레이 프로세스 살아있는지 체크하고 죽었으면 재시작) 까지 도입할 수도 있으나, 현재는 사용자가 인지했을 때 바로 복구할 수 있는 도구를 제공하는 선에서 멈춤. 자동 watchdog 은 좀비 프로세스 위험이 있어 신중히 설계 필요.

---

### [2026-04-23 16:00] v0.10.0 - 구매 내역 조회 UI 추가

#### 작업 개요

수집된 주문/아이템 데이터를 웹 UI 에서 직접 열람할 수 있는 `/orders` 페이지를 신규 추가. SESSION_HANDOFF 의 후속 작업 후보 #5 (수집 데이터 브라우저 조회 UI) 착수. 기존에는 파일 내보내기(`/export/orders`) 경로로만 데이터 접근이 가능했고 웹 UI 는 없었음.

#### 배경

- 초기 v0.7.0 논의 단계에서 거론되었으나, 실제 사용자 요구는 "수집 실패 진단" 이었음이 확인되어 v0.8.0 에서 `/collect-logs` 기능으로 방향 전환 → 조회 UI 는 SESSION_HANDOFF 보류 항목으로 유지.
- 이번 세션에서 2·3·4·5 번 후속 작업을 연속 진행하며 재개.
- DAO (`JbgOrderDataAccessObject.getAllOrders`, `JbgItemDataAccessObject.getItemsByOrder`) 와 서비스 레이어의 order+item 조인 패턴(`ExportService.collectOrderData`) 이 이미 존재해 조회 API 는 얇게 구현 가능했음.

#### 상세 내용

**1. 신규 API (`AdminController`)**
- `GET /api/orders?limit=N&mall=X&dateFrom=YYYYMMDD&dateTo=YYYYMMDD`
  - `JbgOrderDataAccessObject.getAllOrders(limit)` 조회 후, 각 주문에 대해 `JbgItemDataAccessObject.getItemsByOrder(seq)` 를 호출해 `items` 배열과 `item_count` 를 enrich.
  - 쇼핑몰(`mall_name`) / 기간(`date_time`) 서버 사이드 필터 지원.
- `GET /api/orders/{seq}` — 단일 주문 상세.

**2. 신규 페이지 (`/orders`)**
- `HomeController.orders()` 라우트, `activePage=orders`.
- `templates/orders.html` 신규 — `collect-logs.html` 구조 기반:
  - Bootstrap 5 테이블 + 필터 바(쇼핑몰 드롭다운, 최대 건수 선택) + 새로고침 버튼.
  - 요약 배지: 조회된 주문 수, 누적 아이템 수.
  - 상세 모달: 주문 메타데이터 + 아이템 테이블(아이템명/수량/등록시간).
  - 날짜 포매팅: `date_time` (INTEGER YYYYMMDD) → `YYYY-MM-DD` 변환, `insert_time` (millisecond epoch) → 로컬 타임스탬프.

**3. 네비게이션**
- `fragments/header.html` 에 "구매 내역" 메뉴(`bi-bag-check` 아이콘) 추가. 순서: 대시보드 / 구매 내역 / 오류 로그 / 계정 설정.

#### 검증

- `./gradlew compileJava` → BUILD SUCCESSFUL.
- `./gradlew spotlessApply` → 포맷 적용 완료.
- 기존 `/export/orders` 경로에 변화 없음 — 새 UI 는 읽기 전용으로 완전히 분리됨.

#### 메모

- 단일 주문 상세(`GET /api/orders/{seq}`) 는 현재 `getAllOrders(0)` 후 순회하는 간단 구현. 주문 건수가 수만 건 이상으로 늘어나면 `JbgOrderDataAccessObject` 에 `getOrder(seq)` 메서드를 추가해 교체 예정. 현 규모에서는 과잉 최적화로 판단.
- 페이지네이션은 현재 "최대 건수 select" (100/200/500/1000) 로만 제공. 향후 필요 시 커서 기반 무한 스크롤 또는 페이지 번호 방식 추가 가능.

---

### [2026-04-23 14:00] v0.9.1 - packaging/scripts/ 유물 폴더 제거

#### 작업 개요

v0.9.0 세션에서 후속 cleanup 태스크로 미뤄둔 `packaging/scripts/` 폴더를 정리. 구성 파일 3개 모두 jpackage 시도 실패 시절 유물이었음을 재확인하고 전체 삭제.

#### 배경

- v0.9.0 DEVLOG 메모 섹션 및 SESSION_HANDOFF 의 후속 작업 후보 #2 로 명시되어 있던 항목.
- `build.gradle`(138~163행)의 `packageDist` 태스크는 `packaging/distribution/` 과 `packaging/winsw/` 만 ZIP 에 포함 — `packaging/scripts/` 는 참조하지 않음.
- `post-install.bat` 은 `jangbogo.exe --install-complete` 를 호출하는데, 해당 플래그는 v0.9.0 에서 Java 트레이와 함께 제거됨. `jangbogo.exe` 산출물도 jpackage 시절 산물이라 현재는 생성되지 않음.
- `pre-uninstall.bat` 은 동일한 `jangbogo.exe` 프로세스를 종료하는 로직이라 마찬가지로 무효.
- `Jangbogo.bat` 은 `jangbogo-0.5.5.jar` 을 참조하는 v0.5.5 시절 스크립트. 현재 배포 ZIP 에는 `packaging/distribution/Jangbogo.bat` 이 버전 자동 감지 로직과 함께 들어감.

#### 상세 내용

1. **폴더 삭제**: `packaging/scripts/` 전체 제거 (`post-install.bat`, `pre-uninstall.bat`, `Jangbogo.bat`).
2. **.gitignore 정리**: 78행 `!packaging/scripts/*` 화이트리스트 예외 제거 (대상 디렉터리 사라짐).
3. **DISTRIBUTION_IMPLEMENTATION_SUMMARY.md 점검**: `packaging/scripts` 언급 없음 — 수정 불필요.
4. **버전 bump**: 0.9.0 → 0.9.1 (patch, 사용자 영향 제로).

#### 검증

- `packageDist` 태스크 정의 재확인: `from('packaging/distribution')`, `from('packaging/winsw')` 만 참조 → 배포 ZIP 구조 변동 없음.
- 저장소 전체에서 `packaging/scripts` 또는 `packaging\\scripts` grep → 문서(DEVLOG/SESSION_HANDOFF) 내 과거 참조 언급만 남고 실행 경로 참조 0건.

#### 메모

- 태그/릴리스 발행하지 않음 (cleanup patch 원칙).
- SESSION_HANDOFF 후속 작업 후보 #2 항목은 다음 세션 핸드오프 갱신 시 "완료" 로 정리 예정.

---

### [2026-04-23 09:00] v0.9.0 - 트레이 이중화 정리 + 배포 가이드 재구성

#### 작업 개요

v0.7.0 에서 PowerShell 기반 트레이(`Jangbogo-Tray.ps1`)를 배포 기본으로 도입하면서 Java 기반 `TrayApplication` 이 죽은 코드로 남아 있었음. 실제 배포 경로(`install.bat` → WinSW `--service` + PowerShell 트레이)에서 Java 트레이를 호출하지 않음을 확인하고 안전하게 제거. 동시에 `DEPLOYMENT_GUIDE.md` 본문을 "원스톱 표준 / 고급 수동 / 문제 해결" 3부로 재구성.

#### 배경

- SESSION_HANDOFF 의 후속 작업 후보 #3 (DEPLOYMENT_GUIDE 정제) + #4 (트레이 앱 이중화 정리) 를 한 번에 처리.
- 조사 결과, `packageDist` 태스크가 `packaging/distribution/` 과 `packaging/winsw/` 만 ZIP 에 넣으므로 `TrayApplication` 과 `--tray` / `--install-complete` 플래그는 **실제 배포 경로에서 완전히 미사용** 임이 확인됨.
- v0.8.1 docs patch 에서 원스톱 설치 섹션을 추가했으나, 본문 뒷부분은 여전히 수동 절차 중심이라 "원스톱이 표준이고 수동은 참고" 라는 방향성이 명확히 드러나지 않았음.

#### 상세 내용

**1. Java 트레이 제거**
- `src/main/java/com/jiniebox/jangbogo/sys/TrayApplication.java` 삭제.
- `JangbogoLauncher.java`:
  - `MODE_TRAY` / `MODE_INSTALL_COMPLETE` 상수 제거.
  - `launchTrayMode()` / `launchInstallCompleteMode()` 메서드 제거.
  - `ExecutionMode` 열거형을 `SERVICE` / `NORMAL` 로 축소.
  - `filterModeArguments()` 를 `--service` 하나만 필터링하도록 단순화.
  - 클래스 Javadoc 을 배포 트레이는 PowerShell 스크립트가 담당한다는 사실로 갱신.

**2. DEPLOYMENT_GUIDE.md 재구성**
- 목차를 3부 (🚀 표준 / 🔧 고급·수동 / 🚨 문제 해결) 로 재편.
- "설치 방법" → "수동 설치", "실행 방법" → "수동 실행", "Windows 서비스 등록" → "수동 Windows 서비스 등록", "제거 방법" → "수동 제거" 로 개명.
- 각 수동 섹션 상단에 "💡 `install.bat` / `uninstall.bat` 이 이 단계를 자동화합니다" 안내 블록 추가.
- 하단 버전 표시 0.6.0 → 0.9.0, 최종 수정일 2026-04-23 으로 갱신.
- 외부 파일에서 `DEPLOYMENT_GUIDE.md#설치-방법` 등 옛 앵커 참조는 0건 — 링크 깨짐 없음.

**3. CLAUDE.md 동기화**
- 프로젝트 구조 설명의 `sys/` 항목에서 `TrayApplication` 제거.
- 실행 모드 섹션을 `--service` / 인자 없음 두 가지로 축소하고, 배포 트레이는 PowerShell 이 담당한다는 주석 추가.

#### 검증

- `./gradlew compileJava` → BUILD SUCCESSFUL.
- `./gradlew spotlessApply` → 포맷 적용 완료.
- 영향 범위 재확인: `packaging/distribution/install.bat` 과 `packaging/winsw/jangbogo-service.xml` 은 `--service` 만 사용 → 변경 영향 없음.

#### 메모

- `packaging/scripts/` 폴더(post-install.bat / pre-uninstall.bat / Jangbogo.bat v0.5.5 참조)는 jpackage 시도 실패 시절 유물로 확인됨. `packageDist` 어디에서도 참조되지 않음. 이번 범위에서는 건드리지 않고 별도 정리 태스크로 spawn.

---

### [2026-04-18 15:00] v0.8.0 - 수집 실패 상세 진단 기능

#### 작업 개요

쇼핑몰 크롤링이 사이트 구조 변경 등으로 실패할 때, 실패 시점의 컨텍스트(단계명, 현재 URL, 페이지 타이틀, 타겟 셀렉터, 스크린샷)를 자동으로 포착해 DB에 기록하고 UI에서 모달로 상세 확인할 수 있도록 기능을 강화했습니다.

#### 배경

- v0.7.0에 `jbg_collect_log` 테이블과 `/collect-logs` 페이지를 추가했으나, 각 쇼핑몰 크롤러(Ssg/Oasis/Emart/Hanaro)의 `getItems()` 메서드가 내부 try/catch에서 예외를 `log.error`로만 기록하고 빈 배열을 반환하는 패턴이었습니다.
- 이 때문에 상위 `MallOrderUpdaterRunner`는 "성공(0건 수집)"으로 인식하여 FAIL 로그조차 남기지 않았습니다.
- 사용자가 "자꾸 실패하는 사이트가 있는 것 같은데 이력이 없다"고 보고한 원인이 바로 이것이었습니다.
- 또한 설령 FAIL이 기록되어도 스택트레이스만 있어 "어느 단계에서", "어떤 URL에서", "어떤 셀렉터를 찾다가" 실패했는지 알 수 없었습니다.

#### 상세 내용

**1. 신규 유틸 클래스 (`svc/`)**
- `CollectException`: stepName/currentUrl/pageTitle/targetSelector/screenshotPath + cause를 담는 도메인 예외.
- `CollectStep`: `CollectStep.call(driver, mallName, "signin", () -> ...)` 형태로 Selenium 작업을 래핑. 예외 발생 시 WebDriver에서 URL/타이틀을 자동 추출하고 스크린샷을 찍어 `CollectException`으로 변환.
- `ScreenshotUtil`: `logs/screenshots/yyyyMMdd/{mall}-{HHmmss}-{nano}.png` 저장, 30일 이전 폴더 자동 정리.

**2. DB 스키마 마이그레이션**
- `schema.sql`에 5개 컬럼 추가: step_name, current_url, page_title, target_selector, screenshot_path (모두 TEXT)
- `StartupTasks.migrateCollectLogSchema()`: 애플리케이션 시작 시 `PRAGMA table_info`로 기존 컬럼 확인 후 없는 것만 `ALTER TABLE ADD COLUMN`. 기존 사용자 데이터 100% 보존.

**3. DAO 확장 (`JbgCollectLogDataAccessObject`)**
- 확장 시그니처 `addLog(..., stepName, currentUrl, pageTitle, targetSelector, screenshotPath, ...)` 추가.
- 기존 9파라미터 시그니처는 delegation으로 하위호환.
- `getLog(seq)` 단일 조회 메서드 추가.
- `mapRow()`에 `safeGetString()`으로 신규 컬럼이 없어도 안전하게 동작.

**4. 쇼핑몰 크롤러 리팩토링 (Ssg/Oasis/Emart/Hanaro)**
- `getItems()`의 swallow catch 패턴 제거 → `CollectStep.call/wrap`으로 교체.
- 로그인 실패(`signin()` false 반환) 시 "로그인 실패 — 자격증명 또는 사이트 구조 변경 가능성" 메시지와 함께 `CollectException` throw.
- driver.quit()는 여전히 finally에서 수행.

**5. 상위 레이어 수정**
- `MallOrderUpdaterRunner.run()`: catch 블록에서 `unwrapCollectException()`으로 예외 체인을 거슬러 올라가 `CollectException` 추출 후 컨텍스트 필드를 DAO에 전달.
- `MallSchedulerService.runCollectForMall()`: 동일 패턴 적용. `saveFailLog()`에는 step="scheduler-precheck" 지정.

**6. API 확장 (`AdminController`)**
- `GET /api/collect-logs/{seq}`: 단일 로그 상세 조회.
- `GET /api/collect-logs/{seq}/screenshot`: PNG 스트리밍. `logs/screenshots` 경로 외부 접근 차단(보안).

**7. UI 확장 (`collect-logs.html`)**
- 테이블에 "실패 단계" 컬럼 추가 (회색 배지로 step_name 표시).
- 테이블에 "상세" 컬럼 추가 — "보기" 버튼 클릭 시 Bootstrap 모달 오픈.
- 상세 모달 내용: 실행 시간, 쇼핑몰, 상태, 실패 단계, 현재 URL(클릭 시 새 탭 오픈), 페이지 타이틀, 타겟 셀렉터(코드 스타일), 오류 메시지, 스크린샷 썸네일(클릭 시 확대 모달), 스택트레이스(pre).
- 필터 바에 쇼핑몰/실패 단계 드롭다운 추가 (클라이언트 사이드 필터링).
- 스크린샷 확대용 별도 모달(modal-xl).

#### 기대 효과

- 사이트 구조 변경으로 인한 실패를 즉시 인지 가능. 어느 셀렉터를 찾다가 실패했는지 바로 확인.
- 스크린샷으로 시각적 증거 확보. 로그인 페이지가 바뀐 건지, 중간 페이지가 바뀐 건지 한눈에 파악.
- 쇼핑몰별/단계별 실패 분포 집계 가능 (예: "하나로마트는 navigatePurchased 단계에서 반복 실패").

### [2026-04-17 14:00] v0.7.0 - 수집 오류 로그 + Windows 서비스 관리 + 참조 프로젝트 패턴 이식

#### 작업 개요

v0.6.1 이후 누적된 세 덩어리의 미커밋 변경을 v0.7.0으로 묶어 릴리스합니다.
1) 자동 수집 실행 결과를 DB에 기록하고 UI에서 조회 가능하게 하는 오류 로그 기능
2) Windows 서비스 설치/제거를 원스톱으로 처리하는 `install.bat`/`uninstall.bat` 추가
3) 비공개 참조 프로젝트의 성공 패턴을 참고해 배포 패키지 구조를 전면 재정렬

#### 배경 및 요구사항

- 자동 수집 실패 원인을 사용자가 대시보드에서 바로 확인할 수 있어야 함
- Windows 서비스 등록/해제를 수동 CLI 없이 원클릭 처리 필요
- 기존 `install.bat`이 UTF-8로 저장되어 cmd.exe의 CP949 파싱과 충돌해 `'cho'은(는) 내부 또는 외부 명령...` 같은 오류로 실행 불가 상태 → 인코딩 문제를 근본적으로 제거해야 함
- 배포 패키지의 견고성/자동화 수준을 참조 프로젝트 수준으로 끌어올려 릴리스 신뢰도를 확보

#### 상세 내용

**1) 수집 실행 오류 로그 기능**

- DB: `schema.sql`에 `jbg_collect_log` 테이블 추가
- DAO: `JbgCollectLogDataAccessObject` 신규 — `addLog`, `getAllLogs`, `getFailLogs`, `getSummary`
- 서비스 계층:
  - `MallOrderUpdaterRunner.run()` — 수집 완료/실패 시 `saveCollectLog()` 호출
  - `MallSchedulerService.runCollectForMall()` — 검증 실패/예외 시 `saveFailLog()` 호출
- API: `GET /api/collect-logs/summary`, `/failures`, 전체 조회 3개 엔드포인트
- UI: `collect-logs.html` 신규 페이지, 대시보드 "실행 결과 요약" 카드, 헤더 "오류 로그" 메뉴
- `HomeController`에 `/collect-logs` 라우트 추가

**2) Windows 서비스 통합 관리 (참조 프로젝트 패턴 풀 이식)**

- `install.bat` (신규, 영문): 관리자 권한 + JAR 자동 탐지 + Unblock + JRE 자동 다운로드 + XML 자동 동기화 + 기존 프로세스 정리 옵션 + 포트 점유 체크 + 서비스 RUNNING 폴링(20초) + 실패 시 로그 자동 tail + 대시보드 ready polling(45초) + 단축아이콘 생성 + 트레이 기동 + 브라우저 오픈
- `uninstall.bat` (신규, 100% ASCII): 관리자 권한 + 서비스 stop/uninstall + 프로세스 kill + 단축아이콘 삭제
- `Jangbogo-Tray.ps1` (신규): `NotifyIcon` 기반, 메뉴(Open Dashboard / Status / Start / Stop / Restart / Exit), 쇼핑카트 아이콘 on-the-fly 생성
- `create-shortcuts.ps1` (신규): 바탕화면 `Jangbogo Tray.lnk` + `Jangbogo Dashboard.url`, 시작 메뉴 `Jangbogo Tray.lnk`
- `download-jre.ps1` (신규): Temurin JRE 21 Windows x64 자동 다운로드
- `Jangbogo.bat` (전면 재작성, 영문): JAR 자동 탐지, 시스템 Java ≥21 검증, JRE fallback, 포트 충돌 시 대체 포트 프롬프트

**3) 빌드 / WinSW XML**

- `build.gradle`: version 0.6.1 → 0.7.0, `packageDist`에 PS1 3종 include 추가
- `packaging/winsw/jangbogo-service.xml`: JAR 참조 0.6.1 → 0.7.0, `--service` 인자 포함
- `bat/build_package.bat`: `--no-daemon` 옵션 추가

**4) CLAUDE.md 프로젝트 가이드**

- Release/Push 워크플로우 정의 (자동 버전 bump 기본, 애매할 때만 승인)
- DAO 패턴, 실행 모드, DB 스키마, API 목록 명문화

**5) .gitignore**

- `.claude/` 추가

#### 검증

- `./gradlew packageDist --no-daemon` 빌드 성공
- 배포 ZIP 내부 구성 확인: 신규 PS1 3종 + 영문 bat 3종 + JAR + JRE + WinSW XML 포함

---

### [2026-02-15] 애플리케이션 시작 시 1회 수집 기능 추가

#### 작업 개요

애플리케이션 기동 시 스케줄링 복원 **이전**에, 자동 수집이 설정된 쇼핑몰에 대해 **1회 수집**을 실행하도록 기능을 추가했습니다.  
기존에는 스케줄 복원 후 첫 수집이 "주기(분)" 만큼 지연되어 실행되었으나, 이번 변경으로 시작 직후 1회 수집이 먼저 수행된 뒤 주기별 스케줄이 동작합니다.

#### 배경 및 요구사항

- 사용자가 자동 수집을 설정해 두었을 때, PC 재부팅 또는 Jangbogo 재시작 후에도 바로 최신 구매내역을 확보하고자 함
- 스케줄만 복원하면 첫 수집까지 최대 "주기(분)" 만큼 대기해야 하는 문제 해결
- 예: 주기 120분 → 기존에는 시작 후 120분 뒤 첫 수집, 변경 후 시작 직후 1회 수집 + 120분마다 주기 수집


### 2026-01-28 - 하나로마트(Hanaro) 쇼핑몰 통합

#### 새로운 기능

**1. HanaroTest 클래스 생성** (`src/test/java/com/jiniebox/jangbogo/mall/HanaroTest.java`)
- nonghyupmall.com 크롤링을 위한 step-by-step 테스트 클래스 작성
- WebDriver 셋업, 로그인, 페이지 이동, 파싱을 단계별로 검증
- `testFullFlow()` 메서드로 통합 테스트 수행
- 실제 사이트 구조 분석 및 파싱 로직 검증 완료 (26개 품목 파싱 확인)

**2. Hanaro 클래스 완성** (`src/main/java/com/jiniebox/jangbogo/svc/mall/Hanaro.java`)
- `signin()`: nonghyupmall.com 로그인 처리
- `signout()`: `a_id_logout` 버튼 클릭으로 로그아웃
- `navigatePurchased()`: 마트구매영수증 목록 순회 및 수집
  - 목록 페이지(`eltRctwList.nh`) 이동
  - 영수증 행 순회 (여러 건 지원)
  - 각 행 클릭 → 상세보기 버튼(`eltRctwDtlView`) 클릭 → 상세 페이지 파싱
  - DB serial 조회로 이미 수집된 영수증 건너뛰기
- `parseDetailPage()`: 상세 페이지 파싱 로직 분리
  - table[0]: 요약 정보 (구매일자, 구매처, 구매금액)
  - table[1]: 품목 목록 (품목명, 수량, 금액)
- `isAlreadyCollected()`: DB에서 serial+datetime으로 중복 확인

**3. 시스템 통합**
- `JangBoGoManager.getMallSession()`: seq=3 → `new Hanaro()` 매핑 추가
- `MallOrderUpdater.collectItems()`: seq=3 분기 추가로 Hanaro 수집 지원
- `ExportService.getMallIdFromSeq()`: case 3 → "hanaro" 반환 추가

**4. 관리 화면 UI 추가** (`src/main/resources/templates/index.html`)
- HANARO 카드 블록 추가 (연한 초록색 배경 `#e8f8e8`)
- 계정 연결 버튼 (`btn_signin_hanaro`, seq=3)
- 자동 수집 주기 설정 (`data-seq="3"`)
- `openSigninMallForm()` 함수에 seq=3 분기 추가: "하나로마트 계정연결"

#### 변경사항

**1. Serial 형식 개선**
- 기존: 구매일자만 사용
- 변경: `구매일자_구매금액` 조합으로 unique 식별자 생성
- 예시: `20260125_35400` (2026년 1월 25일, 35,400원)

**2. 중복 수집 방지 로직**
- `navigatePurchased()` 단계에서 DB 조회로 이미 수집된 영수증 건너뛰기
- `JbgOrderDataAccessObject.getOrder(serial, datetime, null)` 활용
- 불필요한 크롤링 방지로 효율성 향상

#### 기술적 세부사항

**크롤링 흐름**:
1. nonghyupmall.com 메인 → 로그인 페이지 이동
2. `#userID`, `#password` 필드에 계정 정보 입력
3. 로그인 버튼 클릭 → `a_id_logout` 버튼 존재 확인으로 성공 여부 판단
4. 마트구매영수증 목록 페이지(`BCI1020M/eltRctwList.nh`) 이동
5. 영수증 목록 행 순회 (`//*[@id='content']//table//tbody//tr`)
6. 각 행 클릭 → `eltRctwDtlView` 버튼 클릭 → 상세 페이지
7. 상세 페이지에서 table[0](요약), table[1](품목) 파싱
8. serial 생성 후 DB 중복 체크 → 미수집 건만 결과에 추가

**파싱 구조**:
- 요약 테이블: `th`/`td` 쌍으로 key-value 추출
- 품목 테이블: `tbody//tr` 순회, 각 행의 `td` 3개 (품목/수량/금액)
- 헤더 행 건너뛰기: `"품목".equals(name)` 체크

#### 파일 변경 목록

| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `HanaroTest.java` | 신규 | 크롤링 테스트 클래스 |
| `Hanaro.java` | 수정 | navigatePurchased, signout, parseDetailPage, isAlreadyCollected 구현 |
| `JangBoGoManager.java` | 수정 | seq=3 Hanaro 매핑 추가 |
| `MallOrderUpdater.java` | 수정 | seq=3 수집 분기 추가 |
| `ExportService.java` | 수정 | getMallIdFromSeq case 3 추가 |
| `index.html` | 수정 | HANARO 카드 및 모달 지원 추가 |
| `data.sql` | 기존 | seq=3, id='hanaro' 이미 등록됨 |

---

## 테스트

### 단위 테스트

### 통합 테스트

### 빌드 테스트

---

## 체크리스트

---

## 관련 이슈

---

## 참고 문서

---

## 배포 정보

---

## 통계

---

## 리뷰 요청사항

---

## 향후 계획

---

## 기타 참고사항

### Breaking Changes

### Migration Guide

### Known Issues

---

## 작업 기록 형식 가이드

새로운 작업을 추가할 때는 다음 형식을 사용하세요:

```markdown
### YYYY-MM-DD HH:MM - 작업 제목

#### 새로운 기능
- 기능 설명

#### 변경사항
- 변경 내용

#### 버그 수정
- 수정 내용

#### 개선사항
- 개선 내용
```

**참고**: 
- 날짜 형식: `YYYY-MM-DD HH:MM` (예: 2026-01-15 14:30)
- 작업은 날짜순으로 정렬 (최신 작업이 위에)
